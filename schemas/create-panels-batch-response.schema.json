{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/create-panels-batch-response.schema.json",
  "title": "CreatePanelsBatchResponse",
  "description": "Response for POST /api/panels/batch — the created panels, in the same order as the request's panels array.",
  "type": "object",
  "required": ["panels"],
  "additionalProperties": false,
  "properties": {
    "panels": {
      "type": "array",
      "items": { "$ref": "panel.schema.json" }
    }
  }
}
