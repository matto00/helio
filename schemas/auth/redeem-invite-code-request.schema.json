{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/auth/redeem-invite-code-request.schema.json",
  "title": "RedeemInviteCodeRequest",
  "type": "object",
  "properties": {
    "code": {
      "description": "HEL-704: single-use Beta-access invite code, as issued (plaintext) by backend/scripts/issue-invite-code.sql. Trimmed and hashed (sha256) server-side before lookup -- never stored or logged in plaintext.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  },
  "required": ["code"],
  "additionalProperties": false
}
