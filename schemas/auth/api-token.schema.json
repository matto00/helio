{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/auth/api-token.schema.json",
  "title": "ApiTokenResponse",
  "description": "Personal Access Token metadata as returned by GET /api/tokens. Never contains the raw token or its hash.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "name": {
      "type": "string"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "lastUsedAt": {
      "type": "string",
      "format": "date-time"
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time"
    },
    "scopedPipelineIds": {
      "description": "HEL-369: present (and non-empty) only for a scoped token, surfacing its pipeline allow-list for audit/visibility. Absent for an unscoped, full-access PAT.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["id", "name", "createdAt"],
  "additionalProperties": false
}
