{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/mfa-verify-request.schema.json",
  "title": "MfaVerifyRequest",
  "description": "POST /api/auth/mfa/verify (public) — exchanges a pending-login challenge plus a TOTP or backup code for a session.",
  "type": "object",
  "properties": {
    "challengeToken": {
      "type": "string"
    },
    "code": {
      "type": "string",
      "description": "A 6-digit current TOTP code, or a 10-character backup code."
    }
  },
  "required": ["challengeToken", "code"],
  "additionalProperties": false
}
