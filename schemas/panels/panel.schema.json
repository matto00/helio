{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/panels/panel.schema.json",
  "title": "Panel",
  "description": "Discriminated panel response \u2014 `type` selects the shape of `config`.",
  "type": "object",
  "required": ["id", "dashboardId", "title", "type", "meta", "appearance", "ownerId", "config"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "dashboardId": {
      "type": "string",
      "minLength": 1
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160
    },
    "type": {
      "type": "string",
      "enum": ["text", "markdown", "image", "divider", "output", "form"]
    },
    "meta": {
      "$ref": "https://helio.local/schemas/shared/resource-meta.schema.json"
    },
    "appearance": {
      "$ref": "https://helio.local/schemas/panels/panel-appearance.schema.json"
    },
    "ownerId": {
      "type": "string",
      "minLength": 1
    },
    "config": {
      "type": "object"
    },
    "dataAsOf": {
      "type": ["string", "null"],
      "description": "ISO-8601 timestamp of the most recent pipeline run that wrote to this panel's bound DataType; null when unbound or never run."
    },
    "layout": {
      "type": ["object", "null"],
      "description": "Decision-15 server-owned default grid placement (HEL-909) — present only on the `POST /api/panels` response for a newly placed `output` panel; absent/null everywhere else (layout otherwise lives on the dashboard's own `layout` field).",
      "additionalProperties": false,
      "properties": {
        "x": { "type": "integer", "minimum": 0 },
        "y": { "type": "integer", "minimum": 0 },
        "w": { "type": "integer", "minimum": 1 },
        "h": { "type": "integer", "minimum": 1 }
      }
    }
  },
  "additionalProperties": false,
  "$comment": "Per-subtype config shapes live in $defs and are selected via the top-level oneOf below. dataSourceId is not a valid field. A panel is a placement: the `output` kind binds via config.outputId (see OutputConfig) to an Output, which owns the actual data-shaping config (HEL-904); the other kinds (text/markdown/image/divider) carry only their own literal display config.",
  "oneOf": [
    {
      "properties": {
        "type": {
          "const": "output"
        },
        "config": {
          "$ref": "#/$defs/OutputConfig"
        }
      }
    },
    {
      "properties": {
        "type": {
          "const": "text"
        },
        "config": {
          "$ref": "#/$defs/TextConfig"
        }
      }
    },
    {
      "properties": {
        "type": {
          "const": "markdown"
        },
        "config": {
          "$ref": "#/$defs/MarkdownConfig"
        }
      }
    },
    {
      "properties": {
        "type": {
          "const": "image"
        },
        "config": {
          "$ref": "#/$defs/ImageConfig"
        }
      }
    },
    {
      "properties": {
        "type": {
          "const": "divider"
        },
        "config": {
          "$ref": "#/$defs/DividerConfig"
        }
      }
    },
    {
      "properties": {
        "type": {
          "const": "form"
        },
        "config": {
          "$ref": "#/$defs/FormConfig"
        }
      }
    }
  ],
  "$defs": {
    "OutputConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "Config for the `output` panel kind (HEL-904 task 3.6) \u2014 a placement of one Output. Everything the five retired bound configs (metric/chart/table/collection/timeline) used to carry (fieldMapping, aggregation, chartOptions, columnWidths/density/columnOrder, timelineOptions, metricId, label/unit) now lives on the Output itself (see schemas/outputs/), not on the placement. Tolerant read: an absent/empty outputId decodes to the empty-string sentinel rather than a validation error; the server still 400s an outputId-less output panel at create/update time.",
      "properties": {
        "outputId": {
          "type": "string"
        }
      }
    },
    "TextConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string"
        }
      }
    },
    "MarkdownConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string"
        }
      }
    },
    "ImageConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "imageUrl": {
          "type": "string"
        },
        "imageFit": {
          "type": "string",
          "enum": ["contain", "cover", "fill"]
        },
        "caption": {
          "type": ["string", "null"],
          "description": "Optional static caption rendered as a strip beneath the image (HEL-318). Absent leaves it unchanged on PATCH; null/empty/whitespace clears it. Omitted from the response config when unset."
        }
      }
    },
    "DividerConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "orientation": {
          "type": "string",
          "enum": ["horizontal", "vertical"]
        },
        "weight": {
          "type": ["integer", "null"],
          "minimum": 1
        },
        "color": {
          "type": ["string", "null"],
          "pattern": "^#[0-9A-Fa-f]{3}([0-9A-Fa-f]{3})?$"
        }
      }
    },
    "FormConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "Config for the `form` panel kind (HEL-1083) — a panel that writes rows into a `dataset`-kind data source rather than reading a materialized Output. `fields` is an ordered list; order is preserved across persistence and every read. `control` is a presentation vocabulary deliberately orthogonal to the dataset's own declared field type — a form field never re-declares its own data type.",
      "properties": {
        "dataSourceId": {
          "type": "string"
        },
        "fields": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FormFieldConfig"
          }
        },
        "submit": {
          "$ref": "#/$defs/FormSubmitConfig"
        }
      }
    },
    "FormFieldConfig": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sourceField", "control"],
      "properties": {
        "sourceField": {
          "type": "string",
          "description": "The dataset field this entry writes to, by name (design.md D3) — the dataset's declared schema remains the single authoritative source of the field's type/required-ness/write-time default."
        },
        "control": {
          "type": "string",
          "enum": ["text", "textarea", "number", "date", "select", "checkbox", "file"]
        },
        "label": {
          "type": "string"
        },
        "placeholder": {
          "type": "string"
        },
        "helpText": {
          "type": "string"
        },
        "required": {
          "type": "boolean",
          "description": "Tighten-only (design.md D3a): absent inherits the dataset declaration, true adds a form-level requirement. false is rejected as malformed."
        },
        "initialValue": {
          "description": "Prefill only — seeds the control before first input and never changes what is stored for an omitted field."
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Valid only alongside control: number. A counter (the single-field compact configuration) is expressed as control: number carrying step — step's PRESENCE is the counter discriminator."
        },
        "options": {
          "description": "Required when control is select."
        }
      }
    },
    "FormSubmitConfig": {
      "type": "object",
      "additionalProperties": false,
      "description": "A form's submit behaviour (design.md D4). writeMode is fixed to `append` — `replace` is meaningful to the dataset write API but not to a form submit, and is rejected rather than silently coerced.",
      "properties": {
        "writeMode": {
          "type": "string",
          "enum": ["append"]
        },
        "label": {
          "type": "string"
        },
        "resetOnSuccess": {
          "type": "boolean"
        }
      }
    }
  }
}
