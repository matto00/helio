{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://helio.local/schemas/batch-response.schema.json",
  "title": "BatchResponse",
  "description": "Response body for POST /api/dashboards/:id/batch — the fully updated dashboard and its current panel list.",
  "type": "object",
  "required": ["dashboard", "panels"],
  "properties": {
    "dashboard": {
      "$ref": "https://helio.local/schemas/dashboard.schema.json",
      "description": "The dashboard after all batch operations have been applied."
    },
    "panels": {
      "type": "array",
      "description": "Current panel list for the dashboard after the batch. Eliminates the need for a follow-up GET /api/dashboards/:id/panels.",
      "items": { "$ref": "https://helio.local/schemas/panel.schema.json" }
    }
  },
  "additionalProperties": false
}
