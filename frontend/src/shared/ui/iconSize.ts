// Shared lucide-react icon glyph sizes (HEL-443). Every lucide icon instance
// in the app is sized via one of these three values rather than an ad hoc
// inline `size={N}` literal, so the icon system has one consistent scale.
export const ICON_SIZE = { sm: 14, md: 16, lg: 20 } as const;
