- `frontend/src/theme/themeParityGuard.css.test.ts` — new Jest guard (HEL-444 AC1): asserts every `--app-*` token declared in `:root[data-theme="dark"]`/`:root[data-theme="light"]` in `theme.css` is declared in both blocks; re-formed from the parked `check-theme-parity.mjs` draft per design D9.2 (Jest beside its seven `theme.css`-parsing siblings, not a `scripts/*.mjs` — no husky/gate-chain wiring needed). Mutation-proved on three arms (single-theme token, stale exception, zero-declaration block — the vacuous-pass hole design D9.3/CR3 flagged in the parked draft). Independently re-verified by the evaluator (cycle 1) — unchanged in cycle 2.
- `DESIGN.md` — records the per-theme token-parity rule and the guard, plus the re-tested AC2 conclusion, now scoped to the surfaces actually walked (cycle 2 fix, evaluation-1.md CR1/CR6): the parked walk's two rendered failures (site-wide focus ring, accent-as-text empty-state link) are confirmed FIXED by HEL-1046/HEL-1048/HEL-1050, verified live in both themes on Dashboards+PanelGrid, Sources, Pipelines, Settings, auth `LoginPage`/`RegisterPage`, and the mobile shell. Four required surfaces (`MfaVerifyPage`, `OAuthCallbackPage`, `/proposals/review`, the create-dashboard modal/toasts/popovers) are explicitly reported as unreachable-with-reason, not silently omitted. `OrbitMark`'s location is corrected to the auth pages (CR4, was wrongly cited as Settings/Chat) and its contrast re-measured there under both the fresh per-theme default (CR5: 3.47 light / 6.32 dark, both clear 3:1) and the Yellow adversarial preset (1.87 light, below 3:1 — still excepted as a WCAG 1.4.11 logotype).
- `openspec/changes/light-dark-parity-audit/tasks.md` — all tasks (sections 1-8) checked complete, now true against the tree (cycle 2 fix, CR ticking-against-intent): 4.1/4.6 were prematurely ticked in cycle 1 against a walk that only covered Dashboards; they now reflect the full required-surface walk below.

## Evidence — `.concertino/runs/HEL-444/evidence/`

- `walk-2026-09-09-cycle3/` — 20 screenshots (20/20 md5-distinct, `md5sums.txt`), `walk-transcript.txt`: Connectors (both the wide-viewport standalone `Test connection` button's own inline pending/success/error UI, and the narrow-viewport/`ActionsMenu` path that fires a real success AND error toast — clicked, not asserted), the `Customize dashboard appearance` popover, `MfaEnrollModal` (opened, screenshotted, closed with Escape without confirming), the Connectors add-connector modal, the always-present `Add dashboard` inline create form (both themes — corrects cycle 2's false "unreachable, non-empty list" claim), and a live PAT-creation "Copy" toast (`role="status"` `aria-live="polite"`, "Token copied to clipboard.").
- `walk-2026-09-09-cycle2/` — 20 screenshots (18/20 distinct; `walk-transcript.txt`'s create-dashboard-modal rows are marked SUPERSEDED, corrected in cycle 3), covering Dashboards+PanelGrid, Sources, Pipelines, Settings, auth pages, mobile shell, and the two genuinely-unreachable auth routes (`MfaVerifyPage`/`OAuthCallbackPage`, both self-evidenced by byte-identical `/login` redirect screenshots).
- `walk-screenshots-2026-09-09/` — cycle 1's original 6 screenshots (Dashboards only; kept for provenance, superseded by the above).
- `derived-token-matrix.txt` — transcript of the 8-preset × 2-theme (16 computation) derived-token contrast matrix (all pass by construction).
