- `frontend/src/features/panels/ui/PanelList.css` — Added a `@media (max-width: 768px)` floor rule (placed after the base `.panel-list__add` rule) setting `min-height: 44px` on `.panel-list__add`, following the `EmptyState.css:219-228` convention. Sibling audit found `.panel-list__zoom-button` / `.panel-list__zoom-reset` also under the 44px floor on both axes (the base rule sets `height: 22px` and `.panel-list__zoom-button` additionally sets `width: 22px`); added `min-height: 44px` and `min-width: 44px` to both, scoped to the same 768px breakpoint (the widget already hides below 430px via its own rule).
