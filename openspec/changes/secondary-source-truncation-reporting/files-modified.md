- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineProtocol.scala` — task 1.1: sharpened `RunResultResponse`'s scaladoc to state that `truncatedReads` includes the primary source's entry when the primary was truncated, and is the empty vector (never omitted) when nothing was truncated. No behaviour change.
- `backend/src/test/scala/com/helio/services/pipelines/PipelineRunServiceSpec.scala` — task 3.1/3.2: added a new `PipelineRunService submit` case (primary under cap via `seedRestDsNamed(RestSuccessUrl, ...)`, `lookup` secondary over cap via `seedRestDsNamed(RestBigUrl, ...)`) asserting the run-wide `sourceTruncated`, the (legitimately equal) primary scalars `sourceRowCount`/`sourceAvailableRowCount`, and — the previously-uncovered surface — the secondary `truncatedReads` entry's `dataSourceName`/`rowsRead`/`availableRowCount` as literals, plus the per-entry `availableRowCount > rowsRead` proof of loss. Added alongside all pre-existing cases; none replaced.
- `helio-mcp/src/types.ts` — task 2.1: declared `truncatedReads` on `RunResultResponse` (previously dropped at the type boundary), with a doc comment stating primary-inclusion and empty-when-complete.
- `helio-mcp/src/helioApi.ts` — tasks 2.2–2.4: added `TruncatedRead`; renamed `RunOutcome.availableRowCount`→`primaryAvailableRowCount` and `sourceRowCount`→`primarySourceRowCount`; added required `truncatedReads: TruncatedRead[]`; updated `RunOutcome`'s doc comment; mapped `truncatedReads` in `runPipeline`, defaulted to `[]`.
- `helio-mcp/src/tools/write.ts` — task 2.6: rewrote `run_pipeline`'s tool description to name the renamed fields, state `truncated`'s run-wide scope vs. the primary-only scalars, and describe `truncatedReads` (including the primary-may-be-included caveat).
- `helio-mcp/src/runPipelineTruncation.test.ts` — tasks 3.3/3.4: updated the two existing tests for the renamed fields; added a new secondary-source test asserting the emitted `RunOutcome` field by field (including that no `availableRowCount`/`sourceRowCount` key survives on the object) and a `truncatedReads: []` assertion on the existing complete-run case.
- `openspec/changes/secondary-source-truncation-reporting/mutation-evidence.md` — task 3.5: mutation transcript for design.md D4's two mutations (MCP value-level, backend per-entry numeric), each applied/observed/reverted with the exact failing assertion and confirmation no pre-existing test reddens.
