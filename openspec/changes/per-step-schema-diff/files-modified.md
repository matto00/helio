# Files Modified — per-step-schema-diff (HEL-405)

- `frontend/src/features/pipelines/state/schemaDiff.ts` — new pure helper `computeSchemaDiff(input, output, renames?)`; computes added/dropped/retyped/renamed buckets from a step's analyze `inputSchema`/`outputSchema`, with rename pairing driven only by the `rename` op's `config.renames` map (design.md Decision 1). 69 lines.
- `frontend/src/features/pipelines/state/schemaDiff.test.ts` — new unit tests for `computeSchemaDiff`: added/dropped/retyped, rename-paired, unpaired rename stays add/drop, rename with a type change still pairs, identical schemas → empty diff, empty input/output arrays. 130 lines, 8 cases.
- `frontend/src/features/pipelines/ui/StepSchemaDiffChips.tsx` — new presentational component; computes the diff via `computeSchemaDiff` and renders chips reusing the existing `pipeline-detail-page__step-card-diff-chip` classes (`--added`/`--removed`/`--changed`) plus the new `--renamed` modifier; returns `null` when every bucket is empty. 61 lines.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.css` — added the `--renamed` chip modifier (token-only: `--app-warning-surface`/`--app-warning`/`color-mix(... --app-warning ...)`), mirroring the three existing sibling modifiers (`--added`/`--removed`/`--changed`); no new literals introduced beyond the pattern the existing `-diff-chip` base already carries (HEL-680 owns tokenizing that base). +6 lines.
- `frontend/src/features/pipelines/ui/StepCard.tsx` — renders `<StepSchemaDiffChips>` once in the expanded body, above the op-kind editor branch, passing `analyzeSchema`/`analyzeOutputSchema` and `renamesOf(step)` only for the `rename` op; deleted the hardcoded `col_a`/`col_b`/`col_c` placeholder chips from the no-editor fallback branch (kept its desc text). **440 → 434 lines (net −6)**, well within the ≤~10-line growth budget — the new component/helper absorb the diff logic that used to be inlined. Still past the 400-line soft budget; HEL-682 (not bundled here) owns the split.
- `frontend/src/features/pipelines/ui/StepCard.test.tsx` — added a `StepCard — real schema diff chips (HEL-405)` describe block: all four chip kinds render for a step with analyze data (rename op, paired rename); chips render for an op with a dedicated editor (select), proving the strip left the fallback-only branch; no diff container when schemas are identical; no diff container when analyze data is unavailable (empty schemas); the hardcoded `col_a`/`col_b`/`col_c` placeholder text never appears. 5 new tests.

## Notes

- No backend, schema, or wire changes — frontend-only, as scoped.
- `StepCard.tsx` net line count *decreased* (440 → 434): the deleted placeholder block (12 lines) was larger than the new render call + two new imports (6 lines total).
- Live UI check not run (unit tests only, per instructions); the evaluator may want to visually confirm the new `--renamed` chip in both light and dark themes via `scripts/concertino/start-servers.sh` (ports 5837/8744 per the ticket's delivery notes).
