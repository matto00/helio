- `frontend/src/shared/ui/DataGrid.css` — moves `.ui-data-grid--preview`'s `margin-top: var(--space-3)` off the flex-child scroll container and onto `.ui-data-grid__frame--preview` (the ordinary block-box frame itself). Fixes a confirmed, live-measured regression: HEL-451's D10 reframe made that margin a flex-child margin, which never collapses with the preceding consumer element's margin-bottom (flex formatting contexts don't collapse child margins with anything) — silently summing instead of collapsing-to-max, adding +6px at SourceDetailPanel's preview and +8px at StepCard's preview. Moving the margin onto the frame (a real sibling of the preceding element, in normal flow) restores the pre-reframe adjacent-sibling collapse exactly.
- `frontend/src/shared/ui/DataGrid.test.tsx` — adds a mutation-failable regression guard (static-source CSS assertion, since jsdom cannot compute resolved/collapsed margins) asserting the `margin-top` declaration lives on `.ui-data-grid__frame--preview`, not on `.ui-data-grid--preview`. Verified red before the fix, green after (see evaluation report for the flip transcript).
- `frontend/src/features/sources/ui/SourcePreviewSkeleton.tsx` — cycle 2 (evaluator-1 CR1): the root `<div>` now also carries `ui-data-grid__frame--preview` directly, since it renders no frame wrapper of its own and would otherwise silently lose the margin-top that moved off `.ui-data-grid--preview`. Restores the 12px loading-state gap.
- `frontend/src/features/sources/ui/SourcePreviewSkeleton.test.tsx` (new) — cycle 2 (evaluator-1 CR2): mutation-failable guard asserting the skeleton's rendered root carries `ui-data-grid__frame--preview`. Verified red before the fix, green after.
