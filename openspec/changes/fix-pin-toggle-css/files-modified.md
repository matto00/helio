# Files Modified — HEL-1065

- `frontend/src/shared/ui/SortableTh.tsx` — added a `sortable-th__label` class to the existing unclassed label `<span>` (design.md D1). Inert for `DataGrid`'s own pinnable table (see `DataGrid.tsx` below — `SortableTh` is never rendered there), kept for consistency/any future consumer.
- `frontend/src/shared/ui/DataGrid.tsx` — the load-bearing D1 fix: `DataGrid` has its own inline copy of `SortableTh`'s markup (documented in-file as HEL-448 design D4, since `SortableTh` can't be rendered directly there), so the `sortable-th__label` class was added to *that* `<span>`, the one design.md's D1 described but didn't locate correctly.
- `frontend/src/shared/ui/DataGrid.css` — issue 1 fix (label ellipsis, scoped to `.ui-data-grid__th--pin-reserve .sortable-th__btn`/`.sortable-th__label`, design.md D1/D2/D7), corrected beyond D1's literal text: `min-width: 0` alone left the button's shrink-to-fit `inline-flex` sizing unconstrained (measured live — the label rendered at full, un-truncated width regardless of `overflow`/`text-overflow`), so `width: 100%` was added on `.sortable-th__btn` to give it a definite width its flex child can actually shrink against. Issue 2 fix (`min-height: 48px` → `height`, design.md D3), value corrected post-escalation from 48px → 54px: 48px (and even the naively-computed 52px) left the focus ring clipped by up to ~2.25px per edge, live-measured via CDP-forced `:focus-visible`; 54px re-measured clean on both edges, both themes.
- `frontend/src/shared/ui/DataGrid.test.tsx` — narrowed the row-height regex from `min-height: 48px` to `height: 54px` (kept in sync with the corrected CSS value); all other assertions in that describe block (padding-right calc-value, co-location guard, scoping tests) kept unchanged per design.md D4/D4-correction.
- `frontend/src/shared/ui/__snapshots__/SortableTable.test.tsx.snap` — regenerated (`jest -u`): the `sortable-th__label` class addition (D1) changes `SortableTh`'s rendered markup, which this shell-markup regression snapshot locks exactly. No other markup changed.
- `e2e/hel1065-pin-toggle-css-fixes.spec.ts` (new) — Playwright geometry coverage for both fixes, both themes: label/pin-icon overlap with columns pinned (with an explicit `scrollWidth > clientWidth` truncation precondition), pin-toggle button + full focus-ring containment at a real ≤430px viewport, and a desktop/fine-pointer control asserting the row-height fix doesn't leak outside its media query. Red-before-green proven via a path-scoped `git stash` of `DataGrid.css`/`SortableTh.tsx`/`DataGrid.tsx` at the final (54px) fix value.
