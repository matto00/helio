- `DESIGN.md` — extended the existing `::after` hit-expander clause in the Control-metrics section (above the `**[mechanical]**` tag) with the per-side extension formula, minimum-gap/tiling rule, the `elementFromPoint` bisection verification method, and the epsilon/anti-widen-gap warning, per HEL-777. All numeric claims re-verified directly against `openspec/changes/archive/2026-08-21-anchor-mobile-command-bar/{design.md,evaluation-1.md,tasks.md}`.
