## Skeptic Report — design gate (round 2, skeptic-design-2.md)

### Prior round's change requests — verified fixed (not just reworded)

1. **tasks.md §2.6 / helio-mcp registration.** Now reads: "helio-mcp `write.ts` `add_pipeline_step` op-name list (~376-378) and per-op config docs: add `analyzewithai` (and the missing `convertformat`, a one-word HEL-1105 gap)" — no longer routes the executor through the dead "wherever convertformat is enumerated" grep trail; the file and line range are named explicitly. Verified `helio-mcp/src/tools/write.ts:376-378` is the real op-doc site and confirmed (fresh grep) `convertformat` is absent from both `write.ts` and `schemas/` today, matching the stated gap. Fixed.
2. **proposal.md Impact section.** Now includes: "helio-mcp: `add_pipeline_step` op docs in `helio-mcp/src/tools/write.ts` gain `analyzewithai` (plus the omitted `convertformat`)." Matches tasks.md 2.6's scope; no more proposal/tasks disagreement. Fixed.
3. **D2 ClaudeError wording.** Now reads: "Production adapter: `ClaudeAiStepClient(client: ClaudeClient)` maps the three `ClaudeError`s to `AiStepFailure` (`Guardrail`, `Api`, `Transport`; `Unavailable` is produced only by the context default)". Confirmed `ClaudeModels.scala:48-63` has exactly 3 `ClaudeError` variants (`ApiError`, `TransportFailure`, `GuardrailExceeded`); the design no longer implies `Unavailable` is a 4th mapped `ClaudeError` case. Fixed.

### Independent full review (fresh ground truth)

- **Seam precedent.** `PipelineExecutionContext` (`backend/src/main/scala/com/helio/domain/model/PipelineStep.scala:99-125`) already carries `writeBackSink: WriteBackSink = new WriteBackSink` as a defaulted context field threaded through the engine — the exact shape D2 proposes for `aiClient`. Direct, working precedent, not a novel mechanism.
- **Single engine/service construction chokepoints.** In the current tree (note the file moved to `backend/src/main/scala/com/helio/api/ApiRoutes.scala` since round 1, same content): `new PipelineRunService(` appears once, at line 315; `ClaudeConfig.fromEnv()` gated `new ClaudeClient(...)`/`new HttpClaudeTransport(...)` pairs appear at exactly 3 sites (588, 612, 631), matching design.md's line citations exactly. The DI story is accurately grounded, not stale.
- **Token guardrails.** `ClaudeClient.send` (`backend/src/main/scala/com/helio/ai/ClaudeClient.scala:27-34`) rejects over `config.maxInputTokens` via `GuardrailExceeded` before any network call and clamps `maxTokens` to `config.maxOutputTokens`. Matches the ticket's premise-validation claim.
- **Registration sites.** `rowToDomain` (`PipelineStepRepository.scala:1286`) and the analyze-response dispatch (`PipelineService.scala` ~1711, confirmed by grep of the surrounding `case Success(cfg: ConvertFormatConfig) => ...` / `case Success(other) => throw new IllegalStateException(...)` pattern) both match design.md's cited sites exactly. `PipelineCostEstimator.AiOps` (`PipelineCostEstimator.scala:24`) already contains `"analyzewithai"` — confirmed unchanged, matching D8/C2's "AiOps is unchanged" claim.
- **Stand-in test sites.** `grep -rln "analyzewithai" src/test` → exactly the 4 files D8 names (`PipelineCostEstimatorSpec`, `PipelineAnalyzeRoutesSpec`, `PipelineStepsOpCheckSeededRowsSpec`, `PipelineCreateTransactionalSpec`). Complete enumeration, nothing missed.
- **`StepExecutionException.from` allowlist.** Confirmed at `InProcessPipelineEngine.scala:47-50`: `IllegalArgumentException` message passed verbatim, anything else collapses to "step execution failed" — consistent with D6's reason-code-via-`IllegalArgumentException` design and the no-key degrade path (D3) producing a named `ai-unavailable` message rather than a generic failure.
- **Type subset (D1).** `DataFieldType.CanonicalWireValues` (`domain/model/model.scala:735`) exists and is referenced elsewhere in validation error messages exactly as design.md describes; D1's `string | integer | float | boolean` subset (excluding timestamp/string-body/binary-ref) is a coherent, named restriction of it.
- **Partial-column unreachability (D6).** As designed, output columns are appended only after the entire response validates — this is a design-time guarantee (nothing appends per-field), not implementation, so it is sound to state as the mechanism that satisfies the acceptance criterion ("a non-conforming response fails the step... rather than silently producing partial columns"). No seam in the design lets a partial write occur before validation completes.
- **Response enforcement completeness.** Reason codes in D6 (`field-missing`, `field-not-string`, `ai-unavailable`, `ai-guardrail`, `ai-error`, `response-malformed-json`, `response-not-object`, `response-missing-field`, `response-wrong-type`, `response-extra-field`) cover every failure mode named in tasks.md 3.1/3.2, and each has a corresponding task. No gap between the enumerated reasons and the planned test matrix.
- **HEL-1108 single call point (D4).** `AiStepClient.complete` is the only method `ClaudeAiStepClient` exposes per the trait signature in D2 (`def complete(req: AiStepRequest): Future[Either[AiStepFailure, String]]`) — there is no second entry point into the Claude call from a pipeline step in this design, so "single call point" is accurate as designed.
- **No-key degrade / backend boot.** `AiStepClient.Unavailable` as context default plus `ApiRoutes` wiring gated on `ClaudeConfig.fromEnv()` (same Left/Right + log-warn pattern as the 3 existing `*ServiceOpt` sites) means the backend still boots with no key — mirrors the already-shipped `assistantServiceOpt` pattern exactly (verified at lines 626-631).
- **schemas/ claim.** Fresh `grep -rln "convertformat" schemas/` → zero hits (confirms round-1's finding still holds); task 2.7 correctly instructs the executor to search other op-specific config shapes instead of `convertformat`, and to "edit only if an enumeration exists" — this is now a real, checkable instruction rather than a second dead grep trail.

No new defects found. Design is internally consistent (proposal/design/tasks agree on scope and impacted surfaces), every load-bearing claim in the driver brief and design.md's Context section is grounded against the live code (not hallucinated line numbers or invented precedent), and the acceptance criteria ("declared schema enforced, non-conforming response fails named reason rather than partial columns" and "openspec spec included") are both traceable to specific decisions (D6, D7) and task coverage.

### Verdict: CONFIRM

### Non-blocking notes

- D1's "extra fields fail rather than being dropped" is a reasonable, explicitly self-approved policy call (documented in Planner Notes) — flagging only that this is a stricter-than-typical-API behavior worth calling out to the executor if `write.ts`'s doc string ever needs a one-line note about it (not required by this ticket's scope).
