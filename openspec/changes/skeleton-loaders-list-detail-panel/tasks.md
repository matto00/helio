## 1. Frontend — the shared primitive

- [x] 1.1 Add `--app-skeleton-shimmer: 1.6s` to `theme.css`'s Motion block (theme-invariant, in `:root`)
- [x] 1.2 Extend `DESIGN.md` §3's Motion bullet (currently an exhaustive list of the two transition shorthands) with `--app-skeleton-shimmer` and why a continuous loop needs its own token
- [x] 1.2a Add `Skeleton` to `DESIGN.md` §6's canonical-primitives list (`DESIGN.md:227-248`, beside `Spinner`) with the one-line skeleton-vs-spinner division — the list closes with "Use these; do not hand-roll equivalents", so omitting it invites a competing skeleton next ticket
- [x] 1.3 Create `frontend/src/shared/ui/Skeleton.tsx` with `variant` (`block`/`line`/`circle`), optional `width`/`height`/`className`, `aria-hidden="true"` on every instance
- [x] 1.4 Add the D7 comment to `Skeleton.tsx` stating skeleton = initial structural load, spinner = short in-place refresh
- [x] 1.5 Create `Skeleton.css`: token-only, shimmer ramping `--app-surface-soft` → `--app-surface-raised`, duration from `--app-skeleton-shimmer`, timing function `linear`. Do NOT try to reuse `--transition-slow`'s easing — it is a full shorthand (`0.28s cubic-bezier(...)`) whose curve cannot be extracted, and an entrance curve is wrong for a continuous loop anyway. A bare CSS keyword is not token drift (no token exists for timing functions)
- [x] 1.6 Add `Skeleton.css`'s own `@media (prefers-reduced-motion: reduce)` block setting `animation: none` plus a flat `background: var(--app-surface-soft)`
- [x] 1.7 Export `Skeleton` from `frontend/src/shared/ui/index.ts` (beside `Spinner`)

## 2. Frontend — sidebar and list surfaces

- [x] 2.1 Add a `SidebarRowsSkeleton` built only from `Skeleton`, rendering `<li>`s inside the real `<ul className="dashboard-list__items">` at `.dashboard-list__button`'s 32px/`--app-radius-sm` geometry
- [x] 2.2 Give it a row-shape prop for the two-line `--stacked` variant (D9) and render it from `SidebarItemList`'s loading branch. `SidebarItemList` takes its initial-load flag from the call site, NOT `status === "idle"` — `SidebarBody.tsx:78-101` dispatches only the active section and skips chat on the free tier, so an idle-inclusive gate would park a permanent skeleton there (D11)
- [x] 2.3 Pass the row shape from `SidebarBody`'s five `<SidebarItemList>` call sites (lines 119/148/183/221/299) — `stacked` for the data-types section only, flat for the rest; the sixth sidebar section is Dashboards, which renders `DashboardList` and takes no such prop (task 2.4)
- [x] 2.4 Render the same skeleton from `DashboardList`'s loading branch and make its `<ul>` render consistently with `SidebarItemList` (D4)
- [x] 2.4a Widen the gate to `(status === "idle" || status === "loading") && items.length === 0` on `SourcesPage`, `PipelinesPage`, `TypeRegistryPage` and `DashboardList` ONLY — mount effects run after paint, so a loading-only gate paints the empty branch first (`SourcesPage.tsx:77`'s ~331px hero, `DashboardList.tsx:269`). Safe because `panelsSlice.ts:88` is the only site in the frontend that assigns `status = "idle"`, so their `idle` is reachable once, pre-dispatch (D11)
- [x] 2.4b DO NOT widen `PanelList` to `idle` — its `idle` is re-entrant and terminal. `markDashboardPanelsStale` (`panelsSlice.ts:85-89`) restores it and clears `loadedDashboardId`; `App.tsx:123-129` is keyed only on `selectedDashboardId` and early-returns when null, so no refetch follows. Widening parks a permanent skeleton over the zero-dashboard "New dashboard" CTA (F-003; HEL-554 builds on this surface) and after deleting a dashboard's last panel (D11)
- [x] 2.5 Add a `PanelCardSkeleton` built only from `Skeleton`, matching `.panel-grid-card`'s radius/border/padding and its top/body/footer bands
- [x] 2.6 Build the grid skeleton's placeholders by running the grid's OWN resolver: `resolveDashboardLayout(stubs, selectedDashboard?.layout ?? defaultDashboardLayout)[activeBreakpoint]`, where `stubs` is N synthetic panel-id stubs. This is the single rule for all three cases — covered, empty, and breakpoint-PROJECTED (`pickProjectionSource`/`projectLayout`, `dashboardLayout.ts:150-186`, which fills an empty active breakpoint from the richest saved one; dominant at `xs`, since users drag on desktop). Render via the same `panelGridConfig` (`rowHeight: 52`, `margin: [18,18]`)
- [x] 2.6a Use N = **3** stubs when the saved layout for the active breakpoint has no entries (a DECIDABLE condition — the real panel count is unknown at skeleton time, which is the whole premise; 45 of 71 dev dashboards with panels store zero entries). Do NOT hand-place these: the resolver applies `defaultItemWidth(colCount)` (4 at lg/md, 3 at sm, 2 at xs — `dashboardLayout.ts:51-53`) and `findNextAvailablePosition`, so geometry comes out PIXEL-EXACT for the first N cards and only the COUNT differs, which is the sole delta the spec licenses. "Full cols width" would be 3x too wide at lg (D10)
- [x] 2.7 Render it from `PanelList` *inside* `.panel-list__zoom-container`, sharing that element's `transform: scale()`/sizing so a saved zoom ≠ 1 does not shift the swap; keep an accessible loading name on the wrapper
- [x] 2.7a Gate `PanelList` on `selectedDashboardId !== null && status === "loading" && (items.length === 0 || items[0].dashboardId !== selectedDashboardId)` (`panel.ts:321`) — `fetchPanels.pending` keeps the previous dashboard's items, so a plain `items.length === 0` gate never fires on a dashboard switch, the skeleton's main in-session path. A switch dispatches immediately, so `"loading"` is already set and no reliance on `idle` is needed (D11/D12)
- [x] 2.8 Below `panelGridConfig.breakpoints.sm` (768px) render a `MobilePanelStack`-shaped skeleton instead — same count source and empty-layout fallback as 2.6/2.6a, at the stack's `--space-3` gap/padding and full width, at a single documented neutral height; comment the accepted per-card height delta (D10)

## 3. Frontend — detail and panel surfaces

- [x] 3.1 Replace `PipelineDetailPage`'s `currentPipeline === null` spinner block with header/river/footer band skeletons, keeping the error-first guard order and the `aria-label="Loading pipeline"` wrapper
- [x] 3.2 Replace `PanelContent`'s `isLoading` spinner branch with a single kind-AGNOSTIC body skeleton that fills its container, keeping the `aria-label="Loading data"` wrapper — not shaped per renderer type (D6)
- [x] 3.2a Widen the panel-body loading condition to also cover the pre-dispatch frame: `currentFetchKey !== null && paginationEntry == null` (`usePanelData.ts`). Without it a metric panel paints "--"/"No data" for one frame before the skeleton, on every dashboard load. Safe and non-re-entrant — `:91-92` early-returns with no fetch key and `:99`'s dedupe guard is bypassed when the entry is null, so a dispatch always follows. `PanelDetailModal.tsx:78` inherits it (D13)
- [x] 3.2b DELETE `tableIsLoading` (`PanelCard.tsx:89-94`) and make `:103` pass the hook's `isLoading` for every kind. It re-derives the flag with `paginationEntry != null` — exactly the condition 3.2a widens away — so without this a grid table panel still falls through to `TableRenderer`'s `aria-hidden` ghost table (`:183-208`) and loads differently from the SAME panel in its detail modal. Behaviour-preserving post-dispatch: `tableIsLoading` and `usePanelData.ts:240` are character-for-character equivalent once the entry exists (D13)
- [x] 3.3 Point `PanelSuspenseFallback` at that same kind-agnostic skeleton, leaving `PageSuspenseFallback` on `Spinner size="2xl"`. It takes no props (`SuspenseFallback.tsx:10-17`) and is nested inside `.chart-panel__canvas` while `PanelContent` replaces the whole body — a kind-shaped skeleton could not match across both boxes (D6)
- [x] 3.4 Add a preview-table skeleton to `SourceDetailPanel`'s preview section while its local `isLoading` is true, ONLY when `previewRows === null && previewUnsupported === null` — `isLoading` is also true on a *reload* with rows already populated, and replacing a resolved `DataGrid` would violate the initial-load-only rule and steal the Reload path from D7's button-label spinner. Leave the button's "Loading…" label as-is. `.ui-data-grid--preview` caps at `max-height: 320px` (`DataGrid.css:34-39`) but is shorter for a small result set — pick a documented height and record the accepted delta

## 4. Frontend — page shells carrying the inherited defect

- [x] 4.1 Replace `SourcesPage.tsx:53`'s `<p>Loading sources…</p>` with a skeleton sized to `EmptyState --main`'s 320px floor so the loading↔error transition stops collapsing
- [x] 4.2 Do the same for `PipelinesPage.tsx:35` and `TypeRegistryPage.tsx:24`, gating each on `status === "loading" && items.length === 0` **as widened by 2.4a** to `(idle || loading)`; add `items` to `TypeRegistryPage.tsx:12`'s selector, which currently selects only `{ status, error, errorKind }`
- [x] 4.2a Make each new skeleton branch mutually exclusive with its pre-existing sibling, or five call sites double-render: `SourcesPage.tsx:77` (`succeeded || idle`), `PipelinesPage.tsx:66` (`succeeded || idle`), `TypeRegistryPage.tsx:48` (`succeeded || idle`), `DashboardList.tsx:269` (`status !== "loading"`), and `PanelList.tsx:258` (`items.length > 0` is true with the PREVIOUS dashboard's panels mid-switch)
- [x] 4.3 Verify the three pages' `isRetrying` dead code, and `PanelList.tsx:34-38`'s `isRetryingPanels` (fully dead once its loading branch stops rendering `StatusMessage`), are no more broken than before; leave both otherwise untouched

## 5. Frontend — StatusMessage narrowing

- [x] 5.1 Remove `StatusMessage`'s `loading` branch and narrow `status` to `"idle" | "succeeded" | "failed"`
- [x] 5.2 Fix every call site the narrowing breaks; confirm `tsc`/lint is clean with no remaining `status="loading"`

## 6. Tests

- [x] 6.1 `Skeleton.test.tsx` — base/variant classes, `className` passthrough, `aria-hidden` on every variant (mirror `Spinner.test.tsx`)
- [x] 6.2 `Skeleton.css.test.ts` — assert the `prefers-reduced-motion: reduce` block sets `animation: none` and a flat background; assert no literal colour/duration (mirror `EmptyState.css.test.ts`'s `findMediaBlock` helper)
- [x] 6.3 `StatusMessage.test.tsx` — update for the removed loading branch; assert nothing renders for idle/succeeded
- [x] 6.3a Update the EIGHT existing files that assert loading copy this change removes: `PanelContent.test.tsx`, `ChartRenderer.test.tsx`, `MarkdownRenderer.test.tsx`, `PanelCreationModal.test.tsx`, `StatusMessage.test.tsx`, `SidebarItemList.test.tsx`, `DashboardList.test.tsx`, `SuspenseFallback.test.tsx`
- [x] 6.4 Loading-state tests for `SidebarItemList` (both row shapes), `DashboardList`, `PanelList`, `PanelContent`, `PipelineDetailPage`, `SourceDetailPanel`, `SourcesPage`, `PipelinesPage`, `TypeRegistryPage`
- [x] 6.5 Refetch-does-not-skeleton tests: `status === "loading"` with items present keeps rendering items on each list surface
- [x] 6.5a Lock D10: one placeholder per entry when the saved layout covers the panel set, a non-zero default count when it is empty, and rendered inside the zoom container
- [x] 6.5c Lock D11 (positive): `SourcesPage`/`PipelinesPage`/`TypeRegistryPage`/`DashboardList` mounted at `status === "idle"` with no items render the skeleton on the first frame, not their empty state
- [x] 6.5c-i Lock D11 (mirror-image, `PanelList`): at `status === "idle"` with NO dashboard selected, the "No dashboards yet" + "New dashboard" CTA renders and no skeleton does
- [x] 6.5c-ii Lock D11 (mirror-image, `PanelList`): at `status === "idle"` with a dashboard selected and `items === []` after a delete (post-`markDashboardPanelsStale`), NO SKELETON renders. Do NOT assert an empty state renders — `PanelList.tsx:246` gates it on `status === "succeeded"`, so that branch is false and the area renders nothing. That missing `EmptyState` is a pre-existing §7 gap owned by HEL-548; do not close it here
- [x] 6.5d Lock D12: selecting a different dashboard while the previous one's panels are loaded renders the skeleton, and a refetch of the same dashboard does not
- [x] 6.5f Lock D13 (the decision that had NO test, which is why 3.2b's defect survived four review rounds): drive a bound METRIC and a bound TABLE panel through `PanelCardBody`/`usePanelData` — NOT through `PanelContent`, which receives `isLoading` as a prop and cannot exercise this. Assert that before the fetch is dispatched neither renders its renderer: no "--"/"No data" for the metric, and NO ghost table for the table panel. Cover the detail-modal path too
- [x] 6.5e Lock D6: `PanelContent`'s body skeleton and `PanelSuspenseFallback` render the same treatment, and the body skeleton does not vary by panel kind
- [x] 6.5b Lock the D8 exception: after `markDataTypeRowsStale`, a panel refetch renders the skeleton state (asserted deliberately, since 6.5's list-surface tests never reach this path)
- [x] 6.6 Assert each loading region exposes exactly one accessible loading name and carries no `role="alert"`
- [x] 6.7 Assert `TableRenderer`'s load-more spinner and `PageSuspenseFallback`'s spinner are unchanged
- [x] 6.8 Run `npm run lint`, `npm run format:check`, `npm test`, `npm --prefix frontend run build` — all green, zero new warnings
- [x] 6.8a Suppress or placeholder `PanelList.tsx:156-159`'s "N panels" count while the skeleton is up — it reads "0 panels" on cold boot and the PREVIOUS dashboard's count mid-switch, the same premature-data-claim class D13 rejects for metric panels
- [ ] 6.9 AT ARCHIVE: hand-rewrite `openspec/specs/shared-status-message/spec.md` and `openspec/specs/frontend-code-splitting/spec.md`'s `## Purpose` to the corrected text carried in this change's deltas. `specs-apply.js`'s `parseDeltaSpec` reads only ADDED/MODIFIED/REMOVED/RENAMED blocks and rebuilds the preamble from the BASE spec, so a delta's `## Purpose` is never applied — both base Purposes would otherwise keep asserting behavior this change removes. Also write real `## Purpose` text for the two NEW specs (`shared-skeleton`, `loading-state-pattern`) — `specs-apply.js:281` stamps a literal `TBD - created by archiving change …` placeholder otherwise

## 7. Manual verification (evidence for the gates)

- [x] 7.1 Measure `getBoundingClientRect()` before and after resolve on each surface — this is the headline AC, not a screenshot check. Measure PER-ROW/PER-CARD geometry (height, gap, padding, radius, horizontal position), which must match EXACTLY; a total-container-height delta attributable solely to placeholder count is the accepted, documented concession (CR2). Also assert the container never collapses below its empty/error-state floor. Measure `/registry`'s stacked sidebar rows and the dashboard grid FIRST
- [x] 7.1a Measure the grid swap at a saved zoom level other than 1, and confirm the phone stack's card count and horizontal geometry are unchanged on resolve (the height delta is the one accepted exception)
- [x] 7.2 Drive every listed surface's real loading transition in the running app, in both themes, at 1440/768/430
- [x] 7.3 Confirm under `prefers-reduced-motion: reduce` that the shimmer is fully static, with no parked highlight
- [x] 7.3a Judge light-theme shimmer contrast against the CONTAINING surface, not the page: card/modal/detail skeletons sit on `--app-surface` (`#fdfcfa`), and the ramp's `--app-surface-raised` end is `#ffffff` — near-invisible there, so the sweep may read as the bar dissolving. If light looks flat beside dark, pick a different token pair (still tokens only, DESIGN.md §3)
- [x] 7.4 Compare the sidebar sections and the panel grid side by side to confirm one consistent treatment
