- `frontend/src/shared/chrome/sections.ts` — swapped the Assistant nav icon `MessageSquare` → `MessageCircle` and the Metrics nav icon `Gauge` → `ChartNoAxesColumn`, so both no longer read as near-duplicate/ambiguous glyphs at 16px in the collapsed sidebar rail (`Sidebar.tsx`/`BottomNav.tsx` derive from this shared registry).
- `frontend/src/features/dashboards/ui/DashboardAppearanceEditor.tsx` — restyled the "Customize dashboard" trigger from the bespoke `popover__trigger dashboard-appearance-editor__trigger` text-pill onto the shared `cmd-btn cmd-btn--icon` recipe with a `faSliders` icon; removed the visible `__trigger-copy` text span (icon-only, matching its command-bar neighbors); preserved `aria-label="Customize dashboard appearance"` and added a matching `title` tooltip.
- `frontend/src/features/dashboards/ui/DashboardAppearanceEditor.css` — removed the now-redundant `.dashboard-appearance-editor__trigger` pill-radius override and the now-orphaned `.dashboard-appearance-editor__trigger-copy` rule, since the trigger is now a plain `cmd-btn cmd-btn--icon`.
- `frontend/src/app/CommandBar.tsx` — swapped "Refine with AI"'s icon `faCommentDots` → `faWandMagicSparkles` so it's visually distinct from "Open assistant"'s `faComments` glyph.
