# Files modified — HEL-1048

- `openspec/changes/theme-aware-accent-text/design.md` — cycle 4 (skeptic-final-1.md): D8's ruling corrected in place (per the doc's own rule 1) to record repointing all eight `--app-accent-strong`-as-text sites with no exception, and why the earlier `SidebarBody`/`AddSourceModal` carve-outs were wrong; replaced the drifting `SidebarBody.css` line citations with selector names (`.sidebar-body__locked-notice-link` and its `:hover`) — those line numbers went stale twice, and identifiers do not drift.
- `DESIGN.md` — documents `--app-accent-text` (theme-aware derivation, why it diverges from `--app-focus-ring-color`), the D2 scored background set, `::selection`'s fixed-pair rule, and the accepted Yellow login-card consequence.
- `frontend/src/theme/appearance.ts` — adds `deriveAccentTextColor`, the D2 background-set builders (neutral + token-tint + inline-tint), `buildSelectionBackground` (D7), and wires `--app-accent-text`/`--app-selection-bg` into `buildAccentTokens`/`applyAccentTokens` (now theme-parameterized). Cycle 2: exports the D2 constants so `accentTextSourceSyncGuard.css.test.ts` can cross-check them against parsed source instead of a hardcoded copy (CR-2); corrects the comments to stop claiming an independence the runtime code can't have (it can't read CSS files at runtime) and point at the actual drift-detection test.
- `frontend/src/theme/appearance.test.ts` — updates `buildAccentTokens` call sites for the new `theme` parameter; adds `deriveAccentTextColor` coverage (D2 scored-set contrast, producibility, D7 selection fixed-pair). Cycle 2: corrects the "reconstructed independently" comment on the local D2-set mirror (it's a literal copy, not an independent reconstruction — CR-2) and points readers at the real source-parsing check.
- `frontend/src/theme/accentTextClosureGuard.css.test.ts` — new guard: fixpoint transitive closure over the whole stylesheet corpus, asserts no `color: var(...)` anywhere reaches `--app-accent`, mutation-tested against an untouched synthetic rule. Cycle 2 (CR-1): fixed a same-round chain-collapse bug in the test's own closure reimplementation (snapshot `reach` at round start, not mid-round) and added a genuinely two-hop mutation fixture plus a crippled-fixpoint (`maxRounds=1`) counter-check proving the two-hop arm fails for the stated reason. Cycle 4 (skeptic-final-1.md): removed `ALLOWED_EXCEPTIONS` and the test that pinned it — its two "documented exceptions" were shipping a real WCAG failure, not a covered case; the closure is now asserted empty with zero carve-outs.
- `frontend/src/features/sources/ui/AddSourceModal.css` — cycle 4 (skeptic-final-1.md): repoints `.add-source-modal__type-btn--active`'s base and `:hover` `color` from `--app-accent-strong` to `--app-accent-text`. The prior "false positive" justification (the pill's 20% tint is in D2's scored set) was a true fact about the *background* applied to a *foreground* declaration D2 says nothing about; measured live at 2.876:1 for light Yellow, failing all eight presets including Orange (shipped default) at 3.28:1.
- `frontend/src/theme/tokenAuditSweep.css.test.ts` — cycle 4: bumped three pre-existing, unrelated line-number-pinned baseline entries for `AddSourceModal.css` (131→140, 149→158, 240→249) to match the +9 lines this cycle's `AddSourceModal.css` comment additions shifted; each new line verified against the file's actual content before updating, not assumed from arithmetic alone.
- `frontend/src/theme/accentTextSourceSyncGuard.css.test.ts` — new (cycle 2, CR-2): parses the D2 background set (neutral surfaces + token tints from `theme.css`, inline tints from `BottomNav.css`/`AddSourceModal.css`) directly from source, cross-checks it against `appearance.ts`'s exported constants (drift detection), and re-derives the 4.5:1 sweep against the parsed set. Cycle 3 (evaluation-2.md CR-3): the D14 pin block was rewritten to actually `fs.readFileSync` theme.css's bare `:root` block (previously compared the derivation against a literal in the test file, which a garbage-value probe on theme.css proved caught nothing) plus a garbage-value self-test proving the parser is sensitive to theme.css's real content.
- `frontend/src/theme/theme.css` — adds `--app-accent-text`/`--app-selection-bg`, D14 static `:root` fallback, repoints `::selection` to set both `background`/`color` (D7), corrects the runtime-set-tokens comment (D12). Cycle 3 (CR-3, per the evaluator's D12 ruling): **removed** the two per-theme `:root[data-theme=...]` dead-default pairs for `--app-accent-text`/`--app-selection-bg` entirely — they never render, so a wrong value there is an invisible, untestable-by-parsing drift surface (exactly how the cycle-2 wrong-accent bug survived); the single theme-independent `:root` fallback is now the ONLY static literal, and it is pinned by parsing this file directly.
- `frontend/src/theme/ThemeProvider.tsx` — `applyAccentTokens` effect now keyed on `[accentColor, theme]` (D10) and passes `theme` through.
- `frontend/src/shared/chrome/SidebarBody.css` — repoints the locked-notice link to `--app-accent-text`. Cycle 4 (skeptic-final-1.md): the `:hover` rule's earlier "named exception" (kept on `--app-accent-strong`) was a real WCAG failure (D8's own published figures: Orange 3.93, Cyan 3.45, Green 3.24, Yellow 2.78 in light) — repointed to `--app-accent-text` too; `text-decoration-thickness: 2px` alone now carries the hover state.
- `frontend/src/shared/ui/toast.css` — repoints `.toast--info`'s `--toast-intent-color` alias at `--app-accent-text` (D9 two-hop chain fix); other three intents untouched.
- `frontend/src/features/dashboards/ui/DashboardList.css` — repoints the `--app-info`-aliased badge text and two direct `--app-accent` text sites at `--app-accent-text` (D9/repoint).
- `frontend/src/shared/chrome/BottomNav.css` — repoints the active-tab text color (D6's inline-tint background site is scored, not repointed itself).
- `frontend/src/features/assistant/ui/MessageTurn.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/assistant/ui/ProposalHandoff.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/assistant/ui/QuickLauncherOverlay.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/auth/ui/auth.css` — repoints accent-as-text (includes the AC-2 login-footer "Create one" link) to `--app-accent-text`.
- `frontend/src/features/onboarding/ui/OnboardingChecklist.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/panels/ui/MarkdownPanel.css` — repoints accent-as-text to `--app-accent-text` (the theme-token-bound site; the user-chosen-surface site in this same file is out of scope, HEL-1057).
- `frontend/src/features/panels/ui/OutputPicker.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.appearance.css` — repoints `--app-accent-strong`-as-text (D8) to `--app-accent-text`.
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.binding.css` — repoints one `--app-accent`-as-text and two `--app-accent-strong`-as-text (D8) sites to `--app-accent-text`.
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.sections.css` — repoints `--app-accent-strong`-as-text (D8) to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/CreatePipelineModal.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/OutputsRail.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/PipelineDetailHeader.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.css` — repoints 12 accent-as-text sites and 1 `--app-accent-strong`-as-text (D8) site to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/PipelinesPage.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/pipelines/ui/RunHistoryModal.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/features/sources/ui/SourceListTable.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/shared/ui/DataGrid.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/shared/ui/EmptyState.css` — repoints 2 accent-as-text sites to `--app-accent-text`.
- `frontend/src/shared/ui/SchemaFieldViewer.css` — repoints 2 accent-as-text sites to `--app-accent-text`.
- `frontend/src/shared/ui/SortableTh.css` — repoints 2 accent-as-text sites to `--app-accent-text`.
- `frontend/src/shared/ui/StatusChip.css` — repoints accent-as-text to `--app-accent-text`.
- `frontend/src/shared/ui/inputs.css` — repoints accent-as-text to `--app-accent-text`.
- `openspec/changes/theme-aware-accent-text/tasks.md` — all tasks marked complete.
