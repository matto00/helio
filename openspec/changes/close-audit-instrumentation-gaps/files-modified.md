- `backend/src/main/scala/com/helio/infrastructure/persistence/auth/UserRepository.scala` — `upsertGoogleUser` now returns `Future[(User, Boolean)]` (`wasCreated`), computed from the existing `findByGoogleId` branch (no extra DB round trip).
- `backend/src/main/scala/com/helio/services/auth/AuthService.scala` — `completeOAuth` destructures `(user, wasCreated)` and emits `auth.register` before the existing `auditLoginOutcome` call when `wasCreated`.
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — `refresh` wraps its dispatch result in `.map`, emitting `data_source.refresh` (resourceId = the returned `DataSource.id`) only on `Right`.
- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — private `audit` helper widened to accept an `action` param (default `"data_source.create"`, existing two call sites unedited); `refresh` wraps its dispatch result in `.map`, emitting `data_source.refresh` (resourceId = the pre-dispatch `sourceId.value`) only on `Right`.
- `backend/src/test/scala/com/helio/api/AuditMutationInstrumentationSpec.scala` — new `"DataSourceService.refresh / SourceService.refresh audit instrumentation (HEL-840)"` section: static refresh success/failure via HTTP end-to-end; csv/text/pdf/image refresh success via a dedicated `DataSourceService` instance (real audit repo); CSV refresh failure (negative-assertion barrier); sql refresh success/failure via HTTP end-to-end (embedded Postgres); rest refresh success/failure via a dedicated `SourceService` instance with a stubbed connector.
- `backend/src/test/scala/com/helio/api/routes/auth/GoogleOAuthRoutesSpec.scala` — added real embedded-Postgres `AuditEventRepository`/`AuditService` fixtures (this spec had none before — `auditService` stayed null-default), a `withAudit` flag on `makeAuthService`, and two new test cases: first-time Google signup writes `auth.register` + `auth.login`; returning Google login writes no `auth.register` row (negative-assertion barrier).
- `openspec/changes/archive/2026-08-26-instrument-audit-mutations/route-audit-enumeration.md` — marked all three tracked gaps (`DataSourceService.refresh`, `SourceService.refresh`, `WorkspaceTeardownService.teardown`) as closed (HEL-840/HEL-838).
