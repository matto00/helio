- `backend/src/main/scala/com/helio/services/PipelineProposalService.scala` — new: `PipelineProposalService.apply` — structural pre-validation (D1/D2), source resolution + inline-source creation with companion-DataType-id capture (D3/D4/D5), pipeline+steps+run orchestration, and full rollback on any mid-apply failure, composing only `SourceService`/`DataSourceService`/`PipelineService`/`PipelineRunService`/`DataTypeService`.
- `backend/src/main/scala/com/helio/api/routes/PipelineProposalRoutes.scala` — new: `POST /api/pipelines/apply-proposal` route, mirrors `DashboardProposalRoutes`.
- `backend/src/main/scala/com/helio/api/protocols/PipelineProposalProtocol.scala` — adds `PipelineProposalApplyResponse` case class + `RootJsonFormat` (design.md D7); trait now also extends `PipelineProtocol` for `PipelineSummaryResponse`/`RunResultResponse`.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — constructs `PipelineProposalService` (composing the existing `sourceService`/`dataSourceService`/`pipelineService`/`pipelineRunService`/`dataTypeService` instances) and mounts `PipelineProposalRoutes` alongside `PipelineRoutes`/`PipelineStepRoutes`.
- `backend/src/test/scala/com/helio/api/PipelineApplyProposalSpecBase.scala` — new: shared embedded-Postgres + real-RLS fixture for apply-proposal specs (mirrors `ApplyProposalSpecBase`), with a URL-keyed stub `RestApiConnector`, a pre-seeded caller-owned and other-user-owned static source, and per-table row-count helpers.
- `backend/src/test/scala/com/helio/api/PipelineApplyProposalSpec.scala` — new: happy-path (inline static, existing sourceId) and structural pre-validation guardrail tests (D1/D2/D3).
- `backend/src/test/scala/com/helio/api/PipelineApplyProposalRollbackSpec.scala` — new: mid-apply rollback tests (run failure, source-fetch failure, static-branch addStep failure) and RLS-enforcement test, each asserting the four resource counts are unchanged.
- `openspec/changes/atomic-pipeline-proposal-apply/tasks.md` — all tasks marked complete.
