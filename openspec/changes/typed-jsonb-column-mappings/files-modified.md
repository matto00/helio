- `backend/src/main/scala/com/helio/infrastructure/DashboardRepository.scala` — added `dashboardAppearanceColumnType` and `dashboardLayoutColumnType` in companion; changed `DashboardRow.appearance`/`.layout` from `String` to typed domain types; removed `jsonbStringType`; removed manual `parseJson`/`toJson.compactPrint` in `rowToDomain`, `domainToRow`, and `update`
- `backend/src/main/scala/com/helio/infrastructure/PanelRepository.scala` — added `panelAppearanceColumnType` in companion; changed `PanelRow.appearance` from `String` to `PanelAppearance`; replaced `O.SqlType("jsonb")` on `fieldMapping` with implicit `jsonbStringType`; removed manual `parseJson`/`toJson.compactPrint` in `updateAppearance` and `batchUpdate`
- `backend/src/main/scala/com/helio/infrastructure/PanelRowMapper.scala` — removed `.parseJson.convertTo[PanelAppearance]` in `rowToDomain` and `.toJson.compactPrint` in `domainToRow`; `appearance` is now passed through directly as `PanelAppearance`
- `backend/src/main/scala/com/helio/infrastructure/DataTypeRepository.scala` — added `dataFieldsColumnType` and `computedFieldsColumnType` in companion; changed `DataTypeRow.fields`/`.computedFields` from `String` to typed domain types; removed `jsonbStringType`; removed manual `parseJson`/`toJson.compactPrint` in `rowToDomain`, `domainToRow`, `update`, and `updateInternal`
- `openspec/changes/typed-jsonb-column-mappings/tasks.md` — marked all tasks complete
- `openspec/changes/typed-jsonb-column-mappings/files-modified.md` — this handoff file
