- `frontend/src/features/pipelines/ui/PipelineRiverView.test.tsx` — new `describe("nodePath wiring (HEL-985)", ...)` block: an 8-step, two-root fixture covering all four prop-threading edges (E1 trunk, E2 trunk-step child lane compact+non-compact, E3 second root via `RootColumn`, E4 lane nested under another lane), a shape-verification test, and six exact-string `title`-attribute assertions. No product file changed — see `mutation-evidence.md` for the two reverted mutations proving the guard is real.
