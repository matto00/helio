- `backend/src/main/scala/com/helio/services/workspace/WorkspaceTeardownService.scala` — added nullable-optional `auditService: AuditService = null` constructor param, a private `audit(...)` helper mirroring `DashboardService`, and a single `workspace.teardown` audit call gated strictly on `outcome.committed` (never on nonzero counts), carrying `sourcesDeleted`/`pipelinesDeleted`/`typesDeleted` in metadata.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — wired the existing `auditService` private val through the `workspaceTeardownServiceOpt` construction site.
- `backend/src/test/scala/com/helio/api/AuditMutationInstrumentationSpec.scala` — added `dbContext` wiring to `routesFor()` so `POST /api/workspace/teardown` is mounted in this route-level spec, plus four new tests: committed teardown writes one row with correct actor/resourceId/metadata, committed all-zero-match teardown still writes one row, dryRun writes no row (barrier technique), blocked writes no row (barrier technique).
- `openspec/changes/instrument-teardown-audit/tasks.md` — marked all tasks complete.
