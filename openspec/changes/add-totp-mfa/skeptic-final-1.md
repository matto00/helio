## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

**Ground truth re-established (not trusted from evaluator reports)**
- `git log --oneline main..HEAD`: 2 commits (5631cc2c, 181f5b3e), matches claimed cycle 1+2 commits.
- `git diff main...HEAD --stat`: 69 files changed, matches `files-modified.md`'s inventory (spot-checked every backend file listed against the real diff — no undisclosed files, no missing entries).
- `git status --short` in the worktree before I touched anything: only the evaluator's pre-existing `evaluation-2.md` (untracked) + `workflow-state.md` edit — nothing else pending.

**Gates re-run fresh by me (not pasted from prior reports)**
- `cd backend && sbt "testOnly com.helio.api.MfaApiRoutesSpec com.helio.services.MfaServiceSpec com.helio.infrastructure.TotpSupportSpec com.helio.api.GoogleOAuthRoutesSpec"` → 62/62 passed, migration V89 applied cleanly on top of 87 (V88 gap intact, per dispatch).
- `cd backend && sbt test` (full suite) → **3191/3191 passed**, matches evaluation-2.md's claim.
- `cd frontend && npm run lint` → zero warnings. `npm run format:check` → clean.
- `cd frontend && npm test` (full suite, ran twice — once via a `--testPathPatterns` filter that turned out to match nearly everything, then unfiltered to be sure) → **1892/1892 passed**, matches claim.
- `npm run check:schemas` → clean, 65 protocols checked (MFA's 4 new schemas included; cross-checked `MfaProtocol.scala`'s case classes against `schemas/mfa-{enroll-response,required-response,status-response,verify-request}.schema.json` by hand — field-for-field match. Not every MFA response type has a schema (`MfaBackupCodesResponse`, `MfaConfirmRequest`, `MfaReauthRequest` don't) — confirmed this matches existing repo convention: `AuthResponse`/`LoginRequest`/`RegisterRequest` also have no schema files, so this isn't new drift).
- `npm run check:scala-quality` → clean; only pre-existing file-size soft-budget warnings (121 total, none newly introduced — `MfaServiceSpec.scala` at 336 lines is the only MFA file over the 250-line test soft-budget, informational only).
- `npm run check:openspec` → fails with exactly one issue: "change is complete (26/26) but not archived" — confirmed this is the only failure (archiving is post-gate, per the human-instructed dispatch note), not masking anything else.
- Confirmed DB isolation: `backend/.env` → `DATABASE_URL=jdbc:postgresql://localhost:5432/helio_hel702`; live `flyway_schema_history` query shows `89 | totp mfa` at the head with `87` immediately below it (V88 gap intact, as documented/instructed).
- Both pre-commit `-n` bypasses (5631cc2c, 181f5b3e) are explicitly disclosed in their commit messages with the same justification given in my brief (check:openspec's archive-completeness gate can't pass pre-archive) — satisfies CLAUDE.md's "call it out explicitly" bypass rule.

**Acceptance criteria — traced to real evidence, live-tested by me end-to-end**

I registered a brand-new user (`skeptic702@helio.dev`) via the UI (not reusing the executor's pre-enrolled `hel702test@helio.dev` fixture) and drove the full lifecycle myself, computing real RFC 6238 codes from the enrolled secret with a small Node script I verified against the RFC 6238 Appendix B test vector (`HMAC-SHA1("12345678901234567890", step 59) → 287082`, matched) before using it against the live secret.

1. **"Enroll in TOTP MFA from Settings (QR, confirms with a real code)"** — Settings → Security → "Enable two-factor authentication" opened a modal with a QR code and manual key `JNGEGYZA3H44OHWYT7CG3KOHFYVP74E5`. Computed a real code (`426103`), submitted it, got `enabled: true`, 10 backup codes displayed exactly once. **MET.**
2. **"Both password-login and Google-OAuth-login paths require a valid TOTP code (or backup code) before a session is established"**
   - Password path, live-tested by me: signed out, signed back in → landed on `/login/verify`, not `/`. Confirmed via raw `curl -sD -` against the backend that the login response carries `{"challengeToken":"...","mfaRequired":true}` with **no `Set-Cookie` header** and no `user` field. Submitted a wrong code (`000000`) → rejected with **"Invalid or expired code"** inline, no navigation. Submitted a real computed code (`845300`) → session established, landed on `/`. **MET.**
   - Backup-code branch of the same login-verify gate, live-tested: toggled "Use a backup code instead", submitted one of the 10 saved codes → session established. Confirmed single-use consumption: Settings then showed "9 backup codes remaining" (down from 10).
   - OAuth path: not live-testable (no Google OAuth test credentials in this environment, consistent with evaluation-1.md's note) — verified instead by reading `GoogleOAuthRoutesSpec.scala:330-384`'s "MFA enabled" test, which I ran and confirmed passing: it enables MFA for a real OAuth-created user, drives a second `/api/auth/google/callback`, and asserts `resp.mfaRequired shouldBe true`, `header[Set-Cookie] shouldBe None`, and the raw response body excludes `"user"`. Combined with the `OAuthRoutes.scala` diff showing `completeOAuth` branches on the identical `LoginOutcome` sealed trait the password path uses (same `finishLogin` call, same `MfaRequiredResponse` shape) — this is structural equivalence backed by a real, passing route-level test, not just an assertion. **MET** (test-verified; not independently live-clicked).
3. **"Backup codes are single-use and let a user recover access without their authenticator app"** — live-verified above (successful login via a saved backup code with zero TOTP entry; remaining count dropped 10→9 immediately after). Reuse-rejection of an already-consumed code was not separately re-driven live by me (would have required a second enroll/disable cycle) but is asserted in `MfaApiRoutesSpec`'s "used backup code rejected" case, which passed in my fresh `sbt test` run. **MET.**
4. **"A user can disable MFA (re-authenticating first)"** — live-verified: clicked "Disable", submitted wrong code `111111` → rejected inline with "Invalid or expired code", MFA stayed enabled (button state unchanged). Submitted a real computed code (`986461`) → "Two-factor authentication disabled." toast, Security section reverted to "Disabled" state. Then signed out and back in with the same password → landed directly on `/` (no `/login/verify` detour), confirming the account is no longer gated post-disable. **MET.**

**D1–D7 design compliance, checked against real code**
- D1 (java-otp + commons-codec, client-side QR): confirmed in `build.sbt`, `TotpSupport.scala` (delegates HMAC generation to `java-otp`, owns only window/replay logic), `MfaEnrollModal.tsx` (renders `QRCodeSVG` client-side from the `otpauthUri` the backend returns).
- D2 (V89, 3 tables, no RLS): confirmed via the live `flyway_schema_history` query and reading `V89__totp_mfa.sql` — matches the spec's column/constraint list exactly. `MfaRepository` takes a raw `Slick Database` (not `DbContext`), matching `UserRepository`'s own precedent for pre-identity tables — checked both files side-by-side.
- D3 (minimal one-call-site hunks, defaulted ctor param): read the actual `AuthService.scala`/`AuthRoutes.scala`/`OAuthRoutes.scala` diffs — `login`'s success branch is a single `finishLogin(user)` call, `completeOAuth`'s session-mint line is the same swap, `mfaService: Option[MfaService] = None` keeps every un-updated `new AuthService(userRepo)` call site compiling. This is exactly the isolated, mechanically-resolvable shape the HEL-703 dispatch note asked for — judged against that stated goal, not against "should this look different."
- D4 (5-min TTL, 5-attempt cap): `MfaService.ChallengeTtlSeconds = 300`, `MaxAttempts = 5`, exercised by passing `MfaServiceSpec`/`MfaApiRoutesSpec` cases (attempt-cap exhaustion, expiry).
- D5 (±1-step window, replay guard via `last_used_step`, code-shape dispatch): read `TotpSupport.verify` — filters steps `> lastUsedStep`, tries `currentStep-1..currentStep+1`. `looksLikeTotpCode` (6-digit regex) drives the TOTP-vs-backup-code dispatch in `MfaService.verifyCurrentCode`. Backed by `TotpSupportSpec`'s RFC 6238 Appendix B vectors (passing).
- D6 (enrollment API shapes, re-auth uniform mechanism): all 5 endpoints match the spec's status codes and bodies exactly, live-confirmed for enroll/enroll-confirm/disable, test-confirmed for regenerate.
- D7 (transient `mfaChallenge` in `authSlice`, `/login/verify`, Settings "Security" section): read `authSlice.ts` — `mfaChallenge` is cleared on `verifyMfa.fulfilled`/`clearAuth`/logout, retained on `verifyMfa.rejected` (so the user can retry) — matches the stated intent.

**Cycle-1 → cycle-2 change requests, independently re-verified (not trusted from evaluation-2.md's say-so)**
- CR1 (misleading error text): read the `MfaService.scala` diff — all 8 `ServiceError.Unauthorized()` call sites now reference one shared `InvalidCode` companion-object val with the corrected message. Live-reproduced myself on **two separate surfaces** (`/login/verify` wrong-code, Settings "Disable" wrong-code) — both showed **"Invalid or expired code"**, not the old "Invalid email or password". `MfaApiRoutesSpec`'s new message-content assertions passed in my fresh run.
- CR2 (hardcoded `#ffffff`): read the `MfaSecuritySection.css` diff — `color: var(--app-bg)` replacing the literal hex, with an inline comment citing the precedent. I went one step further than evaluation-2.md's light-theme-only computed-style check: I checked the **dark-theme** computed style on the live "Disable" button and got `rgb(18, 17, 16)` (a near-black `--app-bg` dark value, not white) — confirms the token resolves correctly in both themes, not just the one the evaluator happened to check.

**UI / design judgment (my domain) — screenshots taken and inspected, not just accessibility-tree-read**
- Settings "Security" section, enroll modal, backup-codes display, and the `/login/verify` two-factor card were all screenshotted and visually inspected in **both light and dark theme**. Every new component uses `--app-*`/`--space-*`/`--text-*` tokens exclusively (confirmed by reading `MfaEnrollModal.css`, `MfaSecuritySection.css`, `MfaBackupCodesList.css` in full — zero remaining hardcoded hex outside the one documented QR-contrast exception).
- Shared-component reuse confirmed by reading `MfaEnrollModal.tsx`/`MfaVerifyPage.tsx`: `Modal`, `TextField`, `InlineError` from `shared/ui`/`shared/chrome` are used rather than reinvented; `MfaVerifyPage` reuses the existing `auth-page`/`auth-card`/`auth-field`/`auth-error` classes from `LoginPage`'s own `auth.css`, giving it visual parity with the sibling login/register cards (confirmed via screenshot — same card treatment, same "Helio" brand mark, same typographic hierarchy).
- The one hardcoded-color exception (`MfaEnrollModal.css`'s fixed white QR background) is inline-documented as a fixed-contrast scannability requirement, citing the same documented-exception class DESIGN.md already carves out for accent-preset swatches — a legitimate, narrow exception, not a token-discipline lapse.
- No console errors during any tested flow beyond the pre-existing benign `/api/auth/me` 401 rehydration-check pattern (present before this ticket, unrelated).
- Interactive elements have real accessible names throughout (`"Authentication code"`, `"Backup code"`, `"Manual entry key"`, `"Copy manual entry key"`, `"Re-authentication code"`), confirmed via accessibility snapshots at each step.

### Verdict: CONFIRM

All four acceptance criteria are met and traced to live-reproduced evidence (three of four fully live end-to-end by me; the fourth — OAuth-path gating — is structurally identical code backed by a real, currently-passing route-level test, with no live OAuth credentials available in this environment to go further). Both prior evaluator change requests are genuinely fixed, not just claimed — I independently reproduced both fixes live rather than trusting evaluation-2.md's report, and found the color-token fix holds in dark theme too (a check the evaluator's report didn't show). Design-token discipline, shared-component reuse, and light/dark parity are all solid on inspection of actual screenshots. Gates are green on my own fresh, from-scratch reruns, not pasted from the evaluator. The two `-n` pre-commit bypasses are properly disclosed and match the documented, human-approved constraint (check:openspec's archive-completeness gate necessarily fails pre-archive). The V88/V89 migration gap and DB isolation are exactly as the dispatch instructed and are not defects.

### Non-blocking notes

- `frontend/src/features/settings/state/settingsSlice.ts` is 440 lines, over the ~400-line CONTRIBUTING.md split-proposal trigger. Already flagged in evaluation-1.md and explicitly deferred per orchestrator direction — carrying it forward as a follow-up, not re-blocking on it.
- AC3's "used backup code rejected" reuse path was verified via a passing automated test (`MfaApiRoutesSpec`) rather than a second live click-through by me in this session (would have required a full second enroll/disable cycle) — flagging this distinction for completeness, not as a defect; the test coverage is real and meaningful (it's paired with the exact same DB-level single-use mechanism I did watch decrement live).
