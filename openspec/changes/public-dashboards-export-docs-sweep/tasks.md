## 1. Public read path (API + RLS only — no viewer UI; see task 1.4)

- [x] 1.1 Add `GET /api/dashboards/:dashboardId/panels/:panelId/rows` to `PublicDashboardRoutes`, resolving `panelId → outputId → node_snapshot` via `panelRepo.findAllByDashboardId` (same lookup the panel-list route already uses) + an unauthenticated-safe `OutputService`/`OutputRepository` rows-by-internal-lookup, gated by the existing `authorizeResourceWithSharing("dashboard", ...)` directive. Verify: a curl against a shared dashboard's panel returns rows with no auth header; against a non-shared dashboard returns an authorization error.
- [x] 1.2 Add backend route-spec coverage (`PublicDashboardRoutesSpec` or equivalent) exercising all three `public-dashboards` spec scenarios at the HTTP level: shared-dashboard success, non-shared-dashboard authorization error, and a panel whose Output/pipeline no longer resolves returning an empty rows result (not a 500). Verify: spec passes; this task's own verification IS the "public path works" proof for this ticket — no frontend consumer exists to additionally verify against (see task 1.4).
- [x] 1.3 Add an RLS smoke test (non-superuser `SET ROLE`) proving cross-tenant denial on `outputs`/`node_snapshots` via the public path, and proving itself red by dropping the tested policy first. Verify: test fails when the policy is dropped, passes when restored.
- [x] 1.4 File a follow-up Linear ticket for the public-dashboard viewer page + dashboard share affordance (neither exists in this codebase today — `frontend/src/app/AppRoutes.tsx` has no `/dashboards/:id`/`/share/*`/`/public/*` route and nothing consumes `GET /api/dashboards/:id/panels`), blocked-by this row, and note its id in this PR. Verify: ticket filed, id recorded. (Filed as **HEL-941** by the orchestrator on 2026-09-02, since the executor lacks Linear tool access — see `workflow-state.md`'s cycle-1 note.)

## 2. Export / import — outputId existence + appearance/cross-field validation (no wire-shape reshape)

- [x] 2.1 Add `outputRepo: OutputRepository = null` as a new trailing defaulted constructor parameter on `DashboardService` (mirrors the existing `auditService = null` convention); update the 1 production construction site (`ApiRoutes.scala:242`) to pass the real repo; the 16 test-fixture sites (`grep -rn "new DashboardService(" backend/src`) keep compiling unchanged on the `null` default. In `importSnapshot`, for each output-kind panel entry call `outputRepo.findByIdOwned(outputId, user)` directly, mapping `None` to `ServiceError.BadRequest` naming the unresolvable `outputId` (a `null` `outputRepo` skips the check, matching `PanelService.rejectMissingOutput`'s own existing convention). Verify: round-trip export→import of a migrated dashboard produces an identical dashboard against the same pipelines; a NEW or existing import-focused test spec, constructed with a real (non-null) `outputRepo`, proves import with a fabricated/foreign `outputId` returns a named `400` and creates nothing — a fixture left on the `null` default would make this assertion vacuous, so this task is not done until at least one such non-null-wired test exists.
- [x] 2.2 Close the real HEL-628 gap without touching the repository layer: in `DashboardService.importSnapshot`, before calling `dashboardRepo.importSnapshot`, iterate `payload.panels` and for each entry run `PanelConfigCodec.decodeCreateConfig(entry.type, Some(entry.config))` (config decode) then construct the corresponding `Panel` and call its own `.validateConfig` (the same method `PanelService.buildForCreate` itself calls, not a `PanelService`-only capability — e.g. `OutputPanel.validateConfig`), plus the existing appearance-payload decode/validate path. Return `ServiceError.BadRequest` on the first failing entry, before any repo write. Remove/update `DashboardServiceValidation.scala:39-50`'s in-code comment documenting the old skip. Verify: a payload with an appearance/cross-field violation direct panel creation would reject is also rejected on import, with nothing created; `DashboardSnapshotRepository.importSnapshot`'s own construction/id-minting logic is unchanged (no repo signature change).
- [x] 2.3 Confirm `schemas/dashboards/*export*`/`*import*` and OpenSpec already match this (no wire-shape change expected); update only if a real drift is found. Verify: `check:schemas` and `check:openspec` green.

## 3. HEL-940 fold-in — dataTypeId/DataTypeId/metricId/type_id → outputId (fix the class, not the instance; see design.md Decision 6 for the sweep allowlist)

- [x] 3.1 Enumerate every live reference (per design.md Decision 6's allowlist — excludes `db/migration`, `openspec/changes/**` archives, and `HEL-NNN`-prefixed historical-removal comments) to `dataTypeId`, `DataTypeId` (incl. `WorkspaceContextProtocol`'s `leftDataTypeId`/`rightDataTypeId`/`outputDataTypeId`), `metricId`, and `type_id` via `grep -rn` across `backend/src`, `frontend/src`, `helio-mcp/src`, `schemas/`, `openspec/specs/`, recording the axis list (protocols / services / frontend / helio-mcp / schemas / openspec specs) in this file or a scratch note before editing.
- [x] 3.2 Rename on backend wire protocols (`DashboardProposalProtocol`, `CombinedProposalProtocol`, `AssistantProposalToolSchemas`, `WorkspaceContextProtocol` incl. its `leftDataTypeId`/`rightDataTypeId`/`outputDataTypeId` fields) and their service-layer readers/writers (`ProposalPanelSupport`, `CombinedProposalService`, `WorkspaceContextComputations`, `PatchSet*` resolvers, `PanelRepository`, `PipelineRepository`, `PanelService`). Verify: `sbt test` green.
- [x] 3.3 Rename on frontend proposal/patch-set review surfaces and tests (`outputsService.ts`, `proposal.ts`, `ProposalReviewPage.tsx`, `ProposalReview.tsx`, `CombinedProposalReviewPage.tsx`, `CombinedProposalReview.tsx`, their `*.test.tsx` twins). Verify: `npm run typecheck` and `npm test` green.
- [x] 3.4 Triage every helio-mcp hit found in 3.1 (live tree: ~40 `dataTypeId` + ~8 `DataTypeId` + ~5 `metricId` as of design time — re-`grep` for the current count, do not trust this number) and fix every live tool schema; the sweep in §7 requires zero hits here too (ticket.md AC 6), so nothing may be discharged by filing a follow-up ticket — if a hit requires scope this ticket genuinely cannot absorb, escalate it to the human rather than deferring silently. Verify: `grep -rn "dataTypeId\|DataTypeId\|metricId" helio-mcp/src` returns nothing outside an allowlisted historical comment (design.md Decision 6).
- [x] 3.5 Re-`grep -rln "dataTypeId\|DataTypeId\|metricId" schemas/` (do not trust a hardcoded count — 5 files as of design time, including `schemas/workspace/workspace-context.schema.json`'s live *required* `leftDataTypeId`/`rightDataTypeId`/`outputDataTypeId` fields that 3.2 already renames on `WorkspaceContextProtocol`) and rename every live hit found; triage `schemas/panels/panel.schema.json` and `schemas/pipelines/pipeline-analyze-proposal-response.schema.json` against the design.md Decision 6 allowlist clause (iii) first — a HEL-NNN-prefixed `description` string needs no rename. Verify: `check:schemas` green.
- [x] 3.6 Add `MODIFIED Requirements` spec deltas in this change for every live `openspec/specs/**` capability file carrying `dataTypeId` (19 files as of design time, incl. `patch-set-apply`, `patch-set-preview`, `pipeline-proposal-contract`, `mcp-panel-composition-tools`, `mcp-edit-in-place-tools`, `assistant-conversation-loop` — re-enumerate via `grep -rln dataTypeId openspec/specs` before writing deltas, do not trust this list). Verify: `openspec validate --type change --strict` passes with all deltas included; `check:openspec` green after archive.
- [x] 3.7 Re-run proposal/patch-set review Playwright coverage (if any exercises this field). Verify: green. (No `e2e/` spec targets proposal review, patch-set review, or `dataTypeId`/`outputId` fields — `find e2e -iname "*proposal*" -o -iname "*patch-set*"` is empty. Nothing to re-run; frontend Jest coverage for these surfaces was already re-verified green in cycle 2.)

## 4. Docs

- [x] 4.1 Update `README.md` "How data flows" and feature bullets (drop "Metrics layer"). Verify: no `DataType`/`Metric`/`/registry`/`/metrics` reference remains in `README.md`.
- [x] 4.2 Verify `docs/agent-native.md` documents the canonical Source→Pipeline→Output→Dashboard path and the P1.4 tool rename table. Verify: matches current `helio-mcp` tool set. (Confirmed already current — an earlier row already updated this file; this branch changes it by zero bytes, per skeptic-final-1.md note 2.)
- [x] 4.3 Update `CLAUDE.md`'s key-endpoint list (`/api/outputs/*`, single-call `POST /api/pipelines`, removed `/api/types`, `/api/metrics`). Verify: every listed endpoint exists in `ApiRoutes.scala`.
- [x] 4.4 Update `openspec/` project-level descriptions and audit onboarding/empty-state copy for DataType/Metric stragglers. (The `openspec/specs/**` capability-file renames themselves are §3.6's job, not this task's — this task is project-level prose only.)

## 5. Rebuild scripts (sibling repos)

- [x] 5.1 Update `~/Development/helio-news/news/helio_client.py` (and callers) to `create_pipeline`/`place_outputs`, simplify the panel-id lookup (absorbs HEL-626), run once against dev, commit in that repo, and record the commit/PR link in this PR. (Commits `e6f2b39` + `a9b693b` in `~/Development/helio-news`, a separate git repo, not this one. NOT run live against a dev helio-mcp instance: `config/outlets.yaml`'s default `HELIO_API_BASE_URL` points at the PROD backend, and a real run needs live RSS/ollama/Linear/git-log dependencies unavailable in this session — running it would create real resources against production. Verified instead via the sibling repo's full rewritten test suite: 101/101 passed.)
- [x] 5.2 Search `~/Development` for a `delivery-analytics`/`delivery-analytics-v2` script; if found, update and run it the same way; if not found, state that explicitly in this PR. (Re-confirmed: `find ~/Development -maxdepth 4 -iname "*delivery-analytics*"` returns only an unrelated screenshot, `helio/docs/images/delivery-analytics.png`. No such script exists.)

## 6. End-to-end proof

- [x] 6.1 Write a Playwright spec: source → pipeline → three Outputs → dashboard, asserting ≤ 12 interactions (clicks + Enter, typing excluded) from "New pipeline" with a pasted table. Verify: spec passes locally against the dev servers.
- [x] 6.2 Write a Playwright spec/extend the above: placing an already-existing Output on a dashboard in ≤ 2 interactions. Verify: spec passes.
- [x] 6.3 Wire both into the CI e2e job; confirm the P1.4 Sleeper MCP rebuild is already wired in (or document why it needs a manual gate).

## 7. Final sweep and confirm main is a working app

- [x] 7.1 Confirm main is genuinely a working web app end-to-end (decision 17) by starting dev servers on this worktree's base commit and walking source→pipeline→output→dashboard manually or via an existing e2e spec, not by assuming P1.6 closed the gap.
- [x] 7.2 Run the full grep sweep (`com\.helio\..*DataType`, `DataTypeId`, `MetricDefinition`, `MetricId`, `type_id`, `dataTypeId`, `metricId`, `/registry`, `/metrics`, `computed_fields`, `@deprecated`, `TODO(remodel)`) across `backend/`, `frontend/`, `helio-mcp/`, `e2e/`, `schemas/`, `openspec/`, `docs/`, `README.md`, `CLAUDE.md`. Verify: zero hits outside the design.md Decision 6 allowlist (db/migration; `openspec/changes/**` archives; HEL-NNN-prefixed comments/schema descriptions; tests asserting absence of the retired route/field) — NOT the narrower "db/migration and git history" wording, which Decision 6 supersedes; paste the clean sweep output (including a one-line note per allowlisted hit) into the PR description.
- [x] 7.3 If `e2e/hel813-mobile-touch-target-floor.spec.ts` is touched by any of the above, fix the documented poll-then-walk race at `e2e/support/touchTargetProbe.ts:100-115`; otherwise leave it and, if it's still flaky, file a follow-up ticket rather than silently ignoring it.
