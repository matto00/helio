- `scripts/check-schema-drift.mjs` — added `"DataSourceResponse"` to the `SKIP` set (polymorphic union over 7 subtypes, no single matching case class; mirrors the existing `"Panel"` entry).
- `schemas/sources/data-source.schema.json` — new: documents the `DataSourceResponse` union (`GET/POST /api/data-sources`, `GET /api/data-sources/:id`) as a hand-authored `oneOf` over its 7 case-class subtypes, including the shared `inferredSchema` field.
- `schemas/pipelines/node-capabilities-response.schema.json` — new: documents `NodeCapabilitiesResponse` (`GET /api/pipelines/:id/capabilities`), inlining `PanelCapabilityColumnResponse`/`PanelCapabilityResponse`'s shapes rather than adding them as separate schema files.
- `schemas/pipelines/expand-pipeline-shape-response.schema.json` — new: documents `ExpandPipelineShapeResponse` (`POST /api/pipeline-shapes/:id/expand`), with `outputs` modeled as an absent-optional key (never `null`), matching the actual spray-json wire convention.
- `openspec/changes/remaining-output-pipeline-schemas/` — proposal/design/tasks/ticket/skeptic-design-1/workflow-state (planning artifacts, previously untracked) plus this handoff file, committed together with the implementation.
