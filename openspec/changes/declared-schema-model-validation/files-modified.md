- `backend/src/main/scala/com/helio/domain/model/DataSource.scala` — new `DatasetFieldDeclaration(name, fieldType, required, default)` model + its spray-json codec (absent-vs-null normalization, `validateAndCanonicalize`/`asString` round-trip, `StringType` fallback on unreadable stored type).
- `backend/src/main/scala/com/helio/domain/engine/DatasetRowValidator.scala` (new; revised in cycle 2 per skeptic-final-1.md CR1) — the pure write-time validator: per-type acceptance rules, missing/required/default handling, pinned error-message rendering. Every rejection now uses the ONE pinned `"expected <type>, got <kind>"` template — the non-integral-integer and unparseable-timestamp cases no longer carry bespoke wording.
- `backend/src/main/scala/com/helio/domain/engine/TimestampParsing.scala` (new) — `looksLikeTimestamp` extracted from `SchemaInferenceEngine.isTimestamp` (behavior-preserving), shared by the validator.
- `backend/src/main/scala/com/helio/domain/engine/SchemaInferenceEngine.scala` — delegates `isTimestamp` to the new shared helper; unused time-parsing imports removed.
- `backend/src/main/scala/com/helio/infrastructure/persistence/sources/DataSourceRepository.scala` — `insertDatasetSource`/`replaceDatasetRows`'s `declaredColumns` parameter is now `Vector[DatasetFieldDeclaration]` instead of `Vector[SchemaField]`.
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — `createStatic`/`applyStaticRefresh` build the richer declaration, validate defaults at declaration time, and route every row through `DatasetRowValidator` before any repository write; rejects join into one pinned `ServiceError.BadRequest` message.
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceProtocol.scala` — `StaticColumnPayload` gains `required: Option[Boolean]`/`default: Option[JsValue]` (absent-defaults to `false`/`None`).
- `backend/src/test/scala/com/helio/domain/engine/DatasetRowValidatorSpec.scala` (new; revised in cycle 2) — unit tests for every scenario in spec.md, verbatim, including exact-string assertions (not `include(...)`) for the non-integral-integer case and a new unparseable-timestamp case (skeptic-final-1.md CR1).
- `backend/src/test/scala/com/helio/domain/model/DatasetFieldDeclarationSpec.scala` (new; revised in cycle 2) — JSON codec tests: legacy-shape backward compatibility, null-default normalization, canonicalization, unreadable-type fallback. Renamed the misleadingly-named "double"->float test (skeptic-final-1.md CR5) since it never actually exercised the wire string `"double"`.
- `backend/src/test/scala/com/helio/services/sources/DataSourceServiceSpec.scala` (revised in cycle 2) — new createStatic/refresh reject-path tests (wrong type, missing required, bad default, refresh leaves old rows untouched); the default-fill tests now read back the persisted row and assert the actual stored value (skeptic-final-1.md CR3, was previously `result.isRight` only); new end-to-end test asserting a wire `"double"` column persists its declaration as canonical `"float"` (CR5); `JsArray`/`JsNull` imports added.
- `backend/src/test/scala/com/helio/services/sources/SchemaInferenceRegressionSpec.scala` — fixed a fixture that had become genuinely invalid under the new strict validation (a `"boolean"`-declared field backed by a `JsString` cell is now correctly rejected, not silently accepted); corrected to a real `JsBoolean` cell with a comment explaining why boolean can no longer diverge from its declared type.
- `backend/src/test/scala/com/helio/infrastructure/persistence/sources/DataSourceRepositorySpec.scala` — updated to pass `Vector[DatasetFieldDeclaration]` for `declaredColumns` alongside the still-`SchemaField` `inferredSchema` parameter.
- `backend/src/test/scala/com/helio/infrastructure/persistence/RlsOwnerTablesSpec.scala` (revised in cycle 2 per skeptic-final-1.md CR2) — the original reject-path test was vacuous with respect to RLS (passed even with `new DataSourceService(null, ...)`, since the reject path never reaches the repository); relabeled its comment to say what it actually guards. Added three genuine non-superuser-role exercises: an accepted `createStatic` with a default-fill (asserts the persisted declaration carries `required`/`default`, and RLS scopes the row to its owner), an accepted `refresh`, and a rejected `refresh` that must leave the prior accepted rows intact — all wired through `DataSourceRepository(ctx)` where `ctx.withUserContext` genuinely routes to the `helio_app_test` non-superuser role.
- `frontend/src/features/sources/types/dataSource.ts` — `StaticColumn` gains optional `required`/`default` fields matching the wire shape.
- `frontend/src/features/sources/ui/forms/StaticSourceForm.tsx` — `handleSubmit`'s numeric parsing falls back to the raw string (not `NaN`→`null`) on a failed `parseInt`/`parseFloat`, so a bad numeric cell is rejected by the backend validator instead of silently vanishing as a missing value.
- `frontend/src/features/sources/ui/forms/StaticSourceForm.test.tsx` (revised in cycle 2 per skeptic-final-1.md CR4) — new test that asserts the actual `onSubmit` payload for an unparseable numeric cell is the raw string, not `null`/`NaN`; confirmed red when the NaN-fallback fix is reverted.
- `schemas/sources/static-column-payload.schema.json` — adds optional `required`/`default` properties.
- `openspec/changes/declared-schema-model-validation/specs/dataset-schema-validation/spec.md` (revised in cycle 2 per skeptic-final-1.md CR1) — pinned the exact type-mismatch reason template (`"expected <type>, got <kind>"`, kind set now includes `array`) as the ONE template used for every mismatch including non-integral-integer and unparseable-timestamp; added a new scenario for the unparseable-timestamp case; added the fourth pinned error format for a bad default at declaration time (`"field '<name>' — default <reason>"`).
