- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — added `checkConnectorKind`, invoked in `createRest`'s `(Some(connectorId), None)` branch after `rejectBodyOnSafeMethod` and before `createRestWithConfig` (design.md Decision 2a). Owner-scoped `findByIdOwned` lookup; fails closed when `connectorRepo == null` (Decision 4).
- `backend/src/main/scala/com/helio/domain/connectors/RestApiConnectorDriver.scala` — `resolveConnector` now rejects a resolved Connector whose `kind` is not `rest_api` with a curated `Left`, before `buildResolvedRequest` reaches credential decryption or URI composition (design.md Decision 2b).
- `backend/src/test/scala/com/helio/services/sources/SourceServiceSpec.scala` — added `SourceService.createRest` kind-mismatch coverage: red-then-green mismatch rejection (naming both kinds), matching-kind content assertion (HEL-590 lesson), unresolvable-id curated message unchanged, fail-closed on `connectorRepo == null`, and a bare-url regression guard.
- `backend/src/test/scala/com/helio/domain/connectors/RestApiConnectorDriverKindGuardSpec.scala` — new spec for the fetch-time guard: rejects a stored mismatched-kind Connector with no outbound request and a recorded zero `decryptForUse` call count, and a matching-kind regression case against a real local HTTP server.
- `frontend/src/features/sources/ui/forms/ConnectorSelectField.tsx` — filters the picker to `kind === "rest_api"` Connectors (affordance only); replaces the bare empty control with an `EmptyState` (title/description/CTA) when no matching Connector exists (design.md Decision 5).
- `frontend/src/features/sources/ui/forms/ConnectorSelectField.test.tsx` — new Jest coverage: rendered options include the matching Connector and exclude the mismatched one; the empty state renders explanatory text when no `rest_api` Connector exists. No focus/visibility assertions (jsdom has no layout).
- `openspec/changes/reject-mismatched-connector-kind/tasks.md` — all tasks checked off.
- `openspec/changes/reject-mismatched-connector-kind/evidence/*.txt` — captured red-arm output for tasks 1.1–1.3 and the task 3.3a named-mutation result.

Note on tasks.md 2.6 (agent/MCP path inheritance): both `PipelineProposalService.resolveRestSource` and `PipelineService`'s inline-REST-source path call `SourceService.createRest` directly — the exact method `SourceServiceSpec`'s new cases exercise — so the guard is inherited structurally, without a second implementation, per design.md's own stated rationale. No additional integration-level spec was added through `PipelineProposalService`/`PipelineService` (both would require substantial new DB-backed harness for coverage that is otherwise fully implied by the shared call site, and `PipelineProposalService`'s test surface borders HEL-890-owned files this ticket must not touch). Flagging this call explicitly rather than silently declaring it done.
