- `frontend/src/features/sources/ui/FieldDeclarationTable.tsx` — added `field-declaration-table__checkbox` className to the native Required checkbox input (no other JSX attributes touched).
- `frontend/src/features/sources/ui/FieldDeclarationTable.css` — added `.field-declaration-table__checkbox` rule (18px × 18px, `accent-color: var(--app-accent)`), mirroring `DatasetRowGrid.css`'s `.dataset-row-grid__draft-checkbox` per design.md Decisions 1/2/3 (no `align-self`, since this checkbox sits directly in a `<td>`, not a flex parent).
- `frontend/src/features/sources/ui/FieldDeclarationTable.test.tsx` — added a `Required checkbox (HEL-1125)` test block asserting the checkbox's computed accessible name/state via `getByRole("checkbox", { name: ... })` (checked/unchecked), plus a real-focus + activation test proving it's a genuine, reachable tab stop and toggles on activation.
