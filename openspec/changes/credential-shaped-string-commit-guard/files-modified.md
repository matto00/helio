# Files modified — HEL-846

- `scripts/check-no-credential-in-agent-surface.mjs` — widened the synthetic-marker convention (`_`→`-` normalization, four new markers); added `HIGH_ENTROPY_NAMED_SECRET_REGEX` and `checkDeliverySecrets`; added `deliverySecret` to `KNOWN_CHECKS` and its dispatch/`needsText`; added the `delivery-evidence` (`openspec/`), `docs`, `notes` surfaces to `SURFACES`; removed those three from `ACKNOWLEDGED_UNSCANNED`; retired the now-false "HEL-846 is the intended generic backstop" reasons on `scripts`/`backend`; updated the header (surface table, HEL-846 paragraph, residual-limits entry, coverage-drift listing) and the FAIL summary paragraph.
- `scripts/check-no-credential-in-agent-surface.selftest.mjs` — added mutation-verified self-test cases for all three new surfaces, the vendor and high-entropy rules, marker normalization, surface/check dispatch, the vacuity guard, and `assertSurfacesValid`, following the file's existing subprocess/mutated-copy harness convention; added corresponding cleanup helpers to the startup and `finally` blocks. Plants at each surface's permanent root (`openspec/.hel846-plant.md`, `docs/.hel846-plant.md`, `notes/.hel846-plant.md`), never inside this change's own in-flight directory (evaluation-1.md CR1); writes go through a `writeHel846Plant` helper that `mkdirSync(dirname(path), { recursive: true })`s before every write.
- `.github/workflows/ci.yml` — added `check:no-credential-leak` and `check:no-credential-leak:selftest` to the `frontend` job, with a precedent comment matching the existing HEL-913 one.
- `CONTRIBUTING.md` — added the "Credential handling in delivery evidence (HEL-846)" section (redact-and-revoke rule, the mechanical gate, the synthetic-marker convention, the standing repo-wide constraint, and why the rule lives here rather than in a Concertino render target); added `check:no-credential-leak` to the Pre-Commit Policy list.
- `.gitignore` — added exact-path ignore entries for the HEL-846 self-test/mutation-evidence scratch artifacts (`/openspec/.hel846-plant.md`, `/docs/.hel846-plant.md`, `/notes/.hel846-plant.md`; `/scripts/.hel846-selftest-mutated-surfaces.mjs`; `/scripts/.hel846-selftest-mutated-checks.mjs`). The plant paths sit at each surface's permanent root, not inside this change's own in-flight directory (evaluation-1.md CR1, commit 9a563bbb).
- `openspec/changes/credential-shaped-string-commit-guard/mutation-evidence.md` — new; every mutation transcript required by tasks.md §5, §6, §7.4 and §10.4a, with all planted values elided or absent (design.md Decision 2a).
- `openspec/changes/credential-shaped-string-commit-guard/tasks.md` — all tasks marked complete.
