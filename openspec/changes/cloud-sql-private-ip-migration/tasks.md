# Tasks: Cloud SQL Private IP + Serverless VPC Access migration

## 1. Enable required APIs (additive, no checkpoint)

- [x] 1.1 `gcloud services enable vpcaccess.googleapis.com`
- [x] 1.2 `gcloud services enable servicenetworking.googleapis.com`
- [x] 1.3 Verify both show as enabled via `gcloud services list --enabled`

## 2. Private Services Access peering (additive, no checkpoint)

- [x] 2.1 Allocate a reserved IP range for Google-managed services on the `default` network
      (`gcloud compute addresses create`, `--purpose=VPC_PEERING`) — use `10.8.0.0/20`
      (design.md Decision 4b), outside `default`'s full auto-allocated regional-subnet footprint
      (`10.128.0.0/20`–`10.232.0.0/20`, ~40 regions), not just the local `us-west1` subnet
- [x] 2.2 Create the VPC peering connection (`gcloud services vpc-peerings connect
      --service=servicenetworking.googleapis.com --network=default`)
- [x] 2.3 Verify the peering is active (`gcloud services vpc-peerings list --network=default`)

## 3. Serverless VPC Access connector (additive, no checkpoint)

- [x] 3.1 Create the connector (`gcloud compute networks vpc-access connectors create
      helio-vpc-connector --region=us-west1 --network=default --range=10.9.0.0/28` —
      design.md Decision 4b; outside both the auto-subnet footprint and the 2.1 peering range)
- [x] 3.2 Verify it reaches `READY` state (`gcloud compute networks vpc-access connectors describe`)

## 4. CHECKPOINT — enable Private IP on `helio-db`

- [x] 4.1 **Pause and get explicit human go-ahead before this step** — design.md Decision 2's one
      real risk point (possible brief instance restart/availability gap).
- [x] 4.2 `gcloud sql instances patch helio-db --network=default --no-assign-ip=false` (adds
      private IP; public IP stays enabled per design.md Decision 3)
- [x] 4.3 Record the assigned private IP address (`gcloud sql instances describe helio-db
      --format="value(ipAddresses)"`)
- [x] 4.4 Confirm the instance is healthy post-patch (`gcloud sql instances describe helio-db
      --format="value(state)"` → `RUNNABLE`); check Cloud SQL's own logs for a clean restart with
      no errors

## 5. Deploy-script change (executor-driven, normal review loop)

- [x] 5.1 **`application.conf` requires NO change** — verified it already fully defers
      `helio.db.url` to `${?DATABASE_URL}`. Do not edit this file.
- [x] 5.2 `infra/deploy-backend.sh`'s `--set-env-vars`: change `DATABASE_URL` to
      `jdbc:postgresql://<private-ip from 4.3>:5432/helio?sslmode=require` (design.md Decision
      4a — explicit SSL is required, `helio-db`'s `sslMode` is `ENCRYPTED_ONLY`), no
      `socketFactory`/`cloudSqlInstance` params
- [x] 5.3 `infra/deploy-backend.sh`: add `--vpc-connector=helio-vpc-connector
      --vpc-egress=private-ranges-only`, remove `--add-cloudsql-instances` (or gate both behind a
      flag if a clean cutover needs the old path to remain available as a fallback — executor's
      call, confirm with orchestrator before finalizing)
- [x] 5.3b Append `"$@"` to the end of the `gcloud run deploy` invocation in
      `deploy-backend.sh` (design.md Decision 4c, human-directed resolution to round-3's CR7) —
      lets task 6.1 invoke `./infra/deploy-backend.sh --no-traffic` for the cutover deploy while
      every ordinary future deploy (task 8) is unaffected with no extra args
- [x] 5.4 sbt/Scala test suite green; no application code changes anticipated (deploy-script-only
      change)
- [x] 5.5 `infra/README.md`: document the Serverless VPC Access connector + Cloud SQL Private IP
      prerequisite for `deploy-backend.sh`, and remove any remaining reference to the
      `postgres-socket-factory`/`cloudSqlInstance` connector path as the primary connectivity
      method — required by this change's own spec delta
      (`specs/production-deployment-docs/spec.md`)

## 6. Deploy + verify the new revision with zero live traffic

- [x] 6.1 Deployed `helio-backend-00055-k8h` via `./infra/deploy-backend.sh --no-traffic
      --image=...:release-v1.6-6b269a79`, 0% traffic confirmed
- [x] 6.2 `/health` against the tagged revision URL → `{"status":"ok"}`, 200
- [x] 6.3 No live authenticated request was needed — startup logs directly prove both pools:
      `Database: jdbc:postgresql://10.8.0.3:5432/helio?sslmode=require (PostgreSQL 16.13)`,
      `Successfully validated 90 migrations`, `helio.db - Start completed`, `helio.db.privileged
      - Start completed` (SET ROLE helio_privileged succeeded — design.md Decision 5 confirmed)
- [x] 6.4 Zero connection errors; only 4 pre-existing, unrelated WARNINGs (RESEND_API_KEY unset,
      Hadoop/Spark native-lib noise)

## 7. CHECKPOINT — migrate traffic

- [x] 7.1 Human go-ahead received
- [x] 7.2 100% traffic migrated to `helio-backend-00055-k8h` (confirmed via `status.traffic`)
- [x] 7.3 Watched for several minutes post-cutover: `/health` 200, zero `SQLTransientConnectionException`/`Broken pipe` in a 5-min window, request log shows normal 200s, HEL-118's alerting armed as a backstop
- [x] 7.4 `helio-backend-00054-fzq`/`00053-hcj` confirmed still present and `Ready=True` — instant rollback available via `update-traffic --to-revisions helio-backend-00054-fzq=100`

## 8. Follow-up

- [ ] 8.1 File a standalone follow-up ticket: disable Cloud SQL public IP, once the private-IP
      path has run clean for a validation window (design.md Decision 3)
