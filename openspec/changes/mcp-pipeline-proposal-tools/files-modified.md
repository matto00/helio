- `helio-mcp/src/types.ts` — added `PipelineProposalSource`/`PipelineProposal`/`PipelineAnalyzeProposalResponse`/`PipelineProposalApplyResponse` interfaces mirroring the backend wire shapes (HEL-379/381/383).
- `helio-mcp/src/helioApi.ts` — added `analyzePipelineProposal`/`applyPipelineProposal` thin pass-through client methods (`POST /api/pipelines/analyze-proposal` / `/apply-proposal`).
- `helio-mcp/src/tools/write.ts` — hoisted `boundPipelineStepSchema` from inside `registerWriteTools` to module top-level scope as `export const boundPipelineStepSchema` (design.md D3), so `pipelineProposal.ts` can reuse it instead of a second, drift-prone copy; `create_bound_panel` continues to resolve the same binding unchanged.
- `helio-mcp/src/tools/pipelineProposalValidation.ts` — new pure, zod-free `computePipelineProposalWarnings(source, sourceIds)` helper (design.md D4): mutual-exclusivity/neither-set, inline-source name/config presence, and sourceId-existence checks.
- `helio-mcp/src/tools/pipelineProposalHandlers.ts` — new zod-free, `McpServer`-free handler module (design.md D4b): `proposePipelineHandler`/`analyzePipelineProposalHandler`/`applyPipelineProposalHandler`, each a plain async function taking `HelioApi` + plain TS-typed arguments — isolates the TS2589-safe test surface from `pipelineProposal.ts`'s zod/`registerTool` surface.
- `helio-mcp/src/tools/pipelineProposal.ts` — new thin-shell tool registration module: `registerPipelineProposalTools(server, api)` registers `propose_pipeline`/`analyze_pipeline_proposal`/`apply_pipeline_proposal`, each a zod `inputSchema` + `guarded(() => xHandler(api, ...))` one-liner; no business logic of its own.
- `helio-mcp/src/index.ts` — registers `registerPipelineProposalTools(server, api)` alongside the existing tool registrations.
- `helio-mcp/src/tools/pipelineProposalValidation.test.ts` — new unit tests for `computePipelineProposalWarnings` (all warning cases + no-warning valid cases).
- `helio-mcp/src/tools/pipelineProposalHandlers.test.ts` — new call-routing tests for the three handlers, using a minimal hand-rolled `HelioApi`-shaped mock (mirrors `context.test.ts`'s `makeFakeApi`, the only existing precedent for mocking `HelioApi`).

## Deviation from tasks.md (task 7.3)

`pipelineProposal.test.ts` (the zod-schema-shape assertion test importing `pipelineProposal.ts` directly) was written, then **removed** after confirming it trips TS2589 in practice (`jest --testPathPatterns=pipelineProposal` failed with `TS2589: Type instantiation is excessively deep and possibly infinite` at `pipelineProposal.ts`'s `server.registerTool(...)` call sites, cascading into further errors) — the exact combination design.md D4b predicted. Removed per task 7.3's own documented fallback and task 7.5's explicit instruction ("If task 7.3 does in practice hit TS2589, remove it and note the removal explicitly"). The now-unused `pipelineProposalInputSchema`/`pipelineProposalSourceSchema` re-export was reverted from `pipelineProposal.ts` since nothing consumes it. 7.1/7.2's required coverage (warnings computation + handler call-routing) is unaffected and green.
