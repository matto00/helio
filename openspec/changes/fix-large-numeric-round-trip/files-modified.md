- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/DataTypeRowRepository.scala` — `listRows` now parses the `jsonb::text` cast with a `JsonParserSettings.withMaxNumberCharacters(400)` override (scoped to this call site only), fixing the round-trip failure for large-magnitude/high-precision numeric values. `overwriteRows` (write path) unchanged.
- `backend/src/test/scala/com/helio/infrastructure/persistence/pipelines/DataTypeRowRepositorySpec.scala` — added a boundary-sweep regression suite (large magnitude, negative large magnitude, high-precision decimal, small-magnitude/denormal-style, ordinary control case, exactly-at-boundary, just-over-boundary), each asserting exact `BigDecimal` value equality on round-trip.
