- `scripts/check-no-credential-in-agent-surface.mjs` — closes the four residual "silence reads as green" paths: an unreadable surface file (`readSurfaceFiles`, new), an unreadable file reached through the import-graph BFS (`findBannedImport`), an unlistable directory (`collectFiles`), and a fail-open-shaped CLI entry guard (independent `mainRan`-backed basename backstop). Access errors (read + listing) are now collected and reported first, before drift and vacuity, so a surface whose files are all unreadable is never reduced to a bare `VACUOUS SURFACE` line. Header comment updated to record which paths HEL-993 closed and to correct the entry-guard comment's prior "fixed rather than left as a residual" claim.
- `scripts/check-no-credential-in-agent-surface.selftest.mjs` — adds self-test cases for: an unreadable planted mcp file, an unlistable planted directory, a surface whose only file is unreadable (proves access-error-before-vacuity ordering), the assistant-surface `importGraph` check (transitive banned import through a second planted module), the assistant-surface `credentialProp` check, an unreadable file reached through the import-graph BFS, and the entry guard's fail-closed backstop (via a mutated copy, since the shipped script has no injection hook for it). `chmod 000`-dependent cases print a loud, named SKIP (and the run reports `OK WITH SKIPS (...)`, never a bare `OK`) when running as euid 0. Generalized `runMutatedScript` to accept a destination path so the entry-guard case can use its own mutated-copy file distinct from HEL-956's.
- `.gitignore` — registers the new HEL-993 self-test planted/renamed paths by explicit path, matching the existing HEL-956 convention.
- `openspec/changes/close-silent-green-gate-paths/tasks.md` — all tasks marked complete; 2.7/2.8 now record the skeptic-final-1.md reproduction/fix evidence, not just intent.

Cycle 2 (skeptic-final-1.md REFUTE, addressed):
- `scripts/check-no-credential-in-agent-surface.selftest.mjs` — added `removeHel993McpStandIn()` (CR1: case 3's stand-in `helio-mcp/` was unrecoverable by `restoreMcpRootIfMoved()` after a mid-case crash; new function only ever removes when the real directory is confirmed parked at `mcpMovedRoot`), called from both startup cleanup and `finally`, before `restoreMcpRootIfMoved()`. Moved the BFS-unreadable planted file from under the assistant surface root to `frontend/src/shared/` (non-blocking note: makes both BFS-case assertions site-specific to `findBannedImport`'s own catch site, not masked by `readSurfaceFiles`'s).
- `.gitignore` — added the missing `helio-mcp/.hel993-only-file.ts` (CR2) and updated the BFS-unreadable path to its new location.

Mutation-matrix evidence (task 3.1, seven mutations against the shipped script, each reverted after capture) is at `.concertino/gate-chain-isolation-evidence/mutation-{1..7}-*.md` (gitignored, not part of the diff).
