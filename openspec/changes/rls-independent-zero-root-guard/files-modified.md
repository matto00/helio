- `backend/src/main/resources/db/migration/V100__zero_root_guard_rls_independent.sql` — new migration: re-owns `hel913_prevent_zero_root_pipelines` to `helio_privileged` (BYPASSRLS, V40's precedent) and adds a `SET row_security = off` tripwire, closing the vacuous-under-RLS gap V99's own header documented. Does NOT edit V99 (checksum risk, escalated `v100-header-only` ruling).
- `backend/src/main/scala/com/helio/infrastructure/persistence/sources/DataSourceRepository.scala` — adds `soleRootDependentPipelineCountPrivileged`, a privileged (BYPASSRLS), count-only companion to `soleRootDependentPipelines` (design D9), pinned to the identical sole-root predicate.
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — wires the new privileged pre-check into `delete`, strictly before `deleteFileF`, with a short-circuit when the RLS-scoped check already found a blocker (design D9); extracted `deleteAfterPrecheck` to host the pre-existing delete/recover logic unchanged (cycle 2: re-indented to the method's own 2-space depth — behaviour unaffected).
- `openspec/changes/rls-independent-zero-root-guard/specs/pipeline-zero-root-db-guard/spec.md` — cycle 2 (evaluation-1.md): amended two scenario WHEN clauses to name the privileged-pool connection shape actually exercised by the tests, and added a sentence to the requirement recording why the plain app-pool/GUC-unset shape is unreachable (`data_sources_owner` has no `missing_ok=true`) rather than unprotected.
- `backend/src/test/scala/com/helio/infrastructure/persistence/V100ZeroRootGuardNonSuperuserSpec.scala` — new permanent gate: non-superuser SQL-level positive/parity/negative/liveness/mutation tests (tasks 3.2–3.7a) over a genuine `helio_migration_test` role, plus the D9 divergence/non-disclosure/regression/false-positive gates (3.7c–3.7f) over a two-genuinely-distinct-pool harness (design D10), never `DataSourceRoutesSpec`'s shared-superuser-pool shape.
- `openspec/changes/rls-independent-zero-root-guard/probe.md` — new: verbatim task-1.1/1.2 premise-vacuity transcripts (captured via a now-deleted scratch spec) and the task-3.7/3.7a mutation-state transcripts (state A silent, state B loud `42501`), both message and SQLSTATE.
- `openspec/changes/rls-independent-zero-root-guard/tasks.md` — all 26 tasks checked off.
- `openspec/changes/archive/2026-09-04-multi-root-pipelines/tasks.md` — corrects the HEL-913 caveat: the vacuity gap was real, HEL-974 closed it in V100, records how and why V99 itself was left untouched.
