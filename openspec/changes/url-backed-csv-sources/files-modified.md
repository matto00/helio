# Files modified — HEL-862 URL-backed CSV sources

- `backend/src/main/scala/com/helio/services/sources/CsvUrlFetch.scala` — new shared public helper: https-only scheme gate, reuses `ContentSourceSupport.fetchUrl` (SSRF guard), size limit, non-CSV-body gate; single implementation all three call sites use.
- `backend/src/main/scala/com/helio/domain/model/DataSource.scala` — `CsvSourceConfig` gains `sourceUrl: Option[String] = None`.
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceConfigCodec.scala` — CSV config decode/encode now absent-tolerant for `sourceUrl`, mirroring the text-source idiom.
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceProtocol.scala` — `CsvSourceConfigPayload` gains `sourceUrl`; adds `CsvSourceUrlConfigPayload`/`CsvSourceUrlRequest` + JSON formats; `fromDomain` forwards `sourceUrl`.
- `backend/src/main/scala/com/helio/api/package.scala` — exposes `CsvSourceUrlConfigPayload`/`CsvSourceUrlRequest` type aliases for route-layer use.
- `backend/src/main/scala/com/helio/api/routes/sources/DataSourceRoutes.scala` — new JSON `csv` create branch dispatching to `createCsvUrl`; `csvMaxBytes` now reads `CsvUrlFetch.maxFileSizeBytes` (single source of truth).
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — new `createCsvUrl`; `refreshCsv` re-fetches via `CsvUrlFetch` when `sourceUrl` is set; `csvUrlErrorToServiceError` maps the ADT to the correct status per path.
- `backend/src/main/scala/com/helio/domain/engine/InProcessPipelineEngine.scala` — new `csvUrlFetch` injectable seam (lazy, defaults to "not configured"); `CsvSource` branch re-fetches when `sourceUrl` is present (load-bearing for AC3).
- `backend/src/main/scala/com/helio/services/pipelines/PipelineRunService.scala` — threads `system`/`resolveHost`/`isBlocked` (nullable-defaulted); builds a lazy closure over `CsvUrlFetch.fetch` supplied to the engine.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — passes the implicit `ActorSystem` into `PipelineRunService` so scheduled/manual runs can re-fetch URL-backed CSV sources.
- `backend/src/main/scala/com/helio/services/sources/CsvUrlFetch.scala` — see above.
- `backend/src/test/scala/com/helio/services/sources/CsvUrlFetchSpec.scala` — new: scheme gate, address-denylist reuse, size limit, non-CSV gate (incl. BOM), upstream-error wrapping; real self-signed HTTPS test server.
- `backend/src/test/scala/com/helio/services/sources/DataSourceServiceCsvUrlSpec.scala` — new: `createCsvUrl`/URL-backed `refresh` service-level coverage over a real HTTPS test server.
- `backend/src/test/scala/com/helio/api/protocols/sources/DataSourceConfigCodecSpec.scala` — new: CSV config absent-key decode + round-trip tests.
- `backend/src/test/scala/com/helio/api/protocols/sources/DataSourceProtocolSpec.scala` — adds `CsvSourceResponse`-with-`sourceUrl` round trip and `CsvSourceUrlRequest` decode tests.
- `backend/src/test/scala/com/helio/domain/engine/InProcessPipelineEngineSpec.scala` — adds the engine-level `csvUrlFetch` seam tests (AC3 evidence: two runs, changed bytes, changed rows; snapshot path performs no fetch; failure path; lazy-system-safe default).
- `backend/src/test/scala/com/helio/services/pipelines/PipelineRunServiceSpec.scala` — adds a `seedCsvUrlDs` fixture and a real-DB test proving construction without `system` doesn't throw and a URL-backed CSV run fails gracefully.
- `helio-mcp/src/helioApi.ts` — `createCsvDataSource` accepts `content` OR `sourceUrl`; dispatches to multipart or JSON POST accordingly.
- `helio-mcp/src/tools/write.ts` — `create_csv_data_source` tool schema/description updated; delegates mutual-exclusion validation to `csvDataSourceSchema.ts`.
- `helio-mcp/src/tools/csvDataSourceSchema.ts` — new: `assertExactlyOneCsvInput`, the mutual-exclusion check, extracted for cheap unit testing.
- `helio-mcp/src/tools/csvDataSourceSchema.test.ts` — new: mutual-exclusion unit tests + tool-description content tests (source-text based, avoiding `write.ts`'s expensive full-module type-check).
- `helio-mcp/src/helioApi.test.ts` — new: `createCsvDataSource` transport-dispatch tests (multipart vs JSON body shape).
