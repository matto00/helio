# Files modified — HEL-891

- `backend/src/main/scala/com/helio/domain/engine/SchemaInferenceEngine.scala` — added `inferShallowFromJsObjects`, a shallow (non-flattening) top-level key union + type-widening entry point shared with the source-inference lattice (design D2/D8).
- `backend/src/main/scala/com/helio/services/pipelines/PipelineRunService.scala` — `upsertFieldsFromRows` now derives fields from `jsRows` (all output rows, design D1) via the new shallow entry point, pins `nullable = true` (design D3) and the raw column name as `displayName` (design D7); deleted the dead `inferFieldType` (design D4); call site in `onUnblockedRunSuccess` now passes `jsRows` instead of `resultRows`.
- `backend/src/main/scala/com/helio/services/alerts/AlertEvaluationService.scala` — updated a doc comment that named the now-deleted `PipelineRunService.inferFieldType`.
- `backend/src/test/scala/com/helio/services/pipelines/PipelineRunServiceSpec.scala` — new `PipelineRunService.onUnblockedRunSuccess (HEL-891 schema union)` test group (tasks 1.1–1.11b): heterogeneous-JSON and image-source fixtures, red-then-green coverage of sparse-column union, float/timestamp typing, order-independence, D8's null-handling, D6's eligibility-loss pin, and D2/D3/D7 regression guards.
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineRunRoutesSpec.scala` — updated a pre-existing test that encoded the row-0-only `"double"` behavior (`... and double for fractional`) to assert the fixed canonical `"float"` value; this is the exact D4 defect this ticket fixes, not a weakened assertion.
