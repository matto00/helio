- `schemas/pipeline-analyze-proposal-response.schema.json` — new response schema (task 1.1); `PipelineAnalyzeProposalResponse` shape (`sourceName`, `outputDataTypeName`, `sourceSchema`, `steps`), with a new `AnalyzeProposalStep` `$defs` entry matching the actual `type`-discriminator/object-`config` wire format (design.md D6), not the stale `pipeline-analyze-response.schema.json` `$defs.AnalyzeStep`.
- `backend/src/main/scala/com/helio/api/protocols/PipelineAnalyzeProposalProtocol.scala` — new protocol trait/case class `PipelineAnalyzeProposalResponse` + its `RootJsonFormat`, reusing `SchemaFieldResponse`/`AnalyzeStepResponse`/`analyzeStepResponseFormat` verbatim from `PipelineAnalyzeProtocol` (design.md D4).
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — mixes `PipelineAnalyzeProposalProtocol` into the aggregator trait.
- `backend/src/main/scala/com/helio/services/PipelineService.scala` — new `analyzeProposal` method + `resolveProposalSourceSchema`/`resolveInlineSourceSchema`/`toSchemaFields` helpers implementing design.md D2's source-resolution precedence (`sourceId` always wins; recognized-inline-type-with-absent-config → `BadRequest`, never an unguarded `.get`) and D3's synthetic-positional-step-id conversion; new nullable-optional `connector: RestApiConnector = null` constructor param (mirrors the many other optional-collaborator params `ApiRoutes.scala` already threads) for the inline `rest_api` branch. Cycle 2 (evaluation-1.md CR1): added `validateStepKinds`, called before source resolution, so a proposal step whose `type` isn't a registered `PipelineStepKind` short-circuits to `ServiceError.BadRequest` instead of reaching `toAnalyzeStepResponse`'s re-decode path and throwing an uncaught `IllegalStateException` (unhandled 500).
- `backend/src/main/scala/com/helio/api/routes/PipelineRoutes.scala` — new `POST /pipelines/analyze-proposal` route, registered **before** the `PipelineIdSegment`-based branches per design.md D5 (an unconstrained `Segment` matcher would otherwise swallow the literal `analyze-proposal` segment).
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — threads the existing `connector: RestApiConnector` constructor param through to `PipelineService`'s new optional `connector` param (same instance `SourceService` already receives).
- `backend/src/test/scala/com/helio/api/routes/PipelineAnalyzeProposalRoutesSpec.scala` — new route-level ScalaTest suite (embedded Postgres, mirrors `PipelineAnalyzeRoutesSpec`'s convention) covering tasks 3.2–3.12: existing-sourceId happy path (asserts no source/pipeline/step row created), inline `static`/`sql` sources, DDL/DML short-circuit, inline `csv` rejection, per-step `validationError` (not a 500), structurally-invalid-proposal 400, cross-user `sourceId` 404 (no schema leak), config-absent-for-recognized-type 400, `sourceId`-over-inline precedence, and a `JsonSchemaValidation`-harness check of the new response format against `schemas/pipeline-analyze-proposal-response.schema.json`. Cycle 2 (evaluation-1.md CR1): added a test asserting a proposal step with a `type` outside `PipelineStepKind.All` returns `400`, never a `500`.
