- `backend/src/main/scala/com/helio/domain/pipelines/PipelineCycleValidator.scala` — new pure domain object: multigraph model, three-colour DFS cycle detection, `AdvisoryLockKey` constant, `CycleError`/message builder (tasks 2.1/2.2)
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineCycleGuard.scala` — new shared "lock + graph-read + check" DBIO composition (`checkAction`/`checkExistingGraphAction`/`checkAddReadAction`/`checkAddWriteAction`), `PipelineCycleRejected` failure type (task 3, Decision 4)
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineRootRepository.scala` — `findReadEdgesVisibleTo` (task 1.1, explicit visibility filter); `add` restructured to run the lock+check inside its own `withUserContext` chain (task 3.3)
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepository.scala` — `findUpsertWriteEdges` (task 1.2); `spliceInsertAtInternal`/`attachTailInternal`/`updateInternal` gain `actingUserId` and run the lock+check ahead of the row write, scoped to `upsertsource`+`ExistingSource` (task 3.4)
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineRepository.scala` — `create`/`createAction` prepend `PipelineCycleGuard.checkExistingGraphAction` to their existing DBIO chains (tasks 3.1/3.2)
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — thread `actingUserId`/catch `PipelineCycleGuard.PipelineCycleRejected` at `create`'s two paths, `addRoot`, `addStep`/`updateStep`/`duplicateStep`'s repository call sites
- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineStepConfigCodec.scala` — `encodeConfig` gains an `UpsertSourceConfig` case (uses the type's own `format`, does not touch `PipelineStep.Registry`) so repository-seam tests can serialize a typed config through `spliceInsertAtInternal`/`updateInternal`
- `backend/src/test/scala/com/helio/domain/pipelines/PipelineCycleValidatorSpec.scala` — new unit tests: direct/2-pipeline/3-pipeline cycles, diamond false-positive (three-colour proof), tie-break, message builder
- `backend/src/test/scala/com/helio/infrastructure/persistence/pipelines/PipelineCycleGraphQueriesSpec.scala` — new repository tests: tenancy scoping under both `withUserContext`/`withSystemContext`, `NewSource`/malformed-config skip, cross-tenant exclusion under BYPASSRLS
- `backend/src/test/scala/com/helio/services/pipelines/PipelineCycleDetectionServiceSpec.scala` — new service-level tests covering every edge-adding write path (`create` both paths, `addRoot`, `addStep`/`updateStep` wiring, `PipelineProposalService.apply`), editor-grantee scoping + id-vs-name naming rule, and the concurrency race (task 4.1)
- `backend/src/test/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepositorySpliceSpec.scala` — updated call sites for `spliceInsertAtInternal`/`attachTailInternal`'s new `actingUserId` parameter (no behavior change)
- `openspec/changes/pipeline-cycle-detection/tasks.md` — all 14 tasks marked complete

## Cycle 2 (skeptic-final-1.md, round-1 REFUTE: 3 change requests, all addressed)

- `backend/src/main/scala/com/helio/domain/pipelines/PipelineCycleValidator.scala` — **CR2**: `checkNoCycle` rewritten from "does any cycle exist in the combined graph" (which incorrectly blocked unrelated writes on a standing cycle elsewhere) to forward reachability FROM `writeTarget`, through the EXISTING graph only, to any node in `readSources` — only reports a cycle the PENDING edge itself closes
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineCycleGuard.scala` — `checkExistingGraphAction` doc corrected (now an honest no-op, not a "scan for any cycle"); `checkAddWriteAction` gains `excludeExistingWriteTarget` so an UPDATE's stale old write edge is excluded while checking the new target
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepository.scala` — **CR1**: `insertInternalAction`/`insertInternal` (the create path's own step-insert method, previously the ONE write path with no cycle check at all) now run `cycleCheckForUpsertAction` too, via a new `actingUserId` parameter (safe default, mirrors `updateInternal`'s existing pattern since the check short-circuits for non-`upsertsource` kinds); `cycleCheckForUpsertAction` gains an `oldConfigJson` parameter (threaded from `updateInternal`) to exclude the step's own pre-update target per the `excludeExistingWriteTarget` fix above; blank `actingUserId` reaching an actual `upsertsource`+`ExistingSource` check now fails with a clear named error instead of a raw `UUID.fromString` crash
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — `buildStepsAction` threads `actingUserId` down to `insertInternalAction`
- `backend/src/test/scala/com/helio/domain/pipelines/PipelineCycleValidatorSpec.scala` — updated doc comments for the new reachability algorithm; removed a duplicate (identical-body) two-colour test; added a "standing unrelated cycle does not block" test
- `backend/src/test/scala/com/helio/services/pipelines/PipelineCycleDetectionServiceSpec.scala` — **CR1**: new test composing `PipelineRepository.createAction` + `PipelineStepRepository.insertInternalAction` directly (bypassing the Registry gate) proving a new pipeline's own roots + upsertsource step targeting one of those same roots is rejected. **CR2**: the "3.1"/"3.2"/"3.5" tests that previously asserted rejection from a pre-seeded standing cycle (passing for the wrong reason) are replaced with "accept despite an unrelated standing cycle" tests; wired a real `PipelineRunService` so `PipelineProposalService.apply`'s unconditional post-create run-submit doesn't NPE. **CR3**: added a new test that forces genuine transaction interleaving (one transaction holds the advisory lock via `pg_sleep`, `clock_timestamp()` proves the second transaction's lock acquisition happens only after the first commits) — a direct proof of blocking, not an inference from a pass/fail outcome; the "lock is load-bearing" claim was verified by an actual mutation run (temporarily replacing `lockAction` with a no-op, confirming the original concurrency test then fails with both writes succeeding), documented in a comment rather than left as an assertion in committed code that could bit-rot silently
