### Backend

## 1. Graph query support

- [x] 1.1 Add `PipelineRootRepository.findReadEdgesVisibleTo(userId)` returning `(pipelineId, pipelineName, dataSourceId)` read-edge triples, filtered EXPLICITLY (never via RLS/GUC) by a join mirroring `helio_can_access_pipeline`'s predicate (`pipelines.owner_id = userId` OR `userId` is a `resource_permissions` grantee for that pipeline, per design.md Decision 2) so it returns the correct scoped set from any connection/context; verify with a repository test seeding multiple owners' roots plus a shared grantee and asserting only the caller-visible rows return, run once under `withUserContext` and once under `withSystemContext` to prove the filter (not RLS) is what scopes it.
- [x] 1.2 Add `PipelineStepRepository.findUpsertWriteEdges(userId)` returning `(pipelineId, pipelineName, dataSourceId)` write-edge triples for all `upsertsource`-kind steps, config decoded via `UpsertSourceConfig.decode`, filtered to `ExistingSource`, using the SAME explicit visibility filter as 1.1; steps with a `NewSource` target or an undecodable config are skipped, never thrown; verify with a repository test covering both target kinds, a malformed-config row, and — the round-2 REFUTE's specific concern — another tenant's write edge being excluded even though the query runs under `withSystemContext` (BYPASSRLS). Since `upsertsource` is unregistered, seed test rows by inserting directly through the repository, not through the HTTP API.

## 2. Cycle validator

- [x] 2.1 Implement `PipelineCycleValidator` (new domain object) building a directed, edge-labelled multigraph (edges carry the writing pipeline's id/name — design.md Decision 6) over data-source ids from the two edge sets above, with a method that takes the caller-visible graph plus one pending edge (`readSources: Set[DataSourceId]`, `writeTarget: DataSourceId`, `writingPipelineId`, `writingPipelineName`) and returns `Either[CycleError, Unit]`. Use three-colour (white/gray/black) DFS so only a node still on the current path counts as a cycle-closing back edge — a node already fully explored (black) does not. Verify with unit tests constructing graphs directly covering: direct self-cycle, 2-pipeline transitive cycle, 3-pipeline transitive cycle, and a non-cyclic diamond where the same node is reached via two disjoint paths — this last test MUST fail if the two-colour ("any visited node is a cycle") mistake is (re)introduced. This is also where the same-request self-cycle case (a pipeline's own roots + its own write target) is proven, since the live API cannot construct it yet (see spec.md's note on this).
- [x] 2.2 Implement the cycle-path message builder (walks the DFS back-edge to produce `"S1 -> P1 -> S2 -> P2 -> S1"` naming only nodes/edges already present in the caller-scoped graph); when multiple edges could close the same cycle, prefer the edge belonging to the pipeline whose pending write is under validation, else break ties by ascending pipeline id (design.md Decision 6, deterministic); verify with a unit test asserting the exact path/tie-break for the 2- and 3-pipeline cases and a case with two candidate pipelines producing the same `S1->S2` edge.

## 3. Wire into write paths (design.md "Decisions" §4 — lock + check composed into each path's own DBIO)

- [x] 3.1 Wire into `PipelineService.create`'s simple (roots-only) path: prepend `pg_advisory_xact_lock(72901101)` and the graph-read + check to the same `DBIO.seq` `PipelineRepository.create` already runs under `withUserContext`, before the root rows are inserted; verify with a service-level test creating a pipeline whose roots would close an existing writer-chain cycle (write edge pre-seeded via task 1.2's repository seam on a different, already-persisted pipeline) and asserting rejection naming the cycle. Assert the added check runs as a fixed, bounded number of queries (not one per root).
- [x] 3.2 Wire into `create`'s `createTransactional`/`runTransactionally` path the same way (lock + check ahead of roots/steps/outputs inserts, in the one composed DBIO). This is a READ-EDGE test (round-2 REFUTE: a same-request write edge is impossible since `upsertsource` can't be created via this API) — verify with a service-level test creating a pipeline (with ordinary, registered steps) whose roots close a cycle against a different, pre-seeded writer pipeline (write edge seeded via task 1.2's repository seam), asserting rejection.
- [x] 3.3 Restructure `PipelineRootRepository.add` to accept and run the lock + graph-read (via 1.1's explicit filter) + check inside its own existing `ctx.withUserContext { ... }` flatMap chain, ahead of the `rootsTable += row` insert (design.md Decision 4); wire `PipelineService.addRoot` to use it; verify with a test that adding a root closes a pre-existing writer-chain cycle (write edge seeded via 1.2's repository seam) and is rejected, and a concurrency-shaped test (task 4.1) proving the check and insert are atomic.
- [x] 3.4 Thread an `actingUserId: String` parameter into `PipelineStepRepository`'s `spliceInsertAtInternal`/`attachTailInternal`/`updateInternal` (from `PipelineService.addStep`/`updateStep`'s existing `user: AuthenticatedUser`), and restructure each to run the lock + graph-read (via 1.1/1.2's explicit filter, passing `actingUserId`) + check inside their existing `withSystemContext(...).transactionally` DBIO, ahead of the row write, scoped to `upsertsource` steps with an `ExistingSource` target only (other step kinds are a no-op check, verified by a control test asserting no extra graph query fires for e.g. a `filter` step). Verify with: (a) a REPOSITORY-level test calling the restructured method directly with an `upsertsource`-kind row (bypassing the service's step-kind allow-list, the same seam task 1.2 uses) proving the wiring itself rejects a closing edge — not only the standalone validator; and (b) a repository-level test proving another tenant's write/read edge is excluded from the graph even though the surrounding DBIO runs on the privileged (BYPASSRLS) connection.
- [x] 3.5 Confirm (with a test, not just reading code) that `PipelineProposalService.apply` and the patch-set apply/rollback/undo paths inherit the check transitively through `PipelineService.create`/`addStep`/`updateStep` with no separate wiring — a service-level test driving `apply` with roots that would close a cycle (against a pre-seeded writer pipeline), asserting rejection.
- [x] 3.6 Editor-grantee coverage: a service-level test where the acting user is a non-owner editor grantee of the pipeline being written to, whose OWN visible graph (owner-or-shared) contains a closing cycle — assert rejection naming only pipelines/sources that editor can see, and asserting a data source the editor does not own is named by ID, never by name (design.md "Post-CONFIRM non-blocking fixes"); and (separately) a test where the cycle only closes through a pipeline/source that editor cannot see — assert the write is NOT rejected (documented accepted gap, design.md Risks).
- [x] 3.7 Define `PipelineCycleValidator.AdvisoryLockKey: Long = 72901101L` as a single named constant; reference it (never the literal) at every call site from tasks 3.1-3.4.

## 4. Concurrency

- [x] 4.1 Add a concurrency test firing two simultaneous edge-adding writes (e.g. one `addRoot` and one repository-seam-simulated `upsertsource` write via 3.4's method, or two `addRoot` calls) that would jointly (not individually) form a cycle, asserting the second to actually commit is rejected because it observes the first's edge already applied — this proves the lock+check+write composition from tasks 3.1-3.4 is genuinely atomic, not merely present in code.

### Tests

## 5. AC + spec coverage sign-off

- [x] 5.1 Confirm every scenario in `specs/pipeline-cycle-detection/spec.md` has a corresponding passing test: direct cycle, 2-pipeline transitive, 3-pipeline transitive, diamond false-positive (three-colour DFS proof), tenancy-scoped graph under RLS/non-superuser role (including the privileged-connection exclusion test from 1.2/3.4), editor-grantee write scoped to their own visible graph (3.6), every edge-adding write path (both `create` paths, `addRoot`, `addStep`/`updateStep`), and the concurrency race.
- [x] 5.2 Run full backend suite (`sbt test`) and confirm `PipelineCreateTransactionalSpec`'s existing `upsertsource`-rejected pinned case is untouched (still rejects — this ticket does not register the step).
