- `package.json` — added a version-scoped `overrides` entry, `"minimatch@3.1.5": { "brace-expansion": "^2.1.4" }`, to force the one flagged `brace-expansion` instance that `npm update` couldn't reach (nested under the `eslint`/`eslint-plugin-react`/`jest` transitive `minimatch@3.1.5`) up to a patched version, without touching the unrelated `minimatch@10.2.4` chain that legitimately needs `brace-expansion@^5.0.2`.
- `package-lock.json` — regenerated via `npm update brace-expansion` + `npm install` (after the override was added). All three flagged `brace-expansion` instances (`@typescript-eslint/typescript-estree`'s nested chain, `glob`'s nested chain, `minimatch`'s nested chain) now resolve to patched versions (`5.0.9`, `2.1.4`, `2.1.4` respectively). GHSA-mh99-v99m-4gvg / GHSA-rgw5-rvv9-x895 no longer appear in `npm audit`.
