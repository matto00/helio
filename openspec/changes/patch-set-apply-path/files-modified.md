# Files Modified — HEL-406 patch-set-apply-path

- `backend/src/main/scala/com/helio/api/protocols/PatchSetProtocol.scala` — `Edit.read` now raises a `deserializationError` when `op == "delete"` and the wire JSON's `"patch"` key is present (design.md D6; resolves HEL-403's carried-over follow-up).
- `backend/src/main/scala/com/helio/api/protocols/PatchSetApplyProtocol.scala` (new) — `EditOutcome`/`PatchSetApplyResponse` wire types + formats (design.md D4/D4a/D4b).
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — mixes in `PatchSetApplyProtocol`.
- `backend/src/main/scala/com/helio/services/PatchSetApplyTypes.scala` (new) — internal `ResolvedAction`/`ResolvedEdit` ADT plus the `PatchSetApplyContext`/`PatchSetApplyServices` collaborator bundles shared across the resolvers/forward-apply/rollback files.
- `backend/src/main/scala/com/helio/services/PatchSetApplyServiceJson.scala` (new) — spray-json mixin surface (mirrors `PatchSetProtocol`'s own trait list) shared by the resolvers/forward/rollback files.
- `backend/src/main/scala/com/helio/services/PatchSetApplyResolvers.scala` (new) — pre-validation pass (design.md D2/D2a): per-kind/per-op ACL + embedded cross-resource reference checks, before any mutation.
- `backend/src/main/scala/com/helio/services/PatchSetApplyForward.scala` (new) — forward-apply dispatch (design.md D1): applies each resolved edit via the matching existing per-resource service method only.
- `backend/src/main/scala/com/helio/services/PatchSetApplyRollback.scala` (new; fixed cycle 2 per skeptic-final-1.md CR1) — reverse-order compensation walk (design.md D3/D3a) + full-overwrite inverse-request builders. `fullPanelInverse`'s `config` field now goes through `fullConfigInverse`, which supplies an explicit `null` for every Option-typed config field a panel kind's `*Config.Patch.decode` actually reads (`optionalConfigFieldNames`) — `PanelConfigCodec.encodeConfig` alone omits a `None`-valued Option field from the JSON entirely (plain `jsonFormatN`), and every `*Config.Patch.decode`/`applyPatch` treats an absent key as "leave the current (post-forward-edit) value unchanged" via `.fold(existing)(identity)`, so a rollback built from the bare encoded config could never actually clear an Option field the forward edit had just set. Mirrors the identical fix already present for `appearance.chart` (`fullPanelAppearancePatch`) two functions above.
- `backend/src/main/scala/com/helio/services/PatchSetApplyService.scala` (new) — the public `PatchSetApplyService.apply(patchSet, user)` orchestration entry point (constructor DI + resolve → forward-apply → rollback-on-failure).
- `backend/src/main/scala/com/helio/api/routes/PatchSetRoutes.scala` (new) — `POST /api/patch-sets/apply` thin route shell (mirrors `CombinedProposalRoutes`).
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — constructs `PatchSetApplyService` from the already-constructed per-resource services + repos/`accessChecker`, mounts `PatchSetRoutes`.
- `schemas/patch-set-apply-response.schema.json` (new) — `PatchSetApplyResponse` JSON Schema matching `PatchSetApplyProtocol.scala`'s case classes (`check-schema-drift.mjs`).
- `backend/src/test/scala/com/helio/api/protocols/PatchSetProtocolSpec.scala` — new test: a `delete`-op edit with a populated `patch` raises a `deserializationError` (task 7.1).
- `backend/src/test/scala/com/helio/services/PatchSetApplyServiceSpec.scala` (new; extended cycle 2 per skeptic-final-1.md CR1) — service-level coverage: mixed patch set applies cleanly (7.2), mid-set rollback (7.3), pre-apply rejection incl. editor-grantee-accept vs owner-only-delete-reject (7.4), create-rejected-where-no-viable-path (7.5), dashboard-create `ifExists` rejection (7.6), unrecoverable-delete honesty (7.7), embedded cross-resource reference checks (7.9), prior-state capture (7.10), resulting-state capture (7.11). New regression test (skeptic-final-1.md CR1): a panel-update that sets a config `Option` field (e.g. `aggregation`, previously unset) is genuinely cleared back to unset when that update is rolled back — asserted via a direct DB re-read of the panel's typed config, not just `status == "rolledBack"`. Confirmed RED against the pre-fix `PatchSetApplyRollback.scala` (`Some(Some({"op":"sum"})) was not equal to Some(None)`), GREEN after.
- `backend/src/test/scala/com/helio/api/routes/PatchSetRoutesSpec.scala` (new) — route-level coverage: a cross-owner edit is rejected identically to the target resource's own existing PATCH route (task 7.8).
