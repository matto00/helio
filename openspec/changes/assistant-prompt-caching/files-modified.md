# Files Modified — assistant-prompt-caching (HEL-699)

- `backend/src/main/scala/com/helio/ai/ClaudeWireModels.scala` — add `ClaudeApiCacheControl` (companion `Ephemeral`) and default-`None` `cacheControl` fields to `ClaudeApiContentBlock`, `ClaudeApiTool`, `ClaudeApiMessage`; add `cacheCreationInputTokens`/`cacheReadInputTokens` (default 0) to `ClaudeApiUsage` (design.md D1/D4)
- `backend/src/main/scala/com/helio/ai/ClaudeProtocol.scala` — `claudeApiContentBlockFormat.write`/`claudeApiToolFormat.write` append `"cache_control": {"type": "ephemeral"}` when set; `claudeApiMessageFormat` replaced from `jsonFormat2` with a hand-written format (plain string when unmarked, one-element `text` block array when marked); `claudeApiUsageFormat` reads/writes the two new cache counters, absent-tolerant on read (design.md D2/D4)
- `backend/src/main/scala/com/helio/ai/ClaudeClient.scala` — `toApiToolRequest` marks the last `tools` element and the last content block of the first message (each guarded on non-empty); `toApiRequest` marks the first message (guarded on non-empty); `addUsage` sums all four counters; `toClaudeResponse` maps the two new cache fields (design.md D3/D4)
- `backend/src/main/scala/com/helio/ai/ClaudeModels.scala` — domain `TokenUsage` gains `cacheCreationInputTokens`/`cacheReadInputTokens` (default 0) (design.md D4)
- `backend/src/main/scala/com/helio/ai/HttpClaudeTransport.scala` — widened `buildHttpRequest(ClaudeApiRequest)` from `private` to `private[ai]` (mirroring the existing `ClaudeApiToolRequest` overload) so `HttpClaudeTransportSpec` can assert on the `send` path's serialized request shape directly, per design.md D6
- `backend/src/main/scala/com/helio/services/AssistantTelemetry.scala` — `emitToolLoopOutcome` appends `cacheReadInputTokens`/`cacheCreationInputTokens` fields from `tokens` (design.md D5)
- `backend/src/test/scala/com/helio/ai/ClaudeProtocolSpec.scala` (new) — protocol-level coverage: `cache_control` emitted for a marked block/tool/message, unmarked serialization byte-identical to pre-cache-support shape, usage parse with cache fields present/absent (tasks.md 4.1)
- `backend/src/test/scala/com/helio/ai/HttpClaudeTransportSpec.scala` — new coverage: serialized `sendTool` entity carries `cache_control` on the last tool + first message's last block only; serialized `send` entity carries the first-message block-array marker, and an unmarked `send` request's JSON is unchanged (tasks.md 4.2)
- `backend/src/test/scala/com/helio/ai/ClaudeClientSpec.scala` — updated the pre-existing full-request exact-equality fixture ("wire model/max-tokens/temperature/messages through") to carry the first-message cache marker (tasks.md 4.6, skeptic-design-1.md CR1); new coverage: first-message marking for `send`, empty-messages guard, last-tools/last-first-message-block marking for `sendWithTools`, empty-tools/empty-history guard, and multi-hop cache-counter aggregation (tasks.md 4.3)
- `backend/src/test/scala/com/helio/api/routes/AssistantTelemetrySpec.scala` — new coverage: the zero-tool-call success line now asserts both cache fields default to `"0"`; a new multi-hop test asserts `cacheReadInputTokens` aggregates correctly (4 hops × 50 = 200) on the emitted telemetry record (tasks.md 4.4)
- `openspec/changes/assistant-prompt-caching/tasks.md` — checked off all 14 tasks
