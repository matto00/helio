# Files modified — HEL-984

- `backend/src/test/scala/com/helio/infrastructure/storage/LocalFileSystemSpec.scala` — replaced the racy poller-based atomic-write guard (64 MiB buffer + busy-spinning `Files.list` poller + `AtomicBoolean`, the concrete cause of an fd leak that aborted CI run 33948170131) with two deterministic, synchronous permission-based discriminators (D1: publish-by-rename via a read-only target; D2: staging requires target-directory write permission via a read-only directory), each preceded by a precondition self-check that fails loudly (naming the unmet precondition) rather than passing vacuously on a platform where POSIX permissions aren't enforced. Kept the existing move-failure test unchanged (it was collateral damage from the fd exhaustion, not independently racy, and is the only test exercising `write`'s `deleteIfExists` cleanup branch). Kept a standalone post-write "no `.tmp` residue" assertion. Rewrote the block comment to explain D1/D2, the fd-leak root cause, and that same-directory staging (P2) is deliberately left unguarded (design.md Decision 2b) because no deterministic discriminator exists for it.

No production code file has a net change: `backend/src/main/scala/com/helio/infrastructure/storage/LocalFileSystem.scala` was temporarily mutated to a bare `Files.write` for the mandatory mutation-proof (task 3), then fully reverted — `git diff` on that file is empty.

Cycle 2 (evaluation-1.md change request 1): the first commit bypassed the pre-commit hook (`-n`) citing a "pre-existing" `helio-mcp` typecheck failure. That diagnosis was wrong: `helio-mcp/` is byte-identical to `origin/main`, and `check:helio-mcp-types` exits 0 both on `origin/main` and in this worktree once `helio-mcp/node_modules` is installed. The actual cause was that `helio-mcp/node_modules` was never installed in this delivery worktree, so the typecheck step failed on missing module declarations — a worktree environment gap, not a code regression. `npm ci --prefix helio-mcp` was run and the amended commit passes the full pre-commit hook without a bypass. (Cycle 2 also closed the evaluator's non-blocking suggestion: the new "leaves no .tmp residue" test's `Files.list` call is now wrapped in `Using.resource` rather than left as an unclosed stream, in this file of all files.)
