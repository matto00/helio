## 1. Diagnose

- [x] 1.1 Read the CI failure output for run `33948170131` attempt 1 and record the verbatim failure messages. DONE (orchestrator): both tests failed with `java.nio.file.FileSystemException: ... Too many open files`; the suite then ABORTED with `NoClassDefFoundError: com/helio/infrastructure/storage/LocalFileSystem$`. Verbatim excerpts are in design.md Decision 4.
- [x] 1.2 Determine whether the move-failure test shares the first test's mechanism. DONE (orchestrator): it does NOT fail for its own reason and does NOT share a racy fixture — it failed at line 149 in its own fixture setup (`Files.write(inner.txt)`) as collateral damage from file-descriptor exhaustion caused by test 1's unclosed `Files.list` streams. See design.md Decision 4.
- [x] 1.3 Confirm the fd-leak mechanism directly: demonstrate that `Files.list(...).iterator()` without closing the returned `Stream` leaks descriptors under repeated calls (e.g. count `/proc/self/fd` entries, or run the old poller under a low `ulimit -n` and observe `Too many open files`). Record the evidence. If it cannot be demonstrated, say so plainly rather than assert the mechanism. DONE: standalone probe (`FdLeakProbe.java`) confirms `baseline fds=8` → `after 20000 unclosed Files.list: fds=40010` — matches fd-leak-evidence.md's numbers exactly. A second standalone probe (`OldPollerProbe.java`) recreates the old poller loop verbatim (unclosed `Files.list(dir).iterator()` spun against a 64 MiB write) and, run under `ulimit -n 600`, failed 2/3 times with `java.nio.file.FileSystemException: ... Too many open files` — directly reproducing the CI failure mode outside CI.
- [x] 1.4 Record that the flake does not reproduce on the dev box, with the local fd limit (`ulimit -n` = 524288) as the reason — this is the ticket's "every gate runs on the dev box" blind spot, and is itself a finding. DONE: confirmed `ulimit -n` on this box is 524288 (see verification below); the old poller only failed once forced under a CI-representative low limit (600), which is the blind spot fd-leak-evidence.md and design.md name.

## 2. Replace the racy guard

- [x] 2.1 Delete the 64 MiB buffer, the poller thread, the `AtomicBoolean`s and the `Try`/`scala.util.Try` and `AtomicBoolean` imports if now unused, from the `stages a same-directory temp file during a large write` test. Remove that test.
- [x] 2.2 Implement D1 (design.md Decision 2): existing target file at mode `0444` in a writable directory; assert `fs.write` SUCCEEDS and the target afterwards holds exactly the new bytes. This is only possible via a rename.
- [x] 2.3 Implement D2 (design.md Decision 2): existing writable target file (`0644`) in a `0555` directory; assert `fs.write` fails with `AccessDeniedException`, that the exception's message/path names the staging directory (positive identification — not the bare exception class, which a typo'd fixture path would also produce), and that the target still holds its ORIGINAL bytes (byte comparison, not `Files.exists`).
- [x] 2.4 Add precondition self-checks for both (design.md Decision 3): for D1, that a direct `Files.write` to a `0444` scratch file really is denied; for D2, that a `0555` scratch directory really does refuse file creation. On an unmet precondition, fail with a message naming it (e.g. "precondition not met: POSIX permissions are not enforced here (running as root?) — this guard cannot discriminate"), never a silent pass and never a misleading atomicity verdict.
- [x] 2.5 Restore permissions in a `finally` for every chmod'd fixture, so a failing assertion cannot leave a `0555` directory behind in the shared temp tree. (Note: the spec mixes in `BeforeAndAfterAll` but overrides no `afterAll`, so nothing deletes `tempDir` today — the restore is hygiene for the shared tree, not unblocking of an existing cleanup step. Do not add an `afterAll` as part of this ticket.)
- [x] 2.6 KEEP the existing `cleans up the temp file and leaves the original untouched when the atomic move fails` test unchanged in substance. Per design.md Decision 4 it was collateral damage, is already deterministic, and is the ONLY test exercising `write`'s `Files.deleteIfExists(tmp)` catch branch. Do not fold it into D2 and do not weaken it.
- [x] 2.7 Keep a success-path post-condition assertion that no `.tmp` residue remains after a normal write and that only the target file remains in its directory (this survives from the deleted test and must not be lost with it).
- [x] 2.8 Rewrite the block comment above these tests: explain what D1 and D2 each prove, state explicitly that same-directory staging (P2) is NOT guarded and why (design.md Decision 2b), and record that the previous poller was removed because it leaked file descriptors and took the whole suite down in CI.

## 3. Mutation-prove (acceptance criterion 2 — perform, do not assert)

- [x] 3.1 Revert `LocalFileSystem.write`'s body to a bare `Files.write(target, bytes)` (keeping `createDirectories`).
- [x] 3.2 Run the suite. Record VERBATIM that D1 goes red (expected: `AccessDeniedException` from the bare write) and that D2 goes red (expected: the write unexpectedly SUCCEEDS where a staged write must fail). Confirm each red matches the reason design.md predicts; if a red arrives for a different reason, that is a finding, not a pass. DONE — verbatim output recorded below; both reds matched the predicted mechanism exactly.
- [x] 3.3 Restore `LocalFileSystem.write`; confirm `git diff` on that file is empty. DONE — `git diff backend/src/main/scala/com/helio/infrastructure/storage/LocalFileSystem.scala` produces no output.
- [x] 3.4 Re-run and confirm green. DONE — 17/17 tests passed.

## 4. Verify

- [x] 4.1 `cd backend && sbt test` passes in full.
- [x] 4.2 Ran `testOnly com.helio.infrastructure.storage.LocalFileSystemSpec` 21 times total: 10 baseline, 3 under a deliberately low `ulimit -n 600` (chosen because sbt's own launcher needs >~800 fds to boot; 600 is well below the dev box's 524288 default and representative of CI's much lower ceiling), 5 under CPU contention (4 parallel `yes > /dev/null` loops), 3 more baseline. All 21 runs: 17/17 tests passed, 0 failures. Additionally, under the SAME `ulimit -n 600`, the standalone `OldPollerProbe` (recreating the deleted poller's exact unclosed-`Files.list` loop against a 64 MiB write) failed 2 of 3 runs with the CI's exact `Too many open files` exception — grounding the "old poller fails under this limit, new guard does not" comparison the skeptic asked for.
- [x] 4.3 Confirmed: no Flyway migration, no browser/Playwright use, no `NodeDependencyClosure`/preview-engine or frontend-river files touched. `git diff --name-only main...HEAD` shows only `backend/src/test/scala/com/helio/infrastructure/storage/LocalFileSystemSpec.scala` plus this OpenSpec change's own docs. The temporary mutation to `LocalFileSystem.scala` in 3.1 was fully reverted — `git diff` on that file is empty.
