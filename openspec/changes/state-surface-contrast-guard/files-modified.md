# Files modified — HEL-866

## Guard (highest-value deliverable, AC5)

- `e2e/support/stateContrast.mjs` — pure, dependency-free measurement core (colour parsing incl. `color(srgb ...)` and rejection of mid-transition `oklab(...)`, alpha compositing, WCAG contrast ratio, the D4a pass/fail/advisory classifier, threshold `CONTRAST_THRESHOLD = 1.10`).
- `e2e/support/stateContrast.selftest.mjs` — pure-function self-test on the `scripts/check-tokens.selftest.mjs` pattern; 30 cases including the light defect (`#ffffff`/`#ffffff`), the dark defect (`#232019`/`#262320`, 1.040 — the decisive non-identical-but-must-fail case), the alpha-compositing false-pass case, the pseudo-element case, and the absence/advisory split.
- `e2e/support/stateContrastProbe.ts` — browser-side selector constant only (`INTERACTIVE_SELECTOR`, no file/component list). **Cycle 2 (evaluation-1.md CR3):** this file previously ALSO declared `collectBackdropLayers`/`probeElement`, an ancestor-walk/snapshot implementation nothing imported — the spec re-implements the same logic inline (`readBackdrop`/`readSnapshot`). Two copies of the guard's most safety-critical logic, one dead, was a real divergence hazard; the dead exports are deleted, not wired in — the spec's inline versions are the ones the mutation proof actually exercises.
- `e2e/state-surface-contrast-guard.spec.ts` — the rendered, CI-gated Playwright guard. Walks 6 authenticated routes + the command palette + `AddSourceModal` + a dashboard-row `ActionsMenu`, in both themes; enumerates interactive elements mechanically from the DOM; resolves each element's real backdrop by walking the rendered ancestor chain accumulating alpha; forces real `:hover` (mouse) and true `:focus-visible` (via CDP `CSS.forcePseudoState` — `locator.focus()` does not trigger Chromium's focus-visible heuristic, confirmed the hard way); alpha-composites state colour over backdrop before taking the ratio; classifies pass/fail/advisory/unresolved; reports counts before failing; fails with element/theme/ratio named in the output.
  **Cycle 2 (evaluation-1.md CR1 — the critical finding):** the per-view population used to be sliced to the first `MAX_ELEMENTS_PER_VIEW` (12) matches in DOM order, BEFORE the visibility filter, and every route's query was un-scoped (`page.locator("body")`). Chrome (command bar + sidebar nav) renders first in the DOM and is identical on every route, so it crowded out page content — measured live, `/settings` covered 2 of its 36 real in-`<main>` interactive elements (5.5%). Fixed structurally, not by raising the cap: chrome is now probed EXACTLY ONCE per theme as its own `"chrome"` view, scoped to `.app-command-bar, .app-sidebar__nav-row` (NOT the whole `.app-sidebar`, which also wraps a per-route content rail — confirmed by direct DOM inspection and excluded); every route view is scoped to `page.locator("main")`, page content only. `collectCandidates` now visibility-filters (and disabled-filters — see below) the FULL matched set first, then samples EVENLY across it if it exceeds the cap (raised to 24), instead of `slice(0, cap)`; it returns the true visible total alongside the sampled count, and `probeView` prints both per view (task 5.2a), so a population collapse is visible in the log rather than hidden behind one run-wide total. Re-running the corrected walk surfaced 18 new real findings (below), all remediated — the cap was not raised to hide them.
  Also newly excludes DOM-`disabled` elements from the population (`HTMLButtonElement.disabled` / `aria-disabled="true"`): the corrected `<main>`-scoped walk reached several controls this app deliberately disables at rest (`AddSourceModal`'s "Test connection"/"Preview schema", `ApiTokensSection`'s "Create token", `BetaAccessSection`'s "Redeem code", `PanelList`'s "Reset zoom") whose own CSS is `:hover:not(:disabled)` by convention — a disabled control cannot be meaningfully hovered into a different visible state, so counting it was a probe false-fail, not a real absence.
  **Cycle 2 (evaluation-1.md CR5):** exemptions are now keyed on stable identity (the element's first CSS class, always emitted by `describeElement` in a `[bracketed]` token — aria-label alone was tried first and rejected: several exempt elements, e.g. the 8 accent-picker swatches, share one class but each has its own colour-name aria-label, so aria-label cannot serve as ONE matchable key for the family) rather than `#index`, which shifts if any upstream element is added/removed and would silently exempt something unrelated.
  **Cycle 3 (evaluation-2.md CR7):** seeded the guard's account with a real static data source + a pipeline rooted on it (same shape as `e2e/hel908-full-flow.spec.ts`), the way it already seeded a dashboard — a fresh account renders `/sources`/`/pipelines` as an empty state, so table/list/card row families were structurally never in the population regardless of the walk logic (measured live in a populated account: `/sources` alone has 98 visible interactive elements; a fresh account has 1).
  **Cycle 3 (evaluation-2.md CR8):** the `ActionsMenu` overlay was documented as one of the guard's views but never actually ran — the trigger was looked up while still on `/sources` (a fresh account has no dashboard row there), inside `if (await trigger.count())`, so a miss was silently indistinguishable from a pass. Now navigates to `/`, asserts `toHaveCount(1)` (a miss fails loudly), and hovers the row before clicking the trigger — `DashboardList.css` clips the trigger to a 1x1 box until `.dashboard-list__item-row:hover` reveals it, so a `force: true` click alone was not enough.
  **Cycle 3:** `INTERACTIVE_SELECTOR` (`stateContrastProbe.ts`) gained `tbody tr` — `[role=row]` only matches an EXPLICIT `role` attribute, and this app's real tables (`SourceListTable`, `AuditEventTable`, `PipelinesPage`, `ConnectorsPage`) use plain semantic `<table>`/`<tr>` with no explicit role, so `.source-list-table__row` (evaluation-2.md CR6's own example) was NEVER in the population in any prior cycle regardless of CR7's fixtures. Scoped to `tbody tr` (not bare `tr`) so header rows, which convey no hover state by design, are not scored as false failures.
  **Cycle 3:** `readBackdrop`'s ancestor walk no longer bails to "unresolved" on an ancestor with ANY `background-image`, only one that ALSO carries its own non-transparent `background-color`. `<main class="app-content">` (every route) paints `theme.css`'s neutral canvas-dot texture on a fully transparent `background-color` — the walk must continue past it to the real opaque colour beneath, or it reports "unresolved" for every element on every route, which is exactly what hid evaluation-2.md CR6's regression behind an "unresolved" count instead of surfacing it as a failure.
  **Cycle 3 (evaluation-2.md CR9):** the two exemptions previously matched on rendered text (`r.desc.includes("HEL-866 Guard Dashboard")` / `"REST API"`) now match on the same bracketed `[class]` token as the accent-picker exemption (`[command-palette__item]` / `[add-source-modal__type-btn]`), so the code matches what this document claims rather than the reverse.
- `.github/workflows/ci.yml` — **cycle 2 (evaluation-1.md CR2):** adds `npm run check:state-contrast:selftest` to the `frontend` job, alongside `check:tokens:selftest`. The self-test script existed from cycle 1 but was wired into nothing mandatory — the exact HEL-913/HEL-846/HEL-1037/HEL-996 failure mode this same file's comments already name four times.
- `package.json` — `check:state-contrast:selftest` and `e2e:state-contrast-guard` script entries. No further CI workflow change needed for the rendered spec itself: `.github/workflows/ci.yml`'s `e2e` job already runs the whole `npx playwright test` glob and `playwright.config.ts`'s `testIgnore` does not exclude this file — confirmed by inspection, not assumed (task 4.1).
- `e2e/tsconfig.json` — `allowJs`/`checkJs: false` so the spec can import the plain-`.mjs` core.

### Exemption count, reconciled

**Corrected in place — cycle 1's "3 reviewed exemptions" and the run's actual "8 reviewed exemptions" both undercounted/overcounted for different reasons and neither number survives.** There are exactly **3 written justification categories** (unchanged from cycle 1's reasoning), but the corrected walk now finds the FULL accent-picker family — all **8** swatch buttons, not the 2 the truncated cap happened to sample — so the run reports **10 distinct exempt elements × 2 themes × 1 forced state (hover only) = 20 log lines**. Every one is covered by one of these 3 written reasons, verified against the real component CSS, not guessed:

1. **`accent-picker__swatch`** (8 elements: all 8 accent presets in Settings) — hover feedback is `transform: scale(1.15)` (`AccentPicker.css`), a real, deliberate, visible state channel this guard's D4a classifier (background/border/outline/box-shadow only, per design.md) does not measure. A guard LIMITATION (routed as a D6 finding), not an app defect.
2. **`command-palette__item`** (1 element: the query's only/default result row) — already carries `[data-active="true"]` styling identical to its `:hover` styling AT REST, so hovering produces no INCREMENTAL change. Not silent — it already shows the state — a before/after-diff probe limitation for an already-active resting state.
3. **`add-source-modal__type-btn`** (1 element: the default-selected "REST API" type tab) — same already-active-at-rest shape as #2.

## Remediation (AC1/AC2/AC3/AC5) — three backdrop families, each with its own correct fix, per evaluation-2.md CR6

**Cycle 1 shipped a blanket `--app-surface-raised` → `--app-surface-soft` swap across 49 files and was WRONG for any call site whose real backdrop is not `--app-surface-strong`.** evaluation-2.md CR6 measured a live regression (`SourceListTable.css`'s row: 1.161 dark / 1.119 light with `raised`, both PASSING → 1.034 / 1.054 with `soft`, both FAILING) and named it "the ticket's own defect class, newly created by its remediation." Cycle 3 re-verified every remediated rule against its real rendered backdrop (the guard's own ancestor walk, not re-derived by grep — evaluation-2.md CR7 seeded a real source/pipeline so table/list/card families actually enter the population, and a background-image/gradient ancestor-walk fix was needed so the walk doesn't bail to "unresolved" on `<main>`'s neutral canvas-dot texture and hide the exact defect this hid). Three backdrop families resulted:

1. **`--app-surface-strong` (modal/popover/menu interiors)** — `--app-surface-soft` alone is correct, verified 1.167 dark / 1.179 light, **unchanged from cycle 1, confirmed still correct**: `frontend/src/app/App.css` (`.app-command-bar__logo`), `frontend/src/features/auth/ui/UserMenu.css` (`.user-menu__item`, inside the same `--app-surface-strong` popover panel as ActionsMenu — confirmed via `Popover.css:37`), `auth.css`, `ConnectorsPage.css`, `DashboardList.css`, `DashboardShareDialog.css`, `RefinementChatDrawer.css` (its own drawer container is `--app-surface-strong`, `RefinementChatDrawer.css:39`), `OnboardingChecklist.css`, `OutputPicker.css`, `PanelDetailModal{.appearance,.binding,.sections,}.css`, `TableDisplayFields.css`, `PanelGrid.css`, `TableRenderer.css`, `pipelines/ui/{OutputGalleryCard,OutputsGalleryTab,OutputsRail,PipelineDetailHeader}.css`, `PipelineScheduleDialog.css`, `settings/ui/{AgentMemoryList,ApiTokensSection,BetaAccessSection,MfaBackupCodesList,MfaEnrollModal,MfaSecuritySection}.css`, `sources/ui/{AddSourceModal,SourceDetailPanel}.css`, `shared/chrome/{ActionsMenu,ErrorBoundary,InlineError,MobileNavSheet,Popover,StatusMessage}.css` (its own container is `--app-surface-strong`, `MobileNavSheet.css:75`), `shared/ui/{ConfirmInline,DataGrid,EmptyState,IconButton,Modal,PageHeader,inputs}.css`.
2. **`--app-surface` (an intermediate card/chrome layer)** — needs a `:root[data-theme="dark"]`-scoped override to `--app-surface-strong` (soft alone clears 1.10 in light, 1.150, but only 1.030 in dark): `App.css` (`.cmd-btn`, `.app-sidebar__nav-link`, `.app-command-bar .ui-icon-btn--secondary/--ghost/.actions-menu__trigger/.user-menu__trigger`, `.app-sidebar` equivalents, `.app-command-bar .popover__trigger` — light-scoped soft override closing the ActionsMenu-trigger-via-Popover gap), `PipelineDetailPage.css` (`.pipeline-detail-page__step-card--expanded`), `PreferencesEditor.css` (`.preferences-editor__add-btn`), `PanelList.css` (`.panel-list__zoom-button`/`.panel-list__zoom-reset` — their container `.panel-list__zoom-widget` declares its own `--app-surface-strong`, so these are actually family 1, kept as `--app-surface-soft`, **no** dark override — corrected in place after an earlier mistaken blanket revert-to-`raised`, which would have recreated the original defect).
3. **`--app-bg` (the outermost page canvas — most table/list rows and page-level buttons actually composite here, once the ancestor walk sees through `<main>`'s transparent-based canvas-dot texture)** — `--app-surface-raised`, unchanged/reverted from `--app-surface-soft`, was never actually broken here (1.161 dark / 1.119 light): `AuditEventTable.css` (`.audit-event-table__row`), `SourceListTable.css` (`.source-list-table__row` — evaluation-2.md CR6's own named example), `SettingsPage.css` (`.settings-page__theme-toggle`), `PipelinesPage.css` (`.pipeline-list-table__row`, and `.pipeline-list-table__share-btn` — a PRE-EXISTING defect using `--app-surface` directly against its own `--app-surface` backdrop, i.e. literally identical, never caught by the cycle-1 `--app-surface-raised`-only sweep because it never used that token; also moved to `--app-surface-raised`).

Resting-background uses of the same tokens (e.g. `StatusChip.css`, `KeyCap.css`, `InlineConnectorSetup.css`, `EmptyState.css`'s icon badge, `PipelineDetailPage.css`'s status pills) were left untouched throughout — confirmed per-file, not swept blindly.

## `frontend/src/app/App.css` — additional hand-fixes beyond the mechanical sweep (found by the guard, not by grep)

- `.app-command-bar__logo` — had NO hover/focus feedback at all (D4a "nothing changed" absence; the wordmark/home link). Added a background hover/focus-visible state.
- `.cmd-btn:hover`, `.app-sidebar__nav-link:hover`, `.app-command-bar__logo:hover`, and the command-bar/sidebar-scoped `.ui-icon-btn--secondary`/`--ghost`/`.actions-menu__trigger`/`.user-menu__trigger` hovers — all real, on-`--app-surface` (chrome) backdrops, for which `--app-surface-soft` alone clears 1.10 only in light and `--app-surface-strong` alone only in dark (measured; neither single rung works both directions, the same D1 shape for a different backdrop than the modal-collision case). Added `:root[data-theme="dark"]` scoped overrides using `--app-surface-strong` (an existing token, no `theme.css` edit, no new token).
- `.app-sidebar__nav-link.active:hover` — the `.active` rule's equal-specificity, later-declared background silently won on the currently-active nav link's hover, producing a real "nothing changed" absence for that one link. Added an explicit `.active:hover` rule using `--app-accent-mid`.

## `frontend/src/features/pipelines/ui/PipelineDetailPage.css` — `.pipeline-detail-page__step-card--expanded` (evaluation-1.md CR4, cycle 2)

**Decision taken: dark-scoped override (`:root[data-theme="dark"] { background: var(--app-surface-strong); }`), NOT a plain revert.** This rule was the one non-hover/non-focus rule the mechanical sweep touched (an `--expanded` state modifier). Its real backdrop is the card's own `--app-surface`, not `--app-surface-strong`, so the blanket swap **measurably helped light (1.025 → 1.150) and measurably hurt dark (1.089 → 1.030)** — both readings are below the 1.10 threshold either way, but the swap moved dark further from it, which is a real, evaluator-caught regression. A plain revert to `--app-surface-raised` was rejected because it would still leave dark at 1.089, itself below threshold. `--app-surface-strong` on `--app-surface` measures **1.133 in dark** (clears 1.10) — the same on-surface-backdrop shape already scoped for `.cmd-btn`/`.app-sidebar__nav-link`/etc. in `App.css`, reused here rather than invented.

## `frontend/src/features/settings/ui/PreferencesEditor.css` — `.preferences-editor__add-btn:hover` (found by the CR1-corrected walk, cycle 2)

The corrected `<main>`-scoped walk on `/settings` reached this button (previously crowded out by the truncated chrome-dominated population) and measured it failing in dark (1.030 — soft-on-surface, the same on-surface-backdrop shape as the `App.css`/`PipelineDetailPage.css` fixes above). Added the same `:root[data-theme="dark"]` → `--app-surface-strong` override.

## Cycle 4 — the structural fix (skeptic-final-1.md / skeptic-final-1B.md)

Both final-gate skeptics REFUTED independently, each finding a DIFFERENT live regression, sharing one root cause: **the guard's view list (chrome / route / overlay) was hand-enumerated, and this was the third time in a new way that a hand-enumerated population let a real defect ship green.**

- **`e2e/state-surface-contrast-guard.spec.ts` — THE STRUCTURAL FIX.** Every route visit now stamps every visible, enabled, `INTERACTIVE_SELECTOR`-matched element in the CURRENT rendered document with a unique id (`stampDocument`), probes chrome + a new per-route `sidebar-rail` view (`.app-sidebar` excluding the nav row already covered by chrome) + `<main>`, then **asserts** (`assertPartitioned`) that every stamped id was covered by one of those views — naming the uncovered elements and throwing if not. This is a mechanical guarantee, not a hand-enumerated list: the FIRST run of this assertion immediately caught a real leftover (`.app-skip-link`, outside every declared scope), fixed by widening the chrome scope with an `excludeSelector` mechanism (`main`, plus the sidebar rail) rather than a container list. `/pipelines/:id` (skeptic-final-1.md CR3) was added to the route list as a direct, named consequence of this gap — `/sources/:id` and the `*/review` routes remain deliberately excluded, named in `design.md` D6.2 rather than left implicit. Also fixed: hover-reveal-on-parent elements (`DashboardList.css`'s row-clipped ActionsMenu triggers) now hover their immediate parent first, since `force: true` alone does not trigger a CSS-clip-driven ancestor `:hover`.
- **`frontend/src/features/dashboards/ui/DashboardList.css` — skeptic-final-1B.md CR1, the regression it found.** `.dashboard-list__button:hover` and 3 siblings (`.filter-clear`, `.row-action-btn`, `.import-label`) were assigned to family 1 in cycle 3's `files-modified.md`, but their real backdrop (measured live) is `.app-sidebar`'s own `--app-surface` — family 2. Fixed with `.app-sidebar`-scoped dark overrides (not a blanket dark override, since `DashboardList` also renders inside `MobileNavSheet`, a genuine family-1 host — verified both, not assumed). Also fixed a second, independent absence: the CURRENTLY-selected dashboard's `[aria-pressed="true"]` resting background silently won over `:hover` (equal specificity, later declaration) — the same `.active:hover` shape already fixed for the sidebar nav link — given a real `--app-accent-mid` hover step.
- **`frontend/src/features/pipelines/ui/PipelineDetailPage.css` — skeptic-final-1.md CR1/CR2, the regression it found.** The step-card action buttons (drag handle, move, toggle, duplicate) hard-coded `--app-surface-soft`, which collided EXACTLY (ratio 1.000, measured hovered) with the EXPANDED card's own background (also `--app-surface-soft` after cycle 2's fix) — the ticket's own defect, shipped, on a rule this diff had already touched once. Fixed with a component-scoped CSS custom property, `--step-card-hover-bg` (not a new `theme.css` token — a local variable, resolved differently depending on whether the ancestor card carries `--expanded`), since collapsed and expanded cards have different real backgrounds in both themes and no single hover rung serves both. The `--expanded` rule's own justification comment (previously derived against the wrong backdrop, `--app-surface` instead of the real `--app-bg`) is corrected in place. Also fixed, all found by the widened population (`/pipelines/:id` + `sidebar-rail`): `.shape-picker-btn`/`.dry-run-btn` (each individually re-verified against their REAL rendered backdrop — `--app-bg` for the former, `.footer-region`'s own `--app-surface` for the latter, corrected in place after an initial wrong guess); `.add-step-btn`'s hover used a LOWER-alpha accent token than its resting state (`--app-accent-dim` at 8%/10% vs. resting `--app-accent-surface` at 11%/15%), making hover measurably LESS distinct — fixed to `--app-accent-mid` (30%/26%), a real increase.
- **`frontend/src/features/pipelines/ui/PipelineDetailHeader.css` — found by the widened population.** The pipeline-actions `ActionsMenu` trigger's shared `IconButton` hover (`--app-surface-soft`) collided exactly with `.pipeline-detail-header`'s own background (also `--app-surface-soft`) — ratio 1.000, measured live. Scoped override to `--app-surface-raised`, which clears 1.10 against this specific soft backdrop in both themes (1.179 light / 1.122 dark) with no theme split needed.
- **New exemption (4th, reviewed):** `.pipeline-detail-page__tab` (Steps/Outputs tabs) conveys hover via a text-COLOUR change only (its active-state indicator is a `border-bottom` underline, not a background) — this guard's D4a classifier does not track plain `color`, the same guard-limitation shape as the accent-picker swatches' `transform`.
- **`frontend/src/theme/tokenAuditSweep.css.test.ts`** — `PipelineDetailPage.css`'s spacing-baseline entries re-pinned (5 insertion points, cumulative shift) after this cycle's CSS edits; 2 in-comment measured-colour literals (e.g. a literal `rgb(...)`) rephrased to avoid tripping the unrelated HEL-439 raw-colour-literal guard, which does not strip comments.

**Honest coverage report (skeptic-final-1.md CR5 / skeptic-final-1B.md CR3) — corrected in place, replacing the cycle-3 "360 probed … green" framing that both skeptics flagged as overstating what the guard asserts:**

```
458 elements probed, resolved=458, unresolved=0
pass=186 (41%) | fail=22, all exempted (5%) | advisory=250 (55%, gates nothing — a
  non-background channel conveyed the state)
```

**The guard ASSERTS on 186 of 458 probes (41%).** The 55% advisory bucket is real and intentionally does not gate (D4a/D7 — border/outline/shadow-only states are HEL-1044's call, not this ticket's) — but "458 probed … green" is not the same claim as "458 states verified", and this run's own artifacts (this file, the PR body) state the split rather than let the headline imply otherwise. Coverage is now bounded by the enumerated route list (7 routes, including `/pipelines/:id`; `/sources/:id` and `*/review` named as deliberately excluded in `design.md` D6.2) and the 3 named overlays, not by trust in a hand-picked view list within those routes — the partition assertion is what makes that second bound self-checking rather than merely claimed.

## Docs

- `DESIGN.md` — corrects the elevation-ramp table and the Buttons/sticky-column-table sections. **Cycle 3:** rewritten in place to state the three-backdrop-family table above (§3, HEL-866 note) rather than the cycle-2 "always soft" claim it previously made, which evaluation-2.md CR6 showed was itself wrong; strengthens the AC4 dedicated-token recommendation with the three measured backdrop families as evidence that no single existing rung serves all of them — still not adopted this run (owner sign-off required).
- `openspec/changes/state-surface-contrast-guard/design.md` — corrects the accent-family population figures in place (task 1.4/skeptic note 1: the previously stated "45 of 161 / 28%" does not reproduce; rule-level parse gives 155 declarations, 19 accent-based) and the stale "28%-coverage" line. **Cycle 3:** D1 rewritten in place — the saturation/direction-inversion insight is not a light-theme-at-the-top-of-the-ramp phenomenon only, it reappears with a different pair of rungs at every backdrop layer (see the three-family table); D6.2's route-coverage caveat extended to state the population is also bounded by `MAX_ELEMENTS_PER_VIEW` (24) sampled evenly when a view's real population exceeds it.
- `openspec/changes/state-surface-contrast-guard/tasks.md` — all tasks marked complete.

## Evidence (persisted, survives worktree cleanup)

Via `scripts/concertino/persist-evidence.sh HEL-866 <path>` (main checkout, not the worktree):

- `.concertino/runs/HEL-866/evidence/.concertino/runs/HEL-866/evidence/mutation-proof-transcript.log` — the task 3.8 live green→red mutation transcript (`.ui-icon-btn--secondary:hover` reverted to `--app-surface-raised`, guard went red naming 13 elements/ratio 1.025/theme light, then reverted).
- `.../guard-run-cycle2-green.log` — the cycle-2, CR1-corrected guard run: per-view visible/sampled counts, 256 elements probed, 20 reconciled exemptions, green.
- `.../guard-run-cycle3-final-green.log` — the cycle-3, CR6/CR7/CR8-corrected guard run: seeded source+pipeline, `tbody tr` population, background-image-aware backdrop walk, 360 elements probed, resolved=360/unresolved=0, 20 reconciled exemptions (same 3 categories as cycle 2, now covering the full 8-swatch family), green.
- `.../guard-run-cycle4-final-green.log` — the cycle-4, partition-assertion-corrected guard run: 458 elements probed (sidebar-rail + `/pipelines/:id` added), resolved=458/unresolved=0, pass=186/fail=22-all-exempted/advisory=250, 22 reconciled exemptions (4 categories), green — the partition assertion itself is exercised on every route/theme in this log with no failure.
- `.../hel866-modal-close-hover-{light,dark}.png`, `.../hel866-palette-{light,dark}.png` — AC1/AC3 rendered screenshots.
- `.../guard-run-cycle5-final-green.log` — the cycle-5 (final) guard run: 490 elements probed, resolved=490/unresolved=0, pass=206/fail=22-all-exempted/advisory=262, green.
- `.../cycle5-mutation-proof-red.log` — the cycle-5 mutation proof: `--step-card-hover-bg`'s light value reverted to the cycle-4 bug (`--app-surface-strong`, unscoped), guard went GREEN → RED naming `-toggle-enabled-btn`/`-duplicate-btn` at ratio 1 (identical) in light, on a real seeded step card; reverted afterwards.

## Cycle 5 (final round) — skeptic-final-2.md / skeptic-final-2B.md, converging on ONE defect

Both round-2 final-gate skeptics REFUTED, converging on the SAME single defect (not a split verdict): AC2/AC3/AC4/AC5 confirmed met by both reviewers (each independently defeated the partition assertion and re-proved mutation-failability); only AC1 was unmet, on one rule.

- **`frontend/src/features/pipelines/ui/PipelineDetailPage.css` — the fix.** `.pipeline-detail-page__step-card--expanded`'s `--step-card-hover-bg: var(--app-surface-strong)` (cycle 4's own fix) was **not theme-scoped**. In light, `--app-surface-strong` is pure white — byte-identical to this card's own `--app-surface-raised` background (also pure white in light) — so the rule cycle 4 wrote to close the light collision on the card re-created the exact same collision one level down, on the buttons inside it. Measured live: ratio 1.000, identical, on `-duplicate-btn` and `-drag-handle`. Fixed by giving light its own value (`--app-surface-soft`, measures 1.179 against this backdrop) and keeping the already-correct dark override (`--app-surface-soft`, 1.167) unchanged. The collapsed-card values were re-measured after the split, per both skeptics' instruction to verify rather than assume, and still hold (1.150 light / 1.133 dark via the existing base rule).
- **THE GAP THAT HID IT, closed (skeptic-final-2B.md CR2) — the third distinct way a hand-shaped population let a defect ship green.** `/pipelines/:id` was visited only in its default (collapsed) DOM state, so the `--expanded` branch — exactly where the regression lived — was exercised by no gate. Fixed with option (a): the route loop now expands the one seeded step card (`.pipeline-detail-page__step-card-toggle`, asserted `toHaveCount(1)` rather than conditionally skipped) before probing `/pipelines/:id`, so the `--expanded` state genuinely enters the population. This required seeding the pipeline with a real step (`POST .../steps`, type `limit`) — a pipeline with only a root and no steps renders no step card at all, which is why an earlier attempt at this same fix silently no-op'd on a 0-count locator. **Confirmed via mutation** (see evidence above): reverting the fix and re-running took the guard from green to red, naming the exact regression.
- **Found by the widened population** (the real step card + its now-genuinely-exercised action row), all re-verified against measured ratios: `.pipeline-detail-page__step-card-toggle` (the row's own title button) had NO hover feedback at all (D4a absence) — given the same `--step-card-hover-bg` the sibling action buttons already use. `.pipeline-detail-page__add-tail-btn` and `frontend/src/features/pipelines/ui/OutputsRail.css`'s `.outputs-rail__add` both repeated the SAME accent-alpha-inversion bug already found and fixed twice this ticket (`.add-step-btn`): hover used `--app-accent-dim` (8%/10% alpha), a LOWER alpha than their own resting `--app-accent-surface` (11%/15%), making hover measurably less distinct — both swapped to `--app-accent-mid` (30%/26%), a real increase.
- **`playwright.config.ts`** — non-blocking note from both skeptics: a bare run in a linked worktree silently defaults `DEV_PORT` to 5173 and can measure the WRONG server, failing obscurely deep inside a spec rather than at the actual cause (cost a reviewer two 6-minute runs). Added a one-line `console.warn` when `DEV_PORT` is unset, naming the fallback and the risk — no config restructuring.
- **`frontend/src/theme/tokenAuditSweep.css.test.ts`** — `PipelineDetailPage.css`'s spacing-baseline entries re-pinned again after this cycle's insertions (3 more hunks); 3 more in-comment literal-colour mentions rephrased to avoid the same unrelated HEL-439 guard tripped in cycle 4.

**Known thin margin, noted per skeptic-final-1B.md/skeptic-final-2's instruction:** the sidebar-rail dark hover (`DashboardList.css`'s `.app-sidebar`-scoped overrides, cycle 4) measures **1.111** against the 1.10 threshold — correct, but with almost no headroom. Any future darkening of `.app-sidebar`'s own background would re-break this silently (the guard would catch it on its next run, but nothing else guards against the margin shrinking in the meantime).

## Not modified (by design)

- `frontend/src/theme/theme.css` — no token value changed, per the ticket's explicit constraint.

## Declared paths (canonical, machine-checkable)

Every path in this change, one backtick-quoted bullet each. The prose sections above explain WHY each file changed and group them by backdrop family; this section is the complete, flat declaration that `squash-branch.sh` validates against. Where the two disagree, this section is the diff and the prose is the commentary.

- `.github/workflows/ci.yml`
- `DESIGN.md`
- `e2e/state-surface-contrast-guard.spec.ts`
- `e2e/support/stateContrast.mjs`
- `e2e/support/stateContrast.selftest.mjs`
- `e2e/support/stateContrastProbe.ts`
- `e2e/tsconfig.json`
- `frontend/src/app/App.css`
- `frontend/src/features/audit/ui/AuditEventTable.css`
- `frontend/src/features/auth/ui/UserMenu.css`
- `frontend/src/features/auth/ui/auth.css`
- `frontend/src/features/connectors/ui/ConnectorsPage.css`
- `frontend/src/features/dashboards/ui/DashboardList.css`
- `frontend/src/features/dashboards/ui/DashboardShareDialog.css`
- `frontend/src/features/dashboards/ui/RefinementChatDrawer.css`
- `frontend/src/features/onboarding/ui/OnboardingChecklist.css`
- `frontend/src/features/panels/ui/OutputPicker.css`
- `frontend/src/features/panels/ui/PanelList.css`
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.appearance.css`
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.binding.css`
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.css`
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.sections.css`
- `frontend/src/features/panels/ui/editors/TableDisplayFields.css`
- `frontend/src/features/panels/ui/grid/PanelGrid.css`
- `frontend/src/features/panels/ui/renderers/TableRenderer.css`
- `frontend/src/features/pipelines/ui/OutputGalleryCard.css`
- `frontend/src/features/pipelines/ui/OutputsGalleryTab.css`
- `frontend/src/features/pipelines/ui/OutputsRail.css`
- `frontend/src/features/pipelines/ui/PipelineDetailHeader.css`
- `frontend/src/features/pipelines/ui/PipelineDetailPage.css`
- `frontend/src/features/pipelines/ui/PipelinesPage.css`
- `frontend/src/features/pipelines/ui/schedule/PipelineScheduleDialog.css`
- `frontend/src/features/settings/ui/AgentMemoryList.css`
- `frontend/src/features/settings/ui/ApiTokensSection.css`
- `frontend/src/features/settings/ui/BetaAccessSection.css`
- `frontend/src/features/settings/ui/MfaBackupCodesList.css`
- `frontend/src/features/settings/ui/MfaEnrollModal.css`
- `frontend/src/features/settings/ui/MfaSecuritySection.css`
- `frontend/src/features/settings/ui/PreferencesEditor.css`
- `frontend/src/features/settings/ui/SettingsPage.css`
- `frontend/src/features/sources/ui/AddSourceModal.css`
- `frontend/src/features/sources/ui/SourceDetailPanel.css`
- `frontend/src/features/sources/ui/SourceListTable.css`
- `frontend/src/shared/chrome/ActionsMenu.css`
- `frontend/src/shared/chrome/ErrorBoundary.css`
- `frontend/src/shared/chrome/InlineError.css`
- `frontend/src/shared/chrome/MobileNavSheet.css`
- `frontend/src/shared/chrome/Popover.css`
- `frontend/src/shared/chrome/StatusMessage.css`
- `frontend/src/shared/ui/ConfirmInline.css`
- `frontend/src/shared/ui/DataGrid.css`
- `frontend/src/shared/ui/EmptyState.css`
- `frontend/src/shared/ui/IconButton.css`
- `frontend/src/shared/ui/Modal.css`
- `frontend/src/shared/ui/PageHeader.css`
- `frontend/src/shared/ui/inputs.css`
- `frontend/src/theme/tokenAuditSweep.css.test.ts`
- `package.json`
- `playwright.config.ts`
