- `.github/workflows/ci.yml` — wires `check:repo-integrity`, `check:scala-quality`, `check:schemas`, `check:spec-structure` (previously hook-only) as `run:` steps in the `frontend` job after the existing `check:openspec`/`check:openspec:selftest` steps, plus the new `check:precommit-ci-parity`/`check:precommit-ci-parity:selftest` drift guard.
- `scripts/check-precommit-ci-parity.mjs` — new drift guard: parses `.husky/pre-commit`'s `npm run <script>` lines and bare npm aliases (`npm test`/`npm start`), resolves each script generically via `package.json`'s own `scripts` map (including underlying `node <path>` invocations), scans only the jobs named in `ci-complete`'s `needs:` array in `.github/workflows/ci.yml`, and fails listing any hook script not reachable from that scope.
- `scripts/check-precommit-ci-parity.selftest.mjs` — self-test for the guard above; fixture hook + fixture `ci.yml`-shaped text (including a fixture `ci-complete`/`needs:` block and fixture `package.json` scripts), asserting pass and fail cases including the bare-alias `npm test` case and the indirect `node <path>` invocation case.
- `package.json` — adds `check:precommit-ci-parity` and `check:precommit-ci-parity:selftest` npm scripts.
- `openspec/changes/enforce-precommit-checks-ci/tasks.md` — marks tasks 1.1–4.1 complete; 4.2 (push + confirm CI green) left open since it isn't verifiable from the executor's local worktree.

No changes to `.husky/pre-commit` — the manual mutation-failability demonstration (adding a throwaway `npm run check:does-not-exist-in-ci` line, confirming the guard reports the gap and exits non-zero, then reverting) was done against the real file in a scratch/revert cycle and is not part of this diff. See return summary for the before/after transcript.
