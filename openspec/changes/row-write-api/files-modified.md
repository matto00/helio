# Files modified — HEL-1077 row write API

- `backend/src/main/scala/com/helio/infrastructure/persistence/sources/DataSourceRepository.scala` — added `lockSource` (`SELECT ... FOR UPDATE`); added `appendRows`; restructured `replaceDatasetRows` into `replaceRows(id, declaration: Option[...], rows, maxRows, updatedAt, user)`, moving schema resolution, `DatasetRowValidator.validate`, delete-then-insert, and `inferred_schema` recompute all inside one locked transaction.
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — updated `applyStaticRefresh`'s call site for the `replaceRows` rename/signature (removed its now-redundant pre-call validation); added `appendRows`/`replaceRows` service methods (kind check, empty-array semantics per verb, ACL via `findByIdOwned`); added `RowWriteRow`/`RowWriteResult` result types. skeptic-final-1.md CR1: all three `Instant.now()` call sites that generate a dataset-row write timestamp now `.truncatedTo(ChronoUnit.MICROS)`, matching Postgres' stored precision.
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceProtocol.scala` — added `RowWriteRequest`/`RowWriteRowResponse`/`RowWriteResponse` wire types + spray-json formatters.
- `backend/src/main/scala/com/helio/api/routes/sources/DataSourceRoutes.scala` — added `POST`/`PUT /api/data-sources/:id/rows` routes.
- `backend/src/test/scala/com/helio/infrastructure/persistence/sources/DataSourceRepositorySpec.scala` — updated `replaceDatasetRows` callers for the `replaceRows` rename; added append/replace/lock/concurrency/row-limit/inferred-schema unit + real-concurrency tests, including evaluation-1.md's required concurrent-appends-jointly-exceed-the-limit case and skeptic-final-1.md CR2's strengthened PUT-vs-refresh schema-race test (genuinely different schema shapes, deterministic final-state assertion, probe-confirmed to fail 15/15 with the lock removed).
- `backend/src/test/scala/com/helio/api/routes/sources/DataSourceRoutesSpec.scala` — added route-level tests for both new endpoints (append/replace happy paths, empty-array semantics, validation reuse, row-count limit, non-dataset rejection, cross-owner 404, response shape). skeptic-final-1.md CR1: added per-route DB-round-trip `updatedAt` precision tests (probe-confirmed to fail pre-fix).
- `backend/src/test/scala/com/helio/infrastructure/persistence/RlsOwnerTablesSpec.scala` — added RLS tests exercising `DataSourceService.appendRows`/`replaceRows` under the non-superuser `helio_app_test` role.
- `schemas/sources/row-write-request.schema.json`, `schemas/sources/row-write-row-response.schema.json`, `schemas/sources/row-write-response.schema.json` — new JSON Schemas for the row-write wire contract.
- `frontend/src/features/sources/types/dataSource.ts` — added `RowWriteRequest`/`RowWriteRow`/`RowWriteResponse` types.
- `frontend/src/features/sources/services/dataSourceService.ts` — added `appendSourceRows`/`replaceSourceRows` typed service calls (no UI consumer yet — HEL-1080).
