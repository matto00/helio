- `backend/src/main/scala/com/helio/domain/InProcessPipelineEngine.scala` — adds a nullable-default `connector: RestApiConnector` constructor param, `maxRunRows = 1000`, and `loadRows` cases for `RestSource` (via `connector.fetch`, null-guarded) and `SqlSource` (via `SqlConnector.fetch`), converting connector rows with `PipelineRowJson.jsRowToRow`.
- `backend/src/main/scala/com/helio/domain/PipelineRowJson.scala` — adds the `jsRowToRow` helper (per-field `JsObject` → `Row` conversion, with a scalar fallback), used by the new `loadRows` cases.
- `backend/src/main/scala/com/helio/services/PipelineRunService.scala` — adds a nullable-default `connector: RestApiConnector` constructor param (threaded into `InProcessPipelineEngine`); removes the hardcoded `RestSource`/`SqlSource` rejection blocks in `runPipeline` and `previewStep`; changes `SparkUnsupportedKinds` to `Set.empty[String]` (kept as a forward-looking extension point, not deleted).
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — passes the existing `connector` instance into `PipelineRunService`'s constructor at its one production call site.
- `backend/src/main/scala/com/helio/services/PipelineProposalService.scala` — updates stale comments describing `rest_api`/`sql` as reaching `recordUnrunnable` unconditionally; **fixes a design gap found during test-writing**: the `createPipeline` guard that routes to `recordUnrunnable` (instead of `submit`) now checks `SparkUnsupportedKinds.contains(resolved.kind) || resolved.fetchError.isDefined`, not `SparkUnsupportedKinds` alone — emptying `SparkUnsupportedKinds` had silently broken the *unchanged* base-spec requirement ("Source-fetch failure is a structured, rolled-back error": a schema-fetch failure at inline-source creation time must still report a blocked run, not roll back) because both concerns were previously gated by the same single condition; also drops the now-inaccurate "...once `${kind}` execution is supported" tail from the fetch-error `blockedReason` message.
- `backend/src/test/scala/com/helio/domain/InProcessPipelineEngineSpec.scala` — adds `ScalatestRouteTest`/`BeforeAndAfterAll` mixins, a stub `RestApiConnector`, and an embedded-Postgres fixture; adds `loadRows` coverage for `RestSource` (success, connector-fetch failure, null-connector guard) and `SqlSource` (success, unreachable-database failure).
- `backend/src/test/scala/com/helio/services/PipelineRunServiceSpec.scala` — adds a minimal typed `ActorSystem` + stub `RestApiConnector`, threads `connector` into the test `PipelineRunService`, adds `seedRestDs`/`seedSqlDs` helpers; adds `submit`/`previewStep` coverage for a healthy `rest_api`/`sql` base source (real run, populated output DataType) and an unreachable one (422, `last_run_status = "failed"`, no categorical-rejection message).
- `backend/src/test/scala/com/helio/api/routes/PipelineRunRoutesSpec.scala` — design.md D7 fixture additions: `RestSuccessUrl`/`RestFailureUrl`/`stubConnector`, a `connector` param on `makeRoutes`, and a `seedDsWithConfig` helper (parameterizing the existing `seedDs`); splits the three tests that asserted the removed categorical-rejection outcome (full-run rest_api, full-run sql, preview rest_api) into a success case (200, populated rows) and a renamed unreachable-source case (422).
- `backend/src/test/scala/com/helio/api/PipelineApplyProposalSpecBase.scala` — adds a `RestRunFailUrl` (call-counted: succeeds on the first fetch — schema inference — and fails on every subsequent fetch — the run) to simulate "endpoint becomes unreachable between schema inference and the run"; exposes `sqlPort` so subclasses can seed a reachable inline `sql` source against the shared embedded Postgres.
- `backend/src/test/scala/com/helio/api/PipelineApplyProposalRollbackSpec.scala` — updates the two existing tests that asserted the old "blocked run" outcome for a healthy `rest_api` source (inline and existing-`sourceId`) to assert a real completed, populated run instead; adds a healthy inline `sql` proposal-apply test (same non-blocked assertion); adds a run-time-fetch-failure test (schema inference succeeds, the run's fetch fails) asserting full rollback — the new scenario this change's spec delta adds to `pipeline-proposal-apply`.
