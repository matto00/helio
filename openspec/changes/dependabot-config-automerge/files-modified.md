- `.github/dependabot.yml` — new: four ecosystems (npm root, npm /frontend, github-actions root, sbt /backend), weekly schedule, grouping per design.md Decision 2 (dev-dependencies + react groups for npm entries; catch-all `patterns: ["*"]` groups for github-actions/sbt), `dependencies` label, `open-pull-requests-limit: 10`.
- `.github/workflows/dependabot-auto-merge.yml` — `workflow_run`-triggered auto-merge for the `CI` workflow (design.md Decision 1), re-verifies resolved PR's live head SHA + `dependabot[bot]` authorship + `baseRefName == main` via a single `gh pr view` call, squash-merges patch/minor, labels (creating if absent) and skips merge for major updates. `permissions:` includes `issues: write` for label creation. **Cycle 2 fix (skeptic-final-1.md):** removed the non-existent `pr-number` input to `dependabot/fetch-metadata@v3` (that action cannot run under `workflow_run` at all — it resolves the PR only from `context.payload.pull_request`); now reads the `update-type` signal from a `dependabot-semver-{patch,minor,major}` label instead, applied by the new companion workflow below.
- `.github/workflows/dependabot-metadata.yml` — **new (cycle 2).** `pull_request`-triggered companion workflow (genuine `context.payload.pull_request`, where `fetch-metadata` actually works); job gated to Dependabot PRs only (skips cleanly for human PRs); runs `dependabot/fetch-metadata@v3` and records its `update-type` output as one of three PR labels for `dependabot-auto-merge.yml` to read back.
- `openspec/changes/dependabot-config-automerge/design.md` — **cycle 2:** added Decision 1a documenting the `fetch-metadata`/`workflow_run` incompatibility (verified against the action's real `action.yml` + compiled `dist/index.js`), why a commit-trailer-parsing alternative was checked against this repo's real Dependabot commit history and ruled out (no `update-type` key present on any of the 8 real grouped-PR commits), and the two-workflow label-handoff design adopted instead; updated Risks/Trade-offs.
- `openspec/changes/dependabot-config-automerge/tasks.md` — **cycle 2:** section 2 rewritten to describe the actual label-handoff mechanism (tasks 2.3/2.3a-c) in place of the false `pr-number` claim; added 2.7 (non-Dependabot PR ends the job green, not red — traced by hand against both workflow files). **Cycle 3 (skeptic-final-2.md CR1/CR2):** 2.3c corrected — `fetch-metadata` derives `update-type` entirely from the commit message (no GitHub API call), so an inline parse was not impossible, only a worse engineering tradeoff than reusing the action; 2.6 corrected to state label-create accepts either `issues: write` or `pull-requests: write` (both granted for safety/clarity, not because either alone is insufficient).
- `openspec/changes/dependabot-config-automerge/design.md` — **cycle 3 (skeptic-final-2.md CR1/CR2):** Decision 1a's inline-parse rejection reworded from a false "not viable/impossible" claim to the true engineering-tradeoff rationale (reuse `fetch-metadata`'s maintained semver-diff logic rather than reimplement it); the major-label permissions note corrected to state either `issues`/`pull-requests` write suffices for label creation.
- `.github/workflows/dependabot-metadata.yml` — **cycle 3:** added `issues: write` to `permissions:` alongside `pull-requests: write` (skeptic-final-2.md CR2 — resolves the permission-claim mismatch in the safe direction, matching GitHub's own auto-label example); `grep -qx` → `grep -x ... > /dev/null` to remove the `set -euo pipefail` SIGPIPE hazard (non-blocking note).
- `.github/workflows/dependabot-auto-merge.yml` — **cycle 3:** corrected the `issues: write` permissions comment (no longer claims `pull-requests: write` alone is insufficient for label creation); `grep -qx` → `grep -x ... > /dev/null` in both label-check sites (non-blocking note).
