- `backend/src/main/scala/com/helio/domain/steps/FilterStep.scala` — `=`/`!=` now coerce numerically only when the row value's runtime type is itself numeric (new private `numericFieldValue` helper); string-typed row values, including numeric-looking strings, keep exact string equality unchanged. `contains`, `is null`, `is not null`, and the ordering operators are untouched.
- `backend/src/test/scala/com/helio/domain/steps/FilterStepSpec.scala` — adds coverage on materialised rows returned by `FilterStep.apply`: numeric `=`/`!=` coercion (`"0"`, `"0.0"`, non-matching, non-numeric condition value), string-column equality (`position`), numeric-looking string columns (`player_id "007"` vs `"7"`), null/absent row-value asymmetry for `=`/`!=`, and `contains` staying textual.
