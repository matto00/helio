- `backend/src/main/scala/com/helio/domain/engine/ExpressionEvaluator.scala` — added `parseProblem` (design Decision 1: the write/run-path static parseability predicate) and `compile`/`CompiledExpression` (design Decision 6: parse once, evaluate per row); `evaluate` reimplemented in terms of `compile`.
- `backend/src/main/scala/com/helio/domain/steps/ComputeStep.scala` — `companion.validateRawConfig` override (Decision 3, write-path rejection) and `companion.requiredConfigProblems` extended (Decision 4, run/analyze rejection); `ComputeStep.apply` hoists the parse out of the row loop (Decision 6), staying total.
- `backend/src/test/scala/com/helio/domain/engine/ExpressionEvaluatorSpec.scala` — compile-only red proof test for `parseProblem` against the production expression (task 1.2, comment corrected in cycle 2 per evaluation-1.md to say "compile red, not behavioural" — the behavioural red for this expression is carried elsewhere); guard test asserting `parseProblem`/`evaluate` agreement across strict/legacy/unparseable/empty expressions (task 1.3).
- `backend/src/test/scala/com/helio/domain/steps/ComputeStepSpec.scala` (new) — companion-level write-path tests: proof that the production expression is rejected (task 2.1); guards for empty/whitespace-only expressions staying savable (task 2.4) and legacy bare-identifier expressions staying acceptable (task 2.5, the Decision 1 regression guard); read-path tolerance guard (task 4.3).
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineStepRoutesSpec.scala` — real route-level proof that POSTing the production expression 422s and stores nothing (task 2.2); update-surface test relabelled PROOF in cycle 2 (evaluation-1.md non-blocking correction — it is genuinely red on unmodified `main`, not mutation-only) and a legacy-expression create guard.
- `backend/src/test/scala/com/helio/domain/steps/PipelineStepRequiredConfigSpec.scala` — run-surface proof that a stored unparseable step fails naming step id/kind/reason (task 3.1); analyze-surface proof via the write-path `shapeRejection` route (task 3.5, write-path prefix); guard that an empty expression reports "missing", not a parse error (task 3.4); engine-level guard that row-dependent failures (divide-by-zero, null operand) stay per-row `null` after the Decision 6 hoist (task 4.1, relabelled from tasks.md's "proof" to guard — confirmed already green on unmodified `main`; cycle 2 corrected this test's comment, which had falsely claimed "on MATERIALISED ROWS" — it asserts on an in-memory `engine.execute` return, and now says so, pointing at its materialised-rows counterpart in `PipelineRunServiceSpec`).
- `backend/src/test/scala/com/helio/services/pipelines/PipelineRunServiceSpec.scala` — end-to-end proof, on materialised `DataTypeRowRepository` rows, that a stored unparseable compute step fails the real run rather than writing a null-filled column (tasks 3.1/3.2); proof that preview fails the same way rather than returning blank-column rows (task 3.5's preview half). Cycle 2 (evaluation-1.md Change Request 1) added a new materialised-rows guard test — a parseable expression run through the real `service.submit` over a divide-by-zero row and a NULL-`score` row, asserted via `dataTypeRowRepo.listRows` — plus a `seedDsWithDataIncludingNullScore` fixture; confirmed green on unmodified `main`, so labelled GUARD, not proof.
- `openspec/changes/reject-static-compute-expression/tasks.md` — all tasks marked complete.
