- `backend/src/main/scala/com/helio/domain/model.scala` — added `MetricId`, `MetricFormat`, `MetricAggregation` (allow-list `fromString`/`validate`), and `MetricDefinition` domain case classes (tasks 1.1–1.4).
- `backend/src/main/resources/db/migration/V75__metrics.sql` — new Flyway migration creating the `metrics` table: owner FK to `users(id)`, `data_type_id` FK `ON DELETE CASCADE` to `data_types(id)`, indexes on `owner_id` and `data_type_id`, `allowed_dimensions`/`format` JSONB columns, owner-only RLS (`ENABLE` + `FORCE` + `metrics_owner` policy) (tasks 2.1–2.4).
- `backend/src/test/scala/com/helio/infrastructure/RlsPolicyGuardSpec.scala` — added `metrics` to the `rlsTables` allowlist per CONTRIBUTING.md's "Adding a new ACL'd table" checklist (not an explicit tasks.md item, but required by the binding standard).
- `backend/src/main/scala/com/helio/infrastructure/MetricRepository.scala` — new Slick repository mirroring `DataTypeRepository`/`AlertRuleRepository`: `insert`/`update` validate `aggregation` via `MetricAggregation` before writing; `findByIdOwned`/`listByOwner`/`delete` under `withUserContext`; `findByIdInternal` under `withSystemContext`; JSONB `MappedColumnType`s for `Vector[String]` and `MetricFormat` (tasks 3.1–3.7).
- `backend/src/main/scala/com/helio/api/protocols/MetricProtocol.scala` — new protocol trait: `RootJsonFormat[MetricFormat]` (used directly by the JSONB column encoding) plus `MetricResponse`/`MetricResponse.fromDomain` wire DTO mirroring `AlertRuleResponse` (task 4.1).
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — mixed `MetricProtocol` into the aggregator trait; updated the cross-trait-dependency doc comment (task 4.1).
- `backend/src/test/scala/com/helio/infrastructure/MetricRepositorySpec.scala` — new repository spec: CRUD round-trip, owner-scoping (documented dev/CI RLS-testing gap, matching `AlertRuleRepositorySpec`'s pattern), CASCADE delete from the bound `DataType`, `findByIdInternal`, and `aggregation` allow-list rejection/acceptance at insert/update (tasks 5.1–5.4).
- `backend/src/test/scala/com/helio/api/protocols/MetricProtocolSpec.scala` — new JSON round-trip spec for `MetricFormat` and `MetricResponse.fromDomain` (task 5.5).
- `openspec/changes/metric-definition-model-persistence/tasks.md` — all 22 tasks checked off.
