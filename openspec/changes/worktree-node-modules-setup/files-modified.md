# Files modified — HEL-326

Two repos. The concertino files are the SOURCE of truth; the helio
`scripts/concertino/*` files are GENERATED from them by `concertino sync`.

## Repo: concertino (`~/Development/concertino`, branch `task/worktree-link-node-modules/HEL-326`)

- `core/scripts/setup-worktree.sh` — added the `CONCERTINO_LINK_MODULES` populate step (hardlink copy `cp -al` when lockfiles match, `npm ci` fallback on drift / missing main modules / `cp -al` failure), placed after env-file copy and before the hooks loop; every fallible command guarded under `set -euo pipefail` to degrade to a `note:` and still reach READY. Also documented `CONCERTINO_LINK_MODULES` in the header comment block.
- `bin/concertino` — `withDefaults`: default `worktree.linkModules = []`; `renderEnv`: emit `CONCERTINO_LINK_MODULES` (space-joined, same pattern as `CONCERTINO_ENV_FILES`).
- `config/concertino.schema.json` — documented the `worktree.linkModules` field (array of module dirs relative to repo root; lockfile assumed at `dirname(dir)/package-lock.json`).

## Repo: helio (worktree `.claude/worktrees/task/worktree-node-modules-setup/HEL-326`, branch `task/worktree-node-modules-setup/HEL-326`)

- `concertino.config.json` — added `worktree.linkModules: ["frontend/node_modules"]`.
- `scripts/concertino/setup-worktree.sh` — regenerated by `concertino sync`; now contains the populate step.
- `scripts/concertino/.concertino.env` — regenerated; now contains `CONCERTINO_LINK_MODULES='frontend/node_modules'`.
- `openspec/changes/worktree-node-modules-setup/**` — the OpenSpec change (proposal/design/tasks/spec/ticket + this file).

## Note

Push + PR for both repos is DEFERRED to the delivery phase (orchestrator), per the
executor brief — tasks.md 5.1's "push; open a PR" is intentionally not done here.
</content>
