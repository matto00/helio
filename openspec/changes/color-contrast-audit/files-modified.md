- `frontend/src/theme/theme.css` — darkened light-theme `--app-success`/`--app-warning`/`--app-error` and `--app-text-muted`, lightened dark-theme `--app-text-muted` and `--app-error`, each with a comment recording the failing composite and the new measured margin (AC1/AC2/D4/D8, plus a genuine dark-theme `--app-error` finding design.md's own premise missed).
- `frontend/src/theme/tokenContrastGuard.css.test.ts` — new. Parses both `data-theme` blocks from `theme.css` source (never transcribed), asserts the full neutral-surface + intent-tint-composite + muted-on-tint matrix clears 4.5:1 in both themes, asserts the `--app-accent-ink` floor across all 8 presets, records (without asserting) cross-intent cells, and carries vacuity refusal + mutation arms (AC4, D1-D4, D6).
- `docs/contrast-audit.md` — new. The committed contrast table (AC1): every audited pair per theme with its ratio and verdict, the rendered-confirmation evidence, the accent-ink floor results, the accent-on-surface shortfall (D5, stated untracked), and the documented exceptions (cross-intent cells, D9's out-of-scope accent backdrops).
- `DESIGN.md` — added a pointer to `docs/contrast-audit.md` from the Color token table (D7 — a pointer, not a duplicate copy).
- `openspec/changes/color-contrast-audit/tasks.md` — all tasks marked complete.
