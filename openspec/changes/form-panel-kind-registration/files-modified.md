- `backend/src/main/resources/db/migration/V108__add_form_panel_kind.sql` — new migration: widens `panels_kind_check` to admit `form` and adds `panels.form_config JSONB NULL` (C3, one atomic migration)
- `backend/src/main/scala/com/helio/domain/panels/FormPanel.scala` — new: `FormFieldSpec`/`FormSubmitSpec`/`FormPanelConfig`/`FormPanel` domain types, strict decode (D9 layer i), `validateConfig`, `applyPatch`, companion. Cycle 2 (evaluation-1.md CR3): added a top-level `AllowedKeys` check to `FormPanelConfig.read` (mirrors its two children, closes the `submitt`-typo silent-drop defect); made `"submit": null` reject like any other non-object instead of silently becoming `Default` (non-blocking suggestion); added a comment on why `decodeCreate` is a distinct name from `decode` (non-blocking suggestion)
- `backend/src/main/scala/com/helio/domain/model/Panel.scala` — registers `FormPanel.Kind -> FormPanel.companion` in `Panel.Registry`; adds `PanelKind.Form`
- `backend/src/main/scala/com/helio/domain/model/model.scala` — adds `PanelType.Form` plus `fromString`/`asString` arms and the rejection-message update; `Default` stays `Divider`
- `backend/src/main/scala/com/helio/domain/panels/PanelConfigCodec.scala` — adds the `form` arm to `encodeConfig`, `FormCreate`, `decodeCreateConfig`, `applyConfigPatchUnsafe`
- `backend/src/main/scala/com/helio/services/panels/PanelServiceHelpers.scala` — adds the `FormCreate` arm to `buildNewPanel`; adds `dataSourceIdFromCreateConfig`/`dataSourceIdFromConfigPatch` (mirrors `outputId*`)
- `backend/src/main/scala/com/helio/services/panels/PanelService.scala` — wires a nullable-optional `dataSourceRepo`; adds `rejectMissingDataSource` (D6), called from `buildForCreate` and `update`
- `backend/src/main/scala/com/helio/infrastructure/persistence/panels/PanelRowMapper.scala` — explicit `form` arm in `rowToDomain` (ahead of the `OutputPanel` fallthrough) with a tolerant catch-and-fallback-to-`Empty` decode (D9 layer iii / C11); `domainToRow` writes `formConfig`
- `backend/src/main/scala/com/helio/infrastructure/persistence/panels/PanelRepository.scala` — adds `PanelRow.formConfig`/`PanelTable.formConfig`; folds `form_config` into both `configColumnsOf` and `configColumnValuesOf` (C4)
- `backend/src/main/scala/com/helio/infrastructure/persistence/dashboards/DashboardSnapshotRepository.scala` — adds the `FormCreate` arm to the export/import panel-reconstruction match (needed for the dashboard-import path; caught by a non-exhaustive-match compiler warning)
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — wires the real `dataSourceRepo` into `panelService`'s constructor (previously only `outputRepoOpt`)
- `backend/src/main/scala/com/helio/api/protocols/assistant/AssistantProposalToolSchemas.scala` — adds `form` to `ProposalPanelSchema`'s `type` enum (D7)
- `schemas/panels/panel.schema.json` — adds `form` to the `type` enum, a `oneOf` branch, and `$defs.FormConfig`/`FormFieldConfig`/`FormSubmitConfig`
- `schemas/panels/create-panel-request.schema.json` — adds `form` to the `type` enum and an `allOf` if/then binding `FormConfig`
- `schemas/panels/create-panels-batch-request.schema.json` — adds `form` to the `type` enum
- `schemas/panels/update-panels-batch-request.schema.json` — adds `form` to the `type` enum
- `schemas/dashboards/dashboard-proposal.schema.json` — adds `form` to `ProposalPanel.type`'s enum (D7, no carve-out beside `divider`)
- `helio-mcp/src/tools/proposal.ts` — adds `"form"` to `PANEL_TYPES`
- `helio-mcp/src/tools/write.ts` — adds `"form"` to `update_panel`'s `type` enum
- `frontend/src/features/panels/types/panel.ts` — adds `"form"` to `PanelKind`; adds `FormFieldControl`/`FormFieldSpec`/`FormSubmitSpec`/`FormPanelConfig`/`FormPanel` types, `PanelConfig`/`Panel` union members, `emptyFormConfig`, and the `emptyConfigForKind` arm
- `frontend/src/features/panels/ui/grid/mobilePanelHeights.ts` — adds the `form` arm to `computeMobilePanelHeight` (exhaustive switch)
- `frontend/src/features/panels/state/panelNarrowing.ts` — adds `isFormPanel` and `getFormDataSourceId`
- `frontend/src/features/panels/ui/PanelContent.tsx` — adds an explicit `isFormPanel` branch (neutral `role="status"` unconfigured placeholder) to the if-chain dispatcher, ahead of the `MetricRenderer` fallthrough; corrects the now-false "union is closed" comment (D10/C10)
- `frontend/src/test/panelFixtures.ts` — adds `makeFormPanel`
- `frontend/src/features/panels/ui/PanelContent.test.tsx` — adds the form-kind render test (task 4.10)
- `frontend/src/features/panels/ui/grid/mobilePanelHeights.test.ts` — adds `form` to the existing kind-parameterized cases
- `backend/src/test/scala/com/helio/domain/panels/FormPanelSpec.scala` — new: decode/encode/patch coverage and every structural rejection (task 4.1/4.1b). Cycle 2 (CR3): adds the write-path rejection test for an unrecognized top-level config attribute
- `backend/src/test/scala/com/helio/domain/model/PanelSpec.scala` — updates registry/exhaustiveness assertions for the 6th kind
- `backend/src/test/scala/com/helio/domain/model/PanelTypeSpec.scala` — adds `form` parse/serialize coverage and the "Default is still Divider" assertion
- `backend/src/test/scala/com/helio/infrastructure/persistence/panels/PanelRowMapperSpec.scala` — adds form round-trip, never-decodes-as-OutputPanel, and tolerant-unrecognized-attribute (C11/4.9) coverage; this file's tests are what task 4.7's mutation reddens. Cycle 2 (CR3): adds the tolerant-read test for a stored config carrying an unrecognized TOP-LEVEL key
- `backend/src/test/scala/com/helio/api/routes/panels/FormPanelRoundTripSpec.scala` — new: route-level create/re-read/PATCH/export-import/cross-owner/agent-proposal coverage (tasks 4.3a/4.3b/4.4/4.5/4.6/4.6b). Cycle 2 (CR3): adds a route-level 400 test for an unrecognized top-level config attribute
- `backend/src/test/scala/com/helio/api/routes/proposals/ApplyProposalSpecBase.scala` — exposes `datasetSourceId` (existing seeded dataset source) and seeds+exposes `otherDatasetSourceId` (cross-owner fixture) for form-panel specs
- `openspec/changes/form-panel-kind-registration/mutation-evidence.md` — new (cycle 2, CR4): the persisted mutation transcripts for tasks 4.7/4.8/4.10 — exact mutation, command, per-test red/green, and the C7 note on why 4.3a stays green; re-derived fresh in cycle 2, not merely transcribed from evaluation-1.md
- `openspec/changes/form-panel-kind-registration/proposal.md` — cycle 2 (CR1): Impact list corrected in both directions — adds `ApiRoutes.scala`/`PanelService.scala`/`DashboardSnapshotRepository.scala` and a test-support line for the four test-scaffolding files; explicitly notes `domain/panels/package.scala` was evaluated and found not needed rather than leaving a phantom entry
- `openspec/changes/form-panel-kind-registration/design.md` — cycle 2 (CR2): scopes the Context's snapshot-wire genericity claim to the export direction only; adds D11 recording the import-side `DashboardSnapshotRepository` `FormCreate` arm as an explicit, separate-enumeration-site decision
