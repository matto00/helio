- `backend/src/main/scala/com/helio/infrastructure/PipelineRepository.scala` — added `findLastRunAtByOutputDataTypeId(id: DataTypeId): Future[Option[Instant]]` using `withSystemContext`; fetches all `last_run_at` values for matching succeeded pipelines and returns the max
- `backend/src/main/scala/com/helio/api/protocols/PanelProtocol.scala` — added `dataAsOf: Option[String]` (9th field) to `PanelResponse` case class; updated `fromDomain` factory to accept `dataAsOf: Option[String] = None`; changed `jsonFormat8` → `jsonFormat9`
- `backend/src/main/scala/com/helio/api/routes/PublicDashboardRoutes.scala` — injected `PipelineRepository`; added concurrent `Future.sequence` loop to look up `dataAsOf` via `panel.dataTypeId` for each panel; passes result into `PanelResponse.fromDomain`
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — passes `pipelineRepo` to `PublicDashboardRoutes` constructor
- `backend/src/main/scala/com/helio/api/routes/DashboardRoutes.scala` — fixed eta-expansion: `panels.map(PanelResponse.fromDomain)` → `panels.map(p => PanelResponse.fromDomain(p))`
- `backend/src/main/scala/com/helio/api/routes/DashboardSnapshotRoutes.scala` — same eta-expansion fix
- `backend/src/main/scala/com/helio/api/routes/PanelRoutes.scala` — same eta-expansion fix on batch update and PATCH routes
- `schemas/panel.schema.json` — already had `dataAsOf` in `properties`; no additional change needed
- `frontend/src/features/panels/types/panel.ts` — added `dataAsOf?: string | null` to `PanelBase` interface
- `frontend/src/features/panels/ui/PanelCard.tsx` — imported `formatRelativeTime`; rendered `<p className="panel-grid-card__freshness">Data as of ...</p>` below `<h3>` when `dataAsOf` is truthy
- `frontend/src/features/panels/ui/PanelGrid.css` — added `.panel-grid-card__freshness` rule using `--app-text-muted` and `--text-xs` tokens
- `frontend/src/test/panelFixtures.ts` — added `dataAsOf?: string | null` to `PanelBaseOverrides` and `applyBase`
- `backend/src/test/scala/com/helio/infrastructure/PipelineRepositorySpec.scala` — added `DataTypeId` import; added 5 tests for `findLastRunAtByOutputDataTypeId` (happy path, never run, failed run, unknown id, multi-pipeline max)
- `backend/src/test/scala/com/helio/api/ApiRoutesSpec.scala` — added 2 tests: `dataAsOf` ISO string for a bound panel with a succeeded pipeline run; `dataAsOf` null for an unbound text panel
- `backend/src/test/scala/com/helio/api/protocols/AggregatorRegressionSpec.scala` — added `dataAsOf` to the existing `PanelResponse` round-trip fixture; added a second round-trip test with `dataAsOf = None`
- `openspec/changes/panel-data-freshness/tasks.md` — all 17 tasks marked `[x]`
