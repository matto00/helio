- `frontend/src/hooks/useInFlightGuard.ts` — new shared ref-based re-entry guard hook (design.md Decision 1).
- `frontend/src/hooks/useInFlightGuard.test.ts` — unit tests for the hook (task 2.1).
- `frontend/src/features/panels/ui/PanelCard.tsx` — wires `useInFlightGuard` into `handleDuplicate`; disables the "Duplicate" `ActionsMenuItem` while pending.
- `frontend/src/features/panels/ui/PanelCard.test.tsx` — double-activation + re-enable-after-settle tests for the panel duplicate guard.
- `frontend/src/features/dashboards/ui/DashboardList.tsx` — wires `useInFlightGuard` into `handleDuplicateDashboard`; disables the "Duplicate" `ActionsMenuItem` while pending.
- `frontend/src/features/dashboards/ui/DashboardList.test.tsx` — double-activation + re-enable-after-settle tests for the dashboard duplicate guard.
- `frontend/src/features/pipelines/hooks/usePipelineDetailPage.ts` — wires `useInFlightGuard` into `handleDuplicateStep`; exposes `duplicatingStepIds`.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.tsx` — threads `duplicatingStepIds` into `PipelineRiverView`.
- `frontend/src/features/pipelines/ui/PipelineRiverView.tsx` — threads `duplicatingStepIds` to its own `StepCard` call site and to `LaneColumn`/`RootColumn`.
- `frontend/src/features/pipelines/ui/PipelineRiverView.test.tsx` — adds `duplicatingStepIds` to the two `baseProps`/`wiringProps` fixtures.
- `frontend/src/features/pipelines/ui/RootColumn.tsx` — threads `duplicatingStepIds` through to `LaneColumn`.
- `frontend/src/features/pipelines/ui/LaneColumn.tsx` — threads `duplicatingStepIds` through nested lanes and its two `StepCard` call sites, computing `isDuplicating` inline.
- `frontend/src/features/pipelines/ui/StepCard.tsx` — new `isDuplicating` prop disables the "Duplicate step" button.
- `frontend/src/features/pipelines/ui/StepCard.test.tsx` — adds `isDuplicating: false` to the `baseProps` fixture.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.test.tsx` — same-tick double-activation test (task 2.4) plus re-enable-after-resolve/reject tests for the step duplicate guard.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.css` — cycle-2 evaluation fix: `:not(:disabled)` hover guard and a `.pipeline-detail-page__step-card-duplicate-btn:disabled` rule (matching the sibling `move-btn:disabled` rule) so the `isDuplicating`-gated button reads as visually inert in both themes.
- `frontend/src/theme/tokenAuditSweep.css.test.ts` — re-pinned the `PipelineDetailPage.css` baseline's line numbers (+5 below the CSS edit above), verified byte-identical content at each new line vs. the old one.
