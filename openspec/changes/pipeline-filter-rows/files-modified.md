- `backend/src/main/scala/com/helio/domain/InProcessPipelineEngine.scala` — replaced `applyFilter` with structured multi-condition evaluation supporting 9 operators, AND/OR combinator, null handling, and numeric coercion; added `evalCondition` helper
- `backend/src/test/scala/com/helio/domain/InProcessPipelineEngineSpec.scala` — removed old expression-based filter test; added 13 new tests covering all 9 operators, AND/OR combinators, missing field as null, and empty conditions pass-all
- `frontend/src/components/FilterConfig.tsx` — new component: combinator toggle (AND/OR), dynamic condition rows with field/operator/value inputs, type-aware value input (number for numeric types), unary operator hides value input
- `frontend/src/components/FilterConfig.test.tsx` — new test file: 12 tests covering render, combinator toggle, unary hiding, type-aware input, add/remove conditions, and hydration from persisted config
- `frontend/src/components/PipelineDetailPage.tsx` — added `parseFilterConfig` helper, `analyzeSchema: SchemaField[]` prop and `filterConfig`/`setFilterConfig` state to `StepCard`, `handleFilterChange`, `getAnalyzeSchema` function, wired `FilterConfig` for `op === "filter"`, seeded filter initial config on `handleAddStep`
