# Files Modified — HEL-750

## Round 1 (2026-08-19)

- `backend/src/main/resources/application.conf` — added `connectionTimeout = 5000` to both `helio.db` and `helio.db.privileged` HikariCP pool stanzas, with inline comments explaining the fail-fast rationale (mirrors the existing tuning-comment style for `maximumPoolSize`/`minimumIdle`/`idleTimeout`/`maxLifetime`).
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — added a top-level `ExceptionHandler` (logs full exception + stack trace, completes with `ErrorResponse("Internal server error")`) and a top-level `RejectionHandler` (built from `RejectionHandler.default.mapRejectionResponse`, re-wraps Pekko's existing plain-text rejection body into the same `ErrorResponse` JSON shape, preserving the original message text and status code). Wrapped the existing route tree with `handleExceptions(...)` / `handleRejections(...)` immediately inside `cors(corsSettings) { ... }` so `cors()` always sees the final response and attaches CORS headers to it regardless of how the request fails.
- `backend/src/test/scala/com/helio/api/ApiRoutesCorsErrorHandlingSpec.scala` — new focused spec covering tasks 3.1-3.4.
- `backend/src/test/scala/com/helio/infrastructure/DatabaseConnectionTimeoutSpec.scala` — new spec covering task 3.5 (HikariCP fail-fast against a local blackhole socket).

## Round 2 — fold-in (2026-08-20)

- `backend/src/main/scala/com/helio/api/TopLevelErrorHandlers.scala` — **new file** (design.md D5). `object TopLevelErrorHandlers extends Directives with JsonProtocols`, its own `LoggerFactory` logger. Houses `topLevelExceptionHandler`/`topLevelRejectionHandler`, moved verbatim (with doc comments) out of `ApiRoutes.scala`, plus the new `corsRejectionHandler: RejectionHandler` (design.md D6) — matches `org.apache.pekko.http.cors.scaladsl.CorsRejection`, completes with `403 Forbidden -> ErrorResponse(s"CORS request rejected: ${r.cause.description}")`, logged at `WARN` with no stack trace.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — removed the now-duplicated `topLevelExceptionHandler`/`topLevelRejectionHandler` definitions (now referenced via `TopLevelErrorHandlers.*`); wrapped `cors(corsSettings) { ... }` with an outer `handleRejections(TopLevelErrorHandlers.corsRejectionHandler) { ... }` so a disallowed-`Origin` `CorsRejection` (minted by `cors()` itself, before it ever reaches the pair registered inside it) is also caught and turned into a clean, quiet, curated response instead of Pekko's default `500`/plain-text/stack-trace-logged fallback. Dropped now-unused imports (`ContentTypes`, `HttpEntity`, `HttpResponse`, `ExceptionHandler`, `RejectionHandler`, `spray.json._`). Net effect: 720 → 692 lines (still above the ~250-line soft budget, pre-existing debt).
- `backend/src/test/scala/com/helio/api/ApiRoutesCorsErrorHandlingSpec.scala` — added 3 new tests (task group 6): disallowed-origin request returns `403` with a curated `ErrorResponse` naming the rejected origin and no `Access-Control-Allow-Origin` header (6.1); the body contains no raw exception/stack text (6.2); a logback `ListAppender` capture confirms the rejection logs at `WARN` with no throwable attached and never at `ERROR` (6.4). The existing 5 round-1 tests re-ran unchanged and green, satisfying task 6.3's regression-pass requirement (plus the full `sbt test` suite: 3342/3342).
- `openspec/changes/db-fail-fast-cors-headers/tasks.md` — all 20 tasks marked complete.
