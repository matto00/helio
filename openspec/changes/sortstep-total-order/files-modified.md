- `backend/src/main/scala/com/helio/domain/steps/SortStep.scala` — replaced the per-pair numeric/lexicographic fallback with a three-tier (`finiteNumeric`/`compareNonNullAsc`) comparator decided per value, per design.md D1/D2/D3; rewrote the object-level scaladoc to document the tiering, the two rejected alternatives, and the numbers-before-strings/strict-weak-ordering commitments (D1/D4/D5).
- `backend/src/test/scala/com/helio/domain/steps/SortStepSpec.scala` — added contract guards (irreflexivity 2.1, antisymmetry 2.2, transitivity 2.3) driven by `strictlyBefore`, which observes `SortStep.apply`'s actual pairwise output rather than a reimplemented model; added discriminating fixtures (2.4 ticket repro, 2.5 desc+null, 2.7 NaN/Infinity) and an order-independence guard over sort-key equivalence classes (2.6); HEL-893's two existing guards left unmodified.
- `openspec/changes/sortstep-total-order/mutation-evidence.md` — new: transcripts for mutations A (`all-or-nothing`), B (`numeric-tier-last`), and C (pre-fix per-pair comparator), each with actual observed output and a discrimination-coverage table (tasks.md 2.9-2.12).
- `openspec/changes/sortstep-total-order/tasks.md` — marked all tasks complete.
