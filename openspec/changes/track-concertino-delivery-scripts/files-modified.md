- `.gitignore` — dropped `scripts/concertino/` and `concertino.config.json` from the Concertino ignore block; kept `.concertino/`; added negative exclusions for `scripts/concertino/pricing-table.json` and `scripts/concertino/report-cost.sh` with a rationale comment (Decision 5, AC6).
- `CLAUDE.md` — documented the render-vs-edit contract for `scripts/concertino/` (rendered from Concertino's `core/`, local edits erased on next `concertino sync`, script changes go upstream, sync is now manual in this repo) (AC5).
- `concertino.config.json` — added `"cleanup": { "skipSync": true }` so `renderEnv` emits `CONCERTINO_CLEANUP_SKIP_SYNC=1`; also picked up a pre-existing Prettier reformat when the pre-commit hook ran (unrelated whitespace-only fix, no semantic change beyond the added `cleanup` key).
- `scripts/concertino/.concertino.env` — regenerated by `concertino sync`; now contains `CONCERTINO_CLEANUP_SKIP_SYNC=1`.
- `scripts/concertino/check-agent-merge-permission.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/check-gate-chain-change.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/check-merge-readiness.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/emit-event.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/gather-escalation-context.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/next-report-number.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/next-ticket-id.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/persist-evidence.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/resolve-speed.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/set-ticket-state.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/speeds.json` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/squash-branch.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/test-gate-in-isolation.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/triage-followup.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `scripts/concertino/tui-attached.sh` — newly tracked (one of the 15 previously-ignored delivery scripts; AC1).
- `openspec/changes/track-concertino-delivery-scripts/*` — proposal/design/tasks/ticket artifacts for this change (tasks.md fully checked off).

Not modified: `scripts/concertino/setup-worktree.sh` (Decision 3, explicitly untouched), `scripts/concertino/README.md` (rendered artifact, no hand edit per Decision 7), `scripts/concertino/pricing-table.json` / `report-cost.sh` (excluded strays, AC6).
