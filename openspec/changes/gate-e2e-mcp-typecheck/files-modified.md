- `e2e/tsconfig.json` — new standalone tsconfig gating `e2e/**/*.ts` + `../playwright.config.ts` (design.md D1).
- `e2e/hel773-top-anchored-mobile-nav-sheet.spec.ts` — real type fix: replaced a brittle `Parameters<Parameters<typeof test>[1]>[0]["request"]` derivation (resolves to the wrong overload signature) with a direct `APIRequestContext` import/type.
- `e2e/hel813-mobile-touch-target-floor.spec.ts` — same fix as above.
- `e2e/hel813-mobile-touch-target-floor.regression.spec.ts` — same fix as above.
- `helio-mcp/tsconfig.typecheck.json` — new typecheck-only sibling config (`extends: ./tsconfig.json`, `rootDir: "."`, includes `src/**/*.ts` + `scripts/**/*.ts`) per design.md D1b; build-facing `tsconfig.json`/`npm run build` untouched.
- `helio-mcp/package.json` — `typecheck` script now points at `tsconfig.typecheck.json`.
- `helio-mcp/scripts/compose.ts` — real type fix: `textOf`/`isErrorOf` now typed against the actual `Client["callTool"]` return type (a union including the legacy `toolResult` compatibility shape) via a `hasContent` type guard, instead of an incompatible hand-written narrow type.
- `helio-mcp/scripts/verify-bound-panel.ts` — same fix as `compose.ts`.
- `helio-mcp/scripts/verify.ts` — same fix as `compose.ts`, plus a fix for `readResource`'s content union (`text` vs `blob` variants) at the workspace-context read site.
- `package.json` — new `check:e2e-types` / `check:helio-mcp-types` root scripts (design.md D4).
- `.husky/pre-commit` — wires both new `check:*` scripts in alongside the existing `npm run typecheck` line (design.md D2).
- `.github/workflows/ci.yml` — `frontend` job: adds `npm --prefix helio-mcp ci`, `check:helio-mcp-types`, `check:e2e-types` steps after the existing `npm run typecheck` step, and adds `helio-mcp/package-lock.json` to `setup-node`'s `cache-dependency-path` (design.md D2/D3).
- `openspec/changes/gate-e2e-mcp-typecheck/tasks.md` — all tasks marked complete.
