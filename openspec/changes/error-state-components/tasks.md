## 1. Frontend — shared primitives

- [x] 1.1 Add `frontend/src/services/classifyRequestError.ts` (D1): `{ message, kind }`, 403→forbidden, 404→not-found, else error; `message` calls `services/extractErrorMessage.ts`'s `extractErrorMessage` (do not reimplement, do not touch the per-slice local helpers of the same name — they still serve other thunks).
- [x] 1.2 Extend `InlineError.tsx`'s `banner` variant with `kind` (default `"error"`), `onRetry`, `retrying`, `announced` (default `true`), `retryVariant` (`"button"|"icon-only"`, default `"button"`) (D2).
- [x] 1.3 Add icon (`TriangleAlert`/`ShieldOff`/`SearchX` from `lucide-react` — use `TriangleAlert`, not the deprecated `AlertTriangle` alias — `aria-hidden`, CSS `width:1em;height:1em` on the `<svg>`) to every `banner` render; add Secondary `--control-sm` Retry button ("Retrying…" label swap when `retrying`, meeting the 44px mobile floor at 430/768) when `kind==="error"` and `onRetry` given; `retryVariant="icon-only"` renders the shared `IconButton` (`variant="secondary"`, `RotateCw`, `aria-label`/`title` swapping to "Retrying…", `size="xs"`) instead.
- [x] 1.4 Widen `EmptyState.tsx`'s `icon`/`cta.icon`/`secondaryCta.icon` prop types to `IconDefinition | ReactNode`, branch on `React.isValidElement` (D3); new lucide icons sized via CSS `width:1em;height:1em`.
- [x] 1.5 Add `EmptyState`'s `intent` prop (`"neutral"|"error"`, default `"neutral"`), applied to both `main` and `sidebar` variants: icon-wrap background → `color-mix(in srgb, var(--app-error) 16%, var(--app-surface))` (solid), icon-wrap border → `1px solid color-mix(in srgb, var(--app-error) 30%, transparent)`, glyph `color` → `var(--app-error)` (was `--app-accent`), root gets `role="alert"` and drops `aria-label` (kept for `neutral`).
- [x] 1.6 Add `EmptyState`'s `secondaryCta` prop (same shape as `cta`, Secondary `--control-md` recipe, rendered alongside `cta`'s Primary recipe, meeting the 44px mobile floor) and `disabled` on both `cta`/`secondaryCta` — `EmptyState` only disables the button; the in-flight label text ("Retrying…" vs "Retry") is **caller-supplied** via `cta.label`/`secondaryCta.label` (matching `TypeDetailPanel.tsx`'s existing `previewLoading ? "Loading…" : "Preview"` pattern) — `EmptyState` itself never hard-codes retry copy, since `cta`/`secondaryCta` remain a generic primitive shared by ~10 neutral (non-error) empty states. (Contrast `InlineError.retrying` in 1.2/1.3, which IS component-owned — it's retry-specific by name, not a generic primitive.)
- [x] 1.7 Add `StatusMessage.tsx`'s `onRetry`/`retrying` props (D2's content recipe: Secondary `--control-sm`, `TriangleAlert` icon, "Retrying…" swap) and `role="alert"` on the `failed` branch only — do NOT change `failed`'s existing box metrics (`--text-sm`, `--space-3`/`--space-4` padding, `--app-radius-md`); they must stay identical to `loading`'s so the two states remain visually paired (D4).

## 2. Frontend — thunk/state wiring

- [x] 2.1 `sourcesSlice.ts`: bind the caught error in `fetchSources`'s `catch`, classify with `classifyRequestError`, widen `rejectValue` to `{message, kind}`; add `errorKind`, reset alongside `error` on `pending`/`fulfilled`.
- [x] 2.2 `pipelinesSlice.ts`: same for `fetchPipelines` → `errorKind` (reset with `error`) and `fetchPipelineById` → `currentPipelineErrorKind` (preserved on `pending` exactly like `currentPipelineError` today, reset only on `fulfilled`, replaced on `rejected`) — two independent kind fields, per D1a.
- [x] 2.3 `dataTypesSlice.ts`: same for `fetchDataTypes` → `errorKind`.
- [x] 2.4 `panelThunks.ts`'s `fetchPanelPage`: classify with `classifyRequestError` at **both** rejection sites — the "Panel is not bound to a data type." early-return guard, and the `catch` around `fetchDataTypeRows` — widen `rejectValue` to `{message, kind}` at both.
- [x] 2.5 `usePanelData.ts`: `errorForKey` becomes `{key, message, kind}`. `refresh()` (`:74-77`) clears it eagerly (`setErrorForKey(null)`). Separately, add a `.then()` before the existing `.catch()` on the fetch promise (`:113-117`) that also clears `errorForKey` for that key on **fulfillment** — required because `markDataTypeRowsStale` (`panelActions.ts:20`) deletes the pagination entry and the effect's dedupe-guard bypass at `:88` refetches without ever calling `refresh()` (D6). Expose `errorKind` alongside `error`/`refresh`.
- [x] 2.6 `TypeDetailPanel.tsx`'s `previewError` (the rows/preview fetch state at `:223` — NOT `error` at `:193`, the save-mutation error, which stays out of scope): classify with `classifyRequestError` in its own `.catch`, add `previewErrorKind`.
- [x] 2.7 `SourceDetailPanel.tsx`'s `handlePreview()` (`:124-149`): split the single `error` state (D5a) into `previewError` (the `catch` at `:145` — classify with `classifyRequestError`, add `previewErrorKind`, `onRetry`-eligible) and `previewUnsupported` (the `:141` "Preview is not supported for `<kind>` sources" message — a plain string, NOT run through `classifyRequestError`, since it isn't a caught error at all; never `onRetry`-eligible). Both cleared at the top of `handlePreview()` (today's single `setError(null)` becomes two clears).
- [x] 2.8 `ProposalReviewPage.tsx`'s `loadError` state (fed by `fetchDataTypes` imported from `dataTypes/services/dataTypeService.ts` — a plain async function, distinct from `dataTypesSlice`'s thunk of the same name in task 2.3): classify with `classifyRequestError` in its `.catch` (`:64-66`), add `loadErrorKind`; add a `retryToken`/similar state so the effect (`:58-70`) can be re-triggered by a Retry click, plus a local `retrying` boolean for the disabled state. **The retry MUST also `setLoadError(null)`/`setLoadErrorKind(null)` when it starts** — today `loadError` has exactly one setter (the `.catch`) and nothing ever resets it, so a successful retry would otherwise still render the stale error at `:133` forever (D5).

## 3. Frontend — view wiring (full-surface)

- [x] 3.1 `SourcesPage.tsx`: replace hand-rolled `<p className="sources-page__error">` (remove its now-dead CSS, `SourcesPage.css:56`) with `EmptyState intent="error"`; `title="Couldn't load sources"` / `description={message}` when `errorKind==="error"`, with `cta` (Retry, `disabled` on `status==="loading"`) re-dispatching `fetchSources`; D7's not-found/forbidden copy and no `cta` otherwise.
- [x] 3.2 `PipelinesPage.tsx`: same pattern (remove `.pipelines-page__error`, `PipelinesPage.css:27`), `title="Couldn't load pipelines"`, Retry re-dispatches `fetchPipelines`; update `PipelinesPage.test.tsx:160`'s `getByRole("alert")` assertion for the new markup (still satisfied — `EmptyState intent="error"` carries `role="alert"` per 1.5).
- [x] 3.3 `PipelineDetailPage.tsx`: same pattern for its `:596` branch (remove `.pipeline-detail-page__error`, `PipelineDetailPage.css:804`), `title="Couldn't load this pipeline"`, Retry re-dispatches `fetchPipelineById`, `disabled` on `currentPipelineStatus==="loading"` (already preserved through retry per D1a/existing `:396-400` comment).
- [x] 3.4 `TypeRegistryPage.tsx`: same pattern (remove `.type-registry-page__error`, `TypeRegistryPage.css:29`), `title="Couldn't load types"`, Retry re-dispatches `fetchDataTypes`.
- [x] 3.5 `ProposalReviewPage.tsx`: edit the existing `loadError` branch at `:133-141` (do not add a new branch) — `cta` = Retry (re-triggers 2.8's effect, `disabled`/"Retrying…" while in flight) only when `loadErrorKind==="error"`; `secondaryCta` = the pre-existing "Back to dashboards" action, kept in all cases (this fetch is DEV-only demo-fixture data).

## 4. Frontend — view wiring (inline)

- [x] 4.1 `PanelContent.tsx`: replace the bare error `<span>` with `InlineError variant="banner" announced={false}` (its own wrapping div at `:75` already carries `role="alert"` — do not double it); thread `errorKind` (2.5) as `kind`. Add a `retryVariant` prop to `PanelContent` itself, forwarded to `InlineError`. Wire `onRetry`/`retryVariant` at **both** consumers: `PanelCard.tsx` (`onRetry = usePanelData().refresh`, `retryVariant="icon-only"` — small grid cells) and `PanelDetailModal.tsx:77/355` (today destructures `usePanelData()` without `refresh` — add it; `onRetry = refresh`, `retryVariant="button"` — the modal has room for a labeled control).
- [x] 4.2 `PanelList.tsx`: pass `onRetry`/`retrying` to its `StatusMessage` call, re-dispatching the panels fetch for the selected dashboard.
- [x] 4.3 `SourceDetailPanel.tsx`'s `:240` `InlineError variant="banner"` call site becomes two conditional renders (D5a): when `previewUnsupported` is set, render it with `kind="error"` and **no `onRetry`** (there is nothing to retry — a labeled Retry button that can never succeed is the exact retry-spam the ticket's AC forbids); when `previewError` is set, render it with `onRetry` re-calling `handlePreview()` and `kind` from 2.7's `previewErrorKind`. The two are mutually exclusive (both clear together in 2.7). Its existing "Preview"/"Reload" button stays as-is, unrelated to this Retry — deliberate, per D5.
- [x] 4.4 `TypeDetailPanel.tsx`: replace only the `previewError` `<p>` (`:223`) with `InlineError variant="banner"`, `onRetry` re-running the preview fetch, `kind` from 2.6; the `error` `<p>` (`:193`, save-mutation) is untouched.

## 5. Tests

- [x] 5.1 `InlineError.test.tsx`: banner + kind/onRetry combinations (error shows retry, forbidden/not-found never do, regardless of `onRetry`), `retrying` disables + label-swaps (`aria-label`/`title` swap for `retryVariant="icon-only"`), `announced={false}` omits `role="alert"`, `retryVariant="icon-only"` renders an accessible icon-only control, text variant unchanged.
- [x] 5.2 `EmptyState.test.tsx`: `intent="error"` styling (icon-wrap + border + glyph color) + `role="alert"` + no `aria-label`, `icon`/`cta.icon` ReactNode vs IconDefinition branches, `secondaryCta` renders alongside `cta`, `cta.disabled` renders the caller-supplied label and disables the button (not a component-owned label swap — see 1.6).
- [x] 5.3 New `StatusMessage.test.tsx` (none exists today): `failed` state `role="alert"` + icon, `onRetry` present/absent, box metrics identical between `loading` and `failed`, non-failed states unaffected.
- [x] 5.4 `classifyRequestError` unit tests: 403/404/other/network-error classification, and that `message` matches `services/extractErrorMessage.ts`'s output (not a raw `err.message` fallthrough).
- [x] 5.5 Simulate fetch failure + retry recovery for `SourcesPage`, `PipelinesPage`, `TypeRegistryPage`, `ProposalReviewPage` (regression test for task 2.8 — assert a successful retry actually clears `loadError` and renders the proposal, not the stale error), and `PanelContent` (both `PanelCard` and `PanelDetailModal` consumers) — for `PanelContent` specifically, assert both (a) a successful button-triggered retry clears the error, and (b) a successful background refetch (simulating `markDataTypeRowsStale`) also clears the error without the button (regression test for D6/task 2.5).
- [x] 5.6 Simulate the 403 and 404 paths for at least one view (`PipelineDetailPage` or `TypeDetailPanel`), asserting no retry action renders and the not-found copy does not assert deletion.
- [x] 5.7 `SourceDetailPanel.test.tsx`: selecting a `sql` (or other unsupported-preview) source and clicking Preview renders `previewUnsupported`'s message with **no** Retry action (regression test for D5a/task 2.7/4.3 — the prod-reachable retry-spam defect); selecting a `csv`/`static`/`rest_api` source and simulating a fetch failure renders `previewError` **with** a working Retry.
- [x] 5.8 Spot-check one full-surface and one inline error state in both light and dark theme (token-derived styling makes this largely automatic; the final UI gate covers this in more depth).
- [x] 5.9 `npm run lint` / `npm test` pass with zero new warnings.
