- `backend/src/main/scala/com/helio/domain/WorkspaceResourceType.scala` — new file: closed, parseable resource-type identity (`DataSource`/`DataType`/`Pipeline`/`Dashboard`/`Metric`) with `fromString`/`asString`, mirroring `DataFieldType`'s convention (design.md D4).
- `backend/src/main/scala/com/helio/api/protocols/WorkspaceResourceSearchProtocol.scala` — new file: `WorkspaceResourceSummary` (find's compact result shape), `WorkspaceResourceMetric` (metric's getResource detail), sealed `WorkspaceResourceDetail` (wraps the 4 existing `WorkspaceContext*` types verbatim + the new metric case), and their spray-json formatters (design.md D6).
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — mixes in `WorkspaceResourceSearchProtocol` and documents its cross-domain dependency on `WorkspaceContextProtocol`/`MetricProtocol`.
- `backend/src/main/scala/com/helio/services/WorkspaceContextService.scala` — widens `toDataSourceEntry`/`toDataTypeEntry`/`toDashboardEntry` from `private` to `private[services]` (mirrors the existing `buildPipeline` precedent) so `WorkspaceSearchService` can reuse them verbatim; zero behavior change (task 1.1/5.8).
- `backend/src/main/scala/com/helio/services/DashboardService.scala` — adds owner-scoped `findById(id, user)`, mirroring `DataTypeService.findById`'s shape over `DashboardRepository.findByIdOwned` (task 1.2).
- `backend/src/main/scala/com/helio/services/DataSourceService.scala` — adds owner-scoped `findById(id, user)`, mirroring `DataTypeService.findById`'s shape over `DataSourceRepository.findByIdOwned` (task 1.3).
- `backend/src/main/scala/com/helio/services/WorkspaceSearchService.scala` — new file: `find(user, query, resourceTypes?)` (keyword/substring search across all 5 resource types, bounded to `MaxFindResults = 20`, deterministic ranking) and `getResource(user, id, resourceType)` (owner-only full detail per resource type, including the pipeline dispatch's strict owner-filter on top of the sharing-aware `findSummaryById`, design.md D1a/D1b).
- `backend/src/main/scala/com/helio/services/WorkspaceAssistantTools.scala` — new file: `findTool`/`getResourceTool` `ClaudeTool` schema definitions (HEL-660's shape), colocated with `WorkspaceSearchService` in `com.helio.services` per design.md D7. No executor wiring (HEL-662's responsibility).
- `backend/src/test/scala/com/helio/services/WorkspaceSearchServiceSpec.scala` — new file: DB-backed integration coverage for tasks 5.1–5.6 and 5.9 (per-type `find` matches, empty-result, `resourceTypes` filter, `MaxFindResults` truncation determinism, pipeline-sharing NotFound, DataType detail parity with `assemble`, NotFound for nonexistent/unowned ids across all 5 types, metric detail, `DashboardService`/`DataSourceService.findById`).
- `backend/src/test/scala/com/helio/services/WorkspaceAssistantToolsSpec.scala` — new file: task 5.7, asserts both `ClaudeTool` values are well-formed (non-blank name/description, valid JSON Schema `inputSchema`).
- `openspec/changes/find-get-resource-tools/tasks.md` — all 26 tasks marked complete.
- `openspec/changes/find-get-resource-tools/files-modified.md` — this handoff.
