- `frontend/src/shared/ui/useVirtualRows.ts` — new hand-rolled windowing hook (design D1/D2): computes `{startIndex, endIndex, topSpacerPx, bottomSpacerPx}` from scroll position, row height, row count, and an overscan buffer; reuses the existing `useScrollEdges` scroll-container ref (design D7) rather than attaching a second `ref`. **Cycle 2 (evaluation-1.md CR1):** the scroll-container `useEffect` now also attaches a `ResizeObserver` (feature-detected, mirroring `useScrollEdges`'s own jsdom guard) calling the same `measure()` — closes a probe-confirmed bug where a panel grown via its resize handle without an intervening scroll left the window sized for the old, smaller viewport (a visible blank strip below the last mounted row). Also documents `overscan`'s default (non-blocking #3).
- `frontend/src/shared/ui/DataGrid.tsx` — wires virtualization into the `full` variant: `VIRTUALIZATION_ROW_THRESHOLD` (D4), per-density `ROW_HEIGHT_ESTIMATE_PX` seed table + measured-row-height correction via `useLayoutEffect` (D3), top/bottom spacer `<tr>`s inside `<tbody>` (D2/D6) with `border-bottom: none` applied explicitly to the true last mounted row when a nonzero trailing spacer displaces it from `:last-child`, and `aria-rowcount`/`aria-rowindex` (header-inclusive, true window-independent position) with spacer rows `aria-hidden="true"` (D5). **Cycle 2:** reworded the two stale `set-state-in-effect` comments (evaluation-1.md non-blocking #2) to explain why the disable directives were removed rather than leaving them describing a directive that no longer exists.
- `frontend/src/shared/ui/DataGrid.css` — `.ui-data-grid__row-spacer-cell` (zeroes padding/border/max-width so spacer height renders exactly as requested) and `.ui-data-grid__row--no-border` (D6).
- `frontend/src/shared/ui/DataGrid.test.tsx` — Jest/RTL coverage: windowing math at several thousand rows across all three densities and at a scrolled-deep position, spacer sizing/hiding, `aria-rowcount`/`aria-rowindex` correctness, the D6 border-suppression boundary, and the small-table bypass (at/below and one row above `VIRTUALIZATION_ROW_THRESHOLD`). Confirmed red against the pre-fix tree (compile failure on the new export, since neither the constant nor the windowing behavior existed) before being accepted. **Cycle 2:** added a `FakeResizeObserver`-based regression guard for CR1 (confirmed red against the pre-CR1 hook before being accepted) and a pinning-under-windowing regression guard for CR2 (already-correct live behavior per the evaluator; this is the missing automated coverage, not a fix).

## Not touched (verified, not assumed)

- `frontend/src/features/panels/ui/renderers/TableRenderer.tsx` — no architecture change; `TableRenderer` still passes the full client-loaded row array to `DataGrid`, which now windows it internally. `resolvedDensity` still resolves to `DEFAULT_DENSITY.full` in the live app (design.md's non-blocking note); multi-density coverage above is exercised directly against `DataGrid` in tests.

## Known pre-existing, out-of-scope defects (cited, not fixed — HEL-1065)

Not newly surfaced by this change but present on the same surface per the ticket's Out-of-Scope section: the CSS-only pin-toggle header-label/icon overlap on truncating columns, and the sub-44px tap target with split focus ring at ≤430px/coarse-pointer.

## Live verification (updated cycle 2)

Task 3.3 is now closed out, citing the cycle-1 evaluator's live Phase 3 pass (`evaluation-1.md`) rather than left open — see `tasks.md` 3.3 for the exact scope. **Honest scope note carried forward:** the largest live table available in the dev environment was 200 rows, not "several thousand" as the ticket literally states; the several-thousand-row case remains Jest-only (5,000-row fixtures), never measured live in a real browser. The evaluator's live pass did not re-exercise the CR1 resize-without-scroll fix added in this cycle (it predates the fix) — CR1 is covered here only by the new `FakeResizeObserver` Jest guard, not a fresh live pass.
