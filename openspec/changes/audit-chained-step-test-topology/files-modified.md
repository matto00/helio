# Files Modified — HEL-949

## Cycle 1

- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepository.scala` — renamed `insert` to `insertRootStep` (D4); rewrote its scaladoc to state it deliberately creates a root branch and that `insertInternal(..., parentStepId = ...)` is the chaining API. No defaulted `parentStepId` added (design's explicitly rejected alternative).
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineRunRoutesSpec.scala` — renamed all 12 `stepRepo.insert(...)` call sites to `insertRootStep`; corrected the 2 multi-step tests (468/469 "only applies steps up to and including the target step", 544/545 "excludes a disabled step from the executed prefix") to build a real trunk via `insertInternal(..., parentStepId = ...)`; rewrote the HEL-922 warning comment (~line 485) to reference `insertRootStep` instead of the retired `insert`.
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineAnalyzeRoutesSpec.scala` — renamed all 12 call sites to `insertRootStep`; corrected the 1 multi-step test (234/235 "excluding a disabled step from the response entirely") to a real trunk (disabled `rename` as ancestor of `select`).
- `backend/src/test/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepositorySpec.scala` — renamed all 7 call sites to `insertRootStep` (all single-step, rename-only; no topology change).
- `backend/src/test/scala/com/helio/services/workspace/WorkspaceContextServiceSpec.scala` — renamed both call sites to `insertRootStep`, then corrected the 1 multi-step test (346/347 "report each step's outputColumns in step order") to a real trunk (`select` ancestor of `rename`); corrected the `position` assertion from `Vector(0, 1)` (a parallel-root artifact) to `Vector(0, 0)` (the correct per-parent-scoped value for a genuine trunk, per D3 case (a)); added an `outputColumns`-based assertion that actually carries the ordering claim the test's name makes.

## Cycle 2 (skeptic-final-1.md REFUTE — documentation-only, per the skeptic's own framing)

- `openspec/changes/audit-chained-step-test-topology/audit-report.md` — retracted the "genuine measurement error" characterisation of the 468/469 widen-alone green result and replaced it in place (not appended as an erratum) with the correct account: the recorded mutation is a COMPOUND break of the prefix walk (`PipelineRunService.scala:403`) and the HEL-905 node-keyed lookup (`:423`), and neither half alone is red under either topology (both measured: widen-alone green/green here and by the skeptic; drop-node-keyed-lookup-alone green/green by the evaluator, `evaluation-1.md`). Qualified the bucket-1 label and "now genuinely exercised" wording for 468/469 specifically, in both section 1 and the AC4 section, while leaving 544/545 and 346/347 unqualified. Recorded the measured coverage hole (full 3606-test suite green with the prefix walk alone broken) and its disposition as the filed spinoff **HEL-957**, replacing the prior incomplete "no spinoff ticket is warranted" line. Fixed the stale "both anticipated by design.md" in the D3 section (only one red assertion was predicted). Added the near-miss narrative (orchestrator flagged the compound risk → evaluator tested one leg → skeptic tested the other and found the hole) as permanent record. Added the non-blocking privilege-context note (`insertRootStep`/RLS-user-context → `insertInternal`/ACL-bypassing-system-context) and corrected the `PipelineAnalyzeRoutesSpec` 234/235 `inputSchema` finding from "no claim made" to "not topology-dependent" with its own reasoning.
- `backend/src/main/scala/com/helio/infrastructure/persistence/pipelines/PipelineStepRepository.scala` — comment-only edit (the one explicitly-permitted exception to "documentation-scoped"): clarified `insertInternal`'s scaladoc so "No live caller passes a non-`None` parentStepId" reads as "no PRODUCTION caller," since HEL-949's test tree now does pass one to build real trunks. No behavior change.

See `openspec/changes/audit-chained-step-test-topology/audit-report.md` for the full 33-row per-call-site audit, mutation evidence, and the AC4 answer.
