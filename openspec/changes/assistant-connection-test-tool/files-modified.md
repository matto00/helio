- `backend/src/main/scala/com/helio/api/protocols/AssistantProposalToolSchemas.scala` — adds the `test_connection` `ClaudeTool` schema (`{type, config}` discriminated shape, two decode-pinned examples) and `testConnectionTool` val.
- `backend/src/main/scala/com/helio/api/protocols/AssistantProtocol.scala` — adds `testConnectionTool` to `AssistantProtocol.assistantTools` (between `get_resource` and the `propose_*` tools), growing the bounded set from 6 to 7 tools.
- `backend/src/main/scala/com/helio/services/AssistantToolExecutor.scala` — new `VerifiedConfig` closed ADT + `verifiedConfigs: AtomicReference[Set[VerifiedConfig]]` field; new `sourceService: SourceService` constructor collaborator; `execute`'s `"test_connection"` dispatch + `executeTestConnection` (decodes `type`/`config`, calls `sourceService.testRest`/`testSql`, records the config as verified only on `ok = true`); new `requireVerifiedInlineSource` gate helper, wired into `executeProposePipeline`/`executeProposeCombined` ahead of their existing `validate` calls.
- `backend/src/main/scala/com/helio/services/AssistantService.scala` — threads a new `sourceService: SourceService` constructor param into `AssistantToolExecutor`'s construction; raises `MaxHops` from `3` to `4`.
- `backend/src/main/scala/com/helio/services/AssistantSystemPrompt.scala` — documents the `test_connection` tool in the tools list, adds the "verify before finalizing" hard rule, updates the hop-count rule from 3 to 4.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — passes the already-constructed `sourceService` into the `new AssistantService(...)` call.
- `backend/src/test/scala/com/helio/api/protocols/AssistantProposalToolSchemasSpec.scala` — decode-pins `test_connection`'s rest_api and sql schema examples.
- `backend/src/test/scala/com/helio/services/AssistantToolExecutorSpec.scala` — `newExecutor` gains `sourceService`/`pipelineProposalServiceOverride` params; new `execute("test_connection", ...)` dispatch coverage and a new gate-behavior suite (untested-source rejection, verified-config success, failed/edited-config re-rejection, sourceId/csv/static exemptions).
- `backend/src/test/scala/com/helio/services/AssistantServiceSpec.scala` — `newService`/direct constructions gain `sourceService = null`; hop-cap-exhausted fixtures bumped from 4 to 5 scripted attempts (maxHops 3→4); tools-list test updated to 7 tools including `test_connection`.
- `backend/src/test/scala/com/helio/services/AssistantSystemPromptSpec.scala` — new assertions for the `test_connection` tool bullet, the verify-before-finalizing hard rule, and the updated 4-hop cap text.
- `backend/src/test/scala/com/helio/api/routes/AssistantConversationRoutesSpec.scala` — `assistantServiceWith`/`assistantServiceWithSearch` constructor calls gain a trailing `null` for the new `sourceService` param; stale "3-hop cap" prose comments updated.
- `backend/src/test/scala/com/helio/api/routes/AssistantTelemetrySpec.scala` — `assistantServiceWith` gains a trailing `null` for `sourceService`; hop-cap-exhausted fixture expectations updated for the maxHops 3→4 raise (toolCallCount 4→5, cacheReadInputTokens 200→250, proposeAttempts/proposeDecodeFailures 3→4).
