- `backend/src/main/resources/db/migration/V92__connector_credentials.sql` — new table, owner-only RLS (V35/V42 pattern), index (Decision 2).
- `backend/src/main/scala/com/helio/services/auth/MasterKeyProvider.scala` — `WrappedKey`, `MasterKeyError`, `MasterKeyProvider` trait, and `EnvMasterKeyProvider` (env-backed, fail-closed AES-256-GCM wrap/unwrap; no dev/prod branching) (Decision 1/3/4).
- `backend/src/main/scala/com/helio/services/auth/EncryptedSecretBackend.scala` — `EncryptedPayload` (redacted `toString`) and `EncryptedSecretBackend`, a standalone trait (does not extend `SecretBackend`) implementing envelope encrypt/decrypt (Decision 3a).
- `backend/src/main/scala/com/helio/services/auth/SecretField.scala` — task 3.5: updated scaladoc pointing at `EncryptedSecretBackend` as a sibling, non-`SecretBackend` trait.
- `backend/src/main/scala/com/helio/domain/model/model.scala` — added `ConnectorCredentialId` (value class) and `ConnectorCredentialMeta` (metadata-only shape, no plaintext/ciphertext field — task 4.4).
- `backend/src/main/scala/com/helio/infrastructure/persistence/auth/ConnectorCredentialRepository.scala` — Slick repository: `create`/`get`/`list`/`decryptForUse`/`delete`/`rewrapAllBelow`, all through `DbContext.withUserContext` except the operator-run `rewrapAllBelow`.
- `backend/src/main/scala/com/helio/maintenance/RewrapConnectorCredentialsJob.scala` — one-shot `sbt runMain` maintenance job implementing rotation step 3 (task 5.2); no HTTP route.
- `backend/src/test/scala/com/helio/services/auth/EncryptedSecretBackendSpec.scala` — fail-closed (no key configured), wrong/rotated-key, round-trip, and unknown-key-id unit tests (tasks 3.2/3.3/3.4).
- `backend/src/test/scala/com/helio/infrastructure/persistence/auth/ConnectorCredentialRepositorySpec.scala` — DB-level ciphertext assertion (4.2), RLS proof under the non-bypassing `helio_app_test` role (4.3), metadata-shape assertion (4.4), delete, and rotation re-wrap-job integration test (5.2).
- `CLAUDE.md` — documented `CONNECTOR_MASTER_KEY`/`CONNECTOR_MASTER_KEY_ID`/`CONNECTOR_MASTER_KEY_PREVIOUS`/`CONNECTOR_MASTER_KEY_PREVIOUS_ID` in the production env-var table (task 2.2).
- `docs/cloud-dev-setup.md` — local-dev `.env` note for `CONNECTOR_MASTER_KEY`/`CONNECTOR_MASTER_KEY_ID` (task 2.3).
- `docs/secrets-inventory.md` — new file: secrets inventory + executable rotation runbook, with `helio-anthropic-api-key` and `CONNECTOR_MASTER_KEY` as its first two entries, plus the "provisioning still required" note (task 5.1).
- `openspec/changes/connector-credential-encryption/tasks.md` — all tasks marked complete.
