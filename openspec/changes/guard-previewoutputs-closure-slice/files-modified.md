- `backend/src/test/scala/com/helio/services/pipelines/PipelineRunServiceSpec.scala` — added two mutation-proven closure guards on `previewOutputs`' own path (single-Output and all-Outputs arms), a shared fixture (`seedClosureGuardFixture`) whose enabled off-closure branch node doubles as the second-branch Output for the re-pairing axis and as design.md D3's non-degeneracy positive control.
