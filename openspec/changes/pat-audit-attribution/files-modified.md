- `backend/src/main/scala/com/helio/domain/model/model.scala` — `AuthenticatedUser` gains `source: AuditSource = AuditSource.Ui` and `tokenId: Option[ApiTokenId] = None` (Decision 1/4).
- `backend/src/main/scala/com/helio/infrastructure/persistence/auth/ApiTokenRepository.scala` — `findUserByTokenHash` now also selects and returns the resolving token's id (Decision 2).
- `backend/src/main/scala/com/helio/api/http/AuthDirectives.scala` — `resolveApiToken` sets `source=Pat`/`tokenId=Some(id)`; `resolveIdentity`'s cookie branch sets `source=Ui`/`tokenId=None` (Decision 3).
- `backend/src/main/scala/com/helio/services/auth/ApiTokenService.scala` — call site updated to pass `user.tokenId`/`user.source`.
- `backend/src/main/scala/com/helio/services/auth/MfaService.scala` — `audit` helper gains `source`/`tokenId` params (defaulted to `Ui`/`None`); `confirmEnrollment`/`regenerateBackupCodes`/`disable` now pass `user.source`/`user.tokenId` through (Decision 5); `establishSession`/`recordFailedAttempt` left at the default.
- `backend/src/main/scala/com/helio/services/dashboards/DashboardContentsService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/dashboards/DashboardService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/panels/AutoLayoutService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/panels/BoundPanelService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/panels/PanelService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/pipelines/DataTypeService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineRunService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineScheduleService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineSchedulerService.scala` — the scheduler's constructed `AuthenticatedUser` explicitly sets `source=AuditSource.System` (Decision 6).
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/sources/ImageUploadService.scala` — call site updated.
- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — call site updated.
- `backend/src/test/scala/com/helio/api/http/AuthDirectivesSpec.scala` — `findUserByTokenHash` test fake updated to the new return shape; new session-cookie/PAT provenance assertions (task 4.1).
- `backend/src/test/scala/com/helio/api/http/RateLimitDirectiveSpec.scala` — `findUserByTokenHash` test fake updated to the new return shape (unrelated to this ticket's scope but sharing the interface, per design.md Decision 2).
- `backend/src/test/scala/com/helio/api/AuditMutationInstrumentationSpec.scala` — wires a real `ApiTokenRepository`; new PAT-vs-session dashboard.update attribution tests, token-revocation-preserves-attribution test, and an MFA-via-PAT attribution test (tasks 4.2, 4.3, 4.5).
- `backend/src/test/scala/com/helio/services/pipelines/PipelineSchedulerServiceSpec.scala` — wires a real `AuditService`; new test asserting a scheduler-fired run's `pipeline.run.submit` audit event carries `source=system` (task 4.4).
