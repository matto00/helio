- `backend/src/main/resources/db/migration/V93__connectors.sql` — new `connectors` table (real columns for name/kind/base_url + `config` JSONB, `credential_id` FK RESTRICT to `connector_credentials`, owner-only RLS mirroring V35/V92) per design.md Decision 1/2
- `backend/src/main/scala/com/helio/domain/model/Connector.scala` — new `Connector` domain case class + `ConnectorId` value class, reuses `DataSourceKind` for the kind discriminator, structurally excludes any secret field
- `backend/src/main/scala/com/helio/infrastructure/persistence/sources/ConnectorRepository.scala` — new repository: two-transaction-plus-compensation `create` (design.md Decision 2), owner-scoped `findByIdOwned`/`findAll`/`update` (non-secret fields), `delete` with the `dependentCount` collaborator seam (design.md Decision 4) returning `Either[ConnectorHasDependents.type, Boolean]`
- `backend/src/main/scala/com/helio/services/sources/ConnectorEntityService.scala` — new service: validation + ACL dispatch (`Future[Either[ServiceError, A]]`), maps `ConnectorHasDependents` to `ServiceError.Conflict`
- `backend/src/main/scala/com/helio/api/protocols/sources/ConnectorEntityProtocol.scala` — new `ConnectorMeta`/`ConnectorsResponse`/`CreateConnectorRequest`/`UpdateConnectorRequest` wire types, distinct from the existing `ConnectorProtocol.scala` (design.md Decision 7)
- `backend/src/main/scala/com/helio/api/routes/sources/ConnectorEntityRoutes.scala` — new `POST/GET/GET-list/PATCH/DELETE /api/connectors` route class, distinct from the existing `ConnectorRoutes.scala` (`GET /api/connector-types`, untouched); update handler rejects a request body containing a credential/secret field with 400 (design.md Decision 3)
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — mixes in `ConnectorEntityProtocol`
- `backend/src/main/scala/com/helio/api/package.scala` — re-exports `ConnectorMeta`/`ConnectorsResponse`/`CreateConnectorRequest`/`UpdateConnectorRequest` for `import com.helio.api._` call sites (existing convention for every protocol type)
- `backend/src/main/scala/com/helio/api/protocols/IdParsing.scala` — new `ConnectorIdSegment` path matcher
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — wires `connectorEntityServiceOpt` (gated on the existing nullable `dbContext` param, same nullable-optional pattern as `workspaceTeardownServiceOpt`), constructs `EncryptedSecretBackend`/`EnvMasterKeyProvider`/`ConnectorCredentialRepository`/`ConnectorRepository` (consumed as-is per HEL-536), mounts `ConnectorEntityRoutes` at `/api/connectors`
- `backend/src/test/scala/com/helio/infrastructure/persistence/sources/ConnectorRepositorySpec.scala` — new integration spec: DB-direct ciphertext proof (AC2, task 4.1), read-path enumeration forward pass (task 4.2), cross-user RLS proof under `helio_app_test` (task 4.5), delete-with-dependents seam (task 4.6), real outbound-auth proof against an in-process Pekko HTTP stub server plus a negative control (AC5, task 4.4 / design.md Decision 6a)
- `backend/src/test/scala/com/helio/api/routes/sources/ConnectorEntityRoutesSpec.scala` — new HTTP-layer spec: happy-path CRUD, no-secret-in-response on every route response (task 4.2 forward pass), update rejects a credential field with 400 (task 3.3 / AC3), 409 `ConnectorHasDependents` exercised through the real route (task 4.6)
- `backend/src/test/scala/com/helio/infrastructure/persistence/RlsPolicyGuardSpec.scala` — added `connectors` to the RLS allowlist; noted `connector_credentials` (V92, HEL-536) was never added there — a pre-existing gap, out of this ticket's scope, flagged as a finding
- `backend/.env` — set worktree-local `CONNECTOR_MASTER_KEY`/`CONNECTOR_MASTER_KEY_ID` test-only values (gitignored, never committed) per HEL-536 design.md Decision 4, since the worktree had none

## Out-of-scope findings (task 5.1 — recorded, not fixed)

- Credential rotation UX (design.md Decision 3) — a distinct create-new-credential-and-repoint operation, not built here.
- No MCP tool for Connectors — HEL-822+.
- No preview/test endpoint for Connectors — HEL-822+.
- The real dependent-source-counting query — HEL-822 supplies its own `dependentCount` collaborator implementation when it adds the referencing column; no further route/repository change needed here.
- `connector_credentials` (V92, HEL-536) was never added to `RlsPolicyGuardSpec`'s allowlist — a pre-existing gap discovered while adding `connectors` to the same list, out of this ticket's scope.
