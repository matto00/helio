- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — bare-`url` branch's hand-rolled `RestApiConfig(...)` construction now passes `parameters = request.config.parameters.getOrElse(Map.empty)`, mirroring the existing `headers` absent-field idiom in the same constructor (HEL-983 fix, design.md D1).
- `backend/src/test/scala/com/helio/services/sources/SourceServiceBareUrlParametersSpec.scala` — new guard spec (modelled on `SourceServiceBareUrlQueryParamsSpec`): creates a REST source through the bare-`url` path with a `parameters` map and `{{accountId}}` placeholders in a query-param value and a header value, then fetches through the same persisted Connector/config against a real bound HTTP server and asserts on the query string/header it received, with a secondary assertion on the persisted `parameters` map.
