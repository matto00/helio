## REMOVED Requirements

### Requirement: List metrics
**Reason**: Metrics are retired wholesale (decision 2/11) — aggregation lives only in pipeline steps; Outputs are render-only.
**Migration**: A bound metric panel's aggregation becomes an aggregate/groupBy tail step feeding an Output; the metric's format carries into the Output's config.format.

### Requirement: Create metric
**Reason**: Metrics are retired wholesale (decision 2/11) — aggregation lives only in pipeline steps; Outputs are render-only.
**Migration**: A bound metric panel's aggregation becomes an aggregate/groupBy tail step feeding an Output; the metric's format carries into the Output's config.format.

### Requirement: Get metric
**Reason**: Metrics are retired wholesale (decision 2/11) — aggregation lives only in pipeline steps; Outputs are render-only.
**Migration**: A bound metric panel's aggregation becomes an aggregate/groupBy tail step feeding an Output; the metric's format carries into the Output's config.format.

### Requirement: Update metric (partial)
**Reason**: Metrics are retired wholesale (decision 2/11) — aggregation lives only in pipeline steps; Outputs are render-only.
**Migration**: A bound metric panel's aggregation becomes an aggregate/groupBy tail step feeding an Output; the metric's format carries into the Output's config.format.

### Requirement: Delete metric
**Reason**: Metrics are retired wholesale (decision 2/11) — aggregation lives only in pipeline steps; Outputs are render-only.
**Migration**: A bound metric panel's aggregation becomes an aggregate/groupBy tail step feeding an Output; the metric's format carries into the Output's config.format.

