- `openspec/changes/outputs-model-migration/specs/acl-resource-type-registry/spec.md` — fixed delivery-time archive drift: matched the requirement header to the live spec's since-renamed text, and restored the exact scenario name the live spec still carries (`DataType type is registered`) with an updated negative assertion + note, so `openspec archive` no longer drops scenario coverage.
