- `openspec/changes/outputs-model-migration/specs/alert-evaluation-engine/spec.md` — added a `## RENAMED Requirements` entry (`Load enabled rules for the target DataType` → `... Output`) so the MODIFIED block keys off the live spec's current header, per the archive tool's RENAMED-then-MODIFIED convention (confirmed in `specs-apply.js`/`requirement-blocks.js`).
- `openspec/changes/outputs-model-migration/specs/backend-persistence/spec.md` — same RENAMED fix (`Panels table stores DataType binding` → `... an Output placement`); also reverted two scenario titles under the MODIFIED block back to their live names (`Panel without binding has null type_id`, `Panel binding persists across restarts`) — the scenario-loss guard matches scenarios by literal title, and the delta had renamed the titles while changing the body, which reads as a dropped scenario. Body text (`output_id`) is left updated to the new semantics; only the titles were reverted.
- `openspec/changes/outputs-model-migration/specs/patch-set-preview/spec.md` — found (while sweeping) an unrelated ADDED/MODIFIED mismatch of the same underlying class: `### Requirement: Impact hints are explicit and source-grounded` was declared ADDED even though a requirement of that exact title already exists live, which `openspec archive` rejects as "already exists". Renamed the ADDED requirement to a distinct title (`... for surviving edit kinds`) and retitled the previously mis-named REMOVED entry to the exact live title (`Impact hints are explicit and source-grounded`), mirroring the pattern already used for the sibling "Preview enforces..." pair in the same file (REMOVED keeps the old exact title, ADDED gets a new distinct title).
- `openspec/changes/outputs-model-migration/.openspec.yaml` — added `retire_capabilities: true`. `bound-panel-composition`'s delta REMOVEs all 4 of its requirements (full capability retirement); without this marker `openspec archive` refuses to delete a main spec whose last requirement was removed.
