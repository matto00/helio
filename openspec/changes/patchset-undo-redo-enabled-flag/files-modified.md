- `backend/src/main/scala/com/helio/services/PatchSetUndoInverse.scala` — `fullPipelineStepInverse` now reads `enabled` off the persisted step JSON and always sets an explicit `Some(...)` (full-overwrite inverse, design.md D6; absent key defaults to `true`); `pipelineStepCreateRequestFromResponse` now reads `enabled` and passes it through as `Option[Boolean]` (design.md D7; absent stays `None`, matching the create endpoint's own absent-means-enabled contract).
- `backend/src/test/scala/com/helio/services/PatchSetUndoInverseSpec.scala` — unit-test blocks for `fullPipelineStepInverse` and `pipelineStepCreateRequestFromResponse`: disabled-captured-JSON round-trips to `enabled = Some(false)`, and legacy JSON with no `enabled` key round-trips to `Some(true)` (full-revert path) / `None` (create-request path).
- `backend/src/test/scala/com/helio/services/PatchSetUndoServiceSpec.scala` — `seedPipelineStep` helper extended with an optional `enabled` param. The 5.3c delete-and-recreate case seeds the step disabled and asserts the recreated step is still `enabled = false`. Cycle 2 fold-in (task 2.6): the 5.3a full-revert case now also seeds its `pipelineStep` disabled and asserts `restoredStep.enabled shouldBe false`, giving the full-revert path (`fullPipelineStepInverse`) the same DB-backed coverage symmetry as 5.3c's delete-and-recreate path.
