- `frontend/src/features/settings/ui/SettingsPage.tsx` — adds a new "Appearance" section (before "Preferences") rendering `AccentPicker` via `useTheme()`'s `accentColor`/`setAccentColor`, preserving immediate-apply semantics.
- `frontend/src/features/auth/ui/UserMenu.tsx` — removes the `accentColor`/`setAccentColor` props, the `AccentPicker` import, and the "Accent color" section + its two flanking dividers from the popover; extends the F-082 comment to cover the HEL-728 accent move.
- `frontend/src/app/CommandBar.tsx` — drops `accentColor`/`setAccentColor` from the `useTheme()` destructure and stops passing them as props to `<UserMenu>` (now unused there).
- `frontend/src/features/auth/ui/UserMenu.css` — removes the now-dead `.user-menu__section`/`.user-menu__section-label` rules (grep-confirmed no other usage).
- `frontend/src/features/auth/ui/UserMenu.test.tsx` — drops `accentColor`/`setAccentColor` from `renderMenu` and the F-086 direct-render call site; removes the "renders accent color picker inside popover" test; updates the ArrowDown/ArrowUp focus-order tests to expect "Settings" as the first focusable item instead of the removed accent swatches.
- `frontend/src/features/settings/ui/SettingsPage.test.tsx` — adds coverage for the new Appearance section: renders with the current accent selected, and clicking a swatch applies immediately (writes `--app-accent`) with no "Save preferences" click involved.
- `openspec/specs/user-menu-popover/spec.md` — hand-edited `## Purpose` (design.md Decision 5) to stop claiming the popover consolidates theme/accent controls; now describes it as scoped to session/identity controls, with theme (command bar) and accent (Settings) as documented exceptions living elsewhere.
- `openspec/specs/settings-preferences-ui/spec.md` — hand-edited `## Purpose` (design.md Decision 5) to describe both the existing explicit-save "Preferences" section and the new immediate-apply "Appearance" section as part of `/settings`.
