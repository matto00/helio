- `backend/src/main/scala/com/helio/services/proposals/DashboardAuthoringPrompt.scala` — renamed "DataType"→"Output" in doc comments and the live panel-capability fallback string; left the `dataTypes` param, `c.dataType` scalar-type access, and the quoted spec scenario title untouched.
- `backend/src/main/scala/com/helio/services/patchsets/RefinementPrompt.scala` — renamed "DataType"→"Output" in the doc comment and live prompt strings (entity line, fallback text, "Available pipeline Outputs" header), mirroring `DashboardAuthoringPrompt`.
- `backend/src/main/scala/com/helio/services/assistant/AssistantSystemPrompt.scala` — renamed "DataType"→"Output" in doc comments and the `find`/`get_resource`/`propose_dashboard`/`propose_pipeline` tool-description prose; added a `(resource type "dataType")` disambiguation hint to `find`/`get_resource` since the wire enum value is unchanged.
- `backend/src/main/scala/com/helio/services/workspace/WorkspaceAssistantTools.scala` — renamed "DataType"→"Output" in the `find`/`get_resource` tool `description` fields, with the same `(resource type "dataType")` disambiguation hint; left the `ResourceTypeEnum` wire value `"dataType"` untouched.
- `backend/src/main/scala/com/helio/api/protocols/assistant/AssistantProposalToolSchemas.scala` — renamed "DataType"→"Output" in the `outputId` field description, a doc comment, and the `propose_dashboard`/`propose_pipeline` tool descriptions.
- `backend/src/main/scala/com/helio/services/proposals/DashboardAuthoringService.scala` — renamed "DataType"→"Output" in `EmptyWorkspaceMessage` and `degradeMessage`.
- `backend/src/main/scala/com/helio/services/patchsets/RefinementGrounding.scala` — renamed "DataType"→"Output" in the mirrored `degradeMessage`.
- `backend/src/test/scala/com/helio/services/assistant/AssistantSystemPromptSpec.scala` — added a regression test asserting the wire-value hint `"dataType"` survives the rename.
- `backend/src/test/scala/com/helio/services/workspace/WorkspaceAssistantToolsSpec.scala` — added regression tests asserting `findTool`/`getResourceTool` descriptions still include the literal `"dataType"` wire-value hint.
- `openspec/changes/rename-datatype-prompt-copy/tasks.md` — marked all tasks complete.
