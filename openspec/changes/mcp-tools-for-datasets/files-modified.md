- `helio-mcp/src/httpClient.ts` — `HelioHttpClient.delete<T>` now accepts an optional `query` param (task 1.1), needed for `deleteDatasetRow`'s `updatedAt` query-string precondition.
- `helio-mcp/src/helioApi.ts` — `StaticColumn` gained `required`/`default`; `createDataSource` forwards them (absent-vs-null preserved); added `appendDatasetRows`, `replaceDatasetRows`, `getDatasetRows`, `getDatasetSchema`, `updateDatasetSchema`, `updateDatasetRow`, `deleteDatasetRow`.
- `helio-mcp/src/types.ts` — added `RowResponseRow`, `RowListResponse`, `RowWriteRowResponse`, `RowWriteResponse`, `RowResponse`, `DatasetFieldResponse`, `DatasetSchemaResponse`, `DatasetSchemaUpdateResponse` wire shapes.
- `helio-mcp/src/tools/write.ts` — extended `create_data_source`'s input schema (`required`/`default` per column) and description; registered `append_dataset_rows`, `replace_dataset_rows`, `update_dataset_row`, `delete_dataset_row`, `update_dataset_schema`.
- `helio-mcp/src/tools/read.ts` — registered `get_dataset_rows`, `get_dataset_schema`.
- `helio-mcp/src/helioApi.test.ts` — unit tests for every new `HelioApi` method (request shape, `deleteDatasetRow`'s query-string encoding, `createDataSource`'s absent-vs-explicit-null `default` forwarding).
- `helio-mcp/src/tools/datasetTools.test.ts` (new) — handler-level tests for the new write tools (happy path, schema-violating-row rejection, stale-`updatedAt` precondition rejection, schema rename/`confirmDrop` enforcement) via a real MCP `Client`/`McpServer` pair.
- `helio-mcp/src/tools/read.test.ts` (new) — handler-level tests for `get_dataset_rows`/`get_dataset_schema`, including the `nextCursor`-absent-on-last-page case.
- `helio-mcp/src/server.test.ts` — `EXPECTED_TOOL_NAMES` updated with the 7 new tool names (`append_dataset_rows`, `replace_dataset_rows`, `update_dataset_row`, `delete_dataset_row`, `update_dataset_schema`, `get_dataset_rows`, `get_dataset_schema`).
- `openspec/changes/mcp-tools-for-datasets/tasks.md` — all tasks marked complete.

No backend/Scala changes — this ticket is purely an MCP-surface (helio-mcp) addition over the already-shipped `/api/data-sources/:id/{rows,rows/:rowId,schema}` REST endpoints.

**Live end-to-end verification (task 2.3):** ran a full round trip against a locally started backend + Postgres (`scripts/concertino/start-servers.sh`) using a real MCP client over stdio, with a PAT minted for the run and revoked immediately after (per CONTRIBUTING.md's credential-handling rule): `create_data_source` (empty rows, declared schema with `required`) → `append_dataset_rows` → `get_dataset_rows` (`nextCursor` genuinely absent on the last page) → `update_dataset_row` → `get_dataset_schema` → `update_dataset_schema` (rename `qty` → `quantity`) → `create_pipeline` (with an `outputs[]` entry) → `run_pipeline` (`status: succeeded`, `rowCount: 1`) → `get_output_rows` returned `{"quantity":5,"sku":"widget"}` — reflecting the appended row, the row edit, and the schema rename, closing the loop end to end with no UI involved. The one-shot verification script used to run this was not committed (test coverage for the same scenarios lives in `datasetTools.test.ts`/`helioApi.test.ts`/`read.test.ts`).
