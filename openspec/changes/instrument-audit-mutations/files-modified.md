## Files modified

- `backend/src/main/scala/com/helio/services/dashboards/DashboardService.scala` — wires `AuditService`; instruments `create` (create-only, per Decision 2), `update`, `delete`, `duplicate`, `importSnapshot`; adds `deleteInternal` (rollback-only, no audit call, Decision 10).
- `backend/src/main/scala/com/helio/services/dashboards/DashboardContentsService.scala` — wires `AuditService`; instruments `replaceContents` → `dashboard.contents.replace`.
- `backend/src/main/scala/com/helio/services/panels/PanelService.scala` — wires `AuditService`; instruments `create`, `delete`, `duplicate`, `update`, `batchCreate`, `batchUpdate`.
- `backend/src/main/scala/com/helio/services/panels/AutoLayoutService.scala` — wires `AuditService`; instruments `autoLayout` → `dashboard.update`.
- `backend/src/main/scala/com/helio/services/panels/BoundPanelService.scala` — wires `AuditService`; instruments the wizard's terminal panel-create success → `panel.create`.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — wires `AuditService`; instruments pipeline `create`/`updateName`/`delete`, step `create`/`update`/`delete`, and (skeptic-final-1 round 1) `reorderSteps` → `pipeline.step.reorder` (one row/call) and `duplicateStep` → `pipeline.step.duplicate`.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineRunService.scala` — wires `AuditService`; instruments `submit` → `pipeline.run.submit` (submission only, not every run status transition).
- `backend/src/main/scala/com/helio/services/pipelines/PipelineScheduleService.scala` — wires `AuditService`; instruments `put` (upsert) and `delete`.
- `backend/src/main/scala/com/helio/services/pipelines/DataTypeService.scala` — wires `AuditService`; instruments `update`/`delete` (no `create` — data types are never user-created directly).
- `backend/src/main/scala/com/helio/services/sources/DataSourceService.scala` — wires `AuditService`; instruments every `create*` variant, `update`, `delete`.
- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — wires `AuditService`; instruments `createSql`/`createRest` (both emit `data_source.create`, same resource type as `DataSourceService`).
- `backend/src/main/scala/com/helio/services/sources/ImageUploadService.scala` — wires `AuditService`; instruments `upload` → `image_upload.create`.
- `backend/src/main/scala/com/helio/services/auth/AuthService.scala` — wires `AuditService`; instruments `register`, `login` (`auth.login`/`auth.login.challenged`/`auth.login.failed` split), `logout` (now binds the actor from the found session instead of discarding it), `completeOAuth`.
- `backend/src/main/scala/com/helio/services/auth/MfaService.scala` — wires `AuditService`; instruments `confirmEnrollment` → `auth.mfa.enable`, `disable` → `auth.mfa.disable`, `regenerateBackupCodes` → `auth.mfa.backup_codes.regenerate`, and the login-time `verifyLogin` success/failure paths (`auth.login`/`auth.login.failed`).
- `backend/src/main/scala/com/helio/services/auth/ApiTokenService.scala` — wires `AuditService`; instruments `create` → `token.create`, `revoke` → `token.revoke`.
- `backend/src/main/scala/com/helio/services/proposals/DashboardProposalService.scala` — `createAll`'s rollback branch now calls `DashboardService.deleteInternal` instead of the public `delete`, so a failed proposal apply never writes a false `dashboard.delete` row.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — new `auditEventRepo: AuditEventRepository = null` constructor param; constructs one shared `AuditService` (or `null`) and threads it into every mutating service constructed above.
- `backend/src/main/scala/com/helio/app/Main.scala` — passes the existing `auditEventRepo` into `ApiRoutes(auditEventRepo = ...)` instead of leaving it unwired.
- `backend/src/test/scala/com/helio/services/audit/AuditTestFixture.scala` — new shared test fixture (no-op / failing / capturing `AuditService` builders) per tasks.md 1.2.
- `backend/src/test/scala/com/helio/api/AuditMutationInstrumentationSpec.scala` — integration suite (route testkit + real embedded-Postgres `AuditEventRepository`) covering: dashboard/panel create/update/delete/duplicate row cardinality; auth register/login/login-failed (secret-free metadata); audit-write-failure isolation; MFA-gated login (`auth.login.challenged` at initial login, `auth.login` from `MfaService.verifyLogin`'s success path, `auth.login.failed` on a bad TOTP code — cycle-2 addition, tasks.md 7.7); `panel.batch_create`/`panel.batch_update`/`dashboard.import`/`dashboard.contents.replace` row-cardinality + metadata-count (cycle-2 addition, tasks.md 7.8); a proposal-apply rollback that writes `dashboard.create` but not `dashboard.delete` for the rolled-back dashboard (cycle-2 addition, tasks.md 7.9). Cycle-3 addition (skeptic-final-1 round 1): `pipeline.step.duplicate`/`pipeline.step.reorder` row-cardinality tests.
- `openspec/changes/instrument-audit-mutations/route-audit-enumeration.md` — cycle-2 addition (tasks.md 7.5): every `post`/`put`/`patch`/`delete` route under `backend/src/main/scala/com/helio/api/routes/` walked and mapped to its audit action or an explicit out-of-scope reason; surfaces 3 real, un-instrumented mutations outside the ticket's named scope as tracked follow-up gaps (`DataSourceService.refresh`/`SourceService.refresh`, and — highest severity — `WorkspaceTeardownService.teardown`'s bulk delete).
