- `backend/src/main/scala/com/helio/domain/model/model.scala` — added `RestApiConfig.parameters: Map[String, String] = Map.empty` (domain field for template values).
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceProtocol.scala` — added `RestApiConfigPayload.parameters: Option[Map[String,String]] = None`; threaded through `toDomain`/`fromDomain`; `jsonFormat8` → `jsonFormat9`.
- `backend/src/main/scala/com/helio/api/protocols/sources/DataSourceConfigCodec.scala` — second `jsonFormat8` → `jsonFormat9` site for the stored-config-blob codec (spray-json requires both sites updated together).
- `backend/src/main/scala/com/helio/domain/connectors/TemplateInterpolator.scala` — new file: `{{name}}` resolver (`resolve`), endpoint RFC-3986 path-segment encoding helper (`resolveEndpoint`), header CRLF-injection guard (`guardHeaderValue`), JSON-body escaping helper (`jsonEscape`/`resolveJsonBody`).
- `backend/src/main/scala/com/helio/domain/connectors/RestApiConnectorDriver.scala` — `buildResolvedRequest` now resolves `endpoint`/`queryParams`/`headers` against `config.parameters` via `TemplateInterpolator` before building the `HttpRequest`, short-circuiting to `Left` on any unresolved variable or CRLF-guard failure; `buildEphemeralRequest` documented as deliberately never calling the interpolator (literal passthrough, design.md Non-Goals).
- `backend/src/test/scala/com/helio/domain/connectors/RestApiConnectorDriverTemplatingSpec.scala` — new file: full HEL-823 test coverage (tasks.md section 4) — endpoint/query/header resolution on both the connectorId-carrying path and the real `InProcessPipelineEngine` run-time path, unresolved-variable fail-loud (endpoint/query/header + body-level), escaping (query `&`/quote/newline/unicode, header CRLF rejection, JSON body quote/newline/unicode, endpoint space/`*` RFC-3986 encoding), credential-unreachability with hostile `{{apiKey}}`/`{{credential}}`/`{{secret}}` templates plus a real-decrypt-never-leaks assertion, no-parameters byte-identical regression, missing-`parameters`-key decode regression, body-templating boundary (design.md Decision 7), auth-header-collision-with-templated-value regression, and ephemeral-path literal-passthrough regression.
- `openspec/changes/rest-request-templating/tasks.md` — all tasks marked complete.

## Round 2 (final-gate skeptic CR1, `skeptic-final-1.md`)

- `backend/src/main/scala/com/helio/domain/connectors/TemplateInterpolator.scala` — CR1 fix: `encodePathSegment` is now total on an empty value (`if (value.isEmpty) "" else Uri.Path.Segment(...)`), since `Uri.Path.Segment` throws `IllegalArgumentException` on an empty head and that raw exception was escaping past this file's own "never a raw exception" contract. Also folded `resolve`/`resolveEndpoint`/`resolveJsonBody`'s near-identical scan scaffolding into one shared private `resolveWith` helper (non-blocking DRY note from both evaluator and skeptic) — fixes CR1 in exactly one place.
- `backend/src/main/scala/com/helio/domain/model/model.scala`, `.../api/protocols/sources/DataSourceProtocol.scala` — one-sentence scaladoc addition noting `parameters` is NOT secret storage (plaintext in `data_sources.config` JSONB, unredacted on read).
- `backend/src/test/scala/com/helio/domain/connectors/RestApiConnectorDriverTemplatingSpec.scala` — new regression test under 4.4 (`parameters = Map("v" -> "")`, `endpoint = "/echo/{{v}}"`) demonstrated red (raw `IllegalArgumentException`) before the fix, green after.
- `openspec/changes/rest-request-templating/evaluation-1.md` — corrected wording per skeptic's non-blocking note: test 4.5 uses `authType=none`, not a real bearer auth flow (bearer decrypt-and-apply is covered separately by test 4.8).

## Orchestrator (Setup / Delivery)

- `openspec/workflow-state.md` — rewritten for this ticket at Setup (repo-tracked file inherited main's stale copy from a prior ticket's worktree; per orchestrator instructions, always rewritten to reflect the current run).
