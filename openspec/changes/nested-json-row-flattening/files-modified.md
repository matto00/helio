# Files modified — HEL-599

- `backend/src/main/scala/com/helio/domain/engine/JsonFlattener.scala` — new shared bounded traversal (design D1-D4): `leaves`/`flattenJsObject`, `MaxDepth`, array-as-leaf, deterministic collision resolution. Round 2 (skeptic-final-1.md CR1): `leaves` now deduplicates by path internally (last-in-walk-order wins) instead of relying on each caller to fold into a `Map` — `SchemaInferenceEngine.flattenObject` never folds its `InferredField` `Seq` into a `Map`, so a caller-side-only fold was shipping a duplicate-named field into the schema while the row deduplicated.
- `backend/src/main/scala/com/helio/domain/engine/SchemaInferenceEngine.scala` — `flattenObject` reimplemented as a projection of `JsonFlattener.leaves` through `inferJsonType`/`displayName`; `mergeObjects` left untouched (design D8, HEL-858's territory).
- `backend/src/main/scala/com/helio/domain/engine/PipelineRowJson.scala` — `jsRowToRow`'s `JsObject` branch reimplemented as a projection of `JsonFlattener.leaves` through `jsValueToAny`; this is the actual bug fix (rows now carry the dotted columns schema already advertised).
- `backend/src/main/scala/com/helio/domain/connectors/RestApiConnectorDriver.scala` — added `toRowsEither` (curated `Left` on a broken `rootSelector`, design D5); `toRows` kept as its unset-selector wrapper; `inferSchema`/`fetch(maxRows)`/`inferSchemaEphemeral` now route through `toRowsEither` so a broken selector fails loud instead of yielding zero rows.
- `backend/src/main/scala/com/helio/services/sources/SourceService.scala` — `previewRest` (the 4th `toRows` call site) now uses `toRowsEither` (curated `BadGateway` on selector failure) and flattens preview rows via `JsonFlattener.flattenJsObject` so preview/schema/executed-rows all agree (design D6).
- `backend/src/test/scala/com/helio/domain/engine/JsonFlattenerSpec.scala` *(test)* — unit coverage for the shared traversal: nesting, arrays-as-leaves, collision determinism, empty objects. Round 2: the collision test now asserts on `leaves(...)`'s raw `Seq` directly (not a locally re-folded `Map`); the depth-bound test now pins the exact leaf path length (`MaxDepth` segments), the untouched leaf `JsValue`, and the row/schema materialisation at the bound (CR2 — previously only asserted non-emptiness, which would pass for any `MaxDepth`).
- `backend/src/test/scala/com/helio/domain/engine/SchemaInferenceEngineSpec.scala` *(test)* — round 2 addition (skeptic-final-1.md CR1): asserts `fromJson({"a.b":1,"a":{"b":2}})` yields exactly one `a.b` field (not two), and that the schema's winning value agrees with the row's winning value on the same colliding input.
- `backend/src/test/scala/com/helio/domain/engine/NestedJsonFlatteningSymmetrySpec.scala` *(test)* — the symmetry regression (task 5.2, confirmed RED pre-fix), negative control (5.3), and type-correctness (5.5) tests, all driven by the captured live Sleeper fixture.
- `backend/src/test/resources/hel599/sleeper-wr-projections-slice.json` *(test fixture)* — verbatim 3-row capture of the live Sleeper WR projections endpoint (task 5.4), including third-level `player.metadata`.
- `backend/src/test/scala/com/helio/domain/connectors/RestApiConnectorDriverSpec.scala` *(test)* — new `toRowsEither` test block: unset-selector parity, nested-key selection, missing-segment/non-object-mid-path curated errors, empty-array-is-not-an-error, no-credential-leak.
- `backend/src/test/scala/com/helio/domain/connectors/RestApiConnectorDriverTemplatingSpec.scala` *(test)* — repaired the pre-existing assertion that pinned the old compact-JSON-string `headers` shape; now asserts the dotted `headers.X-Custom` column directly.
- `backend/src/test/scala/com/helio/domain/engine/InProcessPipelineEngineSpec.scala` *(test)* — new RestSource tests: nested-response dotted-column materialisation, broken-rootSelector run failure, and a `select`-with-dotted-column regression (task 5.8).
- `backend/src/test/scala/com/helio/services/sources/SourceServiceSpec.scala` *(test)* — new `preview` tests: nested response returns flat dotted keys, broken selector surfaces as `BadGateway`.
- `openspec/changes/nested-json-row-flattening/specs/nested-json-flattening/spec.md` — added a documented D9 limitation note (dotted columns unreachable from `compute`/`filter` expressions and source computed fields; `rename`/field-override workaround).
- `openspec/changes/nested-json-row-flattening/evidence/live-probe-transcript.md` — task 6.1/6.2 run evidence: a real source/pipeline created against the live Sleeper endpoint via this worktree's dev backend, confirming dotted columns are populated and typed (not wired into the commit gate, per design D7.4).
- `openspec/changes/nested-json-row-flattening/tasks.md` — all tasks marked complete.
