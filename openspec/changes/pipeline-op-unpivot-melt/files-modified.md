- `backend/src/main/scala/com/helio/domain/steps/UnpivotStep.scala` — new domain step: `UnpivotConfig`, tolerant `decode`, `UnpivotStep.evaluate`/`apply` (nested per-row × per-valueVar emission), `companion` codec.
- `backend/src/main/scala/com/helio/domain/PipelineStep.scala` — register `UnpivotStep.Kind -> UnpivotStep.companion` in `Registry`, add `PipelineStepKind.Unpivot`.
- `backend/src/main/scala/com/helio/domain/package.scala` — `UnpivotStep`/`UnpivotConfig` type+val aliases re-exported into `com.helio.domain`.
- `backend/src/main/scala/com/helio/api/protocols/PipelineStepProtocol.scala` — `UnpivotStepResponse` case class, implicit `UnpivotConfig` format, `jsonFormat6`, write/read union arms, `fromDomain` arm.
- `backend/src/main/scala/com/helio/api/protocols/PipelineStepConfigCodec.scala` — `encodeConfig`/`extractConfig` arms for `UnpivotConfig`/`UnpivotStep`.
- `backend/src/main/scala/com/helio/infrastructure/PipelineStepRepository.scala` — `rowToDomain` arm for `UnpivotConfig -> UnpivotStep`.
- `backend/src/main/scala/com/helio/domain/PipelineAnalyzeService.scala` — `"unpivot" -> inferUnpivot` dispatch arm + `inferUnpivot` (idVars + varName(string) + valueName(common-or-string), existence validation, identity fallback).
- `backend/src/main/scala/com/helio/api/protocols/PipelineAnalyzeProtocol.scala` — `UnpivotAnalyzeStepResponse`, `jsonFormat6`, write/read union arms.
- `backend/src/main/scala/com/helio/services/PipelineService.scala` — `toAnalyzeStepResponse` arm for `UnpivotConfig -> UnpivotAnalyzeStepResponse`.
- `backend/src/main/resources/db/migration/V67__add_unpivot_op.sql` — extends `pipeline_steps_op_check` to accept `'unpivot'` (next free VNN reconfirmed at write time and again before delivery).
- `backend/src/test/scala/com/helio/domain/InProcessPipelineEngineSpec.scala` — round-trip execution tests: basic unpivot, row-count multiplication, default varName/valueName, missing-field null handling, valueName/idVars collision, empty-valueVars edge case (zero rows).
- `backend/src/test/scala/com/helio/domain/PipelineAnalyzeServiceSpec.scala` — analyze-schema tests: idVars+varName(string)+valueName(common type) with no error, mixed-type fallback to string, unknown idVars/valueVars validation errors, malformed-config identity fallback.
- `backend/src/test/scala/com/helio/api/protocols/PipelineStepConfigCodecSpec.scala` — codec round-trip + tolerant-decode({}) test for `UnpivotConfig`, added to the `encodeConfig` round-trip table.
- `backend/src/test/scala/com/helio/domain/PipelineStepSpec.scala` — kind-parity/exhaustiveness update to include `unpivot`.
- `backend/src/test/scala/com/helio/api/protocols/PipelineStepProtocolSpec.scala` — wire round-trip coverage for `UnpivotStepResponse`.
- `frontend/src/features/pipelines/types/pipelineStep.ts` — `UnpivotConfig`/`UnpivotStep`/`UnpivotAnalyzeStep` wire types, added to the `PipelineStep`/`PipelineStepConfig`/`AnalyzeStepResult` unions.
- `frontend/src/features/pipelines/state/stepNarrowing.ts` — `OP_TYPES` entry (`faTableList` icon), `defaultConfigFor` case, `unpivotConfigOf` narrowing helper.
- `frontend/src/features/pipelines/ui/UnpivotConfig.tsx` — new config editor: idVars/valueVars multi-selects + varName/valueName text inputs.
- `frontend/src/features/pipelines/ui/UnpivotConfig.test.tsx` — co-located Jest/RTL coverage for field selection, varName/valueName inputs, and onChange round-trip.
- `frontend/src/features/pipelines/hooks/useStepCardState.ts` — `unpivotConfig` state + `onUnpivotChange` handler, wired into the during-render sync block and the returned handlers object.
- `frontend/src/features/pipelines/ui/StepCard.tsx` — render arm for the `unpivot` op type.
- `helio-mcp/src/tools/write.ts` — `add_pipeline_step` tool description updated to document `unpivot`'s type + config shape.
