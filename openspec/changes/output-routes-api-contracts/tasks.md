## 1. Backend — schemas & contracts

- [x] 1.1 Add `schemas/outputs/{output,create-output-request,update-output-request}.schema.json` (+ `output-schema-field.schema.json` helper); `check:schemas` green. **`output-capabilities-response` (backs task 3.4) still NOT added -> HEL-933. `preview-outputs-response` -- task 3.7 shipped cycle 10 as `schemas/pipelines/pipeline-preview-response.schema.json` (a `PipelinePreviewResponse`, not a separate `preview-outputs-response` name), so that half of this note is now resolved, not deferred.**
- [x] 1.2 (partial, cycle 4; completed cycle 7) Added `schemas/pipelines/create-pipeline-request.schema.json` + 2 new sub-schemas (`create-pipeline-transactional-{step,output}-request`) for the new single-call shape. `check:schemas` green. `parentStepId` on the EXISTING `create-pipeline-step-request.schema.json` (`POST /api/pipelines/:id/steps`) landed alongside task 3.2 in cycle 7.
- [ ] 1.3 -> HEL-933 (pairs with task 3.10). Add `schemas/sources/data-source.schema.json` covering the `DataSourceResponse` shape with `inferredSchema` (the base list/get shape — `CreateSourceResponse` already has this field); verify `check:schemas` green
- [x] 1.4 `panel-query.schema.json` already absent (confirmed by HEL-904); grep for `panel-query` under `schemas/` returns nothing
- [x] 1.5 Confirmed: no shape change needed — `create-panel-request.schema.json`'s `output` variant already matches `{type, config.outputId}` (HEL-904 task 3.6); left the description as-is (no drift)

**Cycle 2 (evaluation-1.md FAIL, CR1-CR3/CR7/CR9/CR10 addressed):** see below for updated status.

## 2. Backend — Output routes & service

- [x] 2.1 Added `OutputService` (ACL-checked CRUD against `OutputRepository`/`PanelRepository`, mirroring `PanelService`'s ACL pattern); `OutputRoutesSpec` covers owner/editor-grantee/unrelated-caller for create/list/read/update/delete against a real non-superuser RLS role (see design note in that spec on why plain create/list return 403, not 404, for an authenticated non-grantee — matches `AccessCheckerImpl`'s existing, pre-existing-codebase-wide rule)
- [x] 2.2 Added `OutputRoutes` (`GET/POST /api/pipelines/:id/outputs`, `GET/PATCH/DELETE /api/outputs/:id`) and mounted it in `ApiRoutes.scala` next to `outputRepoOpt`; route-level tests hit each verb
- [x] 2.3 Implemented `DELETE /api/outputs/:id` cascading panel deletion + removed-placement report; test creates two placements, deletes the Output, asserts both panels are gone and reported
- [x] 2.3a Implemented `PATCH /api/outputs/:id` with partial-merge `config` semantics for `legend`/`tooltip`/`seriesColors`/`axisLabels` (HEL-877); tests cover a partial-legend merge and an owner-only-RLS-blocked update returning 404. **Simplified from the ticket's `RequestValidation` absent-vs-null idiom to a plain `Option` for `name`/`config`** — neither field has a null-clearing variant (an Output always has a name and a config object), so `Option[Option[T]]` would add ceremony with no behavior difference; flagging this simplification explicitly rather than silently narrowing scope.
- [ ] 2.3b NOT implemented this cycle — `config.format` (HEL-876) support deferred -> HEL-933
- [ ] 2.4 partial: `GET /api/outputs/:id/panels` implemented and tested (via `listPanels`/cascade-delete test); `GET /api/outputs/:id/rows` NOT implemented this cycle
- [x] 2.5 (cycle 3) Implemented `GET /api/outputs/:id/assertion-status` (`OutputService.assertionStatus`) -- keyed by the Output's own `NodeRef.stepId` (a raw-source Output, `stepId = None`, is always `invalid = false`, no `assert` step to check), scoped to the latest PERSISTED run (a dry run persists nothing, so no separate dry-run filter is needed, correcting the retired DataType-keyed version's premise). Renamed `AssertionStatusResponse.dataTypeId` -> `outputId` (+ matching schema update, `check:schemas` still green). 5 route tests: raw-source no-op, passing run, failing run, a DIFFERENT step's failure not leaking onto this Output, and the owner/grantee/other ACL triad. Alert-rule create/read against `targetOutputId` was ALREADY implemented and tested (`AlertRuleService`/`AlertRuleRoutesSpec`, pre-dating this ticket) -- confirmed, no new work needed for that half of AC 6.
- [x] 2.6 (cycle 7) `GET /api/dashboards` was ALREADY paginated (offset/limit/`PagedResult`, unrelated to this ticket's own changes) -- verified, no code change needed there. Added `GET /api/outputs` (task's actual gap): owner-scoped (mirrors `OutputRepository.findAllByOwner`'s existing owner-only shape, NOT sharing-aware -- distinct from `GET /pipelines/:id/outputs`, which is), offset/limit paginated, mounted alongside `OutputRoutes`' existing `outputs/:id` branch (matched first via `pathEndOrSingleSlash`, mirroring `PipelineRoutes`' own literal-segment-vs-Segment-matcher ordering discipline). 3 tests: owner-only scoping (a grantee-owned Output on the SAME pipeline does not leak into the owner's own list), offset/limit, negative-offset 400.
- [ ] 2.7 -> HEL-933. Extend `PanelService.create`/`POST /api/panels` to compute a decision-15 default layout item per panel kind and append it to the dashboard's layout in the same transaction, returning the placed item; verify a test asserting the layout append and a rollback test if the append fails

## 3. Backend — pipeline creation, capabilities, preview

- [x] 3.1 (cycle 4, REWRITTEN cycle 5 per coordinator ruling D3) Implemented the single-call transactional `POST /api/pipelines`: `steps[]` (resolving `parentStepId` by request-scoped `clientId`) then `outputs[]` (resolving `nodeStepClientId` the same way), built inside ONE real Slick transaction with the pipeline row (`PipelineRepository.runTransactionally`, a genuine `.transactionally` DBIO chain composing `createAction`/`insertInternalAction` from 3 repositories). Cycle 4's compensating-delete implementation was DELETED outright (not patched) once the coordinator confirmed `DbContext.withSystemContext` could run an arbitrary composed `DBIO`. Mutation-tested: splitting the composition into two separate transactions makes the rollback test fail with a real partial write visible, confirming the test exercises the actual transaction boundary. 5 tests in `PipelineCreateTransactionalSpec` (full success path, 2 rollback tests -- now backed by a real transaction plus a raw-SQL count assertion, unresolvable `parentStepId` rejection, pre-existing simple-create shape byte-identical) plus a 458-test pipeline-suite regression sweep, all green. Inline-source variant (vs. `sourceDataSourceId`) still NOT implemented.
- [x] 3.2 (cycle 7) Implemented: `CreatePipelineStepRequest.parentStepId` (optional, precedence over `position`) validated against the pipeline's own step list then spliced via the existing `spliceInsertAtInternal` primitive (an unrelated/nonexistent id -> 422, nothing persisted). `DELETE /api/pipeline-steps/:id` now returns `200 DeletePipelineStepResponse(removedTailStepCount)` instead of a bare `204` -- `deleteInternal`'s already-computed splice-on-delete count (previously discarded) is surfaced. Every pre-existing test asserting `204` on this route was updated to `200` + the new body (5 call sites across `PipelineStepRoutesSpec`/`PipelineAclSpec`), never left red or silently passing on the old assumption. New tests: parentStepId splice (persisted-parent verification, since `PipelineStepResponse` carries no `parentStepId` field), parentStepId-not-in-this-pipeline 422, and a raw-SQL-seeded real branch point proving `removedTailStepCount = 2` on delete. Schema: `parentStepId` added to `create-pipeline-step-request.schema.json`; new `delete-pipeline-step-response.schema.json`.
- [x] 3.3 Implemented `PipelineAnalyzeService.analyzeNodes` — per-node (trunk + every tail) schema projection, walking the `parentStepId` tree (each node's input = its own parent's output, not the linear `analyze` chain). `PipelineAnalyzeServiceSpec` asserts a tail's projection genuinely differs from the trunk's when the tail's `select` drops a column, and that a tail's input schema is its parent's output (not the raw source).
- [x] 3.4 Added `GET /api/pipelines/:id/capabilities?stepId=` (`PipelineService.capabilitiesAtNode` + `NodeCapabilitiesResponse`), evaluating `OutputBindingSpec` against 3.3's projection. `PipelineCapabilitiesRoutesSpec` covers: source-level (stepId absent) metric/chart-bindable with a numeric column present; a tail step's dropped numeric column reported unbindable independent of the trunk; unknown-stepId 404; owner/editor-grantee/unrelated-caller ACL (200/200/404 — pipelines' own `findByIdShared` has no 403 branch, unlike Output CRUD's `AccessChecker`-based 403).
- [x] 3.5 Cycle 2 grepped only `PipelineAnalyzeService` and found/fixed `aggResultType` (sum/avg) + `inferWindow` (running_sum) + `inferDateBucket` (all non-canonical `"number"`/`"date"`). **Cycle 3 (evaluation-2.md finding): that sweep was too narrow.** A repo-wide grep for `"number"`/`"double"` under `backend/src/main/scala/com/helio/` found and fixed TWO MORE real producers: `ExpressionEvaluator.inferTypeOf` (feeds `PipelineAnalyzeService.inferCompute`'s primary path) emitted `"number"` for every numeric literal/arithmetic/`length()` result -- fixed to `"float"`. `inferCompute`'s `getOrElse(wireType)` FALLBACK path (hit when `inferType` itself fails) and `inferCast`'s `casts` map both pass a caller-supplied config string straight into a `SchemaField` type unnormalized -- added `canonicalizeLegacyType` (number/double->float, long->integer, date->timestamp) and applied it at both call sites, closing the same "silently dropped from capabilities" bug class for `compute`'s fallback AND `cast`. `DemoData.scala`'s seed data fixed too (not a producer in the AC-3 sense, but trivial). Every existing test assertion pinned to an old non-canonical value was corrected (8 in `ExpressionEvaluatorSpec`/`PipelineAnalyzeServiceSpec`), and 3 new regression tests added (`cast` double/long/date canonicalization, `compute` fallback-path canonicalization). Re-ran the repo-wide grep a second time after all fixes landed -- the only remaining `"number"`/`"double"` hits are `ExpressionEvaluator.typeName` (error-message text only, never a `SchemaField` producer) and `CastStep.castValue`/`SparkJobSubmitter`'s runtime cast-kind dispatch (execution semantics, not a schema-type producer) -- confirmed out of AC-3's scope, not fixed. **Cycle 4 (evaluation-3.md finding): even the repo-wide string-grep approach could only find HARDCODED non-canonical emissions, never pass-throughs.** Redid the sweep by enumerating every `SchemaField(` construction site in `backend/src/main/scala/` (31 sites, full checkable list in execution-progress.md) rather than grepping for known-bad strings. Found and fixed 3 MORE live bugs this way: `inferAggregate`'s `groupBy` field type (evaluator-named), `PipelineService`'s inline static-source dry-analyze path (evaluator-named), and `SchemaInferenceFacade.toSchemaFields`' override branch PLUS `DataSourceService.createCsv`'s own separate override-branch duplicate (both found only by the enumeration, not evaluator-named). All 5 now route through the shared `DataFieldType.canonicalizeLegacy` (promoted from `PipelineAnalyzeService`-local to the `DataFieldType` companion object so both `PipelineAnalyzeService` and the sources-layer callers share one definition). New regression tests for all 3: `PipelineAnalyzeServiceSpec` (`groupBy`), `PipelineAnalyzeProposalRoutesSpec` (inline static dry-analyze), `SchemaInferenceFacadeSpec` (override canonicalization), `DataSourceServiceSpec` (`createStatic` override, confirming the earlier fix still holds).
- [x] 3.6 `PipelineCapabilitiesRoutesSpec` HEL-644 test: a `select`-produced numeric column survives projection and stays metric-bindable. **Cycle 3**: `OutputBindingSpec.validateFieldMapping` (HEL-892) is now wired into `OutputService.create`/`update` (validated against `config.fieldMapping`, keyed off the Output's own kind) -- an unknown slot name now genuinely 400s over HTTP naming the valid slots, for BOTH create and update (update validates the MERGED config, not the raw patch, so a patch that legitimately fixes an existing bad mapping is judged on its result). 4 new `OutputRoutesSpec` tests (create 400/201, update 400 + config-never-written verification).

**Cycle 5 (evaluation-4.md coordinator ruling on AC-3, all four pieces required):**
1. Boundary validation: `createStatic` + `applyStaticRefresh` (found live this cycle) validate every column type via `DataFieldType.validateAndCanonicalize`, 400 naming every bad value + all valid types, nothing persisted on failure. `inferAggregate`'s `groupBy` restructured to a specific validationError (not the generic "aggregate config error") naming the bad field/value/valid types.
2. `enum` added to every schema `type`/`dataType` property that is genuinely a `DataFieldType` (2 existing files + 2 brand-new schemas for `StaticColumnPayload`/`FieldOverridePayload`, which had no schema at all before).
3. Structural guard: `SchemaField`'s constructor now `require`s a canonical type -- the one choke point every construction site passes through. `SchemaFieldStructuralGuardSpec` proves it. This immediately found and required fixing: 9 pre-existing (predating this ticket) non-canonical test fixtures across 4 files, plus a 6th live producer (`applyStaticRefresh`) not named by the ruling. `schemaFieldJsonFormat` made tolerant on READ so already-persisted legacy data doesn't 500 (see #4).
4. Dev-DB check: found 12 of 141 `data_sources` rows in the shared local dev DB with an already-persisted `"number"` type (full list in execution-progress.md) -- recommending a spinoff ticket for the data fix; not fixed by this executor (data migration, not code, and no Linear tool access in this cycle's toolset). **Filed by the coordinator as HEL-932** (data fix for the 12 already-poisoned `data_sources` rows).

**Cycle 6 (evaluation-5.md CR1/CR2, closing AC-3):**
1. Added the coordinator's required fixture-based invariant guard, `SchemaFieldRealDumpInvariantSpec`: loads the real scrubbed `hel904-real-dump.sql` dump into a fully-migrated Postgres, reads a representative real pipeline's persisted source schema + `pipeline_steps`, runs them through the actual `analyzeNodes` production path (trunk + every tail), and asserts every field of every resulting node's projected schema satisfies `DataFieldType.fromString`.
2. Resolved the residual hole that test surfaced as a real question: `schemaFieldJsonFormat.read` previously still threw (500) for a persisted type that was neither canonical nor one of `canonicalizeLegacy`'s four known synonyms. Decision: widened the tolerant-read fallback to `StringType` with a loud warning log, rather than leave the throw -- a read-only deserialization path should not be able to 500 on stray future data. New regression coverage in `SchemaFieldJsonFormatTolerantReadSpec`.
3. **The mechanism this ticket shipped for AC-3 is: boundary validation (`DataFieldType.validateAndCanonicalize` at the two named producer boundaries) + `enum` constraints in `schemas/` + the `SchemaField` constructor structural guard** -- deliberately NOT a compiler-enforced typed `DataFieldType`/`SchemaField` refactor (every `SchemaField.type`/`DataField.dataType` remains a plain `String`, validated/guarded at runtime rather than made unrepresentable at compile time). **The compiler-enforced version is deferred to HEL-931** (typed-`DataFieldType`/`SchemaField` refactor follow-up), filed by the coordinator.
- [x] 3.7 (cycle 7, completed cycle 10) Implemented `POST /api/pipelines/:id/preview` -- `outputId` OPTIONAL (P1.4's `preview_outputs(pipelineId, outputId?)` MCP tool needed this genuinely optional, not narrowed to required; coordinator's explicit ruling on evaluation-8's flagged decision). Both arms return the SAME `PipelinePreviewResponse{outputs: [{outputId, preview}]}` envelope: `outputId` present narrows to one entry; absent fans out to every Output on the pipeline, computing each DISTINCT node's preview ONCE (not once per Output) and failing the whole call on the first node-level failure encountered. Never mutates run state in EITHER arm -- verified by dedicated tests for BOTH arms at the service layer (`PipelineRunServiceSpec`) and the HTTP layer (`OutputRoutesSpec`), each proving the assertion mechanism itself can detect a mutation via a real run on a separate pipeline. `schemas/pipelines/pipeline-preview-response.schema.json` added.
- [x] 3.8 (cycle 7) Implemented, with one interpretation decision made explicit: `ExpandPipelineShapeRequest` carries only `params` and no `pipelineId` at all (`pipeline-shapes` is a pipeline-AGNOSTIC template catalog, per its own class doc) -- there is no real, already-persisted step for a caller-supplied `parentStepId` to anchor into. Interpreted "accept a parentStepId target" as: the RESPONSE carries chaining metadata (`clientId`/`parentStepId` per entry, assigned `"step-0"`, `"step-1"`, ... in expansion order, each entry's `parentStepId` referencing the PRIOR entry's `clientId`) mirroring `CreatePipelineTransactionalStepRequest`'s own convention EXACTLY -- so a caller can feed `steps` straight into that create-transactional endpoint's `steps[]` without re-deriving the chain itself, rather than the request accepting an id it has nothing to anchor to. Response envelope changed from a bare `Vector[ShapeStepExpansionResponse]` array to `ExpandPipelineShapeResponse{steps, outputs}` (BREAKING). `outputs` is `Option[JsArray] = None` for every shape today -- `PipelineShape.expand`'s domain contract has no output-declaration concept yet, so this is forward-compatible wire shape, not a populated field (no live shape data to put in it -- adding a fabricated non-empty value would overclaim). Every existing `PipelineShapeRoutesSpec`/`ApiRoutesSpec` test asserting the old bare-array shape was updated to the new envelope, never left red or silently passing on the old assumption; 2 new tests (single-step + multi-step chaining). Schema-drift-clean (no `schemas/` entry existed for this response before or after -- not a new gap this task introduces).

  **Known consumer break, deliberately not fixed here (contracts-only scope, per design.md's Non-Goals "frontend consumption (P1.5/P1.6)")**: `frontend/src/features/pipelines/{ui/shapes/ShapePickerModal.tsx, types/pipelineShape.ts, services/pipelineService.ts, ui/shapes/ShapeParamsFields.tsx, services/pipelineService.test.ts}`, `e2e/hel399-shape-instantiate.spec.ts`, and `helio-mcp/src/{tools/write.ts, helioApi.ts, types.ts}` all still expect the OLD bare-array response and will break at runtime against this backend change until updated -- named here explicitly so it is not silently discovered later. None of these are backend/contract-layer files this ticket's UI Gate covers; flagging for whichever ticket picks up frontend/helio-mcp consumption of P1.3's routes.
- [x] 3.9 (cycle 7) Added `POST /api/pipelines/:id/validate-expression?stepId=`, delegating to `ExpressionEvaluator.validate` against the target node's projected schema field names -- extracted `capabilitiesAtNode`'s node-resolution logic into a shared `projectedSchemaAtNode` helper rather than a second schema-projection codepath. 5 tests: valid expression (stepId absent), unknown field naming it, node-scoped validation (a column dropped by a select step fails there but not at the source), unknown-stepId 404, and the full owner/grantee/other ACL triad.
- [ ] 3.10 -> HEL-933. Expose `DataSource.inferredSchema` on `DataSourceResponse` (`GET/POST /api/data-sources`, `GET /api/data-sources/:id`); verify a test asserting the list response includes `inferredSchema.fields`

## 4. Backend — cleanup & removal

- [x] 4.1 Confirmed (already true, no code change needed): `ApiRoutesSpec` now has a route test asserting `/api/types/*`, `/api/metrics/*` 404, and `/api/panels/bound` 405 (path resolves to `PanelIdSegment`'s bogus-id branch, not a route — proves the same "no such endpoint" fact AC 5 needs).
- [x] 4.2 Confirmed (already true): `ApiRoutesSpec` route test asserts `GET /api/panels/:id/query` 404.
- [x] 4.3 (cycle 6, CR6) Rewired `PublicDashboardRoutes` off the retired `findLastRunAtByOutputDataTypeId` onto `panel → output → pipeline.lastRunAt`: `resolveDataAsOf` matches on `OutputPanel` (the only panel kind with a direct output binding), looks up `outputRepo.findByIdInternal`/`pipelineRepo.findByIdInternal` (both unauthenticated-safe internal lookups; the route's own dashboard-level `authorizeResourceWithSharing` is the ACL gate), and degrades to `None` for every other panel kind or an unresolvable Output/pipeline. `outputRepoOpt`/`pipelineRepo` wired through from `ApiRoutes.scala`. New `PublicDashboardRoutesSpec` (3 tests): an Output-backed placement returns `dataAsOf` = the bound pipeline's real persisted `lastRunAt`; a non-Output panel kind still returns `None` (unchanged pre-existing behavior); an Output-backed placement whose pipeline has never run also returns `None`.
- [x] 4.4 (cycle 6) Verified (no code change needed): grepped `PipelineRepository`/`PipelineService`'s create/list path (`PipelineSummaryResponse`, `PipelineRepository.create`/`listSummaries`) for `outputDataTypeId`/`outputDataTypeName`/`output_data_type_id` — zero hits. `PipelineSummaryResponse` (the `GET /api/pipelines` wire shape) has never carried either field; the only remaining hits repo-wide are in the SEPARATE, P1.4-owned pipeline-PROPOSAL protocol (`PipelineProposalProtocol`/`PipelineAnalyzeProposalProtocol`/`WorkspaceContextProtocol`), which per this ticket's own design.md D7/D11 scope notes is explicitly out of this ticket's `schemas/patch-sets/**`/`pipeline-proposal.schema.json` boundary, not this task's target.
- [x] 4.5 Re-ran the sweep across the full cycle-1+2+3 diff (`git status --short`) each cycle. Cycle 3 also re-ran the SEPARATE, more targeted `"number"`/`"double"` repo-wide grep the evaluator specifically asked for (see 3.5) -- both sweeps clean, no live dead reference, no `@deprecated`.

## 5. Tests

- [x] 5.1 (expanded cycle 3) Route ACL tests now also cover: `GET /api/pipelines/:id/capabilities`, `GET /api/outputs/:id/panels`, `GET /api/outputs/:id/assertion-status` (owner/editor-grantee/unrelated-caller — 200/200/404 or /403 per each route's own ACL mechanism), the HEL-892 create/update 400s, and the retired-route 404/405 assertions. Still NOT covered: single-call `create_pipeline`, `parentStepId` step routes, preview, validate-expression, pipeline-shapes/expand envelope — deferred to a follow-up cycle (see execution-progress.md).
- [ ] 5.2 NOT implemented this cycle — single-call `create_pipeline` deferred (CR4)
- [x] 5.3 HEL-895/638 repro implemented and green: `PipelineCapabilitiesRoutesSpec` "a metric Output binds over a sum/avg aggregate" — asserts the sum-aggregate's own output column is present (not silently dropped by `DataFieldType.fromString` the way the pre-fix `"number"` emission would have been) and eligible for `metric`'s `value` slot. HEL-892's bad-slot-name 400 is now a real HTTP regression test too (see 3.6).
- [x] 5.4 `check-schema-drift.mjs` green; `DashboardProposalService.scala`/`proposal.ts` confirmed byte-for-byte untouched via `git diff origin/main` (zero-line diff) — re-verified after cycle-2 changes.
- [x] 5.5 `openspec validate` (340/340 passed), `check:openspec` (clean), `check:spec-structure` (338 specs, 0 issues) all green; no `@deprecated`, alias, or shim added
