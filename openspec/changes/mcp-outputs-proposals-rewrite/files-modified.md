## Cycle 1
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyRollback.scala` — HEL-766 fix (tasks.md 1.5): step rollback/recreate now threads `enabled`/`parentStepId` explicitly.
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetUndoServiceSpec.scala` — HEL-766 `parentStepId` regression test (task 5.5 partial).

## Cycle 2
- `backend/src/test/scala/com/helio/services/workspace/WorkspaceTeardownServiceSpec.scala` — task 5.8: new test proving a tagged pipeline's Outputs/placements cascade-delete on teardown (task 2.1 was already correctly implemented; only the test was missing).

## Cycle 3 — tasks 1.1 (pipeline-proposal half), 1.3 (pipeline-proposal service half), 3.10, plus one adjacent bug fix found along the way

Backend:
- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineProposalProtocol.scala` — `PipelineProposal` retargeted: `outputDataTypeName: String` removed outright; `steps: Vector[CreatePipelineTransactionalStepRequest]` (was `CreatePipelineStepRequest`, no clientId/parentStepId) and new optional `outputs: Vector[CreatePipelineTransactionalOutputRequest]`, reusing P1.3/HEL-906's single-call transactional shapes verbatim. New `ProposalOutputSummary` case class; `PipelineProposalApplyResponse.outputDataTypeId: String` replaced by `outputs: Vector[ProposalOutputSummary]` (a proposal can now create zero/one/many Outputs, not always exactly one).
- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineAnalyzeProposalProtocol.scala` — `PipelineAnalyzeProposalResponse.outputDataTypeName` dropped (was a pure echo of the request's now-removed field); `jsonFormat4` → `jsonFormat3`.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineProposalService.scala` — rewritten: `validateStructure`/`validateSteps`/`validateOutputs` updated for the new shapes (clientId uniqueness, parentStepId/nodeStepClientId resolution, `OutputKind.fromString`); `createPipeline` now delegates the whole pipeline+steps+outputs creation to `pipelineService.create`'s existing single-call transactional path (P1.3) instead of a manual `addSteps` loop + one hardcoded Output insert; `rollback`/`rollbackAll` simplified — deleting the pipeline row alone now cascades its steps AND every Output (and each Output's placements) via `ON DELETE CASCADE` (V23/V94), so no separate Output delete is issued anymore.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — (a) `analyzeProposal`/`validateStepKinds` updated for the new `CreatePipelineTransactionalStepRequest` steps type. (b) **Bug fix found while wiring the above**: `createTransactional` (P1.3/HEL-906's single-call `POST /api/pipelines` path) never carried the join/union/lookup cross-owner ACL pre-flight the older per-step `addStep` path has always had (HEL-278/HEL-384/HEL-386) — any caller of `POST /api/pipelines` with a non-empty `steps[]` could reference another user's DataSource as a join/union/lookup right-source with no ownership check. Added `validateStepCrossOwnerRefs` (new private method), run before the transaction starts, closing this gap for both the raw route and this ticket's proposal-apply path.
- `backend/src/main/scala/com/helio/services/proposals/CombinedProposalService.scala` — `$pipelineOutput` sentinel resolution reworked (new `resolveSentinelOutputId`/`applyDashboardPhase`) to handle a pipeline proposal creating zero, one, or many Outputs: no panel referencing the sentinel is a no-op; a panel referencing it requires exactly one Output to have been created, else 422 (with rollback) naming the real count — deferred, not silently guessed, addressing to a *specific* Output among several.
- `backend/src/main/scala/com/helio/api/protocols/assistant/AssistantProposalToolSchemas.scala` + `backend/src/main/scala/com/helio/services/assistant/AssistantSystemPrompt.scala` — the in-app top-level assistant's `propose_pipeline`/`propose_combined` tool JSON-schemas and system-prompt text updated to the new shape (these decode directly into the same `PipelineProposal` Scala type via `AssistantToolExecutor`, discovered this cycle to be a second, non-MCP consumer of the retargeted contract).
- 9 backend test files updated for the new shapes (see `git diff --stat` for the exhaustive list) — `PipelineProposalProtocolSpec`, `PipelineAnalyzeProposalRoutesSpec`, `PipelineApplyProposalRollbackSpec`, `PipelineApplyProposalSpec`, `CombinedApplyProposalSpec`, `AssistantServiceSpec`, `AssistantToolExecutorSpec`, `PipelineProposalServiceValidateSpec`, `PipelineServiceInlineRestBodySpec`, `CombinedProposalServiceValidateSpec`.

Schemas:
- `schemas/pipelines/pipeline-proposal.schema.json` — retargeted to match the new `PipelineProposal` case class (steps/outputs $ref the existing `create-pipeline-transactional-{step,output}-request.schema.json` files verbatim, no new schema files).
- `schemas/pipelines/pipeline-analyze-proposal-response.schema.json` — `outputDataTypeName` dropped from `required`/`properties`.

MCP (task 3.10 — proposal.ts/combinedProposal.ts paired unit, plus pipelineProposal.ts/pipelineProposalHandlers.ts which construct/consume the same retargeted `PipelineProposal` type and would otherwise be left broken mid-ticket):
- `helio-mcp/src/types.ts` — `PipelineProposal`/`PipelineAnalyzeProposalResponse`/`PipelineProposalApplyResponse` retargeted; new `PipelineProposalStep`/`PipelineProposalOutput`/`ProposalOutputSummary` interfaces mirroring the backend's transactional shapes.
- `helio-mcp/src/tools/pipelineProposal.ts` — new local `pipelineProposalStepSchema`/`pipelineProposalOutputSchema` zod schemas (NOT the shared `write.ts` `boundPipelineStepSchema`, deliberately — that one is still the old `{type,config}` shape until `create_pipeline`'s own rewrite, task 3.2, out of this cycle's scope); `pipelineProposalInputSchema` and all three tool bodies (`propose_pipeline`/`analyze_pipeline_proposal`/`apply_pipeline_proposal`) updated; tool descriptions updated.
- `helio-mcp/src/tools/pipelineProposalHandlers.ts` — handler input/construction updated for `steps`/`outputs`.
- `helio-mcp/src/tools/combinedProposal.ts` — `apply_combined_proposal`'s pipeline-field construction and doc text updated.
- `helio-mcp/src/tools/pipelineProposalHandlers.test.ts`, `helio-mcp/src/tools/combinedProposalHandlers.test.ts` — updated fixtures for the new shapes.

Not touched (deliberately, out of this cycle's scope): `helio-mcp/src/tools/write.ts` (`create_pipeline`/`create_pipeline_from_shape`/`create_bound_panel` — task 3.2/3.6, section 3's own MCP tool rewrite), `helio-mcp/src/context.ts`/`helio-mcp/src/helioApi.ts`'s `PipelineSummaryResponse`/`PipelineAnalyzeResponse`-shaped `outputDataTypeName`/`outputDataTypeId` fields (task 1.7/3.8, `get_workspace_context` slimming — a different DTO, not `PipelineProposal`), `backend/.../api/protocols/pipelines/PipelineAnalyzeProposalProtocol.scala`'s *request* shape reuse of `PipelineProposal` (already handled automatically since it takes the same type), frontend (task 4, its own section).

## Cycle 4 — task 1.2 (patch-set schema retarget onto node/output/placement)

- `schemas/patch-sets/patch-set.schema.json` — `EditTarget.kind` enum gains `"output"`; descriptions updated to name node=`pipelineStep`/placement=`panel`/output=`output` explicitly (per design.md/spec "Patch-set operations target nodes, outputs, and placements" — "node" and "placement" were already the pre-existing `pipelineStep`/`panel` kinds under their original names, only `output` itself was missing).
- `backend/src/main/scala/com/helio/api/protocols/patchsets/PatchSetProtocol.scala` — `Edit` gains `outputPatch: Option[UpdateOutputRequest] = None` (defaulted, placed LAST so every pre-existing positional `Edit(...)` test call site keeps compiling); `recognizedKinds` gains `"output"`; reader/writer dispatch `target.kind = "output"` to `UpdateOutputRequest`. No `create` op for `output` (mirrors `pipelineStep`'s own precedent — neither `CreateOutputRequest` nor `EditTarget` carries a not-yet-existing Output's parent-pipeline id).
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyTypes.scala` — `ResolvedAction.OutputUpdate`/`OutputDelete`; `PatchSetApplyContext`/`PatchSetApplyServices` gain nullable-optional `outputRepo`/`outputService` fields (mirrors this file's existing nullable-optional convention).
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyResolvers.scala` — `resolveOutputUpdate`/`resolveOutputDelete` (owner-only, mirrors `OutputService.update`/`delete`'s own check — `findById` is sharing-aware RLS but only the owner may mutate, 404 not 403 on mismatch); `("output", "create")` explicitly rejected with the same rationale as `pipelineStep`.
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyForward.scala` — forward-apply cases for `OutputUpdate`/`OutputDelete`, via `OutputService.update`/`delete`.
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyRollback.scala` — `OutputUpdate` rollback is a full-overwrite inverse (`UpdateOutputRequest(name = prior.name, config = priorConfig)`, mirrors `DataSourceUpdate`/`PipelineUpdate`'s existing pattern); `OutputDelete` rollback is `"unrecoverable"` (mirrors the `dashboard`/`dataSource`/`pipeline` delete precedent in the SAME file — an Output delete cascades to every placement Panel bound to it, so recreating both would duplicate a multi-step composition this ticket does not reimplement).
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyServiceJson.scala` — mixes in `OutputProtocol` (gives the four files above `outputResponseFormat`/`updateOutputRequestFormat`/`outputResponseFrom` without duplicating the trait).
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetApplyService.scala` — constructor gains nullable-optional `outputRepo`/`outputService` params, threaded into `PatchSetApplyContext`/`PatchSetApplyServices`.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — `patchSetApplyService` construction site wires the already-existing `outputRepoOpt`/`outputServiceOpt` (`.orNull`) through.
- `backend/src/test/scala/com/helio/api/protocols/patchsets/PatchSetProtocolSpec.scala` — new tests: output-update round-trip, output-update patch decodes into `outputPatch` only (no cross-kind bleed), output-create patch decodes untyped (rejection is a resolver-level concern, not this protocol layer's).
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetApplyServiceSpec.scala` — new `outputRepo`/`outputService` fixture wiring + `seedOutput` helper; 5 new integration tests: apply-update, non-owner-rejected, rollback-on-later-failure (restores original name), delete-rollback-unrecoverable (Output genuinely stays deleted), create-rejected.

**Deferred, not started this cycle (documented, not silently dropped):**
- `PatchSetPreviewService`/`PatchSetPreviewProjection` — an `output`-kind edit going through the READ-ONLY preview/diff path is not wired (would currently NPE on the still-null `outputRepo` `PatchSetPreviewService` constructs `PatchSetApplyContext` with, since that construction site wasn't touched this cycle). Preview is a separate feature (HEL-408) from apply; wiring it is mechanically the same shape as this cycle's apply-path work but is real, additional, untouched scope.
- `PatchSetUndoService`/`PatchSetUndoInverse`/`PatchSetUndoConflictCheck` — undo (restoring an already-committed application later, HEL-413) has no `output` case at all yet; an `output` edit in a journaled application would hit the generic `"no undo path for target.kind 'output'"` fallback (a safe, honest rejection — not a crash or silent wrong-behavior — but still incomplete coverage).
- **Separately discovered, real, NOT fixed this cycle**: `PatchSetUndoInverse.pipelineStepCreateRequestFromResponse` (the UNDO path's own step-recreate inverse builder, a DIFFERENT file from cycle 1's `PatchSetApplyRollback` fix) still cannot thread `parentStepId` through — not because of an oversight parallel to cycle 1's, but because `PipelineStepResponse` (the wire response trait/its 12+ subtypes) has NO `parentStepId` field on the wire AT ALL, so the undo path's journaled-JSON reconstruction structurally cannot recover it (unlike `PatchSetApplyRollback`, which captures a live `PipelineStep` domain object with `.parentStepId` directly, never touching the wire format). Fixing this needs a wire-shape change to `PipelineStepResponse` and every subtype's format, touching `check-schema-drift.mjs`'s panel-type-adjacent surfaces — sized as a follow-up, not absorbed into this cycle's schema-only task 1.2.

## Cycle 5 — tasks 1.4 (per-node Output fieldMapping grounding) and 1.6 (HEL-670 refinement targeting)

- `backend/src/main/scala/com/helio/domain/panels/OutputBindingSpec.scala` — new `validateFieldMappingColumnsExist(fieldMapping, schema)`: checks a fieldMapping's VALUES (column names) exist in a given projected schema. Distinct from the pre-existing `validateFieldMapping` (checks the mapping's KEYS/slot names against the kind's spec, never touching schema at all).
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — `createTransactional` computes `PipelineAnalyzeService.analyzeNodes` over the request's own `steps` before entering the DBIO chain; `buildOutputsAction`/`validateOutputFieldMapping` now thread the per-node projected schema (or the source's own `inferredSchema` for a source-attached Output, `nodeStepClientId` absent) through, grounding `fieldMapping` against the RIGHT node, not the trunk.
- `backend/src/test/scala/com/helio/services/pipelines/PipelineCreateTransactionalSpec.scala` — 3 new tests: Output-on-tail rejected when referencing a column the tail's own select step dropped; source-attached Output accepted with the SAME mapping (grounded against the source, not the narrower trunk step); source-attached Output rejected when referencing a column absent from the source itself.
- `backend/src/test/scala/com/helio/domain/panels/OutputBindingSpecSpec.scala` — 5 new unit tests for `validateFieldMappingColumnsExist`.
- `backend/src/main/scala/com/helio/services/patchsets/RefinementEditShape.scala` — **HEL-670 fix**: removed the dangerous "SEPARATE follow-up edit targeting this create's resulting panel id" guidance (a create edit's real id does not exist until the whole patch set is applied — `PatchSetApplyResolvers` resolves every edit BEFORE any forward-apply runs — so this instruction told the LLM to reference an id it could not possibly know, risking a fabricated/hallucinated id silently mistargeting an unrelated real resource). Also replaced the ENTIRE stale panel-kind worked-example set (`metric`/`chart`/`table`/`collection`/`timeline` — panel `type` values `PanelType.fromString` has not accepted since HEL-904; every one of these examples would have been REJECTED by the real backend, undetected because this file's own regression test never covered them) with output-panel (`config.outputId` only) and content-panel (`text`/`markdown`/`image`/`divider`) examples, plus a worked `output` create/update example. Also fixed the pipeline-create example text (`outputDataTypeName` doesn't exist on `CreatePipelineRequest` — a pre-existing prompt inaccuracy, not introduced by this ticket, fixed regardless since it's in the same file being touched).
- `backend/src/main/scala/com/helio/services/patchsets/RefinementPrompt.scala` — `Instructions` text retargeted: dead `dataTypeId`/metric-`aggregation`/chart-`aggregation` rules replaced with `config.outputId` + Output-fieldMapping-grounding rules; new explicit HEL-670 rule ("never emit a second edit in the SAME patch set whose target.id is meant to mean 'the thing the create edit above just made'").
- `backend/src/test/scala/com/helio/services/patchsets/RefinementEditShapeSpec.scala` — 4 new tests covering the retargeted output/content-panel examples' real decode; 2 new HEL-670 protocol-level tests (a create edit's target carries no id; a follow-up edit's target.id decodes verbatim, never aliased to the create edit beside it).
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetApplyServiceSpec.scala` — 1 new full-stack HEL-670 test: a create edit + a same-patch-set update-of-a-different-real-panel apply correctly against two independent targets (new panel keeps its own create-time title; the pre-existing panel is renamed by the update edit and ONLY it).

**Not touched this cycle**: `OutputService.create`/`update`'s own grounding (the standalone `add_output`/`update_output` write paths, not yet built as MCP tools) — deliberately deferred; that class's own docstring already documents a pre-existing, explicit design tradeoff ("re-resolving the node's projected schema on every write" is a real per-write cost) that this cycle did not have a mandate to overturn. The ticket's own stated AC ("Proposal grounding test: an Output on a tail validated against the tail's projected schema") is about the proposal/single-call-create flow, which IS now grounded. Flagged as a natural follow-up once `add_output`/`update_output` MCP tools land in section 3.

## Cycle 6 — section 3 output-tools slice (task 3.5 + 3.7's rename)

- `helio-mcp/src/tools/outputsHandlers.ts` (new) — zod-free call-routing logic for all 10 new Output tools, mirroring `pipelineProposalHandlers.ts`'s design.md D4b split (kept separate from `outputs.ts` so a test can exercise the logic without pulling zod/`registerTool` into the compile graph — the TS2589 risk this repo has hit before).
- `helio-mcp/src/tools/outputs.ts` (new) — thin shell: zod `inputSchema` + `guarded(() => xHandler(...))` one-liners for `add_output`, `update_output`, `delete_output`, `list_outputs`, `get_output`, `get_output_rows`, `get_output_panels`, `get_output_assertion_status`, `preview_outputs`, `get_output_capabilities`.
- `helio-mcp/src/tools/outputsHandlers.test.ts` (new) — 15 tests, one call-routing + result-passthrough test per handler, plus `HelioApiError` propagation coverage for `add_output`/`get_output_capabilities`.
- `helio-mcp/src/helioApi.ts` — 10 new methods (`createOutput`/`listOutputsByPipeline`/`listAllOutputs`/`getOutput`/`updateOutput`/`deleteOutput`/`listOutputPanels`/`getOutputAssertionStatus`/`getOutputRows`/`previewOutputs`/`getOutputCapabilities`), each a thin pass-through to the P1.3/HEL-906 Output routes. **Removed** `getPanelCapabilities` (task 3.7/3.9): it called `GET /api/types/:id/panel-capabilities`, a route HEL-904 deleted months ago — every call has 404'd since then, undetected because no test covered it. Replaced by `getOutputCapabilities`, which calls the route that actually exists (`GET /api/pipelines/:id/capabilities?stepId=`).
- `helio-mcp/src/tools/read.ts` — **removed** the `get_panel_capabilities` tool registration (same dead-route reason as above; no alias, matches decision 10's "removed tools have no aliases").
- `helio-mcp/src/types.ts` — new `OutputSchemaFieldResponse`/`OutputResponse`/`OutputsResponse`/`CreateOutputRequest`/`UpdateOutputRequest`/`OutputPanelPlacementResponse`/`DeleteOutputResponse`/`AssertionStatusResponse`/`OutputPreviewEntry`/`PipelinePreviewResponse`/`NodeCapabilitiesResponse` interfaces. Removed the now-fully-dead `PanelCapabilitiesResponse` interface (keyed by `dataTypeId`, described the deleted route); kept and re-docstringed `PanelCapabilityColumnResponse`/`PanelCapabilityResponse` (still genuinely reused by the new `NodeCapabilitiesResponse`).
- `helio-mcp/src/index.ts` — registers `registerOutputTools`.

**Not touched this cycle** (deliberately, next slice of section 3): `write.ts`'s `create_pipeline`/`create_pipeline_from_shape`/`create_bound_panel`/`bind_panel` (tasks 3.2/3.6, still the old `{type,config}` per-step shape + DataType/metric-era tools), `context.ts`'s `get_workspace_context` (task 3.8), the `list_data_types`/`update_data_type`/`delete_data_type`/`get_data_type_rows`/`list_metrics`/`get_metric`/`create_metric`/`update_metric`/`delete_metric` removals (task 3.9 — these live in `read.ts`/`write.ts`, untouched beyond the one `get_panel_capabilities` removal that was directly coupled to this slice), `metricSchemas.ts` deletion, same-tab invalidation re-keying (3.11), HEL-934 absorption (3.12).

## Cycle 6 (continued) — create_pipeline + add_pipeline_step rewrite (tasks 3.2/3.3)

- `helio-mcp/src/tools/pipelines.ts` (new) — thin shell for the rewritten `create_pipeline`: `source` (sourceId OR inline `{type: rest_api|sql|static, name?, config?}` — csv deliberately excluded, no bytes channel), `steps[]` (reusing `pipelineProposalStepSchema`, clientId/parentStepId), optional `outputs[]` (reusing `pipelineProposalOutputSchema`), optional `tag`.
- `helio-mcp/src/tools/pipelinesHandlers.ts` (new) — `createPipelineHandler`: resolves an inline source (issuing `POST /api/data-sources` or `POST /api/sources` first) or uses an existing `sourceId`; calls `POST /api/pipelines` with the single-call transactional shape; on failure AFTER creating an inline source, re-throws naming the orphaned source id (never silently discarded, per design.md decision 2); on success, when `outputs` was non-empty, follows up with `listOutputsByPipeline` to attach the created Outputs to the response (`POST /api/pipelines`'s own response never reports them).
- `helio-mcp/src/tools/pipelinesHandlers.test.ts` (new) — 7 tests: existing-sourceId path, inline-static-source path (verifies the two-call composition + which id gets used), orphaned-source-id-in-error on a second-call failure, no orphan-mention when the source was pre-existing, `csv` rejected with the `create_csv_data_source` pointer, outputs follow-up read attached when requested, follow-up read skipped when not.
- `helio-mcp/src/tools/pipelineProposal.ts` — exported `pipelineProposalStepSchema`/`pipelineProposalOutputSchema` (were module-private) so `pipelines.ts` can reuse them verbatim instead of a second, drift-prone zod copy.
- `helio-mcp/src/tools/assertSchemas.ts` — `addPipelineStepHandler` (task 3.3) gains `parentStepId`/`position`/`enabled`, passed through to `api.addPipelineStep`; its pre-existing `assert`-config validation logic is UNCHANGED (this is the file `add_pipeline_step`'s real logic has always lived in — extended here, never duplicated into a colliding same-named handler elsewhere, an early draft of this cycle's work caught and corrected that collision before it shipped).
- `helio-mcp/src/tools/write.ts` — `create_pipeline` tool registration REMOVED outright (no alias, moved to `tools/pipelines.ts`). `add_pipeline_step`'s `inputSchema`/call site/description gain `parentStepId`. `create_pipeline_from_shape` (task 3.4, NOT rewritten this cycle) has its `outputDataTypeName` parameter dropped from both schema and call site — kept minimally compiling only; it now creates a pipeline + expanded steps with ZERO Outputs (a real, documented, temporary behavior regression pending its own task-3.4 rewrite onto `add_outputs_from_shape`) rather than silently ignoring a parameter that no longer does anything.
- `helio-mcp/src/helioApi.ts` — `createPipeline` retargeted onto `{name, sourceDataSourceId, tag?, steps?, outputs?}` (drops `outputDataTypeName`, no alias); `addPipelineStep` gains `parentStepId`/`position`/`enabled`; `createPipelineFromShape`'s own input type and internal `createPipeline` call updated to compile against the above (same "kept minimally compiling, not rewritten" scope note as write.ts's tool).

Gates: `tsc -p tsconfig.typecheck.json` clean; `eslint --max-warnings=0` clean; `prettier --check`/`--write` clean; the verified scoped helio-mcp jest command — **16/16 suites (up from 15), 271/271 tests (up from 264)**, no OOM.

## Cycle 7 — add_outputs_from_shape (task 3.4, closes the zero-Outputs gap from cycle 6)

- `helio-mcp/src/tools/pipelinesHandlers.ts` — new `addOutputsFromShapeHandler(pipelineId, stepId?, shapeId, params, outputName, outputKind?)`: expands the shape via `expandPipelineShape` (fail-fast, nothing added on a 404/422), chains each expanded step onto the EXISTING pipeline via `parentStepId` (first step branches off `stepId`, absent means the raw source; each subsequent step branches off the one before it), then creates ONE Output on the terminal step (`kind` defaults to `"table"` when omitted — no shape API surfaces an intended Output kind, so this is a documented, easily-revisited default, not a data-loss-risk decision).
- `helio-mcp/src/tools/pipelines.ts` — new `add_outputs_from_shape` tool registration, replacing the retired `create_pipeline_from_shape`.
- `helio-mcp/src/tools/write.ts` — `create_pipeline_from_shape` tool registration **removed outright** (no alias).
- `helio-mcp/src/helioApi.ts` — `createPipelineFromShape` method and the now-dead `PipelineFromShapeResult` type alias **removed outright**; one stale docstring cross-reference in `createBoundPanel`'s comment fixed to point at the new handler.
- `helio-mcp/src/tools/pipelinesHandlers.test.ts` — 6 new tests: multi-step shape expansion + correct `parentStepId` chaining + Output anchored on the terminal step; branching off the raw source when `stepId` is omitted; `outputKind` defaulting to `table`; `outputKind` passed through when given; nothing added when `expand` fails; Output anchored directly on `stepId` when the shape expands to zero steps.

Gates: `tsc` clean; `eslint --max-warnings=0` clean; `prettier` clean; the verified scoped helio-mcp jest command — **16/16 suites, 277/277 tests (up from 271)**, no OOM.

## Cycle 7 (continued) — place_outputs + create_content_panel (task 3.6)

- `helio-mcp/src/tools/placements.ts` (new) — thin shell for `place_outputs`/`create_content_panel`.
- `helio-mcp/src/tools/placementsHandlers.ts` (new) — `placeOutputsHandler` (batch-creates `output`-kind placement panels via `POST /api/panels/batch`, then, only for items carrying a `w`/`h` size hint, follows up with `auto_layout_dashboard` to position exactly those newly-created panels — mirrors the pre-existing, already-documented create-then-auto-layout composition pattern) and `createContentPanelHandler` (single non-bound panel: text/markdown/image/divider).
- `helio-mcp/src/tools/placementsHandlers.test.ts` (new) — 8 tests: correct `placeOutputs` call shape, no auto-layout follow-up when nothing needs sizing, auto-layout follow-up scoped to ONLY sized items using their newly-created panel ids, missing-dimension default (4), error propagation without a stray auto-layout call, content-panel call-routing + error propagation.
- `helio-mcp/src/helioApi.ts` — **removed outright** `createPanel`, `createPanels`, `bindPanel`, `createBoundPanel` (no alias). Found a THIRD currently-broken tool while removing `createBoundPanel`: its backend route (`POST /api/panels/bound`) no longer exists at all (retired during the Outputs remodel) — every call has 404'd since then, undetected. `bindPanel`'s PATCH body (`config: {dataTypeId, fieldMapping}`) was also already meaningless against an output-kind panel's real config shape (`OutputPanelConfig` only ever carries `outputId`). Added `placeOutputs`/`createContentPanel`, reusing the existing `withCompleteChartAppearance` helper (still valid — `PanelAppearance.chart` remains a generic, kind-independent field on every panel, unrelated to the retired "chart" panel *type*).
- `helio-mcp/src/tools/write.ts` — `create_panel`/`create_panels`/`bind_panel`/`create_bound_panel` tool registrations **removed outright**. The now-orphaned `boundPipelineStepSchema` export removed too (its only real caller was `create_bound_panel`). `update_panel`'s docstring/`type` enum rewritten from the stale 9-kind (`metric`/`chart`/`table`/`text`/`markdown`/`image`/`collection`/`timeline`/`divider`) config-patch catalogue — describing backend dispatch behavior `PanelType.fromString` hasn't supported since HEL-904 — down to the real 5 kinds (`output`/`text`/`markdown`/`image`/`divider`) and their actual `Patch` fields (verified against each `*Panel.scala`'s own `Patch` case class, not re-derived). No functional/schema change was needed for `update_panel` itself — `config` was already a generic passthrough; only the docstring was stale.
- `helio-mcp/src/types.ts` — removed the now-dead `BoundPanelResponse` interface (described the deleted `/api/panels/bound` route).

Gates: `tsc` clean; `eslint --max-warnings=0` clean; `prettier` clean; the verified scoped helio-mcp jest command — **17/17 suites (up from 16), 284/284 tests (up from 277)**, no OOM.

## Cycle 8 — get_workspace_context rewrite (task 3.8) + 25-source/43-pipeline fixture (task 5.2)

Sole focus this cycle per the peer's directive, plus a full tasks.md ledger reconciliation (see below).

- `helio-mcp/src/context.ts` — retargeted onto design.md Decision 6: `WorkspaceContext.dataTypes`/`.metrics`/`.joinHints` REMOVED outright (no alias); `dataSources[]` gains `inferredSchema: [{name, type}]`; `pipelines[]` loses `outputDataTypeId`/`outputDataTypeName`, gains `outputs: WorkspaceContextOutputSummary[]` (id/name/kind/nodeStepId/schema/placements), grouped from ONE paginated `listAllOutputs` fetch (not a per-pipeline fan-out — keeps the call budget flat in pipeline count) via new `fetchAllOutputs`/`buildOutputSummariesByPipeline`/`fetchPlacements` helpers (each Output's placement lookup independently `.catch`-guarded, same "degrade the one section, never fail the whole call" precedent as `buildAgentContext`/`buildConnectors`). The entire sample-rows/column-stats/semantic-role/join-hints/tiered-budget-shed machinery (`sanitizeSampleRows`, `truncateCell`, `ColumnStats`, `asNumeric`/`rawNumericCandidate`, `computeColumnStats`/`computeColumnStatsForField`, `roundHalfAwayFromZero`/`roundToFourDecimals`, `classifySemanticRole`/`normalizedNameTokens`/`isTemporalName`/`isIdentifierName`, `SemanticRole`, `computeJoinHints`/`JoinCandidate`/`typeBucket`/`jaccard`/`joinHintConfidence`, `WorkspaceContextJoinHint`, the 3-tier `applyBudget` shed order and its `sampleRowsLenAt`/`exampleValuesLenAt`/`joinHintsLenAt`/`trimSampleRows`/`trimExampleValues`/`findLargestFittingCap` helpers) is DELETED OUTRIGHT, not kept dormant — confirmed unused anywhere else in helio-mcp (`grep` before deleting) and explicitly designed-out by decision 6 ("the new shape is expected to be well under cap without a separate truncation strategy"). `applyBudget`/`WorkspaceContextTruncation` simplified to measure-and-report only (`applied`/`budgetBytes`/`estimatedSizeBytes`/`structuralFloorExceedsBudget`/`paginationTruncatedResources` — no more `sampleRowsCap`/`exampleValuesCap`/`joinHintsKept`/`joinHintsOmittedByBudget`, nothing left to shed a tier of). File shrank 1301 → 531 lines. **Known parity gap, explicitly flagged, not silently left inconsistent**: the backend's `WorkspaceContextService`/`WorkspaceContextProtocol.scala` (a server-side structural-parity port of this same file, used by `GET /api/workspace/context`, unrelated to the MCP call path) is NOT retargeted this cycle — that's task 1.7, still open, deferred by the peer's own sequencing across multiple cycles now.
- `helio-mcp/src/types.ts` — added `InferredFieldResponse`/`InferredSchemaResponse`; `DataSourceResponse` gains `inferredSchema?: InferredSchemaResponse` (mirrors the backend's `DataSourceProtocol.scala`'s `inferredSchema: Option[InferredSchemaResponse]`, omitted on the wire when `None` — read as `inferredSchema?.fields ?? []`, same convention as `tag`).
- `helio-mcp/src/context.test.ts` — REPLACED wholesale (the old 2051-line file exercised almost entirely the now-deleted sample-rows/column-stats/semantic-role/join-hints/tiered-budget functions). New file (≈530 lines): shape assertions (no `dataTypes`/`metrics`/`joinHints`), `inferredSchema` mapping + `[]` default, pipeline `outputs[]` grouping/nodeStepId-normalization/placement-degrade/pagination-across-multiple-pages, `stepsError` degrade doesn't drop `outputs`, `agentContext`/`connectors` wiring carried forward, `applyBudget`'s simplified fast-path/structural-floor behavior, `paginationTruncatedResources`, and **the task-5.2 acceptance-criteria fixture test**: 25 sources × 10 fields + 43 pipelines × 1 Output × 10 schema fields, asserting `truncation.applied: false`, `structuralFloorExceedsBudget: false`, and `estimatedSizeBytes < DEFAULT_BUDGET_BYTES`. **Test count dropped from 284 (cycle 7) to 190 (this cycle) — a large, EXPECTED drop, not a regression**: the deleted sample-rows/column-stats/semantic-role/join-hints/budget-tier test suites (the largest fraction of the old file) tested functions that no longer exist per decision 6's explicit design; every other suite (16 of 17) is untouched and still green.
- `helio-mcp/src/tools/read.ts` — `get_workspace_context`'s tool description rewritten: no more "DataTypes (with columns)"/metrics-catalog text; now documents `inferredSchema`/pipeline `outputs[]` (kind/schema/placements)/the Source→Pipeline→Output→Dashboard path.
- `helio-mcp/README.md` — "Context serializer" section's example payload, call-budget formula (`4 + 2N(pipelines)`, was `4 + N(pipelines)`), and spray-json gotcha callout retargeted from `sourceId`/DataType to `nodeStepId`/Output; "pipeline-only binding rule (V41)" paragraph updated to the Source→Pipeline→Output→Dashboard path (HEL-904/HEL-907), since the V41 DataType-sourceId rule this text described no longer exists.
- `openspec/changes/mcp-outputs-proposals-rewrite/tasks.md` — full ledger reconciliation per the peer's addendum (real work across cycles 1-7 had gone unticked, "false-empty" the inverse of HEL-906's "false-green"): ticked every task with a specific commit sha for genuinely COMPLETE work; left PARTIAL work explicitly unticked with what remains named inline (never tick-and-qualify) — notably 1.1/1.3/3.10's dashboard-proposal half (only the pipeline-proposal half is done, per `9c4263cc`'s own commit message), 1.7 (backend `WorkspaceContextService` split/retarget, still fully open), 3.1/3.9 (partially decomposed/removed), 5.5/5.7/5.10/5.11 (partial or not re-verified this cycle).

**Not done this cycle, explicitly flagged** (ticket AC text: "update HEL-865 to say what remains"): HEL-865 is a Linear ticket; no Linear-write tool is available in this session's tool set (`Read`/`Edit`/`Write`/`Bash`/`SendMessage` only) to update it. Flagged for whoever picks this up next with Linear access — see final report.

Gates: `npx tsc --noEmit` clean; the verified scoped helio-mcp jest command — **17/17 suites, 190/190 tests** (down from 284 — see the large-EXPECTED-drop note above), no OOM. `eslint`/`prettier` run below before commit.

## Cycle 9 — tool-removal sweep (task 3.9) + exact-tool-name-set test (task 5.3)

- `helio-mcp/src/tools/read.ts` — removed `list_data_types`/`get_data_type_rows`/`list_metrics`/`get_metric` tool registrations outright (no alias); fixed stale "output DataType"/"panel-bindable DataType" docstring language in `list_pipelines`/`analyze_pipeline`.
- `helio-mcp/src/tools/write.ts` — removed `create_metric`/`update_metric`/`update_data_type`/`delete_data_type`/`delete_metric` tool registrations outright (no alias); fixed stale docstrings in `delete_data_source`/`delete_pipeline` (referenced the retired "output DataType"/`delete_data_type`, now describe Output cascade instead); removed the dangling `// ── Metrics ...` section header comment and a stale `mirroring update_metric` code comment.
- `helio-mcp/src/tools/metricSchemas.ts` — **deleted outright** (its only callers, `create_metric`/`update_metric`, are both removed).
- `helio-mcp/src/tools/updateSchemas.ts` — `dataFieldSchema`/`computedFieldSchema`/`buildUpdateDataTypeBody` removed (served only the now-removed `update_data_type`); `buildUpdatePipelineStepBody`/`buildUpdatePanelBody` (still-live tools) untouched.
- `helio-mcp/src/tools/updateSchemas.test.ts` — `describe("buildUpdateDataTypeBody", ...)` block + its import removed; header comment updated.
- `helio-mcp/src/tools/write.test.ts` — `describe("buildUpdateMetricBody", ...)` block + its `./metricSchemas.js` import removed (the file's `addPipelineStepHandler` coverage, unrelated to metrics, is untouched); header comment updated.
- `helio-mcp/src/helioApi.ts` — removed the now-dead `getDataTypeRows`/`listMetrics`/`getMetric`/`updateDataType`/`createMetric`/`updateMetric`/`deleteDataType`/`deleteMetric` methods and their now-unused type imports (`CreateMetricRequest`/`DataTypeRowsResponse`/`MetricResponse`/`UpdateDataTypeRequest`/`UpdateMetricRequest`). `listDataTypes` **deliberately kept** — still actively called by `tools/proposal.ts` for `propose_dashboard`'s DataType grounding (the still-open dashboard-proposal half of tasks 1.1/1.3), NOT dead code. Fixed a stale docstring cross-reference (`updateDataType`/`updateMetric` → `updatePipelineStep`/`updatePanel`) and `deletePipeline`'s docstring (now describes Output cascade, not the retired `delete_data_type`).
- `helio-mcp/src/types.ts` — removed the now-dead `DataTypeRowsResponse`/`UpdateDataTypeRequest` interfaces and the entire Metric CRUD section (`MetricFormat`/`MetricResponse`/`CreateMetricRequest`/`UpdateMetricRequest`), replaced with a one-line pointer comment naming what was removed and why. `DataTypeResponse` **deliberately kept** (`listDataTypes`'s return type, still live per above).
- `helio-mcp/src/index.ts` — **split**: tool/resource registration (`createServer`) moved to new `helio-mcp/src/server.ts`; `index.ts` now just wires config/transport/the `import.meta.url` direct-invocation guard and re-exports `createServer`. Root cause: `index.ts`'s top-level `import.meta.url` check makes it uncompilable by ts-jest's CJS-ish target (`TS1343`) when imported from a test — the exact same reason every other test in this codebase imports a narrow module rather than `write.ts`/`index.ts` directly. `createServer`'s `workspace-context` resource description also updated (dropped stale "DataTypes (with columns)" text, now documents `inferredSchema`/Output-shaped pipelines).
- `helio-mcp/src/server.ts` (new) — `createServer` + `WORKSPACE_CONTEXT_URI`, extracted verbatim from `index.ts` (behavior-preserving move, not a rewrite) plus the resource-description fix above.
- `helio-mcp/src/server.test.ts` (new, task 5.3) — the exact-tool-name-set test: an in-process MCP `Client`/`McpServer` pair over the SDK's own `InMemoryTransport` (mirrors `scripts/verify.ts`'s "connect a real MCP client" shape, just without stdio), asserting (a) all 15 tools removed across this whole ticket (this cycle's 9 + cycles 6-7's `bind_panel`/`create_bound_panel`/`get_panel_capabilities`/`create_panel`/`create_panels`/`create_pipeline_from_shape`) are genuinely absent, (b) the Output/pipeline/placement tools that replaced them ARE present, (c) no tool name is registered twice.
- `helio-mcp/README.md` — removed the tool-catalog table rows for `list_data_types`/`get_data_type_rows`/`get_panel_capabilities`/`create_panel`/`bind_panel`/`create_pipeline_from_shape`/`update_data_type` (all retired); `get_workspace_context`'s row updated for the new shape; added an explicit flagged-gap note that the newer Output/pipeline/placement tool families are still undocumented in this table (pre-existing gap from cycles 6-7, not fixed here — out of 3.9's stated scope, noted rather than silently left).
- `openspec/changes/mcp-outputs-proposals-rewrite/tasks.md` — 3.9/5.3 ticked.

**Notable finding surfaced while verifying `listDataTypes`'s liveness** (not fixed this cycle — belongs to the still-open dashboard-proposal half of tasks 1.1/1.3, flagged for the next cycle's sizing): `backend/src/main/scala/com/helio/api/ApiRoutes.scala` confirms `DataTypeRoutes` was deleted OUTRIGHT by HEL-904 ("`DataTypeService`/`DataTypeRoutes` deleted outright — DataTypes no longer exist"). This means `GET /api/types` itself no longer exists as a route — `HelioApi.listDataTypes()`, still called by `tools/proposal.ts` for `propose_dashboard`'s grounding, is ALSO calling a dead route right now. This is a fifth pre-existing-breakage finding in the same family as `get_panel_capabilities`/`create_bound_panel`/`bind_panel` (all found in cycles 6-7): every `propose_dashboard` call is very likely 404ing today, not just "not yet retargeted onto the new model" — it is actively broken. Confirms the dashboard-proposal half of 1.1/1.3 is the highest-priority remaining item, not merely next-in-line.

Gates: `npx tsc --noEmit` clean; `npx eslint . --max-warnings=0` clean; `npx prettier --check .` clean (after one `--write` pass); `npm run build` (tsc project build) clean; the verified scoped helio-mcp jest command — **18/18 suites (up from 17), 178/178 tests** (down from 190 at the start of this cycle — expected: removed `buildUpdateDataTypeBody`/`buildUpdateMetricBody` test coverage for the removed tools, added `server.test.ts`'s 3 new tests), no OOM.

## Cycle 10 — dashboard-proposal.schema.json/DashboardProposalService correction + the live propose_dashboard fix (tasks 1.1/1.3/3.10 dashboard half)

**Investigation finding, corrects cycle 8's own ledger conclusion**: re-read `schemas/dashboards/dashboard-proposal.schema.json` and `backend/src/main/scala/com/helio/services/proposals/{DashboardProposalService,ProposalPanelSupport}.scala` directly this cycle rather than trusting `git log main..HEAD`'s empty result for these paths. Both were ALREADY fully retargeted onto Outputs — by HEL-904 (task 3.8/3.9/4.1/3.10), already merged to `main` before this branch (`mcp-outputs-proposals-rewrite`) was ever created. `git log main..HEAD -- <path>` finding zero commits meant "nothing needed to change on this branch", not "not done" — cycle 8 conflated the two. Backend schema/service work for tasks 1.1/1.3 needed ZERO changes this cycle.

The REAL gap, found by tracing what actually still called `api.listDataTypes()`: only `helio-mcp/src/tools/proposal.ts`'s `propose_dashboard` tool (`combinedProposal.ts` was already clean — no DataType dependency found there). Confirmed via `ApiRoutes.scala`'s own comment ("`DataTypeService`/`DataTypeRoutes` deleted outright — DataTypes no longer exist") that `GET /api/types` has had NO backend route since HEL-904 — every `propose_dashboard` call has been either 404ing outright or silently degrading its binding-warning check ever since. This is the live bug cycle 9 surfaced and cycle 10 was assigned to fix.

- `helio-mcp/src/tools/proposalValidation.ts` — `computeProposalWarnings` retargeted from `Map<string, DataTypeResponse>` to `Map<string, OutputResponse>`; the "source companion, not a pipeline output" warning case REMOVED outright (no such concept exists for an Output — every Output IS the bindable projection by construction, mirroring the backend's `ProposalPanelSupport.validateDataTypeBinding`, which only checks `OutputRepository.findByIdOwned` existence, nothing else). Warning message updated to name "output" not "dataType".
- `helio-mcp/src/tools/proposal.ts` — `propose_dashboard`'s grounding fetch replaced `api.listDataTypes()` (dead route) with a new local `fetchAllOutputs` helper (paginated fetch across every page of `api.listAllOutputs`, mirrors `context.ts`'s helper of the same name — duplicated locally per this codebase's established small-file-local-concern convention, not cross-imported). Both tool descriptions (`propose_dashboard`/`apply_proposal`) rewritten to drop every reference to the retired `metric`/`chart`/`table`/`collection`/`timeline` panel kinds and their per-type config surfaces (all already deleted by HEL-904; the descriptions had never been updated to say so) — now correctly describe `output`/`text`/`markdown`/`image` only, and correct the stale "text/markdown panel's config.dataTypeId IS a real binding" claim (that Source-mode path was removed outright by HEL-904 task 4.1 — a text/markdown panel's `dataTypeId` is now always inert). `panelSchema`'s `aggregation` field simplified from a typed `MetricAggregationSpec | ChartAggregationSpec` union to a generic passthrough record (the field is legacy/never-applied per the schema's own field description — the typed union added no real validation value and referenced concepts that no longer exist).
- `helio-mcp/src/tools/proposal.test.ts` — rewritten: `Output` fixtures instead of `DataType` fixtures; the "source companion" test case removed (no longer a real scenario); all other cases (missing dataTypeId, not-found, valid, non-data-panel-type skip, multi-panel) retargeted and kept.
- `openspec/changes/mcp-outputs-proposals-rewrite/tasks.md` — 1.1/1.3/3.10 ticked (corrected from cycle 8's "PARTIAL, dashboard half remains open" to "[x], dashboard half was already done via HEL-904 -- verified this cycle").

Gates: `npx tsc --noEmit` clean; `npx eslint . --max-warnings=0` clean; `npx prettier --check .` clean (after one `--write` pass); `node scripts/check-schema-drift.mjs` green (re-verified explicitly, though no schema file was touched this cycle — Decision 4 hard-constraint discipline); the verified scoped helio-mcp jest command — **18/18 suites, 177/177 tests** (down 1 from 178 — the removed source-companion test case, no new tests added since this was a retarget not new-surface work), no OOM.

## Cycle 11 — WorkspaceContextService split (task 1.7)

Pure file-size decomposition of the 923-line `WorkspaceContextService.scala` (over `check-scala-quality.mjs`'s 250-line soft budget) — NOT a retarget onto Outputs (that's a separate, larger concern already tracked as a structural-parity gap against the MCP-side `context.ts`, out of this task's stated scope: "Split `WorkspaceContextService` by resource").

- `backend/src/main/scala/com/helio/services/workspace/WorkspaceContextComputations.scala` (new) — a trait holding every PURE computation method + its own constants, extracted VERBATIM (mechanical cut/paste, zero body changes) from `WorkspaceContextService.scala`: `contentFieldNames`, `overflowStructuredFieldNames`, `fieldCategory`, `TemporalNameTokens`/`IdentifierNameTokens`, `normalizedNameTokens`, `isTemporalName`, `isIdentifierName`, `classifySemanticRole`, `DimensionCardinalityThreshold`, `sanitizeSampleRows`, `truncateCell`, `SampleRowLimit`/`SampleColumnLimit`/`SampleCellCharLimit`/`TruncationMarker`, `ColumnFold`, `computeColumnStats`, `computeColumnStatsForField`, `DistinctCountCap`/`ExampleValueLimit`/`MeanRoundingScale`, `roundToFourDecimals`, **`asNumeric`** (the HEL-373 caution-zone method — its single-exit-point finiteness filter and `BigDecimal.setScale` rounding technique are UNCHANGED, verified both by direct diff-by-eye during the cut/paste and by the identical-test-set A/B run below), `JoinCandidate`, `typeBucket`, `jaccard`, `joinHintConfidence`, `computeJoinHints`, `MaxColumnsPerNameBucket`/`MaxJoinHints`/`MinDistinctForFullConfidence`. Three members (`contentFieldNames`, `overflowStructuredFieldNames`, `SampleColumnLimit`) bumped from `private` to `protected` — the ONLY modifier change anywhere in this cycle's diff — because plain Scala `private` is invisible to a subclass even via inheritance (unlike Java); everything else keeps its exact prior visibility (`private[services]` members stay `private[services]`, still directly callable from test specs exactly as before, since that visibility spans the whole `com.helio.services` package regardless of which file a member physically lives in). `WorkspaceContextService extends WorkspaceContextComputations` — every prior `service.methodName(...)` call site (including all 8 existing test specs) keeps compiling and behaving identically via ordinary trait-mixin inheritance; no call site anywhere in the codebase needed to change.
- `backend/src/main/scala/com/helio/services/workspace/WorkspaceContextService.scala` — shrunk from 923 to 427 lines: the constructor, `assemble`, `buildAgentContext`, `buildConnectors`, `rankMemoryEntries`, `buildPipeline`, `toStepEntry`, `toPipelineEntry`, `toDataSourceEntry`, `toDataTypeEntry`, `toDashboardEntry`, `panelCountFor`, `distinctPanelCount`, plus `StatsRowLimit`/`AgentMemoryTopN` (the two constants NOT shared with the computation trait) remain here unchanged; gained one line, `extends WorkspaceContextComputations`.

**Verification (per the peer's explicit instruction: baseline first, diff after, treat any numeric difference as stop-and-reconsider)**:
1. `git stash`-isolated the pre-split file, ran the 8 existing `WorkspaceContextService*Spec` suites (136 tests) as the baseline — all 136 passed.
2. Restored the split (`git stash pop`), re-ran the identical 8 suites — all 136 passed AGAIN.
3. Diffed the full sorted test-name listing from both runs (not just pass counts) — byte-identical, confirming no test was silently skipped/renamed/added that could mask a behavioral difference.
4. Ran the FULL backend suite (`sbt -batch 'set Test/parallelExecution := false' test`) post-split as final confirmation — 3517/3517 tests, 235 suites, 0 failures.

Gates: `sbt compile` clean (two pre-existing-style "outer reference" warnings on the two case classes moved into the trait — the SAME warning class already present elsewhere in this codebase, e.g. `DashboardAuthoringService.scala`'s own private nested case classes; not new/introduced risk). `node scripts/check-scala-quality.mjs` — both files still flagged over the 250-line SOFT budget (427/536 lines) but this is a non-blocking warning (`Scala code-quality check: clean (N soft warning(s))`), a real improvement from 923, and a defensible stopping point per the peer's own "stop at a safe boundary rather than force it" allowance.

## Cycle 12 — docs/agent-native.md rewrite (task 2.2) + task 1.8/section 4 scope investigation

- `docs/agent-native.md` — full rewrite, not just the requested tool-rename table bolted on. Fixed EVERY section still describing the retired DataType model: intro's canonical-path summary, "The canonical path" section (ASCII diagram + binding-rule wire note retargeted from DataType/sourceId onto Output/nodeStepId), the "Endpoint → tool map" table (removed rows for retired tools, added the Output/pipeline/placement tool rows), a new "Tool renames (HEL-903/904/906/907)" section (the peer's explicit ask — a full before/after table for every tool renamed/removed across cycles 1-11, matching design.md decision 10's "no aliases"), and the "End-to-end proof" narrative (marked as describing the ORIGINAL pre-remodel composition, not re-verified against a live backend since — an honest "not currently verified" flag rather than presenting stale claims as current fact).
- **New finding, flagged in the doc rather than fixed (separate, unscoped work)**: `scripts/agent/*.sh` (`create-panel.sh`/`bind-panel.sh`/`compose-demo.sh`/`workspace.sh`) and `helio-mcp/scripts/compose.ts` are ALL still calling retired DataType-era endpoints/tool shapes (`GET /api/types`, `create_panel`/`bind_panel`) — confirmed by direct inspection, not assumed. Marked STALE inline in both places the doc references them.

**Investigation only, no code changed (task 1.8/section 4)**: traced `fetchDataTypes()` (`frontend/src/features/dataTypes/services/dataTypeService.ts`) to `GET /api/types` — the same dead route HEL-904 deleted outright (cycles 9/10's finding pattern, now confirmed frontend-side too). A repo-wide grep for `fetchDataTypes`/`/api/types` usage found 20 frontend files across `dataTypes`, `panels` (editors + `PanelCreationModal`), `pipelines` (`PipelineDetailPage`), `metrics`, `sources`, and `dashboards` (`ProposalReview.tsx`/`ProposalReviewPage.tsx`) — meaning task 1.8's "close the two HEL-829 loose ends" framing significantly undersells the real scope: this is a full frontend migration onto Outputs, not a two-item polish pass on an otherwise-current review page. Deliberately NOT attempted this cycle (too large to land safely alongside 2.2); the exact file list and root cause are recorded in `tasks.md`'s 1.8 entry for whoever picks this up next.

Gates: `npx prettier --check docs/agent-native.md` clean (after one `--write` pass). No code file touched this cycle (`docs/agent-native.md` is prose-only) — no jest/tsc/eslint/sbt gate applicable to this cycle's actual diff, per the workflow's own "run the gates whose `when` matches" rule.

## Cycle 13 — review pages retargeted onto Outputs (tasks 1.8/4.1, narrowed scope per HEL-936)

Peer rescoped task 1.8 down to exactly section 4 (tasks 4.1/4.2) after filing HEL-936 for the broader ~18-file frontend migration cycle 12 found (TypeRegistryPage/TypeDetailPage/dataTypesSlice, panel editors, PipelineDetailPage, metrics/sources pages — pre-existing HEL-904 breakage, not new scope this ticket created). This cycle lands that narrow slice: `ProposalReviewPage` and the sibling patch-set/pipeline-proposal/combined review pages.

**`ProposalReview`/`ProposalReviewPage` (the dashboard-proposal review page)**:
- `frontend/src/features/dashboards/services/outputsService.ts` (new) — `fetchOutputs()`, a minimal paginated `GET /api/outputs` fetch (mirrors `helio-mcp/src/context.ts`'s own `fetchAllOutputs` helper) returning just `{id, name}` per Output. Deliberately narrow — not a full Outputs feature/slice (that belongs to HEL-936).
- `frontend/src/features/dashboards/ui/ProposalReview.tsx` — `ReviewDataType{name, sourceId}` → `ReviewOutput{name}`; `dataTypesById` prop → `outputsById`; the "source companion" binding-issue case removed outright (no such concept for an Output); "Data type" label → "Output"; warning text updated.
- `frontend/src/features/dashboards/ui/ProposalReviewPage.tsx` — `fetchDataTypes()` (calling the dead `GET /api/types`) replaced with `fetchOutputs()`; `synthesizeDemoProposal` rewritten to build a single `"output"`-kind panel (was three panels using the retired `metric`/`chart`/`table` kinds with `fieldMapping`, which is not meaningful for an output panel).
- Tests: `ProposalReview.test.tsx` (source-companion case removed, replaced with a not-found case), `ProposalReviewPage.test.tsx`/`ProposalReviewPage.demoFixture.test.tsx` (mock retargeted from `fetchDataTypes` to `fetchOutputs`), new `outputsService.test.ts` (3 tests: single page, multi-page pagination, empty).

**Pipeline-proposal and combined review pages (found necessary once inside — the ticket text itself names "pipeline-proposal, combined" review pages under task 4.1, and their contract was equally stale)**:
- `frontend/src/features/pipelines/types/pipelineProposal.ts` — `PipelineProposalStep` gains `clientId`/`parentStepId` (mirrors the backend's `CreatePipelineTransactionalStepRequest`); new `PipelineProposalOutput`/`ProposalOutputSummary` interfaces (mirror `CreatePipelineTransactionalOutputRequest`/`ProposalOutputSummary`); `PipelineProposal.outputDataTypeName: string` removed outright, replaced with optional `outputs?: PipelineProposalOutput[]`; `PipelineProposalApplyResponse.outputDataTypeId: string` replaced with `outputs: ProposalOutputSummary[]`.
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalSummary.tsx` — the single "Data type: {outputDataTypeName}" row replaced with a real Output list (`OutputRow`, mirroring the existing `StepRow` pattern) showing each proposed Output's kind/name/attachment point.
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReviewPage.tsx` — `demoPipelineProposal()`'s `outputDataTypeName` field replaced with a real `outputs: [{kind, name}]`; stale "DataType-derived fixture" comment cross-references fixed.
- `frontend/src/features/proposals/ui/CombinedProposalReview.tsx` — `boundDataTypeLabel` → `boundOutputLabel`; "Data type" label → "Output"; stale docstring fixed.
- `frontend/src/features/proposals/ui/CombinedProposalReviewPage.tsx` — `demoCombinedProposal()`'s pipeline half gains real `outputs[]` (was `outputDataTypeName`); dashboard half's demo panel retargeted from the retired `"metric"` kind + `fieldMapping` to `"output"` (no `fieldMapping` — not meaningful for an output panel).
- Tests retargeted for the new shapes: `PipelineProposalReview.test.tsx`, `PipelineProposalReviewPage.test.tsx`, `CombinedProposalReview.test.tsx`, `CombinedProposalReviewPage.test.tsx`, `combinedProposalsSlice.test.ts`, `pipelinesSlice.test.ts` (one `PipelineProposal`-shaped fixture only — its many OTHER `outputDataTypeName`/`outputDataTypeId` occurrences are `PipelineSummary`'s own unrelated field, untouched, HEL-936 scope), `unresolvedConnectorRefs.test.ts`, `ProposalHandoff.test.tsx` (also had a stray `"metric"`-kind demo panel in its combined-proposal fixture, fixed).
- `frontend/src/features/patchSets/ui/PatchSetReview(Page).tsx` — verified, needed NO changes (already Output-based from earlier cycles' patch-set retarget work).

Gates: `npx tsc --noEmit` (frontend) clean; `npx eslint . --max-warnings=0` (frontend) clean; `npx prettier --check` clean on every touched file; full frontend jest suite — **275/275 suites, 2969/2969 tests** (up from 2966 at cycle start — the 3 new `outputsService.test.ts` tests), no failures.

Not touched this cycle (deliberately, HEL-936 scope or a different task): `frontend/src/features/dataTypes/**`, `frontend/src/features/metrics/**`, `frontend/src/features/panels/ui/editors/**`, `PipelineDetailPage.tsx`, `frontend/src/features/sources/**`, `pipelinesSlice.ts`'s `createPipeline` thunk / `outputDataTypeId → name` map (lines ~592-627, a DIFFERENT concern from the proposal-apply thunk touched here), task 4.2 (`replace_dashboard_contents`/`auto_layout_dashboard` placements).

## Cycle 14 — task 4.2 doc fixes, HEL-934 helio-mcp share (3.12, a real bug fix), 3.1/3.13 verification closures

- `helio-mcp/src/tools/write.ts` — `replace_dashboard_contents`'s docstring: "V41 pipeline-only binding" → "Output binding"; "delete_panel-per-panel followed by create_panel-per-panel" → "...place_outputs-per-panel" (create_panel no longer exists). `auto_layout_dashboard`'s docstring: "Create + bind panels first" → "Create/place panels first (create_pipeline + place_outputs / create_content_panel)". Neither tool's actual behavior/schema needed a change — `replace_dashboard_contents` already reuses `panelSchema`/`ProposalPanel` (retargeted cycle 10); `auto_layout_dashboard` is pure `{panelId,w,h}` geometry with no binding concept at all. `delete_pipeline_step`'s docstring also updated to mention `removedTailStepCount` (see below).
- **`helio-mcp/src/helioApi.ts` — a real, live bug fix (task 3.12/HEL-934)**: `expandPipelineShape` was typed AND parsed as if `POST /api/pipeline-shapes/:id/expand`'s real response were a bare `ShapeStepExpansionResponse[]` array. It is not — the backend's `ExpandPipelineShapeResponse` (`PipelineShapeProtocol.scala`) wraps it as `{steps: [...], outputs: Option[JsArray] = None}`. Every real call would have handed `addOutputsFromShapeHandler` (`pipelinesHandlers.ts`, `add_outputs_from_shape`) an object instead of an array; its `for (const expansion of expansions)` loop would throw `TypeError: expansions is not iterable` at runtime the first time a shape expanded to any steps — i.e. on every real call except a zero-step shape. Fixed by having `expandPipelineShape` unwrap `response.steps` internally; its external contract (`Promise<ShapeStepExpansionResponse[]>`) is unchanged, so `addOutputsFromShapeHandler` needed no edit. Also fixed `deletePipelineStep`, which discarded `DELETE /api/pipeline-steps/:id`'s real `200 {removedTailStepCount}` splice-on-delete body entirely (`await this.http.delete(...)`, never reading the response) — now surfaced as a third field on the method's return value.
- `helio-mcp/src/types.ts` — new `ExpandPipelineShapeResponse` interface (`{steps, outputs?}`) mirroring the backend's real wire envelope.
- `helio-mcp/src/helioApi.test.ts` — 4 new tests exercising the REAL HTTP-layer parsing via `HelioHttpClient` + injected `fetchImpl` (not a mocked `HelioApi`, which is exactly what `pipelinesHandlers.test.ts`'s existing coverage used and which never touched this bug at all — a textbook "evidence-shaped non-evidence" gap): `expandPipelineShape` correctly unwraps a real `{steps, outputs: null}` envelope; returns `[]` for a zero-step shape without throwing; `deletePipelineStep` surfaces a real `removedTailStepCount` (both non-zero and zero cases).
- Investigated (no code change needed) tasks **3.1** (re-verified OOM-free import across the now-decomposed module set: 18/18 suites, 181/181 tests) and **3.13** (repo-wide grep audit for un-normalized `Option`-field reads across every helio-mcp file this ticket touched — zero violations found; the `?? null` discipline held throughout).
- Investigated, deferred to HEL-936 (not this ticket's scope): **3.11** — `markDataTypeRowsStale`'s dispatch site is `PipelineDetailPage.tsx`, a file HEL-936 already carved out in cycle 12/13; re-keying this one call site in isolation without fixing the rest of that broken page would not be a coherent slice.
- Verified (no code change needed), closing as formally re-confirmed rather than left as prior-cycle claims: **5.6** (typecheck + scoped jest gates), **5.7** (`check-schema-drift.mjs`, green at every commit since cycle 3/4, re-verified via this cycle's own commit hook), **5.11** (`RefinementEditShapeSpec.scala`'s explicit `"HEL-670: create-edit-then-follow-up-edit shape (protocol layer)"` describe block, confirmed present and passing).

**For the peer to post on HEL-934 (no Linear write access this session)**: "helio-mcp's share of this ticket is closed as of HEL-907 cycle 14 (commit <fill in sha after commit>). `expandPipelineShape` (helio-mcp/src/helioApi.ts) was unwrapping the `{steps, outputs?}` shape-expand envelope incorrectly — every real call would have thrown a runtime TypeError the first time a shape expanded to any steps. Fixed, with new HTTP-layer test coverage (helioApi.test.ts) that exercises the real response shape, not a mocked API surface. `deletePipelineStep` now also surfaces the real `removedTailStepCount` from `DELETE /api/pipeline-steps/:id`'s 200 body instead of discarding it."

Gates: `npx tsc --noEmit` (helio-mcp) clean; `npx eslint . --max-warnings=0` (helio-mcp) clean; `npx prettier --check .` (helio-mcp) clean; the verified scoped helio-mcp jest command — **18/18 suites, 181/181 tests** (up from 177 — the 4 new `helioApi.test.ts` tests), no OOM. No frontend/backend file touched this cycle.

## Cycle 15 — MCP E2E Sleeper-rebuild script (task 5.1, partial) + ledger re-audit

- `helio-mcp/e2e/sleeper-rebuild.ts` (new) — a real, complete, typechecked E2E script: connects a real MCP client over stdio (mirrors `scripts/compose.ts`/`scripts/verify.ts`'s existing pattern), tears down any prior tagged run, then rebuilds four representative Sleeper-shaped dashboards (rosters/matchups/standings/transactions) each via ONE `create_pipeline` call (inline static source + `outputs[]`), `run_pipeline`, a daily cron schedule (`set_pipeline_schedule` then `get_pipeline_schedule` to prove persistence), and `place_outputs`. Every created resource is tagged (`E2E_TAG`) for `teardown_resources`-based cleanup/re-run. The script's own header docstring is explicit that it does NOT re-fetch the real Sleeper API (no external network dependency assumed) — it proves the actual thing task 5.1's text asks for (the single-call `create_pipeline` + `place_outputs` composition, from a clean workspace, with a daily schedule read back), driven by representative data shaped like the real domain rather than a live pull.
- `helio-mcp/tsconfig.typecheck.json` — `e2e/**/*.ts` added to `include` (was `src/**/*.ts`/`scripts/**/*.ts` only), so this script stays covered by the project's own `check:helio-mcp-types` gate going forward instead of silently bit-rotting outside it.

**NOT executed against a live backend this cycle** — the only reachable backend (`localhost:8080`) is a pre-existing process running `main`'s code against the shared dev Postgres DB (confirmed via `ps aux`: `cd /home/matt/Development/helio/backend && sbt run`, i.e. the MAIN checkout, not this worktree), not a build of this worktree's own branch. Running a write-heavy E2E composition against a shared DB via an ambiguously-owned already-running server carries real collision/attribution risk (this project's own memory notes document a prior real incident: a stale migration from one parallel worktree poisoned `flyway_schema_history` and broke a *different* ticket's dev-server gate). A tag-based `teardown_resources` cleanup mitigates but doesn't eliminate this. Per the peer's explicit "land what you can, continue next cycle rather than forcing it" allowance, stopped here with a real, typechecked, ready-to-run script rather than either skip it entirely or run it recklessly against shared infrastructure.

**Ledger re-audit (per the peer's addendum)**: re-verified all 9 `@ <sha>` citations in `tasks.md` resolve to real commits actually delivering the claimed work (`git log --oneline -1 <sha>` for each, cross-checked against the commit's own subject line). Found and fixed one real defect from an earlier cycle's find-and-replace: task 4.1's line had a corrupted, contradictory trailing fragment ("...replacing `outputDataTypeName`). — NOT started this branch") left over from an incomplete string replacement in cycle 13 — the task WAS actually done (ticked `[x]`) but the leftover text from the old unticked version's tail survived the edit. Fixed to end cleanly at the real, accurate description.

Gates: `npx tsc --noEmit -p tsconfig.typecheck.json` (helio-mcp) clean (now covers `e2e/`); `npx eslint e2e/sleeper-rebuild.ts --max-warnings=0` clean; `npx prettier --check` clean; the verified scoped helio-mcp jest command — 18/18 suites, 181/181 tests (unchanged — `e2e/` is not a jest test path), no OOM.

## Cycle 16 — MCP E2E run for real (task 5.1 closed), placement-undo regression (task 5.5), fresh full backend suite (task 5.10)

**Task 5.1, closed for real**: verified the peer's port-isolation claim independently (`workflow-state.md` confirmed `DEV_PORT: 6339`/`BACKEND_PORT: 9246`, matching `scripts/concertino/setup-worktree.sh`'s deterministic per-ticket port derivation) rather than trusting it blind, then started THIS worktree's own backend via `scripts/concertino/start-servers.sh "$(pwd)" 6339 9246 HEL-907` — a genuinely isolated instance, not the shared main-checkout one from cycle 15. Minted a throwaway PAT (login + `POST /api/tokens` with the required `X-Helio-Requested-With` CSRF header — cookie-based login, not the bearer-token response CLAUDE.md's example curl snippet implies), compiled `e2e/sleeper-rebuild.ts` standalone, and ran it for real against `localhost:9246`. **All four dashboards built successfully end-to-end** (League Rosters/Weekly Matchups/League Standings/Waiver Transactions): `create_pipeline` (single call) → `run_pipeline` → `set_pipeline_schedule`/`get_pipeline_schedule` (byte-identical read-back) → `place_outputs`. Re-ran a second time to prove the tag-based teardown-then-rebuild path is genuinely idempotent (fresh ids both runs, no residue collision). Ran `--cleanup-only` afterward and revoked the throwaway PAT, leaving this worktree's dev DB clean.

**Task 5.5, closed** — `backend/src/test/scala/com/helio/services/patchsets/PatchSetUndoServiceSpec.scala`: added `outputRepo`/`seedOutput` to the spec's fixture wiring (new `OutputRepository` collaborator, `PanelService` constructor call updated to pass it as the 5th positional arg) and one new test, `"restore an output-kind placement panel's create/delete-undo, preserving config.outputId (5.5)"`. Investigated first (per this ticket's own discipline) rather than assuming the task's "add/remove/modify" framing applied literally to `pipelineStep` -- confirmed there is no `create` edit kind for `pipelineStep` at all (matches the pre-existing `Output` precedent: neither carries a parent-id field an `EditTarget` could target before the resource exists), so the pipelineStep half of this task was already fully covered by 5.3a (update)/5.3c (delete-undo-recreate with `enabled`/`parentStepId`, HEL-766). The real, previously-unverified gap was placement-field preservation: the existing panel create/delete-undo coverage (5.3b) only ever used a `divider`-kind panel, never `output`-kind, so `OutputPanelConfig.outputId` had never been proven to survive undo in either direction. Discovered along the way that `panels.output_id` is FK-constrained at the DB level against a real `outputs` row -- an `outputRepo == null` fixture (this spec's pre-existing `panelService` construction) only skips the app-level existence CHECK, never the DB-level FK -- so a synthetic `outputId` string fails with a Postgres FK violation; the test needed a genuinely seeded `Output` (via the new `outputRepo.insertInternal`, itself needing a real seeded pipeline).

**Task 5.10, closed** — `sbt -batch 'set Test/parallelExecution := false' test` re-run fresh (several backend files have changed since cycle 11's last full run, this cycle's own `PatchSetUndoServiceSpec.scala` addition included): **3517/3517 tests, 235 suites, 0 failures.**

Gates: `sbt compile` clean; the new/modified spec run standalone first (`testOnly ...PatchSetUndoServiceSpec`, 11/11) then as part of the full suite (3517/3517); `npx prettier --check` on the touched `.md` files clean. No frontend/helio-mcp file touched this cycle.

## Cycle 17 — evaluation-1.md FAIL, all 6 change requests addressed

Full report at `openspec/changes/mcp-outputs-proposals-rewrite/evaluation-1.md`. Read directly, worked all 6 CRs (not just the peer's summarized 4).

**CR3 — dashboard tagging (new backend feature, the largest fix)**:
- `backend/src/main/resources/db/migration/V95__dashboard_tag.sql` (new) — extends HEL-366's tag-based teardown system (V73) to `dashboards`, the one top-level agent-creatable resource that had never gotten it. Nullable `tag TEXT CHECK (length(tag) <= 200)` + partial `(owner_id, tag)` index, same shape as V73's three columns.
- `backend/src/main/scala/com/helio/domain/model/model.scala` — `Dashboard` gained `tag: Option[String] = None` (appended last).
- `backend/src/main/scala/com/helio/infrastructure/persistence/dashboards/DashboardRepository.scala` — `DashboardRow` gained `tag`; `DashboardTable` gained a `tag` column accessor (constructor param renamed `tag: Tag` -> `slickTag: Tag` to avoid colliding with the new column accessor of the same name); `rowToDomain`/`domainToRow` map it both ways.
- `backend/src/main/scala/com/helio/services/dashboards/DashboardService.scala` — `CreateDashboardInput` gained `tag`; threaded through both `insertNew` call sites.
- `backend/src/main/scala/com/helio/api/protocols/dashboards/DashboardProtocol.scala` — `CreateDashboardRequest`/`DashboardResponse` gained `tag` (jsonFormat2->3, jsonFormat6->7).
- `backend/src/main/scala/com/helio/api/routes/dashboards/DashboardRoutes.scala` — threads `request.tag` into `CreateDashboardInput`.
- `backend/src/main/scala/com/helio/infrastructure/persistence/workspace/WorkspaceTeardownRepository.scala` — `teardown()` now also queries/deletes tagged dashboards (no cross-resource dependent guard needed — panels cascade via the pre-existing `panels.dashboard_id ON DELETE CASCADE`, V2); `TeardownOutcome` gained `dashboardsDeleted`.
- `backend/src/main/scala/com/helio/services/workspace/WorkspaceTeardownService.scala` / `backend/src/main/scala/com/helio/api/protocols/workspace/WorkspaceProtocol.scala` — `dashboardsDeleted` threaded into the audit payload and `TeardownResponse` (jsonFormat7->8).
- `backend/src/test/scala/com/helio/services/workspace/WorkspaceTeardownServiceSpec.scala` — new test: a tagged dashboard is deleted, an untagged control dashboard is untouched, `dashboardsDeleted` reports 1.
- **Manual cleanup**: verified (by id/name/timestamp) then deleted, via `DELETE /api/dashboards/:id`, the 8 real orphaned dashboards the evaluator found live in the shared dev Postgres — left there by cycle 16's own E2E run, since `create_dashboard` had no `tag` param at all until this fix.
- `helio-mcp/src/types.ts` — `DashboardResponse` gained `tag?`; `TeardownResponse` gained `dashboardsDeleted` and dropped the already-stale `typesDeleted` (dead since HEL-904, found independently, not evaluator-cited).
- `helio-mcp/src/helioApi.ts` — `createDashboard` accepts `tag`.
- `helio-mcp/src/tools/write.ts` — `create_dashboard` documents/accepts `tag`.
- `helio-mcp/e2e/sleeper-rebuild.ts` — passes `tag: E2E_TAG` on `create_dashboard`; header comment corrected (was falsely claiming dashboards were already reclaimable by teardown).

**CR1 — live-guidance sweep (re-ran the evaluator's exact grep, triaged every hit)**:
- `helio-mcp/src/tools/write.ts` — `teardown_resources` description rewritten (no more DataType mentions; fixed tag example; `dashboardsDeleted` in the documented response fields).
- `helio-mcp/src/tools/read.ts` — `list_pipeline_shapes` description: all 3 `create_pipeline_from_shape` mentions -> `add_outputs_from_shape` (with the correct current call signature).
- `helio-mcp/src/tools/proposal.ts` — 3 live `create_panel` mentions inside `propose_dashboard`/`apply_proposal` description strings -> `place_outputs`/`create_content_panel`.
- `helio-mcp/src/context.ts` — one doc comment `create_pipeline_from_shape` -> `add_outputs_from_shape`.

**CR2 — helio-mcp/scripts staleness, no ticket owning it (fixed here, not deferred)**:
- `helio-mcp/scripts/compose.ts`, `helio-mcp/scripts/verify-bound-panel.ts` — deleted outright (premised entirely on retired tools: `create_panel`/`bind_panel`/`create_bound_panel`/`get_data_type_rows`/old positional `create_pipeline_from_shape` shape; no salvageable current-model role, `sleeper-rebuild.ts` supersedes `compose.ts`'s demonstrative purpose more completely).
- `helio-mcp/package.json` — removed the now-dangling `compose`/`verify-bound-panel` npm scripts.
- `helio-mcp/scripts/verify.ts` — retargeted (its read-tool-verification role stays valid): `list_data_types` section -> `list_outputs`; `get_data_type_rows` section -> `list_outputs(pipelineId)` + `get_output_rows`; the `create_pipeline_from_shape` setup/valid/invalid-params/unknown-shape/no-orphan test block -> `create_pipeline` (single-call setup) + `add_outputs_from_shape`, with the orphan-check re-targeted from "no orphan pipeline" (stale — `add_outputs_from_shape` never creates a new pipeline) to "no orphan Output".
- `helio-mcp/README.md` — `teardown_resources` table row (DataType -> dashboard); "End-to-end composition" section rewritten to point at `e2e/sleeper-rebuild.ts` instead of the now-deleted `compose.ts`; `bind_panel` mention at line 114 confirmed already a correct tombstone (left as-is).
- `docs/agent-native.md` — "End-to-end proof" section's stale `compose.ts`/`verify-bound-panel.ts`/`verify.ts` callout rewritten to describe the actual fix (2 deleted, 1 retargeted).
- `helio-mcp/e2e/sleeper-rebuild.ts` — header comment's `scripts/compose.ts` precedent reference fixed to `scripts/verify.ts` (compose.ts no longer exists).

**CR4 — exact tool-name-set test**:
- `helio-mcp/src/server.test.ts` — new `EXPECTED_TOOL_NAMES` const (the full, verified 60-tool alphabetical list, cross-checked against every `registerXTools` call in `server.ts`) and a new test asserting `[...names].sort()` `toEqual` the expected set exactly — an accidentally re-added/renamed tool now fails, not just a removed one.

**CR5**:
- `docs/agent-native.md` — "End-to-end proof" no longer falsely states the Sleeper composition proof is "still open work" (task 5.1 was closed for real in cycle 16).

**CR6**:
- `helio-mcp/src/types.ts` — `ProposalPanel`'s doc block rewritten to correctly frame metric/chart/table/collection/timeline as HEL-904-retired legacy fields and state `config.dataTypeId` (text/markdown) is silently inert, never a real binding attempt — was self-contradictory with `dashboard-proposal.schema.json`, which already said this correctly.

**Non-blocking suggestion**:
- `openspec/changes/mcp-outputs-proposals-rewrite/ticket.md`/`design.md`/`tasks.md` — the verbatim scoped helio-mcp jest command gained `"testPathIgnorePatterns":["/node_modules/","/dist/"]`; verified with `helio-mcp/dist/` actually present (18/18 suites, 181/181, zero spurious `dist/*.test.js` failures).

Gates (fresh, this cycle): see final report for pasted output — `sbt compile`/`Test/compile` clean; `WorkspaceTeardownServiceSpec` 11/11; full `sbt test` re-run; helio-mcp `tsc --noEmit`/`eslint`/`prettier --check` clean; scoped helio-mcp jest suite green.

## Cycle 20 — evaluation-2.md FAIL (one precise finding), fixed

Full report at `openspec/changes/mcp-outputs-proposals-rewrite/evaluation-2.md`.

- `backend/src/main/scala/com/helio/api/routes/dashboards/DashboardRoutes.scala` — the POST `/api/dashboards` handler now gates `request.tag` through `RequestValidation.validateTag` (curated 400 on an over-length tag) BEFORE calling `dashboardService.create`, matching this file's own existing `offset < 0` curated-400 pattern and the sibling DataSourceService/PipelineService's in-service gate. Deliberately fixed at the route layer, not by widening `DashboardService.create`'s `Future[(Dashboard, Boolean)]` return type (no `Either` convention at all — would ripple through `DashboardProposalService`/`PatchSetApplyForward`/every caller for one field).
- `backend/src/test/scala/com/helio/api/ApiRoutesSpec.scala` — new test `"reject an over-length tag on dashboard create with a curated 400, not a raw DB-constraint 500"`, exercising the REST boundary directly (a 201-char tag), asserting 400 with the curated message — not through the MCP client, whose zod schema already caps `tag` at 200 chars and structurally cannot express the failing input.
- `backend/src/main/scala/com/helio/infrastructure/persistence/workspace/WorkspaceTeardownRepository.scala` — `TeardownOutcome.dashboardsDeleted`'s dead `= 0` default removed (confirmed exactly one construction site, always explicit).
- `schemas/dashboards/create-dashboard-request.schema.json` — documented the `ifExists`+`tag` interaction (tag only applies on the fresh-insert branch).

**Peer addendum audit (no fix needed, findings only)**: checked every other new length-constrained free-text field this ticket added across any service for the same unguarded-boundary shape. Found one candidate (`outputs.tag`, V94) but confirmed via direct code inspection it has zero live write paths — every `insertInternal`/`insertInternalAction` call site (`DemoData.scala`, `OutputService.scala`, `PipelineService.scala`'s transactional create path) uses the `tag = None` default; no REST/MCP-exposed input ever sets it. Not reachable, not the same class of bug.

Gates (fresh, this cycle): `sbt compile` clean; new test standalone (`testOnly ...ApiRoutesSpec -- -z "over-length tag"`, pass); full backend suite re-run TWICE (once after the route fix, once again after the dead-default removal) -- both 3520/3520 tests, 235 suites, 0 failures; `check-schema-drift.mjs` green.

## Cycle 21 -- final-gate 4-way skeptic fan-out (1 CONFIRM, 3 REFUTE), all fixed

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools,2-proposal-contract,3-deletion-sweep,4-wire-contract}.md`.

**MCP tools dimension (finding 1, blocking) + peer follow-up sweep:**
- `helio-mcp/src/types.ts` — `CreateSourceResult`: `dataType` (dead since HEL-904) replaced with the real `inferredSchema`/`rowCapNotice` fields; removed `DataTypeResponse` import (now dead in this file).
- `helio-mcp/src/helioApi.ts` — `RawCreateSourceResponse` fixed to match; `createRestDataSource`/`createSqlDataSource` now surface `inferredSchema`/`rowCapNotice` for real instead of an always-null dead field; deleted the fully-dead `listDataTypes()` method (zero call sites since cycle 10, called a route HEL-904 removed); doc comments updated throughout.
- `helio-mcp/src/tools/write.ts` — swept every remaining stale "DataType" mention this ticket's own file family had missed: header docstring (canonical path diagram), `create_data_source` (fixed a genuine copy-paste-residue sentence wrongly claiming this source-only tool "creates the pipeline"), `create_csv_data_source`, `create_rest_data_source`, `create_sql_data_source`, `run_pipeline`, `delete_dashboard`, `delete_panel`.

**Proposal-contract dimension (findings 2/3/4, blocking):**
- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineStepProtocol.scala` — `PipelineStepResponse`'s sealed trait + all 23 per-kind subtypes gained `parentStepId: Option[String]`; `fromDomain` threads it; every per-subtype `jsonFormat7`->`jsonFormat8`. Root cause of finding 2: the wire response `priorState` snapshots into the patch-set journal never carried this field at all.
- `backend/src/main/scala/com/helio/services/patchsets/PatchSetUndoInverse.scala` — `pipelineStepCreateRequestFromResponse` now reads and threads `parentStepId` from the (now-complete) captured JSON.
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetUndoServiceSpec.scala` — HEL-766 test's fixture rewritten (3-step pipeline, branch off the non-trunk-last step) so the assertion actually distinguishes the fix from its absence; verified failable by mutation.
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetApplyServiceSpec.scala` — new sibling test exercising `PatchSetApplyRollback`'s own inverse-builder directly (mid-apply rollback, distinct code path from the undo test); `seedPipelineStep` helper gained an optional `parentStepId` param; verified failable by mutation.
- `helio-mcp/src/tools/combinedProposal.ts` — `apply_combined_proposal`'s description rewritten: the `$pipelineOutput` sentinel's ONLY blessed slot is `dataTypeId` on an `output`-kind panel; the dead, self-contradicting `config.dataTypeId` sentinel arm removed entirely (was contradicting `proposal.ts`'s own accurate text and `ProposalPanelSupport.bindingCandidate`'s real HEL-904-task-4.1 behavior).

**Wire-contract dimension (finding 5, blocking) + non-blocking note 2:**
- `schemas/dashboards/dashboard.schema.json` — added `tag`, matching `DashboardResponse`'s real field list exactly; was masked by `check-schema-drift.mjs`'s `SKIP` set (this schema is hand-composed, title `"Dashboard"`, not 1:1-diffable). HEL-928 comment text for the peer to post is in execution-progress.md's Cycle 21 section.
- `backend/src/test/scala/com/helio/services/pipelines/PipelineCreateTransactionalSpec.scala` — new cross-owner-join-rejection + own-owner-join-acceptance pair for `PipelineService.validateStepCrossOwnerRefs`, the `create_pipeline` transactional path's own ACL check (previously zero direct coverage, unlike the sibling `addStep` path's six tests).

Gates (fresh, this cycle): `helio-mcp` `tsc --noEmit`/`eslint --max-warnings=0`/`prettier --check` all clean; scoped helio-mcp jest — 18/18 suites, 182/182 tests; `sbt compile` clean; `PipelineCreateTransactionalSpec` standalone — 10/10; full backend suite — 3523/3523 tests, 235 suites, 0 failures; `check-schema-drift.mjs` green.

## Cycle 22 — final-gate round 2 (targeted re-check): 2 NEW findings, both fixed

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools-round2,2-proposal-contract-round2}.md`.

**MCP tools dimension (sibling instance of round 1's class, on `run_pipeline`):**
- `helio-mcp/src/types.ts` — removed dead `outputDataTypeName`/`outputDataTypeId` from `PipelineSummaryResponse` (CR1) and `PipelineAnalyzeResponse` (CR5); neither has existed on the real backend wire shape since HEL-904.
- `helio-mcp/src/helioApi.ts` — `RunOutcome` no longer declares `outputDataTypeId`; `runPipeline` no longer maps `summary.outputDataTypeId` (CR2); rewrote 4 stale docstrings still describing the retired DataType model (`createDataSource`, `createCsvDataSource`, an orphaned stray comment block, `teardownResources`) (CR6); deleted an orphaned doc comment with no attached method.
- `helio-mcp/src/tools/write.ts` — `run_pipeline`'s "Returns { ... }" enumeration no longer promises `outputDataTypeId` (CR3).
- `helio-mcp/src/runPipelineTruncation.test.ts` — fake `get` fixture rewritten as a typed `PipelineSummaryResponse` const matching the real 9-field wire shape exactly, instead of a loose object literal that happened to include a dead field (CR4) — this fixture is exactly why nothing caught CR1-3 earlier.
- `helio-mcp/scripts/verify.ts` — dropped the same dead field from an unused inline type annotation, found during the required "one more full pass over every helio-mcp tool/wrapper."
- `helio-mcp/src/context.test.ts` — fixed 4 fixture literals that no longer typecheck once the dead fields were removed from the interfaces; deleted one now-vacuous regression test whose entire premise (an input fixture carrying the dead fields) became structurally impossible.
- `helio-mcp/src/tools/pipelinesHandlers.test.ts`, `helio-mcp/src/tools/pipelineProposalHandlers.test.ts`, `helio-mcp/src/tools/combinedProposalHandlers.test.ts` — same fixture fix (3 more typecheck failures from the CR1 field removal).

**Proposal contract dimension (doc-only, replaced one wrong sentinel-guidance claim with an accurate one read from the real code):**
- `helio-mcp/src/tools/combinedProposal.ts` — `apply_combined_proposal`'s sentinel guidance rewritten from `CombinedProposalService.flatIsBlessed`/`configIsBlessed`'s actual behavior (read directly, not guessed): distinguishes "produces a real binding" (flat `dataTypeId` on `output` only) from "accepted but inert" (flat or `config.dataTypeId` sentinel on text/markdown/image) from "400s the whole call" (`config.dataTypeId` on an `output` panel — a kind mismatch — plus shadowing/duplicate/wrong-position cases).
- `helio-mcp/src/tools/proposal.ts` — `apply_proposal`'s "flat OR via config" clause (contradicting the same file's own accurate line 145) fixed to state flat-only, matching `ProposalPanelSupport.bindingCandidate`'s real HEL-904 task 4.1 behavior.

**Non-blocking notes:**
- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineStepProtocol.scala` — one-word stale comment (`jsonFormat7` -> `jsonFormat8`) fixed.
- `backend/src/test/scala/com/helio/services/patchsets/PatchSetApplyServiceSpec.scala` — the cycle-21 rollback test's comment now explicitly labels it a GUARD (production code was already correct pre-cycle-21), distinguishing it from the genuine HEL-766 regression proof it sits beside in the sibling `PatchSetUndoServiceSpec`.

Gates (fresh, this cycle): `sbt compile`/`Test/compile` clean; helio-mcp `tsc --noEmit`/`eslint --max-warnings=0`/`prettier --check` all clean; scoped helio-mcp jest — 18/18 suites, 181/181 tests; full backend suite — 3523/3523 tests, 235 suites, 0 failures; `check-schema-drift.mjs` green.

## Cycle 23 — final-gate round 3: server.test.ts silently ran ZERO tests in CI

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools-round3,2-proposal-contract-round3}.md`. Proposal-contract dimension CONFIRMed cleanly — no files touched for that axis.

- `jest.config.cjs` (repo root) — added an explicit ts-jest `transform` override forcing `module`/`moduleResolution` to `NodeNext` (matching `helio-mcp`'s own real `tsconfig.json`/ESM `package.json`/build). Root cause: the previous config compiled `helio-mcp/src/**` (the ONLY test tree this config actually collects — confirmed by direct enumeration) under root `tsconfig.json`'s classic Node10 `commonjs`/`node` resolution, a mode `helio-mcp` never builds or runs under, which tripped a genuine `TS2589` "excessively deep" instantiation in `tools/read.ts`'s `registerTool` calls. `server.test.ts` — this ticket's exact-60-tool-name-set test, the definitive proof of the whole tool-removal AC — was the FIRST test in the repo to ever import `read.ts`, so it silently ran ZERO of its 4 tests under CI's real config through three prior evaluation rounds (the ticket's own worktree-scoped test command uses a different, NodeNext config that never hit this).
- `helio-mcp/src/types.ts` — non-blocking note: added `SourceSchemaDriftResponse`/`TypeChangedColumnResponse` and `PipelineAnalyzeResponse.sourceSchemaDrift`, mirroring the real 6-field `PipelineAnalyzeProtocol.scala` case class exactly (was missing entirely — an under-specification, the inverse of the round-2 CR class).
- `openspec/changes/mcp-outputs-proposals-rewrite/tasks.md` — task 5.9's "sole evidence" framing retired; documents the round-3 finding + fix inline.

Gates (fresh, this cycle): helio-mcp `tsc --noEmit`/`eslint --max-warnings=0`/`prettier --check` all clean; scoped helio-mcp jest — 18/18 suites, 181/181 tests; **CI-shaped root-config jest run (the actual regression test for this fix) — 18/18 suites, 181/181 tests, `server.test.ts` 4/4 actually executed** (was 0/4 before the fix — reproduced both states directly, not assumed); `check-schema-drift.mjs` green. No backend file touched this cycle.

## Ledger reconciliation (pre-delivery, squash-branch.sh check)

`squash-branch.sh` refused to run because this file's declared touched-file set
didn't match the actual full diff (`git diff --name-status main...HEAD`, cross-checked
programmatically against every backtick-quoted path in this file, not recalled from
memory). The gaps below are genuinely-touched paths from earlier cycles whose bullets
covered only PART of a multi-file batch, or whose cycle summary omitted the path
entirely. Added here for completeness -- no code change, ledger-accuracy only.

**Backend test specs** (real test-fixture/coverage changes across earlier cycles,
never individually declared):
- `backend/src/test/scala/com/helio/api/protocols/pipelines/PipelineProposalProtocolSpec.scala`
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineAnalyzeProposalRoutesSpec.scala`
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineApplyProposalRollbackSpec.scala`
- `backend/src/test/scala/com/helio/api/routes/pipelines/PipelineApplyProposalSpec.scala`
- `backend/src/test/scala/com/helio/api/routes/proposals/CombinedApplyProposalSpec.scala`
- `backend/src/test/scala/com/helio/services/assistant/AssistantServiceSpec.scala`
- `backend/src/test/scala/com/helio/services/assistant/AssistantToolExecutorSpec.scala`
- `backend/src/test/scala/com/helio/services/pipelines/PipelineProposalServiceValidateSpec.scala`
- `backend/src/test/scala/com/helio/services/pipelines/PipelineServiceInlineRestBodySpec.scala`
- `backend/src/test/scala/com/helio/services/proposals/CombinedProposalServiceValidateSpec.scala`

**Frontend test files** (updated fixtures/assertions for the retargeted Outputs
contract, batched together in early cycles under a summary that didn't enumerate
every individual test file):
- `frontend/src/features/assistant/ui/ProposalHandoff.test.tsx`
- `frontend/src/features/dashboards/services/outputsService.test.ts`
- `frontend/src/features/dashboards/ui/ProposalReview.test.tsx`
- `frontend/src/features/dashboards/ui/ProposalReviewPage.demoFixture.test.tsx`
- `frontend/src/features/dashboards/ui/ProposalReviewPage.test.tsx`
- `frontend/src/features/pipelines/state/pipelinesSlice.test.ts`
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReview.test.tsx`
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReviewPage.test.tsx`
- `frontend/src/features/proposals/state/combinedProposalsSlice.test.ts`
- `frontend/src/features/proposals/ui/CombinedProposalReview.test.tsx`
- `frontend/src/features/proposals/ui/CombinedProposalReviewPage.test.tsx`
- `frontend/src/features/proposals/utils/unresolvedConnectorRefs.test.ts`

**Other real source files:**
- `jest.config.cjs` — cycle 23's NodeNext ts-jest transform override (documented in this file's own Cycle 23 section by content, but the bare path was never individually bulleted).
- `schemas/workspace/workspace-teardown-response.schema.json` — cycle 17's `dashboardsDeleted` field addition (documented by content in the Cycle 17 section; bare path never individually bulleted).

**OpenSpec change-scaffold and process/report files** (proposal.md/design.md/tasks.md/
ticket.md/.openspec.yaml, this file's own path, execution-progress.md, every
evaluation-N.md, skeptic-design-N.md, and skeptic-final-*.md report, and every
`specs/*/spec.md` file this change generated) — these are the change's own required
OpenSpec artifacts and the evaluator/skeptic reports each review cycle produced, not
narrated individually in the file-by-file sections above since they're process
scaffolding rather than implementation:
- `openspec/changes/mcp-outputs-proposals-rewrite/.openspec.yaml`
- `openspec/changes/mcp-outputs-proposals-rewrite/design.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/evaluation-1.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/evaluation-2.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/evaluation-3.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/execution-progress.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/files-modified.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/proposal.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-1.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-2.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-3.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-4.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-5.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-design-6.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-1-mcp-tools.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-1-mcp-tools-round2.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-1-mcp-tools-round3.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-1-mcp-tools-round4-targeted.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-2-proposal-contract.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-2-proposal-contract-round2.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-2-proposal-contract-round3.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-3-deletion-sweep.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-4-wire-contract.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-4-wire-contract-round2.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/ticket.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/tasks.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-data-source-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-metric-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-output-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-panel-composition-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-patch-set-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-pipeline-proposal-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/mcp-pipeline-shape-tools/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/nl-dashboard-proposal-authoring/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/output-panel-placement/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/output-routes-api/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/outputs-model/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/patch-set-apply/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/patch-set-contract/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/patch-set-preview/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/patch-set-undo/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/pipeline-proposal-analyze-api/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/pipeline-proposal-apply/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/pipeline-proposal-contract/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/pipeline-proposal-review-ui/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/refinement-chat-surface/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/workspace-context-agent-section/spec.md`
- `openspec/changes/mcp-outputs-proposals-rewrite/specs/workspace-context-assembly/spec.md`

## Squash-declaration fixup (orchestrator, pre-delivery)

The following paths were already covered in prose above but as the second (or later) backtick-quoted
path on a multi-path bullet line — `squash-branch.sh`'s parser only extracts the first backtick-quoted
path per `- `/`* ` bullet. Re-declared here, one path per bullet, so the squash allowlist covers them:

- `backend/src/main/scala/com/helio/api/protocols/workspace/WorkspaceProtocol.scala` — see the `WorkspaceTeardownService.scala` bullet above (`dashboardsDeleted` threaded into `TeardownResponse`, jsonFormat7->8).
- `backend/src/main/scala/com/helio/services/assistant/AssistantSystemPrompt.scala` — see the `AssistantProposalToolSchemas.scala` bullet above (in-app assistant's propose_pipeline/propose_combined tool schemas + system-prompt text updated to the new shape).
- `helio-mcp/scripts/verify-bound-panel.ts` — see the `compose.ts` bullet above (deleted outright, premised entirely on retired tools).
- `helio-mcp/src/tools/combinedProposalHandlers.test.ts` — see the `pipelineProposalHandlers.test.ts` bullets above (updated fixtures for the new shapes).
