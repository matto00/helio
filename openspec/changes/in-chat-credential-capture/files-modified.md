# Files Modified — HEL-829 In-Chat Credential Capture

## Backend — new proposal-only types + validation (task 1)

- `backend/src/main/scala/com/helio/api/protocols/pipelines/PipelineProposalProtocol.scala` — adds `ProposalRestApiConfig`/`NewConnectorDraft` (new, proposal-only types, design.md Decision 2), their spray-json formatters, and `ProposalRestApiConfig.toRestApiConfigPayload` (the shared adapter). Changes `PipelineProposalSource.restConfig`'s type from `Option[RestApiConfigPayload]` to `Option[ProposalRestApiConfig]`.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineProposalService.scala` — adds `validateRestConfig` (exactly-one-of `connectorId`/`url`/`newConnector`), wires it into `validateInlineSource`; `resolveRestSource` now converts via the shared adapter before calling `sourceService.createRest` (the only change to `apply`'s existing code path).
- `backend/src/main/scala/com/helio/api/protocols/assistant/AssistantProposalToolSchemas.scala` — documents the `newConnector` branch in `propose_pipeline`'s tool schema: when to use it, that `retrievalInstructions` must describe where to find the key and must never contain a key value.
- `backend/src/main/scala/com/helio/services/assistant/AssistantToolExecutor.scala` — mechanical consequence of the `restConfig` type change (task 1.1): `requireVerifiedInlineSource`'s `test_connection`-verification comparison now converts the proposal's `ProposalRestApiConfig` through `ProposalRestApiConfig.toRestApiConfigPayload` before comparing against a verified `RestApiConfigPayload`. Behavior-preserving for the `connectorId`/`url` branches; a `newConnector` draft correctly falls through to "unverified" (nothing live to test yet). This file is listed in the ticket's "do not modify" set for unrelated reasons — this edit was structurally required by the type change task 1.1 explicitly asks for; see the executor's return summary for the full explanation.
- `backend/src/main/scala/com/helio/services/pipelines/PipelineService.scala` — same mechanical consequence in `analyzeProposedSource`'s inline `rest_api` branch: converts via the same adapter before `RestApiConfigPayload.toDomain`; the old `payload.auth.isDefined` guard is removed since `ProposalRestApiConfig` has no `auth` field at all (structurally enforced now, not runtime-checked).
- `backend/src/test/scala/com/helio/api/protocols/assistant/AssistantProposalToolSchemasSpec.scala`, `backend/src/test/scala/com/helio/services/assistant/AssistantServiceSpec.scala`, `backend/src/test/scala/com/helio/services/assistant/AssistantToolExecutorSpec.scala`, `backend/src/test/scala/com/helio/services/pipelines/PipelineServiceInlineRestBodySpec.scala` — updated fixture construction from `RestApiConfigPayload` to `ProposalRestApiConfig` to compile against the `restConfig` type change; behavior unchanged.
- `backend/src/test/scala/com/helio/api/protocols/pipelines/ProposalRestApiConfigSpec.scala` (new) — round-trip JSON tests for `ProposalRestApiConfig`/`NewConnectorDraft`, and `toRestApiConfigPayload` field-mapping tests for both the `connectorId`-only and legacy `url`-only cases (task 1.4).
- `backend/src/test/scala/com/helio/services/pipelines/PipelineProposalServiceRestConfigSpec.scala` (new) — `validateRestConfig` exactly-one-of tests (task 1.4).

## Frontend — detection + inline setup UI (task 2)

- `frontend/src/features/proposals/utils/unresolvedConnectorRefs.ts` (new) — pure detection helper (`detectUnresolvedConnectorRefs`/`detectUnresolvedConnectorRefsForCombined`) reading `proposal.source.config` with a runtime shape check (never throws), plus `resolvePipelineConnectorRef`/`resolveCombinedConnectorRef` for patching the local proposal copy once a Connector is created.
- `frontend/src/features/proposals/utils/unresolvedConnectorRefs.test.ts` (new) — unit tests for the above (task 2.2).
- `frontend/src/features/connectors/ui/InlineConnectorSetup.tsx` (new) — the credential form: retrieval instructions, the "agents never see this key" statement, `ConnectorCredentialField` (reused verbatim, `mode="create"`), name/base-URL fields, submit dispatching `createConnector` directly. Security-load-bearing carrier discipline documented inline (design.md Decision 3, Point 7).
- `frontend/src/features/connectors/ui/InlineConnectorSetup.css` (new) — DESIGN.md-token styling, 44px mobile touch-target floor at 768/430.
- `frontend/src/features/connectors/ui/InlineConnectorSetup.test.tsx` (new) — the demonstrated-red runtime backstop (task 3.4): asserts the test credential is present ONLY in the `POST /api/connectors` body, absent from router state and the full Redux store snapshot, before and after submission.
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReview.tsx` — renders `InlineConnectorSetup` per unresolved connector reference; disables Accept while any remain.
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReviewPage.tsx` — holds the local patchable proposal copy, fetches the connector list, computes unresolved refs, passes them + the resolve callback down.
- `frontend/src/features/pipelines/ui/proposalReview/PipelineProposalReviewPage.test.tsx` — adds `connectors` reducer + mocks `connectorEntityService.fetchConnectors` so the suite issues no real network call.
- `frontend/src/features/proposals/ui/CombinedProposalReview.tsx` — same wiring for the nested pipeline half.
- `frontend/src/features/proposals/ui/CombinedProposalReviewPage.tsx` — same page-level wiring as the pipeline review page.
- `frontend/src/features/proposals/ui/CombinedProposalReviewPage.test.tsx` — same test-store/mock addition as the pipeline review page test.

(`frontend/src/features/dashboards/ui/ProposalReview.tsx`, the dashboard-proposal review page, needs no wiring — `DashboardProposal` has no source/connector-bearing field on its type at all per design.md Decision 3, so the AC's `dashboard`-kind requirement is satisfied vacuously by the schema, not silently skipped.)

## Mechanical enforcement (task 3)

- `scripts/check-no-credential-in-agent-surface.mjs` (new) — import-graph + text-pattern lint over `frontend/src/features/assistant/**`: fails on any transitive import of `ConnectorCredentialField`/`InlineConnectorSetup`, or any property literally named `credential`.
- `package.json` — adds the `check:no-credential-leak` script.
- `.husky/pre-commit` — adds `npm run check:no-credential-leak` as its own new line, sibling to `check:scala-quality`, never composed into `lint`.
- `backend/src/test/scala/com/helio/services/assistant/CredentialSurfaceEnumerationSpec.scala` (new) — token-grep change-detector over `com.helio.ai`/`com.helio.services.assistant` (asserts zero matches) and `helio-mcp/src/` (asserts the exact seven allow-listed files, each justified).
- `backend/src/test/scala/com/helio/api/protocols/sources/ConnectorSummaryCredentialAbsenceSpec.scala` (new) — pins `ConnectorSummary`'s formatter field set directly, and asserts a serialized `WorkspaceContext` payload for a workspace with a credentialed Connector never contains the credential string.

## Change-management artifacts

- `openspec/changes/in-chat-credential-capture/tasks.md` — all 18 tasks marked complete.
- `openspec/changes/in-chat-credential-capture/files-modified.md` — this file.

## Demonstrated-red evidence (captured, then reverted before commit)

- **Task 3.3** (lint check): temporarily threaded `import type { ConnectorCredentialFieldValue } from "../connectors/ui/ConnectorCredentialField"` into `proposalExtraction.ts` — `node scripts/check-no-credential-in-agent-surface.mjs` failed, naming the exact 5-file import chain; also temporarily added a `credential:` property to `types.ts` — same script caught it too. Both reverted; `git status` confirmed clean before continuing.
- **Task 3.2 Husky-wiring verification**: repeated the same violating import and ran a REAL `git commit` (not a standalone script invocation) — Husky's pre-commit hook fired `check:no-credential-leak` and blocked the commit with the same 5-violation output; `git log` confirms no commit was ever created. Reset and reverted.
- **Cycle-2 fix (skeptic-final-1.md CR1)**: the original `extractRelativeImports` regex (`/(?:from|import)\s+["']([^"']+)["']/g`) required whitespace immediately after `import`, so the call form `await import("./x")`/`React.lazy(() => import("./x"))` never matched — a dynamic import of a banned module silently passed (rc=0). **Reproduced the gap first**: `await import("../../connectors/ui/InlineConnectorSetup")` matched against the OLD regex returned `null`. **Fixed** the regex to `/(?:from|import|require)\s*\(?\s*["']([^"']+)["']/g` and verified it still matches every static-import shape (bare `from`, `import type`, side-effect `import "..."`, `export * from`) with no regression, via a standalone probe before touching the real script. **Demonstrated red on the fixed script**: temporarily threaded `async function __hel829TempDynamicImportFixture() { return await import("../connectors/ui/InlineConnectorSetup"); }` into `proposalExtraction.ts` — the check now correctly failed, naming the same 5-file import chain (`proposalExtraction.ts -> InlineConnectorSetup.tsx`, `ActiveConversationPanel.tsx -> ... `, `ChatPage.tsx -> ...`, `ProposalHandoff.tsx -> ...`, `QuickLauncherOverlay.tsx -> ...`) — confirming the fix closes the gap the skeptic reproduced. Reverted (`git checkout --`); re-ran `node scripts/check-no-credential-in-agent-surface.mjs` green (0 violations) against the real, unmodified tree. Also re-confirmed the static-import and `credential:`-property fixtures still demonstrate red on the fixed script (same output as cycle 1). Added a "Known residual limits" note to the script's own header docstring (exact-word-only `credential` scan; relative-import-only walk) per the skeptic's non-blocking-but-requested documentation ask.
- **Task 3.4** (runtime test): temporarily had `InlineConnectorSetup` call `navigate(location.pathname, {state: {...location.state, leaked: credentialValue.credential}, replace: true})` on submit success — `InlineConnectorSetup.test.tsx` failed with `Expected substring: not "test-fake-key-do-not-use" / Received string: "...leaked":"test-fake-key-do-not-use"...`. Reverted; re-ran green.
- **Isolation-test transcript**: `scripts/concertino/test-gate-in-isolation.sh HEL-829 scripts/check-no-credential-in-agent-surface.mjs check:no-credential-leak` → `PASS`, transcript at `.concertino/runs/HEL-829/evidence/.concertino/gate-chain-isolation-evidence/scripts__check-no-credential-in-agent-surface.mjs.md` (main checkout).

No real credential/token was ever used in any evidence capture — all fixtures used the literal string `test-fake-key-do-not-use`.
