- `backend/src/main/resources/application.conf` — raised `maxLifetime` from `60000` to `1800000` (30 min, HikariCP's own default) on both the `helio.db` and `helio.db.privileged` pools, and rewrote the tuning comment above each stanza to explain the corrected rationale: HikariCP's housekeeper proactively retires idle pooled connections at `maxLifetime` regardless of use, the previous 60s value forced a fresh Cloud SQL TLS handshake roughly every 60-90s independent of traffic, and the Cloud SQL Java connector already manages ephemeral-cert freshness on its own independent background schedule, so the old recycling cadence bought no security benefit. `minimumIdle`, `maximumPoolSize`, and `idleTimeout` are untouched on both pools (confirmed via `git diff`).
