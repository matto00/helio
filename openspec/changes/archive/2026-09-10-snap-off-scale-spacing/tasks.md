# Tasks: Snap off-scale spacing literals (HEL-830)

- [x] 1. Re-run `spacing-scan.js` against the executor's own worktree base to confirm the 102/18 worklist still holds at execution time (main may have moved further between Planning and Execution). Also `grep` for any line matching the spacing pattern twice (the scanner's known `/g`-less blind spot, see design.md) and handle any such line by hand if the base has moved.
- [x] 2. For each of the 18 files in `enumeration.md`, snap every listed off-scale literal to the nearest `--space-*` token per `design.md`'s policy, applying per-context judgment for 6px/10px/14px per the design doc's guidance. **Exception: the `14px` shared-left-edge cluster (design.md "Coupled cluster") snaps every member to the same target step (16px) as one decision, not independently** — update both of HEL-1022's explanatory comments (`OutputsRail.css:12-18`, `PipelineDetailPage.css:449-457`) so they no longer assert `14px` verbatim once the value changes. Add an inline CSS comment for every deviation from the mathematically-nearest step, and for every kept (unsnapped) exception with its reason.
- [x] 3. Resolve the 7px/6px chip-padding sites (HEL-680 overlap) by snapping the literal only — do not introduce a new `--chip-padding` custom property.
- [x] 4. **After all CSS edits in steps 2-3 are complete**, regenerate `frontend/src/theme/tokenAuditSweep.css.test.ts`'s `SPACING_BASELINE` from scratch: run the test file's own `SPACING_PATTERN` + `spacingIsDisallowed` predicate against `SWEPT_FILES` on the post-edit tree, and diff the regenerated list against the current 62-entry baseline as a reviewable artifact. Do NOT hand-adjust old line numbers by offset arithmetic (HEL-442/HEL-732 precedent of this failing — see design.md). Confirm the regenerated baseline still contains the ~10 pre-existing entries this ticket doesn't touch, and confirm it's RED before applying (temporarily revert one snap, confirm test fails) then GREEN after.
- [x] 5. Run `npm test` (full suite) and confirm `tokenAuditSweep.css.test.ts` passes with the regenerated baseline.
- [x] 6. **Touch-target measurement (not inference):** import `e2e/support/touchTargetProbe.ts` directly and measure every interactive control whose padding this ticket reduces (see design.md's list: PipelineDetailPage save/cancel/dry-run buttons, chip-like controls, `2px 7px`/`2px 6px` chips, etc.) at 430px and 768px, before and after. Also run the existing `e2e/hel813-mobile-touch-target-floor.spec.ts` suite for regression coverage of its own 7 surfaces (expected unaffected, but confirm). Any control under 44px post-snap gets a less-aggressive target step for that site.
- [x] 7. Capture before/after Playwright screenshots at desktop (1280px), 430px, and 768px for every one of the 18 touched files' representative rendered surface. Store under the run's evidence directory (never repo root).
- [x] 8. Run `npm run lint`, `npm run typecheck`, `npm run format:check` — all clean.
- [x] 9. Write `files-modified.md` per the standard executor handoff convention.
