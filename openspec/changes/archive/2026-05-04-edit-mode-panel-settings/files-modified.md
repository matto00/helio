- `frontend/src/components/PanelDetailModal.tsx` — primary implementation: removed Tab type/activeTab state/handleTabChange, added unified `handleEditSubmit`, added `title` local state, replaced per-tab forms with single `<form id="panel-detail-edit-form">` containing Appearance + type-specific section headings, added auto-fetch of data types on edit mode entry, updated `isDirty` to include title
- `frontend/src/components/PanelDetailModal.css` — removed `.panel-detail-modal__tabs` and `.panel-detail-modal__tab` styles; added `.panel-detail-modal__edit-section-heading` styles for the unified section separators
- `frontend/src/components/PanelDetailModal.test.tsx` — removed tab-switching tests; updated tests to use unified form; added 2.1–2.4 spec tests (all sections visible without tab bar, title pre-filled, unified save dispatches both sections, section-level inline error); changed `beforeEach` to `mockResolvedValue([])` to prevent crashes when edit mode auto-fetches data types
- `frontend/src/app/App.test.tsx` — updated button label `"Save panel style"` → `"Save panel settings"` to match unified Save button
- `frontend/src/components/ComputedFieldPicker.test.tsx` — removed Data tab click (unified form shows Data section without tab navigation)
