## Evaluation Report — Cycle 1

### Phase 1: Spec Review — PASS
Issues: none.

Verified against `ticket.md`, `proposal.md`, `design.md`, `tasks.md`, and all seven spec deltas:
- Collection panel type appears in the creation modal (card → template → DataType step → name), Metric-collection ships and renders correctly from a multi-row DataType, and the config shape is designed for future base types via `itemOptions.<baseType>` keying with no schema/migration needed for a new type (only a JSON Schema enum widen + code arms) — all DoD items satisfied.
- All 26 tasks.md items are checked and each one traced to a real diff hunk (backend V57 migration, `CollectionPanel.scala`, `Panel.Registry`, `PanelConfigCodec` 3 dispatch points, `PanelRepository`/`PanelRowMapper` both directions, the second `PanelType` enum in `model.scala` + `PanelServiceHelpers.buildNewPanel` + `DashboardServiceValidation` + `DashboardProposalService.DataPanelKinds`, full frontend model/state/rendering/editor chain, mobile height policy, CSS-lock tests).
- No AC silently reinterpreted; no scope creep — every touched file appears in `files-modified.md` and matches proposal.md's Impact section (checked `git diff --stat` against it 1:1).
- No regressions: `sbt test` (1382 tests) and `npm test` (1092 tests) both green; existing panel-kind match arms are only appended to, never altered.
- Schema (`schemas/panel.schema.json`) updated in the same change as the Scala/TS code that uses it; `npm run check:schemas` passes.
- Planning artifacts (design.md D1–D8, both skeptic design-gate reports) accurately describe the final implementation — re-verified the two round-1 skeptic Change Requests are genuinely fixed in code (see Phase 2).

### Phase 2: Code Review — PASS
Issues: none blocking.

- **Mechanical compliance**: `npm run check:scala-quality` clean (no inline FQN violations across any new/touched Scala file); `npm run lint` (ESLint zero-warnings) clean; `npm run format:check` implied clean via pre-commit parity. File-size soft budget: `CollectionEditor.tsx` is 285 lines (over the ~250 informational soft budget, well under the ~400 propose-a-split threshold) — non-blocking per CONTRIBUTING.md ("informational only").
- **DESIGN.md [mechanical]**: `CollectionRenderer.css` and the `PanelDetailModal.css` additions (`__segmented`/`__segmented-btn`) use only `--app-*`/`--space-*`/`--text-*`/`--weight-*`/`--control-*` tokens — no hardcoded hex/px-for-tokenized-values found (`frontend/src/features/panels/ui/renderers/CollectionRenderer.css:1-48`, `frontend/src/features/panels/ui/PanelDetailModal.css:424-465,671-678`). The 140px grid-track minimum is a documented layout constraint (no design token exists for grid-track widths), consistent with design.md's Planner Notes. Canonical breakpoint (`max-width: 768px`) reused, not a new one. Shared components reused throughout (`Select`, `InlineError`, `DataTypePicker`, `BoundOrLiteralField`/`useBoundOrLiteralState`, `MetricRenderer`) — no hand-rolled equivalents.
- **DRY**: Collection item rendering reuses `MetricRenderer` verbatim (`CollectionRenderer.tsx:67`); the editor reuses the entire HEL-243 bound-or-literal family and `DataTypePicker` rather than re-implementing binding UI.
- **Readable/Modular**: `CollectionRenderer.tsx` (72 lines) and `CollectionEditor.tsx` are each single-purpose; `CollectionPanel.scala` (200 lines) mirrors `MetricPanel.scala`'s shape exactly, as designed.
- **Type safety**: no `any` in new TS; `CollectionPanelConfig`/`CollectionItemOptions`/`CollectionMetricItemOptions` fully typed; Scala side uses `DataTypeId` value class (not raw `String`), consistent with CONTRIBUTING.md's ID-wrapping rule.
- **Security**: `layout` enum validated strictly on create/patch (`deserializationError` → 400, not silently coerced); JSON Schema `additionalProperties: false` + enum constraints on the wire shape.
- **Error handling**: tolerant read-path decode never throws (malformed/legacy `collection_options` → defaults, verified by `PanelRowMapperSpec`'s NULL-column test); editor save surfaces failures via `InlineError`, never silently swallowed.
- **Tests meaningful**: backend covers absent-field decode (fields ABSENT per spray-json's `Option=None` omission, not just null — `PanelSpec.scala:872-877` explicit comment + test), patch absent-vs-null, create→duplicate→read round-trip preserving `collection_options` in both directions, non-active-base-type-key preservation, codec dispatch, `PanelType` round-trip. Frontend covers N-row expansion, literal-override application, grid/list layout classes, unbound/no-data states, mobile height policy (explicit non-default-fallthrough case), CSS-lock ≥44px assertions, creation-modal flow, editor save-patch shape.
- **No dead code**: no TODO/FIXME/console.log in the diff; no unused imports (lint would have failed).
- **No over-engineering**: single new JSONB column, no premature abstraction beyond what the extensibility requirement (D2/D3) demands.
- **Behavior-preserving**: all 7 pre-existing panel-kind match arms are untouched aside from mechanical reformatting-for-alignment (e.g. `PanelConfigCodec.scala`, `DashboardSnapshotRepository.scala` gained a trailing new arm plus alignment whitespace on sibling lines) — no drive-by behavior changes detected in any pre-existing kind.

**Non-blocking suggestion**: no dedicated HTTP-level `ApiRoutesSpec` test exercises `POST /api/dashboards/import` with a `type: "collection"` panel end-to-end (only the generic "unknown type" rejection and non-collection import flows are covered there). The underlying `DashboardSnapshotRepository.scala` `CollectionCreate` arm is structurally verified via diff (mirrors all 7 sibling arms exactly) and the row-mapper round-trip is unit-tested; a manual export→import smoke test hit an unrelated 403 (looks like a CSRF/anti-forgery guard on raw `fetch`, not a collection-specific issue — the app's own axios client presumably attaches the required header/token). Recommend adding one `ApiRoutesSpec` case for completeness in a follow-up, not a blocker for this cycle.

### Phase 3: UI Review — PASS
Issues: none.

Dev servers started via `scripts/concertino/start-servers.sh` / `assert-phase.sh` (PASS) on ports 5420/8327. Built a fresh test dashboard end-to-end:

- **Happy path**: Creation modal → Collection card → template step (curated "Metric Collection" template + "Start blank" both present) → DataType step (required, confirmed collection is in `DATA_BOUND_TYPES`) → name → panel created unbound (correct placeholder: "Bind a data type to populate this collection"). Bound to a demo DataType with 0 rows first (correctly showed "No data available" — a genuine zero-row scenario in this dev DB, not a bug), then rebound to `AggregateResult` (5 rows): value=`profit`, label=`date` bound, unit=`"yr"` literal. Result: **5 rendered metric items**, each showing its own row's value/label, shared literal unit applied to all — confirmed via screenshot and via the raw `GET /api/dashboards/:id/panels` response (`config.itemOptions.metric.unit: "yr"`, `fieldMapping: {label: "date", value: "profit"}`).
- **Grid vs list layouts**: both verified visually — grid wraps to 2 columns at panel width, list shows a single column with hairline dividers; desktop overflow scrolls **inside the panel card** in list mode (confirmed via screenshot, scrollbar visible, card height fixed).
- **Duplication**: `Duplicate` action produced a byte-for-byte config copy (verified both visually — identical 5-item render — and via the panels API response: `dataTypeId`, `fieldMapping`, `baseType`, `layout`, `itemOptions` all identical between original and copy) — the PanelRowMapper both-directions guard holds under a real E2E flow, not just unit tests.
- **Unhappy paths**: unbound placeholder and bound-zero-rows "No data available" state both exercised live (the latter accidentally, via a real 0-row demo DataType) — no blank screens, no unhandled exceptions.
- **Mobile (390×844, read-only per HEL-304)**: Metric collection panels render legibly in the `MobilePanelStack` — grid layout showed a clean 2-column tile grid, list layout showed a single-column divided list, both intrinsic-height with no internal scroll (per `mobilePanelHeights.ts`'s explicit `collection` case). No horizontal overflow: confirmed both visually and programmatically (`document.documentElement.scrollWidth === clientWidth === 390`). New detail-modal controls measured via `getBoundingClientRect()` at 390px: layout segmented buttons, base-type Select, value/label/unit Selects, and bound/literal mode-toggle buttons **all render at exactly 44px height** — meets the ≥44px touch-target requirement. No config edits were saved below 768px (viewed the editor, then `Cancel` — consistent with the HEL-304 read-only constraint).
- **No console errors** across the entire session (0 errors, only pre-existing informational warnings).
- **Breakpoints**: 1440 and 1100 render cleanly (two-panel grid, no overlap); 768 correctly activates the mobile PWA shell (collapsed tap-to-expand cards) consistent with the pre-existing HEL-300/301/302 baseline — not a collection-specific regression; 390 clean at both grid and list layouts, zero horizontal overflow.
- **Accessible names/keyboard**: Base type control, Value/Label/Unit selects, Label/Unit mode-toggle groups, and the Grid/List layout group (`role="group"` + `aria-labelledby`) all exposed accessible names in the a11y snapshot; layout buttons use `aria-pressed`.
- Screenshots were taken to the repo root by the Playwright tool's relative-path default and were deleted immediately after each inspection per the operational-hygiene rule (never left in the repo).

Mandatory emphasis items (from the user, treated as acceptance criteria) — all confirmed:
1. Mobile first-class: (a) legible multi-row rendering ✓, (b) ≥44px touch targets (CSS + measured + CSS-lock test) ✓, (c) no horizontal overflow at 390px including grid layout ✓.
2. Desktop pass: creation flow, multi-row expansion (1 row = 1 item), grid AND list layouts, literal label/unit overrides, unbound + no-data states, duplication preserving config — all verified live.
3. DESIGN.md compliance for new frontend surfaces — verified (tokens, canonical breakpoint reuse, shared-component reuse).
4. Round-1 skeptic findings genuinely fixed in code — `model.scala` `PanelType.Collection` + `fromString`/`asString`, `PanelServiceHelpers.buildNewPanel`'s `CollectionCreate` arm, `DashboardServiceValidation` (via shared `PanelType.fromString`), `DashboardProposalService.DataPanelKinds` gaining `"collection"` — all confirmed present in the diff; `usePanelData.ts`'s `case "collection" => 50` confirmed (not the silent 10-row `else`).
5. spray-json absent-field decode tests exist — `PanelSpec.scala` explicitly tests fields ABSENT (not just `null`), with a comment calling out the spray-json `Option=None` omission behavior.

### Overall: PASS

### Non-blocking Suggestions
- Add an `ApiRoutesSpec` HTTP-level test exercising `POST /api/dashboards/import` with a `type: "collection"` panel for full end-to-end coverage of the import path (currently covered at the unit level via `PanelRowMapperSpec` and structurally via diff review of `DashboardSnapshotRepository.scala`'s `CollectionCreate` arm, but not at the HTTP-integration level).
- `CollectionEditor.tsx` (285 lines) is over the ~250-line soft budget; consider extracting the item-fields section into a small sub-component if it grows further — not required now.
- Minor scalafmt alignment drift on sibling match arms in `PanelConfigCodec.scala` and `DashboardSnapshotRepository.scala` (cosmetic column-alignment only, from appending a longer new arm) — a formatting pass would tidy this but does not affect behavior or the `check:scala-quality` gate.
