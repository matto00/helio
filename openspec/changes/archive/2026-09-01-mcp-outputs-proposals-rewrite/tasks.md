## 1. Backend — proposal & patch-set contracts

- [x] 1.1 Retarget `schemas/dashboards/dashboard-proposal.schema.json` and `schemas/pipelines/pipeline-proposal.schema.json` onto pipelines (steps+outputs) / dashboards (placements+content panels) — `pipeline-proposal.schema.json` retargeted @ 9c4263cc (this branch); `dashboard-proposal.schema.json` verified this cycle to ALREADY be fully Output-based -- correction to cycle 8's own conclusion: it was retargeted by HEL-904 (already on `main` before this branch existed), not by any commit on this branch, which is why `git log main..HEAD` found nothing and cycle 8 wrongly read that as "not done". Re-read the schema file directly this cycle to confirm.
- [x] 1.2 Retarget `schemas/patch-sets/*` onto node/output/placement operations, preserving `PipelineStep.enabled` (Outputs have no `enabled` field) and any placement `enabled`-equivalent field — [x] @ 30cd6f4a
- [x] 1.3 Rewrite `DashboardProposalService`/`PipelineProposalService`/`CombinedProposalService` for the new schemas — `PipelineProposalService`/`CombinedProposalService`'s `$pipelineOutput` sentinel @ 9c4263cc (this branch); `DashboardProposalService`/`ProposalPanelSupport` verified this cycle to ALREADY be fully Output-based (HEL-904 task 3.8/3.9/4.1, already on `main`) -- same correction as 1.1 above.
- [x] 1.4 Ground Output `fieldMapping` validation: step-targeted Outputs via `PipelineAnalyzeService.analyzeNodes` at their specific node (not the trunk); source-attached Outputs (`nodeStepId: null`) via the source's own `inferredSchema` directly, since `analyzeNodes` omits the source — @ eec9c5d4
- [x] 1.5 Fix `PatchSetApplyRollback.scala`'s `fullPipelineStepInverse`/`pipelineStepCreateRequestFromPrior` (HEL-766): both currently omit `enabled` (silently taking `CreatePipelineStepRequest.enabled`'s `None` default) AND `pipelineStepCreateRequestFromPrior` also omits `parentStepId` (silently reparenting a recreated step under the HEL-904 tree model — structural tree corruption, worse than the boolean loss). Fix both builders to explicitly set `enabled = prior.enabled` in both, and `parentStepId = prior.parentStepId` in `pipelineStepCreateRequestFromPrior` only — `UpdatePipelineStepRequest` (used by `fullPipelineStepInverse`) has no `parentStepId` field to set; verify placement rollback/recreate similarly preserves its own fields if applicable — @ c40d9242
- [x] 1.6 Re-verify HEL-670 (chart-create-with-implied-output mistargeting) against the new model; fix if reproducible (HEL-670's own report notes it occurred in 1 of 3 live-LLM trials — non-reproduction in a bounded number of manual attempts does not close this task; the regression test in 5.11 is required unconditionally regardless of whether 1.6 reproduces the bug) — @ eec9c5d4
- [x] 1.7 Split `WorkspaceContextService` by resource; do not alter `asNumeric`'s filter structure or `BigDecimal.setScale` rounding — this cycle: extracted `WorkspaceContextComputations` (mechanical, verbatim move -- every moved method/constant unchanged) from the 923-line `WorkspaceContextService.scala`, now 427/536 lines across the two files (still over the 250-line soft budget but a real reduction, a defensible stopping point per the peer's own "stop at a safe boundary" allowance rather than risk a finer split for a non-blocking warning). Verified behavior-preserving via `git stash`-based A/B: the 8 existing WorkspaceContextService specs (136 tests) pass IDENTICALLY before and after (diffed the two full test-name listings -- byte-identical), plus the FULL backend suite (3517 tests, 235 suites) green after. `WorkspaceContextService`/`WorkspaceContextProtocol` still carry the pre-HEL-907 `dataTypes`/`outputDataTypeId`/`outputDataTypeName` shape (structural-parity gap against the MCP-side `context.ts` retargeted in 3.8 -- NOT this task's scope, task 1.7 as worded is a pure file-size split, not a retarget; flagged, not silently left inconsistent).
- [x] 1.8 Close the two HEL-829/HEL-848 review-UI loose ends as part of the review-page data contract rewrite -- rescoped by the peer to exactly section 4 (tasks 4.1/4.2) after HEL-936 absorbed the broader ~18-file frontend migration (TypeRegistryPage/TypeDetailPage/dataTypesSlice, panel editors, PipelineDetailPage, metrics/sources pages) found in cycle 12's investigation. This cycle: retargeted `ProposalReview.tsx`/`ProposalReviewPage.tsx` AND (found necessary once inside the pipeline/combined review pages, same "pipeline-proposal, combined" review pages the ticket text itself names) `PipelineProposalSummary.tsx`/`PipelineProposalReviewPage.tsx`/`CombinedProposalReview.tsx`/`CombinedProposalReviewPage.tsx`/`types/pipelineProposal.ts` off the retired DataType/`outputDataTypeName`/dead-panel-kind (metric/chart/table) shapes onto real Outputs -- see 4.1/4.2 below and files-modified.md for the full list. `PatchSetReview(Page).tsx` needed no changes (already Output-based from earlier cycles' patch-set work).

## 2. Backend — teardown & docs

- [x] 2.1 Extend `teardown_resources`-backing tag-cascade so deleting a tagged pipeline removes its Outputs and their placements — @ 0843409b
- [x] 2.2 Update `docs/agent-native.md` with the tool rename table (old name -> new name / removed) — this cycle: full rewrite, not just a bolted-on table -- the canonical-path diagram, binding-rule section, endpoint->tool map, and "End-to-end proof" narrative were ALL still describing the retired DataType model too, so fixed all of it, not just added the rename table. New finding surfaced while doing this: `scripts/agent/*.sh` and `helio-mcp/scripts/compose.ts` are ALSO stale (call the same dead `GET /api/types` route / retired `create_panel`/`bind_panel` tools) -- flagged in the doc, not fixed (separate, unscoped work). **evaluator-1 CR2 (cycle 17): the helio-mcp-side gap closed for real** -- `scripts/compose.ts` and `scripts/verify-bound-panel.ts` were premised entirely on retired tools with no salvageable role and were deleted outright (plus their `npm run compose`/`npm run verify-bound-panel` package.json scripts); `scripts/verify.ts` retained its valid read-tool-verification role and was retargeted onto the current tool surface (`list_data_types`->`list_outputs`, `get_data_type_rows`->`get_output_rows`, the `create_pipeline_from_shape` test block->`add_outputs_from_shape`); `docs/agent-native.md`'s "End-to-end proof" section and `helio-mcp/README.md`'s "End-to-end composition" section updated to match. `scripts/agent/*.sh` remains genuinely out of scope (a separate, unrelated shell-script surface, not part of this ticket's helio-mcp/backend work) and stays flagged STALE in the doc, not fixed.

## 3. helio-mcp — decomposition & tool rewrite

- [x] 3.1 Decompose `write.ts`/`helioApi.ts`/`context.ts` into resource-scoped modules as the rewrite proceeds (absorbs HEL-882/658/648); confirm the verified scoped helio-mcp jest command (task 5.9 below; root `npm test` does NOT cover helio-mcp inside this worktree) imports every module without OOM (HEL-647) -- this cycle: re-verified the OOM-free import (18/18 suites, 181/181 tests, no OOM) across the now-decomposed set (`outputs.ts`/`pipelines.ts`/`placements.ts`/`server.ts` split out across cycles 6-9, `context.ts` slimmed in cycle 8). `write.ts` (792 lines)/`helioApi.ts` (1031 lines) remain large -- no TS-side file-size check enforces a budget on them the way `check-scala-quality.mjs` does for Scala, and further splitting is a soft-nice-to-have, not what this task's own text ("confirm... imports every module without OOM") actually requires.
- [x] 3.2 Rewrite `create_pipeline` (sourceId or inline source, `steps[]` with `parentStepId`, optional `outputs[]`); for an inline source, issue `POST /api/data-sources` then `POST /api/pipelines` with the resulting id as `sourceDataSourceId` (the backend route requires an existing id, `additionalProperties: false` — no inline-source arm exists there); on the second call failing, surface the orphaned data-source id in the tool's error — @ 355ff4a2
- [x] 3.3 `add_pipeline_step` gains `parentStepId` — @ 355ff4a2
- [x] 3.4 `create_pipeline_from_shape` -> `add_outputs_from_shape(pipelineId, stepId?, shape, params)` — @ 34d348af
- [x] 3.5 New tools: `add_output`, `update_output`, `delete_output`, `list_outputs`, `get_output_rows`, `preview_outputs(pipelineId, outputId?)` — @ 3145c81a
- [x] 3.6 `create_panel`/`create_panels`/`bind_panel`/`create_bound_panel` -> `place_outputs(dashboardId, [{outputId,title?,w?,h?}])` + `create_content_panel`; `update_panel` keeps placement fields only — @ f8422829
- [x] 3.7 `get_panel_capabilities` -> `get_output_capabilities(pipelineId, stepId?)` — @ 3145c81a
- [x] 3.8 Rewrite `get_workspace_context`: drop types/metrics, list pipelines with outputs (kind, schema, placements) and sources with `inferredSchema` — this cycle (see files-modified.md); backend-side `WorkspaceContextService`/`WorkspaceContextProtocol` structural-parity companion deferred to 1.7 (still open)
- [x] 3.9 Remove (no aliases): `list_data_types`, `update_data_type`, `delete_data_type`, `get_data_type_rows`, `list_metrics`, `get_metric`, `create_metric`, `update_metric`, `delete_metric`, `bind_panel`, `create_bound_panel`, `get_panel_capabilities`; delete `metricSchemas.ts` — this cycle (`bind_panel`/`create_bound_panel`/`get_panel_capabilities` already removed in 3.6/3.7; the remaining 9 tools + `metricSchemas.ts` removed this cycle, plus their now-dead `HelioApi` methods/types.ts interfaces)
- [x] 3.10 Update `proposal.ts`/`combinedProposal.ts` MCP-side handlers for the new schemas (paired with task 1.1-1.3) — pipeline-proposal MCP-side @ 9c4263cc; `combinedProposal.ts`'s dashboard half was already correct (no `listDataTypes()`/DataType dependency found there); `proposal.ts`/`proposalValidation.ts` (the actual gap -- still calling `GET /api/types`, a route HEL-904 deleted outright, causing a LIVE 404 in `propose_dashboard`) retargeted onto Outputs this cycle.
- [ ] 3.11 Re-key same-tab invalidation (`markDataTypeRowsStale`) by output id — INVESTIGATED this cycle, deferred to HEL-936: the dispatch site is `PipelineDetailPage.tsx` (`dispatch(markDataTypeRowsStale(outputDataTypeId))`), a file HEL-936 already carved out (cycle 12's finding, cycle 13's explicit carve-out) since it's broken beyond just this one call site (`outputDataTypeId` there depends on the same broken frontend `pipelinesSlice.ts` machinery HEL-936 covers). Re-keying this ONE dispatch site in isolation, without fixing the rest of that page, would not be a coherent, independently-verifiable slice -- correctly belongs with the rest of `PipelineDetailPage.tsx`'s HEL-936 work, not split out separately.
- [x] 3.12 Absorb HEL-934's helio-mcp-side consumers of `{steps, outputs?}` expand envelope and 200-body `DELETE /api/pipeline-steps/:id`; comment on HEL-934 confirming closure — this cycle, a REAL live bug fixed, not just a docs/types exercise: `helioApi.ts`'s `expandPipelineShape` was typed AND parsed as if the real wire response were a bare `ShapeStepExpansionResponse[]` array, when it is actually `ExpandPipelineShapeResponse` (`{steps, outputs?}`) -- every caller (`addOutputsFromShapeHandler`) iterating the result directly would have thrown at runtime the first time a shape expanded to any steps at all. Fixed by unwrapping `.steps` inside `expandPipelineShape` itself (external contract unchanged, so no caller needed to change). `deletePipelineStep` also fixed: it discarded the real `200 {removedTailStepCount}` splice-on-delete body entirely, now surfaced in its return value and the tool's own docstring. New `helioApi.test.ts` coverage exercises the REAL HTTP-layer parsing (not a mocked `HelioApi`, which is what the existing `pipelinesHandlers.test.ts` coverage used and which never touched this bug at all). Executor has no Linear write access -- the HEL-934 closure comment itself is left for the peer to post (see execution-progress.md for the exact text).
- [x] 3.13 Handle every spray-json-omitted-`Option` field (`outputs?`, `nodeStepId?`, `parentStepId?`, etc.) as possibly-absent, never `=== null`, across all touched TS client code — this cycle: ran an explicit repo-wide audit (`grep` for `.nodeStepId ===`/`.parentStepId ===`/`.sourceId ===`/raw non-normalized reads of each) across every helio-mcp file this ticket touched. Found zero violations -- every direct-comparison/display use already normalizes via `?? null` (`context.ts`'s `nodeStepId: o.nodeStepId ?? null`); every other occurrence is either an outbound-request field (where `undefined` = absent IS the correct value to send, not a bug) or prose/docstring text. The discipline held; this closes the task as verified, not just practiced ad hoc.

## 4. Frontend — review pages

- [x] 4.1 `ProposalReviewPage` and sibling patch-set/pipeline-proposal/combined review pages render Output previews instead of "panel bound to type X" -- this cycle: `ProposalReview`/`ProposalReviewPage`, `PipelineProposalSummary`/`PipelineProposalReviewPage`, `CombinedProposalReview`/`CombinedProposalReviewPage` all retargeted; new `outputsService.ts` (fetchOutputs, paginated) added under `dashboards/services/` for `ProposalReviewPage`'s demo-fixture + binding-name resolution. `PatchSetReview(Page)` already Output-based, no change needed. `types/pipelineProposal.ts` retargeted onto the backend's `CreatePipelineTransactionalStepRequest`/`CreatePipelineTransactionalOutputRequest`/`ProposalOutputSummary` shapes (`clientId`/`parentStepId` on steps, `outputs[]` replacing `outputDataTypeName`).
- [x] 4.2 `replace_dashboard_contents`/`auto_layout_dashboard` consumers accept placements (`outputId`) instead of bindings — this cycle: verified both were ALREADY structurally correct (`replace_dashboard_contents` reuses `panelSchema`/`ProposalPanel` from `proposal.ts`, retargeted onto Outputs in cycle 10; `auto_layout_dashboard` is pure `{panelId,w,h}` geometry with no binding concept at all, never needed a change) -- only their docstrings still said "V41 pipeline-only binding"/"Create + bind panels first", fixed to name Output binding and `place_outputs`/`create_content_panel`.

## 5. Tests

- [x] 5.1 MCP E2E: rebuild the four Sleeper dashboards via `create_pipeline` + `place_outputs` from a clean workspace with a daily schedule read back; commit under `e2e/` or `helio-mcp/e2e/` -- this cycle: `helio-mcp/e2e/sleeper-rebuild.ts` (added cycle 15) run for REAL against THIS worktree's own dedicated backend/DB (`localhost:9246`, `scripts/concertino/start-servers.sh`'s isolated per-worktree ports -- `DEV_PORT=6339`/`BACKEND_PORT=9246` per `workflow-state.md`, confirmed independently before use, not just trusted from the peer's message). All four dashboards (League Rosters/Weekly Matchups/League Standings/Waiver Transactions) built successfully end-to-end: `create_pipeline` (single call, inline source + `outputs[]`) -> `run_pipeline` -> `set_pipeline_schedule`/`get_pipeline_schedule` (daily cron, read back byte-identical) -> `place_outputs`. Re-ran a second time to prove the tag-based teardown-then-rebuild path is genuinely idempotent (fresh ids both times, no leftover-resource collision). Ran `--cleanup-only` afterward to leave this worktree's DB clean. Minted/used/revoked a throwaway PAT (`e2e-sleeper-rebuild`) for the run, cookie jar discarded after.
- [x] 5.2 `get_workspace_context` fixture test: 25 sources / 43 pipelines, assert result under the MCP result cap — this cycle, `context.test.ts` (see files-modified.md); HEL-865 (Linear) NOT updated this cycle — no Linear-write tool available in this session, flagged as a follow-up for whoever has Linear access
- [x] 5.3 Exact-tool-name-set test asserting removed tools are absent — this cycle (`server.test.ts`, new; in-process MCP client over `InMemoryTransport` asserts every one of the 15 removed tools across this ticket is absent and no duplicate name exists)
- [x] 5.4 Proposal grounding test: Output on a tail validated against the tail's projected schema, not the trunk's; plus a source-attached Output (`nodeStepId: null`) validated against the source's `inferredSchema` — @ eec9c5d4 (`OutputBindingSpecSpec.scala`/`RefinementEditShapeSpec.scala`)
- [x] 5.5 Patch-set undo test: add/remove/modify of a PipelineStep (covering `PipelineStep.enabled` AND `parentStepId` preserved through rollback/recreate — both the HEL-766 defect and the tree-corruption defect found alongside it) and of a placement (whatever fields it carries, if any) through both directions — this cycle, closing the real gap: verified there is no `create` edit kind for `pipelineStep` at all (`EditTarget`/patch-set schema have no parent-id field to target one, matching the pre-existing `Output` precedent), so the full matrix for `pipelineStep` is exactly `update` (5.3a, pre-existing) + `delete`-undo-recreate with `enabled`/`parentStepId` preserved (5.3c, pre-existing, @ c40d9242) -- nothing was missing there. The REAL gap was placement-field preservation: the pre-existing panel create/delete-undo test (5.3b) only ever exercised a `divider`-kind panel, never an `output`-kind one, so `OutputPanelConfig.outputId` (the ONE field a placement actually carries beyond the common identity/appearance fields every Panel has) had never been proven to survive either direction of undo. Added a new test exercising BOTH a create edit's undo (delete) and a delete edit's undo (recreate) for a real, DB-persisted `output`-kind panel -- discovered along the way that `panels.output_id` is FK-constrained against a real `outputs` row (an `outputRepo == null` fixture only skips the APP-level existence check, never the DB-level FK), so the test needed a genuinely seeded `Output` via a new `outputRepo`/`seedOutput` wired into this spec's fixture, not a synthetic id. All 11 tests in `PatchSetUndoServiceSpec.scala` (10 pre-existing + 1 new) pass.
- [x] 5.6 helio-mcp typecheck gate green; the verified scoped helio-mcp jest command (task 5.9) imports every decomposed module without OOM — root `npm test` does not exercise helio-mcp inside this worktree and must NOT be used as evidence here — re-verified green this cycle (`npx tsc --noEmit` clean, scoped jest 18/18 suites, 181/181 tests, no OOM); closing as formally verified per task 3.1's same evidence.
- [x] 5.7 `check-schema-drift.mjs` green with proposal + patch-set schemas and both service sides changed together — re-verified this cycle via the commit's own pre-commit hook run (green); no schema file has been touched since cycle 3/4's paired schema+service commits, and drift has stayed green at every commit since (Decision 4's hard constraint), so this closes as continuously verified rather than a one-off check.
- [x] 5.8 `teardown_resources` by tag test: removes a pipeline's Outputs and their placements — @ 0843409b
- [x] 5.9 Run the verified scoped helio-mcp test command every cycle (root `npm test` finds ZERO helio-mcp tests INSIDE THIS WORKTREE ONLY — `jest.config.cjs` excludes `/.claude/worktrees/` from both `testPathIgnorePatterns` and `modulePathIgnorePatterns`, a worktree-execution constraint, not a config-correctness one): `cd helio-mcp && npx jest --rootDir . --config '{"preset":"ts-jest","testEnvironment":"node","testMatch":["**/?(*.)+(spec|test).[tj]s?(x)"],"moduleNameMapper":{"^(\\.{1,2}/.*)\\.js$":"$1"},"testPathIgnorePatterns":["/node_modules/","/dist/"]}'` — confirm all suites green, none skipped, no OOM (proves HEL-647; the suite count may legitimately grow past today's 14 as decomposition adds files — a higher count is not a failure) — run and green this cycle (17 suites, 190 tests). **evaluator-final round 3 correction**: this command was, until cycle 23, ALSO silently masking a real bug — root `jest.config.cjs`'s (unfixed, pre-cycle-23) ts-jest compiled `helio-mcp/src/**` under the ROOT `tsconfig.json`'s classic Node10 `module: "commonjs"`/`moduleResolution: "node"` settings, a resolution mode `helio-mcp` never actually builds or runs under (its own `tsconfig.json` is `NodeNext`/`NodeNext`, matching its real ESM package.json), which tripped a genuine `TS2589` "excessively deep" instantiation in `tools/read.ts` that `server.test.ts` — the FIRST test ever to import that file — silently failed to compile under (0 of 4 tests executed, "Tests: 0 total", not caught as a failure by three prior evaluation rounds because this scoped command's own NodeNext config never hit it). Fixed by making root `jest.config.cjs` type-check under NodeNext too (cycle 23, see below) — this scoped command is now genuinely just the worktree-execution workaround it always claimed to be, not simultaneously covering for a config divergence CI never had a chance to catch.
- [x] 5.10 Run backend suite with `sbt -batch 'set Test/parallelExecution := false' test`; classify any failures by isolation before reporting — this cycle, fresh (several backend files have changed since cycle 11's own clean run, most recently this cycle's own `PatchSetUndoServiceSpec.scala` addition): **3517/3517 tests, 235 suites, 0 failures.**
- [x] 5.11 Add the HEL-670 regression test (chart-create-with-implied-Output must not mistarget a follow-up edit) unconditionally, independent of whether task 1.6 reproduces the original defect live — independently re-verified this cycle: `RefinementEditShapeSpec.scala` carries an explicit `"HEL-670: create-edit-then-follow-up-edit shape (protocol layer)"` describe block (a deterministic, code-level regression proving a create edit's target is never fabricated/aliased onto a sibling edit's target -- not a live-LLM reproduction, since HEL-670's own report was 1-of-3 stochastic). Prior cycle's claim confirmed true, not just repeated.

## 6. Cycle 17 — evaluation-1.md change requests (FAIL -> re-verified)

- [x] 6.1 CR1: re-ran the evaluator's class-sweep grep (`create_panel|create_panels|bind_panel|create_bound_panel|get_panel_capabilities|create_pipeline_from_shape|list_data_types|get_data_type_rows|(create|update|delete|list|get)_metric`) over `helio-mcp/src/**` and triaged every hit; fixed all LIVE agent-facing guidance (not just the 3 cited): `write.ts` (`create_dashboard`, `teardown_resources`), `read.ts` (`list_pipeline_shapes`, 3 occurrences), `proposal.ts` (`propose_dashboard`/`apply_proposal` description strings), `context.ts` (a `WorkspaceContext` doc comment), plus an independently-found stale `TeardownResponse.typesDeleted` field in `types.ts` (dead since HEL-904, not evaluator-cited). Tombstone comments (fine, left as-is) distinguished from live guidance throughout.
- [x] 6.2 CR2: `helio-mcp/scripts/compose.ts` and `helio-mcp/scripts/verify-bound-panel.ts` deleted outright (premised entirely on retired tools, no salvageable role) plus their npm scripts; `helio-mcp/scripts/verify.ts` retargeted onto the current tool surface (its read-tool-verification role stays valid); `helio-mcp/README.md` (`teardown_resources` row + "End-to-end composition" section) and `docs/agent-native.md` ("End-to-end proof" section) updated to match — no fix left silently stale with no ticket owning it.
- [x] 6.3 CR3: `create_dashboard` gained a real `tag` param (new `dashboards.tag` column, V95 migration, extends HEL-366's tag-teardown system); `teardown_resources` now also deletes tagged dashboards (`WorkspaceTeardownRepository`/`Service`, `dashboardsDeleted` in the response); `helio-mcp/e2e/sleeper-rebuild.ts` passes `tag: E2E_TAG` on `create_dashboard` and its header comment corrected (was falsely claiming teardown reclaimed dashboards when `create_dashboard` had no tag param at all); the 8 real orphaned dashboards the evaluator found in the shared dev Postgres (left by cycle 16's own run) were individually verified by id/name/timestamp then deleted via `DELETE /api/dashboards/:id` (all 204).
- [x] 6.4 CR4: `server.test.ts`'s tool-list test now asserts an EXACT set (`expect([...names].sort()).toEqual([...EXPECTED_TOOL_NAMES].sort())` against the full enumerated 60-tool list), not just `not.toContain`/`arrayContaining`/a duplicate check — an accidentally re-added/renamed tool now fails the test, not just a removed one.
- [x] 6.5 CR5: `docs/agent-native.md`'s "End-to-end proof" section no longer falsely states the live Sleeper composition proof is "still open work" — rewritten to describe the real cycle-15/16 live-backend run (task 5.1), consistent with tasks.md's own closed status.
- [x] 6.6 CR6: `helio-mcp/src/types.ts`'s `ProposalPanel` doc block rewritten to correctly frame metric/chart/table/collection/timeline as HEL-904-retired legacy fields and to state `config.dataTypeId` on text/markdown is silently inert (never a real binding attempt), matching what `dashboard-proposal.schema.json` already said — was previously self-contradictory with the schema it documents.
- [x] 6.7 Non-blocking suggestion: added `"testPathIgnorePatterns":["/node_modules/","/dist/"]` to the verbatim scoped helio-mcp jest command in `ticket.md`/`design.md`/`tasks.md` (task 5.9) — verified with `helio-mcp/dist/` actually present (18/18 suites, 181/181 tests, zero spurious `dist/*.test.js` failures).

## 7. Cycle 20 — evaluation-2.md change request (FAIL -> fixed)

- [x] 7.1 `DashboardService.create` accepted `request.tag` without gating it through `RequestValidation.validateTag` first, unlike every sibling tag-bearing service (DataSourceService's 6 call sites, PipelineService:114) — an over-length tag on `POST /api/dashboards` hit V95's `CHECK (length(tag) <= 200)` at the DB and surfaced as a raw 500 instead of a curated 400. Fixed at the ROUTE layer (`DashboardRoutes.scala`, matching this file's own existing `offset < 0` curated-400 pattern) rather than widening `DashboardService.create`'s `Future[(Dashboard, Boolean)]` signature (no `Either`/`ServiceError` convention at all — a breaking ripple through `DashboardProposalService`/`PatchSetApplyForward`/every other caller for one field). New test in `ApiRoutesSpec.scala` exercises the REAL REST boundary directly (not the MCP client, whose zod schema already caps `tag` at 200 chars and so can never express the over-length input) — asserts 400, not 500.
- [x] 7.2 Peer addendum: audited every OTHER field V95 or this ticket introduced across any service for the same unguarded-REST-boundary shape. Found exactly one other new length-constrained free-text column this ticket added (`outputs.tag`, V94) but confirmed it has NO live write path anywhere — both `insertInternal` call sites (`DemoData.scala`, `OutputService.scala`) and the `create_pipeline` transactional path (`PipelineService.scala`'s `outputRepo.insertInternalAction` call) all use the `tag = None` default; nothing sets it from any REST/MCP-exposed input. Not the same class of bug (no attacker-controlled reachability) — no fix needed there.
- [x] 7.3 Non-blocking note 1: documented the `ifExists`+`tag` interaction in `create-dashboard-request.schema.json` — `tag` only applies on the fresh-insert branch, never used to retag an `ifExists: "return"` match.
- [x] 7.4 Non-blocking note 2: removed `TeardownOutcome.dashboardsDeleted`'s dead `= 0` default — confirmed (via a repo-wide grep) this class has exactly ONE construction site (the `teardown()` `yield`), which always sets it explicitly; the "source compatibility for positional construction" rationale that justified the default on the sibling `deletedSources` field never applied here.

## 8. Cycle 21 — final-gate 4-way skeptic fan-out (round 1 of 2): 1 CONFIRM, 3 REFUTE, all fixed

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools,2-proposal-contract,3-deletion-sweep,4-wire-contract}.md`.

- [x] 8.1 MCP-tools dimension: `create_rest_data_source`/`create_sql_data_source` always claimed `dataType: null` (dead since HEL-904 — the real field is `inferredSchema`, a sibling of `source`, never nested inside it) and silently dropped `inferredSchema`/`rowCapNotice` entirely. Fixed `CreateSourceResult`/`RawCreateSourceResponse` (types.ts/helioApi.ts) to surface `inferredSchema`/`rowCapNotice` for real, and both tool descriptions. Peer's follow-up ask ("check every OTHER source-creating tool for the same shape") swept: `create_data_source`/`create_csv_data_source` never claimed a `dataType` return field (not the same defect), but their descriptions DID carry the same stale "auto-created companion DataType" prose and — worse — `create_data_source`'s description had a genuinely wrong copy-paste-residue sentence ("HEL-907: this tool creates the pipeline with ZERO Outputs") describing behavior that belongs to `create_pipeline`, not this source-only tool; fixed both, plus `run_pipeline`/`delete_dashboard`/`delete_panel`'s remaining stale "DataType" prose found in the same file during the sweep (write.ts's header docstring included). Non-blocking cleanup done as part of the same pass: deleted the fully dead `listDataTypes()` method (zero call sites since cycle 10, calling a route HEL-904 deleted outright) and its now-unused `DataTypeResponse` import.
- [x] 8.2 Proposal-contract dimension (test-validity defect, escalated by peer addendum into a real live bug): the HEL-766 regression test exercised `PatchSetUndoService`, but its fixture branched off the pipeline's ONLY trunk step — coincidentally identical to `addStep`'s own default trunk-append target, so the test passed whether or not `parentStepId` was actually threaded through the undo path. Investigating further (peer's critical addendum) found the REAL, live bug: `PatchSetUndoInverse.pipelineStepCreateRequestFromResponse` never read `parentStepId` at all, because `PipelineStepResponse` (the wire shape `priorState` snapshots into the journal) never carried a `parentStepId` field in the first place — a genuinely deeper root cause than a missing read; a branched step's delete-undo silently re-parented it onto the trunk in production. Fixed: added `parentStepId: Option[String]` to `PipelineStepResponse`'s sealed trait + all 23 per-kind subtypes + `fromDomain` + bumped every `jsonFormat7`->`jsonFormat8` (`PipelineStepProtocol.scala`); `PatchSetUndoInverse.pipelineStepCreateRequestFromResponse` now reads and threads it. Swept every `*CreateRequestFrom*` builder across both files (4 total: 2 panel, 2 pipelineStep) — panel builders have no parentStepId-equivalent concept (not applicable); `PatchSetApplyRollback.pipelineStepCreateRequestFromPrior` was already correct. Rewrote the HEL-766 test's fixture (3-step pipeline, branch off the non-trunk-last step, so the default-trunk-append target and the real branch-point target structurally diverge) and added a SIBLING test exercising `PatchSetApplyRollback`'s own inverse-builder directly (a mid-apply rollback, not a post-application undo — the peer's original ask, distinct code path). BOTH tests verified failable by mutation (reverted each fix locally, confirmed red with the real assertion-diff pasted in the report below, restored, confirmed green) — not just asserted to be sound.
- [x] 8.3 Proposal-contract dimension: `combinedProposal.ts`'s `apply_combined_proposal` description told agents the `$pipelineOutput` sentinel could go on `metric/chart/table/collection/timeline` panels' `dataTypeId` OR a non-data panel's `config.dataTypeId` — the real `PANEL_TYPES` enum is `["text","markdown","image","output"]` (zod rejects all five listed kinds outright), and `ProposalPanelSupport.bindingCandidate` (HEL-904 task 4.1) reads ONLY the flat `dataTypeId` field, never `config.dataTypeId` — the exact same defect class already fixed in `RefinementEditShape` (cycle 5). Rewritten to name the one real blessed slot (`dataTypeId` on an `output`-kind panel) and state plainly that `config.dataTypeId` is never read for binding on any panel kind.
- [x] 8.4 Same finding as 8.3, filed by the wire-contract dimension as its own item: the `config.dataTypeId` sentinel arm was self-contradicting `proposal.ts`'s own accurate text ("a config.dataTypeId/dataTypeId on a text/markdown panel is silently inert, never a real binding") and the schema it's supposed to mirror. Resolved by the same 8.3 fix (removed the arm entirely rather than leaving dead, contradictory guidance) — no separate code change needed.
- [x] 8.5 Wire-contract dimension: `DashboardResponse` gained a `tag` field (cycle 17/20's V95 work) but `schemas/dashboards/dashboard.schema.json` still omitted it under `additionalProperties: false` — a declared contract forbidding a field the backend now actually sends. `check-schema-drift.mjs`'s `SKIP` set includes `"Dashboard"` (this schema is hand-composed from `Dashboard`+`DashboardLayout`+meta, not a 1:1 case-class match the automated checker can verify), which is exactly why the drift gate never caught it. Added `tag` to the schema, matching `DashboardResponse`'s real field list exactly. HEL-928 comment text for the peer to post: see execution-progress.md.
- [x] 8.6 Non-blocking note 2: added a cross-owner ACL test for `PipelineService.validateStepCrossOwnerRefs` (the `create_pipeline` single-call transactional path's own version of the sibling `addStep` path's join/union/lookup cross-owner check, `PipelineStepRoutesSpec.scala`'s six tests) — had zero direct coverage of its own despite being the identical security-relevant check. New reject+accept pair in `PipelineCreateTransactionalSpec.scala`.

## 9. Cycle 22 — final-gate round 2 (targeted re-check): 2 dimensions REFUTE on NEW findings, both fixed

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools-round2,2-proposal-contract-round2}.md`. Wire-contract dimension CONFIRMed cleanly (no action). Proposal-contract's cycle-21 `parentStepId` fix was independently re-verified by the skeptic's own fresh mutation run (two distinct failures, one per file/code path) — genuinely solid, no further action there.

- [x] 9.1 MCP tools CR1-6 (a sibling instance of round 1's class, on `run_pipeline` this time — the "sweep the class" ask from cycle 21 stopped one file short): `PipelineSummaryResponse`/`PipelineAnalyzeResponse` (types.ts) had `outputDataTypeName`/`outputDataTypeId` fields that haven't existed on the wire since HEL-904 (`PipelineProtocol.scala`'s real `jsonFormat9` has exactly 9 fields, none of them these); `RunOutcome`/`runPipeline` (helioApi.ts) mapped `outputDataTypeId` from that dead field, so it was `undefined`, silently dropped by `JSON.stringify` — an agent-visible defect: `run_pipeline`'s own description (write.ts) explicitly enumerated the field in its "Returns { ... }" list, so an agent reading the description would expect a key that never actually arrives. Root cause of why nothing caught it: `runPipelineTruncation.test.ts`'s fake `get` fixture itself asserted the dead field, exactly the same "evidence-shaped non-evidence" class as the earlier `expandPipelineShape` bug — fixed to mirror the real 9-field shape via a shared `fakeSummary: PipelineSummaryResponse` const (typed, so a future field drift on either side fails typecheck, not silently). All 6 CR locations fixed (types.ts x2, helioApi.ts x2 incl. 4 stale DataType docstrings, write.ts, the test fixture) plus `scripts/verify.ts`'s own unused inline type annotation for the same dead field, found during the "one more full pass" the peer required. Also fixed a now-vacuous `context.test.ts` regression test whose premise (constructing input fixtures carrying the dead fields) became structurally impossible once CR1/CR5 removed those fields from the type — deleted rather than left as an assertion that can never fail.
- [x] 9.2 Proposal contract CR1/CR2 (doc-only, not a behavior gap): `combinedProposal.ts`'s cycle-21 rewrite replaced one wrong claim ("ONLY blessed slot is dataTypeId on output panels") with a differently wrong, self-contradicting one — the live `CombinedProposalService.flatIsBlessed`/`configIsBlessed` are actually kind-permissive (a text/markdown/image panel's `config.dataTypeId` sentinel IS accepted, substituted, then silently ignored at panel-create — never a 400) while the one case that really DOES 400 (`config.dataTypeId` on an `output`-kind panel, a kind mismatch since `configIsBlessed` explicitly excludes `DataPanelKinds`) went unmentioned. Rewrote from the two service methods' actual code, not a guess, distinguishing "produces a real binding" (flat `dataTypeId` on `output` only) from "accepted but inert" (flat or config `dataTypeId` sentinel on text/markdown/image) from "400s the whole call" (config sentinel on an `output` panel, a shadowed config sentinel, or any other position/duplicate). Matching stale clause at `proposal.ts`'s `apply_proposal` description ("flat OR via config") — contradicted that same file's own accurate line 145 — fixed to say flat-only.
- [x] 9.3 Non-blocking notes: fixed `PipelineStepProtocol.scala:234`'s stale `jsonFormat7` comment (now `jsonFormat8`, matching cycle-21's actual bump); labelled the cycle-21 `PatchSetApplyServiceSpec` rollback test explicitly as a GUARD (its production code was already correct pre-cycle-21, confirmed via `git show d36bb991^:`) rather than leaving it ambiguous with the genuine HEL-766 regression proof it sits beside.

Gates (fresh, this cycle): `sbt compile`/`Test/compile` clean; helio-mcp `tsc --noEmit`/`eslint --max-warnings=0`/`prettier --check` all clean; scoped helio-mcp jest — 18/18 suites, 181/181 tests (one fewer than cycle 21's 182 — the vacuous context.test.ts test removed, accounted for); full backend suite — 3523/3523 tests, 235 suites, 0 failures (unchanged count — comment-only backend changes this cycle); `check-schema-drift.mjs` green.

## 10. Cycle 23 — final-gate round 3: proposal-contract CONFIRMs clean, MCP-tools REFUTEs on a real CI-blocking compile gap

Full reports at `openspec/changes/mcp-outputs-proposals-rewrite/skeptic-final-{1-mcp-tools-round3,2-proposal-contract-round3}.md`. Proposal-contract dimension re-verified the round-22 `parentStepId` fix independently (mutation-tested again, same two failures reproduced) and walked the sentinel guidance branch-by-branch against the live service code -- CONFIRM, no action.

- [x] 10.1 The `outputDataTypeId` class sweep (round 2) is confirmed genuinely exhausted by an independent, from-scratch grep + wire-shape read -- no action needed on that specific class.
- [x] 10.2 NEW, serious finding: `helio-mcp/src/server.test.ts` -- this ticket's flagship exact-60-tool-name-set test -- failed to COMPILE under CI's actual root `tsconfig.json` (`module: commonjs`, `moduleResolution: node`, classic Node10 resolution), silently running ZERO of its 4 tests there (`Tests: 0 total`, not a jest failure the pre-commit hook or any prior review round's evidence surfaced, since the ticket's own documented worktree-scoped command uses a different, NodeNext config that never hits the bug). Root cause, probe-confirmed at the compiler level (not a flaky jest run): `tools/read.ts`'s `registerTool` calls trip a genuine `TS2589` "excessively deep" type instantiation under classic/Node10 resolution specifically -- reproduced directly via `npx tsc --noEmit --module commonjs --moduleResolution node ... helio-mcp/src/tools/read.ts` (2 errors) vs the identical command with `--module nodenext --moduleResolution nodenext` (0 errors). `server.test.ts` is the FIRST test in this repo to ever import `read.ts` (`./server.js` -> `read.ts`), which is why this went undetected through three prior evaluation rounds -- no pre-existing helio-mcp test touched that import path. **Fixed at the root**, not with a type-level workaround: `jest.config.cjs` (repo root, the config `npm test`/CI actually uses) now gives ts-jest an explicit `transform` override forcing `module`/`moduleResolution` to `NodeNext` -- this is the FAITHFUL fix, not a band-aid, because `helio-mcp` genuinely IS built and run under NodeNext (its own `tsconfig.json`, its real ESM `package.json`); the root config was type-checking it under a hybrid mode nothing in production ever exercises. Verified this is safe and complete: every test file the root jest config actually collects (outside its documented exclusions) lives under `helio-mcp/src/**` -- confirmed by direct repo-wide enumeration, not assumed, so there is no OTHER test tree this override could silently mis-compile.
- [x] 10.3 Proved the fix with CI-shaped commands (not the worktree-scoped one): bare `npx jest` (default config discovery, no `--config` override, matching `npm test`'s literal invocation) with only the worktree-specific exclusion pattern lifted (this worktree cannot literally run the un-lifted version and prove anything, since it sits inside the excluded path by construction) -- **18/18 suites, 181/181 tests, `server.test.ts` 4/4 actually executed** (not "0 total"). Re-ran three times across the fix's development (before the fix: reproduced the CI-shaped 0-tests failure 1x directly plus the isolated `tsc` probe; after: the 18/18 pass reproduced 3x, including once immediately after a `prettier --write` reformat of `jest.config.cjs` itself, to rule out the reformat silently breaking the config object). The worktree-scoped command (task 5.9) was independently re-run too and stayed green (18/18, 181/181) -- both configs now agree, which they never did before this fix.
- [x] 10.4 Retired the "sole evidence" framing on task 5.9's own line: it now states plainly that the worktree exclusion is a worktree-EXECUTION constraint only, and documents the round-3 finding + fix inline so a future reader doesn't rediscover the same gap from scratch.
- [x] 10.5 Non-blocking note: `PipelineAnalyzeResponse` (types.ts) was missing `sourceSchemaDrift` entirely (the inverse of the round-2 CR class -- under-, not over-specification). Added `SourceSchemaDriftResponse`/`TypeChangedColumnResponse` mirroring the real 6-field Scala case class (`jsonFormat6`), for symmetry with round-2 CR5's own claim that this interface now mirrors the backend exactly.

Gates (fresh, this cycle): helio-mcp `tsc --noEmit`/`eslint --max-warnings=0`/`prettier --check` all clean; scoped helio-mcp jest -- 18/18 suites, 181/181 tests; **root-config CI-shaped jest run (the actual fix under test) -- 18/18 suites, 181/181 tests, `server.test.ts` 4/4** (previously 0/4, "Test suite failed to run"); `check-schema-drift.mjs` green. No backend file touched this cycle -- `git status backend/` clean, so the backend suite's last fresh run (cycle 22, 3523/3523) stands unchanged.
