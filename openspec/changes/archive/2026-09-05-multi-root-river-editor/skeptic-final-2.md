## Skeptic Report — final gate (round 2, skeptic-final-2.md)

### What I verified (with evidence)

- **Scope isolation, still zero.** `git diff --stat 8bb88c0e...HEAD -- backend/ schemas/` returns empty. All 44 new lines in `pipelineService.ts` are the client-side `addRoot`/`removeRoot` calls only (frontend-only, consistent with the ticket).
- **The round-1 defect is fixed as asked, and the fix is genuine — not a grep-bait call site.** Read `nodePath.ts` (unchanged, correct R5 implementation) and the full diff of `PipelineRiverView.tsx`. `nodePathByStepId` is computed once via `useMemo` over `steps`/`roots` and threaded as a plain `Record<string,string>` prop into every rendering surface: the primary root's `pipeline-detail-page__step-section` (`PipelineRiverView.tsx:381`), `LaneColumn`'s full-column mode (`LaneColumn.tsx:216`) and its compact tail-chain mode (`LaneColumn.tsx:171`), and `RootColumn` (which forwards the same map straight into its own nested `LaneColumn`, `RootColumn.tsx:114`). `grep -rn "nodePath(" frontend/src/features/pipelines --include=*.tsx --include=*.ts | grep -v test` now returns exactly one real call site (`PipelineRiverView.tsx:294`) plus the definition — round 1's exact reproduction command now passes.
- **Verified live in the running app, not just by reading the diff.** Started servers (`scripts/concertino/start-servers.sh`, `assert-phase.sh servers` → `PASS`), opened an existing multi-step pipeline (`HEL-968 eval multi-root`), and read the DOM directly:
  - Root-0 primary step: `document.querySelector('.pipeline-detail-page__step-section').getAttribute('title')` → `"root:7e4d7dc3-45a3-4dd7-a7b5-0519ee0a3bfd > c52f9157-72c7-4bdf-9ff4-49195c90b244"` — correct R5 format (`root:<rootId> > <stepId>`), not the stale bare `root > ...` form.
  - Added a lane via Branch → Union/append rows; the new tail step's `LaneColumn` row rendered `title="root:7e4d7dc3-... > c52f9157-... > a4b7d371-..."` — a correct 3-level chain, confirming the accumulation logic (not just the root-level base case) is wired end to end.
  - Added a second root via "+ Add root" (the D4 disabled-until-selected control is real: the dialog's submit button had the `disabled` attribute until a source was picked, matching the design's claimed double guard).
  - This IS a real, user-reachable surface, not merely a call site invented to satisfy a grep: `title` is this codebase's own established tooltip convention (`DESIGN.md` §5, "Tooltip pattern": `title` gives every interactive element a native hover tooltip; already used identically for "Move step up"/"Disable step"/"Duplicate step" on the same `StepCard`). The executor did not invent a new mechanism to dodge the requirement — they used the pattern this app already relies on for supplementary per-element info, applied to the exact element (the step card's wrapping section) a user would hover to learn "where am I in the graph."
  - I was not able to get Chromium's native OS-drawn title-tooltip to show up in an automated `page.screenshot()` (a known headless-screenshot limitation — native title tooltips render outside the normal paint tree in some configurations) despite hovering and waiting up to 2s. This is a tooling limitation of my verification method, not evidence the feature doesn't work: the DOM-level check above is the authoritative signal (the browser's native tooltip behavior for a `title` attribute is not something this app's code can defeat), and the same `title` mechanism already visibly works for e.g. "Move step up" elsewhere in this app.
- **D3's own scoping is honored, not overreached.** `nodePath.ts`'s docstring and `design.md` D3 both say display-name substitution is a separate, later render-time concern — the canonical id form is what the contract (R5) requires. The wiring correctly renders the id form (raw UUIDs), matching the design decision as written; this is not a shortcut invented post hoc, it is what the design specified.
- **AC5 — gates re-run fresh by me, not merely cited.** `npx jest --testPathPatterns="nodePath|laneLayout|stepTree|RootColumn|PipelineRiverView|AddRootModal|PipelineDetailPage|LaneColumn"` → `7 passed / 182 passed`. `npm run lint` → clean (zero-warnings policy). `npm run typecheck` → clean.
- **HEL-970 boundary re-confirmed untouched.** `grep -rn "pathToRoot\|previewAtNode" frontend/src/features/pipelines` still returns only `nodePath.ts`'s doc comment (a read of the shared edge-union helper, `laneLayout.ts`'s `secondaryInputOf`), same as round 1. This change neither fixes nor works around HEL-970's rejoin-preview defect.
- **Mobile spot-check.** Resized to 375px and screenshotted the live app: root columns/lanes stack vertically, no overlap or clipped controls observed. Consistent with round 1's CSS-based finding; not the focus of this round since round 1 already treated it as non-blocking evidence.

### Verdict: CONFIRM

### Non-blocking notes

- **No regression test exercises the wiring itself.** `nodePath.test.ts` tests the pure function; nothing in `PipelineRiverView.test.tsx`/`LaneColumn`/`RootColumn` asserts a `title` (or any node-path string) is actually rendered on a step. Today's evidence for AC3 is my live DOM read plus the grep-for-a-real-call-site check — both real, but neither is a standing regression guard. If a future refactor drops the `title={nodePathByStepId[step.id]}` prop, no automated gate would catch it (the round-1 grep would still find the one `nodePath(...)` call inside the `useMemo`, so even that check wouldn't regress-detect a dropped prop downstream). Worth a follow-up: a small RTL assertion (e.g. `screen.getByTitle(/root:/)` after rendering a two-root fixture) in one of the existing `PipelineRiverView`/`LaneColumn` test files would close this gap cheaply.
- Everything round 1 already verified clean (R3, wire-shape on root-create, R4, HEL-970 boundary) I re-spot-checked rather than re-litigated in full, per the brief, and found no regressions from the round-2 commit.
