## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

- **Scope isolation.** `git diff --stat 8bb88c0e...c484956a -- backend/ schemas/` returns empty — zero diff under backend/schemas, consistent with the "frontend-only" claim and the run constraints.
- **R3 (no semantic branch on root `position === 0`).** `grep -rn "position === 0\|position == 0" frontend/src/features/pipelines` returns exactly one hit: `stepTree.ts:176`, `kids.findIndex((k) => k.position === 0)`. Read the surrounding code (`stepTree.ts:160-187`): this `position` is a **step's** list-index among its sibling children (an existing HEL-908 lane-continuation concept, unchanged), not a **root's** position field. No code branches on a root's `position`. R3 holds.
- **Wire-shape check on the root-create path (the class of defect this epic keeps shipping).** Read `pipelineService.ts` in full. `createPipelineStep`'s `rootId` param is only sent when `parentStepId` is undefined (correct — backend derives root from parent when parenting). Traced both call sites that pass `roots[0]?.id` (`usePipelineDetailPage.ts:494-505`, `:700-711`): both are documented and structurally restricted to root 0's own top-level lane. Traced the only other insertion path into a non-primary root, `RootColumn.tsx` (task 6): a non-primary root's `LaneColumn` only exposes `onAddLaneStep` (parent-anchored `attachAsTail`, no `rootId` needed), and an *empty* non-primary root renders a "join this source into another lane's step" message instead of any add-step affordance at all. So there is no code path that inserts a root-level step into a non-primary root and could omit `rootId` — the second wire-shape bug the brief asked me to hunt for does not exist in this diff.
- **R4 (every step-response path carries `rootId`).** `getPipelineSteps`, `createPipelineStep`, `duplicatePipelineStep`, `reorderPipelineSteps` all pass through `normalizePipelineStep`/raw `response.data` untouched — `rootId` is whatever the backend sent, never stripped or defaulted client-side. Consistent with R4.
- **HEL-970 boundary respected.** `grep -rn "pathToRoot\|previewAtNode" frontend/src/features/pipelines` — only `nodePath.ts`'s own doc comment references the shared edge-union logic (`laneLayout.ts`'s `secondaryInputOf`), which is a **read**, not a modification of HEL-970's preview backend. No diff under any preview-owned file. This ticket neither fixes nor works around HEL-970's defect.
- **AC1/AC2/AC5 — tests actually run, not just claimed.** `npx jest --testPathPatterns="nodePath|laneLayout|stepTree|RootColumn|PipelineRiverView|AddRootModal|PipelineDetailPage"` → `7 passed, 7 total / 182 passed, 182 total`, freshly run by me. e2e Playwright spec exists (`e2e/hel968-multi-root-editor-flow.spec.ts`, 239 lines) covering the add-root/join/place-output flow described in AC1.
- **AC4 — mobile.** `PipelineDetailPage.css` has explicit 430px/375px-covering media queries and multiple `min-height: 44px; min-width: 44px` rules plus a documented F-131 fix extending the 44px floor to both 430px and 768px breakpoints. Static evidence is solid; did not additionally screenshot at 375/430 given the AC3 finding below is already dispositive.
- **AC3 — THE DEFECT.** Read `nodePath.ts` in full: it correctly implements the R5 `root:<rootId> > s1 > s4` format (verified: `nodePath.ts:92`, `[`root:${bestRootId}`, ...trail].join(" > ")`). But: `grep -rn "nodePath(" frontend/src/features/pipelines --include=*.tsx --include=*.ts | grep -v test` returns **only the function's own definition line** — zero callers anywhere in the app outside `nodePath.test.ts`. I additionally confirmed with `git grep` against the pre-change commit (`8bb88c0e`) that no display site anywhere in the pre-existing codebase ever rendered the stale `root > s1 > s4` form either — this isn't "the old form is still there," it's "no form is rendered anywhere, old or new." The design's own D3 states the function is meant to be "used everywhere a path is shown" (design.md:83-94) — but it is used nowhere. AC3 literally says "Lane paths render in the multi-root format" — they do not render anywhere in the shipped UI; the only thing that exists is an orphaned pure function exercised by its own unit test. The evaluator's PASS (`evaluation-1.md:7`) credited this AC based solely on the existence of `nodePath.ts` + `nodePath.test.ts`, without checking for a real call site — which is exactly the kind of evidence-shaped-non-evidence (inline copy, never actually wired to the thing it claims to prove) this project's own review discipline exists to catch.

### Verdict: REFUTE

### Change Requests

1. **Wire `nodePath()` into an actual UI display site**, per design.md D3's own stated intent ("used everywhere a path is shown") — e.g. a title/tooltip attribute on the lane header or step card in `PipelineRiverView.tsx`/`LaneColumn.tsx`/`RootColumn.tsx`. Until a real caller exists outside test code, AC3 ("lane paths render in the multi-root format") is unmet — nothing renders a lane path today, old or new format. Re-run `grep -rn "nodePath(" frontend/src/features/pipelines --include=*.tsx --include=*.ts | grep -v test` after the fix and confirm it returns at least one non-definition, non-test hit.

### Non-blocking notes

- The R3/wire-shape/R4 diligence items called out in the brief all check out cleanly; no further concerns there.
- AC4's CSS evidence is solid; a live 375/430px screenshot pass would still be worth doing once AC3 is fixed and this returns for another round, but is not itself blocking.
