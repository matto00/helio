## 1. Measure the ground truth

- [x] 1.1 Write a throwaway probe that parses both `data-theme` blocks out of `frontend/src/theme/theme.css` and computes the WCAG ratio for every text-capable foreground x every backdrop, per theme. Parse from source; do not transcribe hexes.
- [x] 1.1a The backdrop set is the five neutral surfaces **plus each `--app-*-surface` intent tint resolved as a composite** over every neutral parent it can sit on. Parse the tint's `color-mix` percentage and base from source (do not transcribe). Expected light-theme composites to reproduce: `--app-error` on `--app-error-surface` = 4.37 / 4.03 / 3.81 over `--app-surface` / `--app-bg` / `--app-surface-soft`; `--app-warning` = 4.31 / 3.97 / 3.79; `--app-success` = 4.22 / 3.89 / 3.71. Dark intent-token-on-its-own-tint expected clean (5.11-8.13) — but do NOT generalise that to "dark is clean": dark `--app-text-muted` `#9b948a` on the dark intent tints (14%) measures as low as **3.82**, worse than light's equivalent. `--app-text` on tints passes everywhere in both themes (worst 10.57). **Also expect `--app-text-muted` `#6c655c` on the success / warning / error tints over `--app-surface-soft` at 4.25 / 4.27 / 4.25 — already sub-AA before any edit.** The tints are backdrops for every text-capable foreground, not only their own intent token.
- [x] 1.1b Score the **full cross-product** (every text-capable foreground x every in-scope backdrop), not just the pairs named below — including cross-intent cells such as `--app-error` on `--app-warning-surface` over `--app-surface-strong` (dark, **4.05**). Every cell is measured and tabulated; per D10 only cells with a rendered normal-size instance oblige a token change. Expect cross-intent cells to resolve as "never renders" under D4's third branch, carrying the 2.7 re-runnable-enumeration obligation. If any cross-intent cell DOES render as normal-size text, escalate it rather than darkening a token to satisfy a pairing the design language never intended.
- [x] 1.2 Confirm the five expected light-theme sub-AA neutral-surface pairs reproduce (`--app-success` 4.25 on `--app-surface-soft` / 4.48 on `--app-bg`; `--app-warning` 4.32; `--app-error` 4.38; `--app-text-muted` 4.87 passing). If any figure disagrees, stop and report the disagreement rather than adopting either number.
- [x] 1.3 Derive `--app-accent-ink` for all 8 presets in both themes via `buildAccentTokens` and record each ink's ratio against its accent. Record any preset below 4.5:1 explicitly.
- [x] 1.4 Derive raw `--app-accent` against all 5 surfaces per theme for all 8 presets and record the shortfall range (D5 — measured and documented, not fixed).

## 2. Confirm what actually renders (D4 — gates all of section 3)

- [x] 2.1 Start the dev servers via the canonical script on this run's assigned ports. Do not reuse a stale server.
- [x] 2.2 For each failing pair from 1.2 **and 1.1a**, find rendered instances in the running app by computed style — enumerate elements whose computed `color` resolves to the intent token's value and whose resolved painted backdrop is the surface in question. Enumerate from the DOM, not from a CSS grep.
- [x] 2.3 Record each instance's computed `font-size` and `font-weight` and classify it normal-size or large-size per WCAG (large = >=24px, or >=18.66px bold).
- [x] 2.3a For the intent-tint composites specifically, confirm which neutral parent each tinted block actually sits on in the running app (resolve the painted backdrop by walking the rendered ancestor chain and accumulating alpha). Do not assume a single parent. Known starting points: `shared/chrome/StatusMessage.css:16-17`, `features/settings/ui/ApiTokensSection.css:31-32`, `AgentMemoryList.css:34,121`, `MfaSecuritySection.css:31,96`, `PipelineScheduleDialog.css:130`, `PanelGrid.css:151,206`, `ToolCallIndicator.css:71,84`, `MessageTurn.css:45`, `RunHistoryModal.css:73,92`, `UserMenu.css:136`, `DashboardList.css:445`.
- [x] 2.4 **Click the control; do not read the record.** Reach the surfaces these tokens live on rather than asserting they are unreachable — settings panels, form validation errors, the schedule dialog, panel content states, MFA/API-token sections. An assertion that a surface is unreachable is exactly the class of claim that failed three cycles on HEL-444; if a surface genuinely cannot be reached, state the specific thing that was tried.
- [x] 2.5 Take the reading in **both** themes, with a fresh reload and the theme already light, letting `ThemeProvider`'s server-preference adoption settle. Clearing `localStorage` alone is not sufficient (the accent is seeded once in a mount-time initializer). Self-authenticate the reading: assert the theme actually in effect at measurement time.
- [x] 2.6 Produce the D4 three-way classification per pair: renders-as-normal-text / renders-only-as-large-or-non-text / never-renders-on-that-surface. Each verdict names its evidence.
- [x] 2.7 Any "never renders" verdict SHALL record the specific enumeration that produced it — the exact DOM query run, and the routes/dialogs/states visited — not the conclusion. The evaluator must be able to re-run it verbatim and reach the same answer. A "never renders" claim backed only by a conclusion is a defect, not an exception.

## 3. Remediate only what section 2 justifies

- [x] 3.1 For each pair classified renders-as-normal-text, darken the light-theme token (lightness only; hold hue and chroma) until it clears 4.5:1 against the **worst backdrop it renders on, tint composites included**, with a small margin. For the intent tokens that target is ~3.71-3.81, not the ~4.25-4.38 neutral-only figure — sizing the correction against the neutral surfaces alone is insufficient by construction. Hold the dark theme's **intent tokens** fixed — they clear AA on every in-scope backdrop. This is not a blanket "do not touch the dark theme": task 3.4a mandates correcting dark `--app-text-muted`.
- [x] 3.2 Verify no surface/opacity invariant is disturbed (DESIGN.md §3) and that the token remains a visually coherent step within its family.
- [x] 3.3 For each pair classified otherwise, change nothing and draft its documented exception, naming the rendered evidence that justifies it. An exception with an empty reason is a defect.
- [x] 3.4 Re-run 1.1 and 1.1a after the edits and confirm every changed pair now clears its threshold, no previously-passing pair regressed, **and no already-failing pair was degraded further**. The second and third checks are different: a pair that was already below AA is outside a "no regression among passing pairs" check, which is exactly how the muted-on-tint degradation escaped the previous draft. Check the thin-margin pairs explicitly: `--app-warning` on `--app-bg` (4.56 today, passing by 0.06) and `--app-error` on `--app-bg` (4.62, by 0.12).
- [x] 3.4a Handle the muted-on-tint pairs per D8, in that order: first classify them with D4's rendered walk; if muted text never lands on a tinted block as normal-size text, record documented exceptions carrying the re-runnable-enumeration obligation (2.7). If it does render, correct `--app-text-muted` in BOTH themes as needed — light `#6c655c` -> approximately `#676057`, dark `#9b948a` -> approximately `#aba398` — each measured to clear every tint composite and neutral surface in its theme; target >= 4.6 rather than the bare 4.51 the minimum light step yields. Do NOT reduce the tint percentages (D8 option 3, rejected: they are the DESIGN.md §3 surface/opacity invariants AC2 requires preserving); if options 1 and 2 both fail, escalate rather than deciding it here.
- [x] 3.4b If `--app-text-muted` is changed, call it out explicitly in the PR body: `theme.css:216-218` records that its current value was deliberately tuned against `--app-surface-soft`, so this reopens a prior decision and must not be slipped in silently.
- [x] 3.4c If `--app-text-muted` changes in either theme, re-check `shared/ui/Toggle.css:53`, the one site that uses it as a **background** rather than as text. Both corrections move it away from its parent surface so separation should improve, but verify the toggle track still clears the 3:1 non-text boundary against its surface and that any content on the track is unaffected. Verify; do not assume the direction is safe.
- [x] 3.5 Confirm the darkened intent tokens remain visually distinguishable from `--app-text` and from each other, and that the tinted blocks still read as their intent colour rather than as neutral dark text. A correction large enough to clear 3.71 is not small; verify it against the running app, not by eye on a hex.

## 4. Guard (D1/D2/D3)

- [x] 4.1 Add `frontend/src/theme/tokenContrastGuard.css.test.ts`, parsing both `data-theme` blocks from `theme.css` source.
- [x] 4.2 Cover the **full matrix**, not only today's failures: every foreground x backdrop pair (neutral surfaces and tint composites) is asserted, including comfortably-passing ones, so a future edit that breaks a thin margin fails here. Assert each covered pair clears the threshold its rendered role earned (4.5 for normal text, 3.0 for large-text/UI-boundary/fill roles), with the role and its justification stated per pair in the test.
- [x] 4.3 Assert the `--app-accent-ink` floor across all 8 presets in both themes (D6).
- [x] 4.4 Vacuity refusal: assert the parsed surface set, tint set and foreground set are non-empty and pin their expected counts, so a regex that matches nothing fails rather than passes. Include the tint mix-percentage parse in this — a percentage regex that silently fails would compute composites against a wrong base and still "pass".
- [x] 4.5 Add mutation arms proving the guard is failable: perturb a token value on a temp copy of `theme.css` and assert the computation reports a failure; include a negative control that a correct value passes.
- [x] 4.6 Confirm the guard runs in the `frontend` CI job's `npm test` and adds no new npm script, husky line, or CI job.

## 5. The committed table (AC1)

- [x] 5.1 ~~Emit the contrast table from the same computation the guard uses, so the two cannot disagree~~ — **as originally planned, this task specified building an emitter. That emitter was never built** (skeptic final-gate, cycle 3). What shipped instead: the table is hand-transcribed by running a temporary `it(...)` block against `tokenContrastGuard.css.test.ts`'s own (module-private) `assertedMatrix`/`crossIntentCells`/`tintComposites` helpers and copying the printed numbers in — verified correct independently by both the evaluator and the skeptic across review, and it is the guard (not this file) that fails CI on a regression. Committed under `docs/` at a tracked path as planned; the "cannot disagree" property is not automatic and requires re-running the reproduction by hand on a future edit. See design.md D7 for the full accounting of why the emitter was dropped.
- [x] 5.2 Include: every text-capable foreground x backdrop pair per theme with its measured ratio, **passing pairs included**, covering both the neutral surfaces and the intent-tint composites (with the parent each composite was resolved over); the derived accent tokens across all 8 presets; the accent-ink floor results; and the accent-on-surface shortfall with an explicit FAIL verdict (D5).
- [x] 5.1a State the D9 boundary in the table: the audited backdrop set is the five neutral surfaces plus the three intent tints, and accent-derived backdrops are excluded because the accent is written inline at runtime (C1) and is already covered for 8 presets by `accentTextSourceSyncGuard`. Record the residual explicitly: non-accent foregrounds on accent tints, and all foregrounds on `--app-accent-mid` (3.17-3.63 for the default light accent), are unguarded by this change. Note `--app-bg-accent`/`--app-bg-secondary` have zero background call sites, and `--app-text` on tints passes everywhere (worst 10.57), so the boundary is measured on both sides.
- [x] 5.2a Resolve the D5 tracking reference honestly: either file a real follow-up ticket for the light-theme accent-on-surface shortfall and cite it by identifier, or state in the table that the shortfall is currently untracked. Do not emit a pointer to a ticket that does not exist.
- [x] 5.3 Record every documented exception from 3.3 in the table with its reason.
- [x] 5.4 Add a short pointer from `DESIGN.md` to the table. A pointer, not a duplicate copy — two copies of the same numbers is two sources of truth.
- [x] 5.5 Confirm the committed path is not gitignored (HEL-444's matrix was lost to `.gitignore:87`). Verify with `git check-ignore`.

## 6. Verification

- [x] 6.1 `npm run lint`, `npm run typecheck`, `npm test` — zero new warnings.
- [x] 6.2 Confirm the new guard actually executes (named in the Jest output), and that it fails when a token is perturbed.
- [x] 6.3 Re-confirm the existing seven theme guards still pass, in particular `themeParityGuard` (pinned at 29 tokens per block) and `accentTextSourceSyncGuard` (drift-detects `appearance.ts` against `theme.css`).
- [x] 6.4 Capture before/after screenshots of a surface showing a changed intent token, in both themes.
