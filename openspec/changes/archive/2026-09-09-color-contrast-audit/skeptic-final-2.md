## Skeptic Report — final gate (round 2, skeptic-final-2.md)

Fresh cold spawn. Every conclusion below is derived from my own commands on
this worktree at `b1e19ce6`, not from skeptic-final-1.md or evaluation-2.md.
I read both as claims and re-derived the load-bearing ones independently.

### What I verified (with evidence)

**1. The documentary blocker is genuinely closed.**

Repo-wide search for surviving generation claims:

```
grep -rniE "auto-?generat|regenerat|generated by|emitt?er|emits|cannot drift|can't drift|never drift" \
  --include=*.md --include=*.ts --include=*.css --include=*.json .
```

Only three hits touch this ticket's artifacts, and all three are honest:

- `docs/contrast-audit.md:7` — "no emitter exists, the numbers below were
  hand-transcribed, and four blocks were already wrong at commit time."
- `docs/contrast-audit.md:12` — "It CAN drift if a future token edit changes
  `theme.css` without this file being regenerated."
- `DESIGN.md:111` — "it is NOT auto-generated, and it can go stale… It is the
  **guard**, not this table, that fails CI on a regression."

I read `DESIGN.md:104-118` in full. The binding standard — the one that
matters most — now states the hand-transcription, the staleness risk, and the
guard-not-table CI locus plainly, in the same paragraph as the pointer. No
false claim survives anywhere I could find it. Blocker closed.

`docs/contrast-audit.md:17-27`'s regeneration recipe now correctly says the
temporary `it(...)` block must live *inside* `tokenContrastGuard.css.test.ts`.
I confirmed the premise: `grep -n "^\(const\|function\)"` on that file shows
`assertedMatrix`/`crossIntentCells`/`tintComposites` are module-private (no
`export`), so a sibling test file genuinely could not import them. The recipe
is executable as written.

**2. Correction style — my judgement.**

I read `MISTAKES.md:229-237` directly. Applying it concretely:

- **`tasks.md:43` (5.1) — compliant.** The original wording is `~~struck
  through~~`. A struck line is a supersession marker, not accumulation: a
  reader who stops at the first glance sees the wrong answer *visibly crossed
  out*. This is the correct form.
- **`DESIGN.md:104-118` — compliant.** The correction is inline in the live
  sentence, not appended beneath a surviving false one.
- **`design.md:77` (D7 heading) — technically violates the rule.** The heading
  still reads "The table lives in `docs/`, generated from source", and line 79
  still asserts the emitter property before line 81 retracts it. This is
  literally the "reader who stops at the heading gets the wrong answer" case.

I weighed this as a REFUTE and decided against it, for reasons I can name:
(a) the body's very first words are "**Plan as originally written:**", so the
paragraph is explicitly framed as history before the claim is made, not left
standing as a live ruling; (b) `design.md` is a change-scoped planning artifact
about to be archived, not a standard anyone consults to decide future work —
the binding document (`DESIGN.md`) and the durable artifact
(`docs/contrast-audit.md`) are both fully correct; (c) nothing that ships to a
user or to a future implementer is wrong. It is improvable, not misleading in
the shipping sense. Recorded as a non-blocking note below.

**3. Cycle 3 broke nothing.**

- Gates, all run by me in `frontend/`: `npm run lint` clean (`--max-warnings=0`),
  `npm run typecheck` clean, `npm run format:check` — "All matched files use
  Prettier code style!", `npm test` — **299 suites / 3141 tests passed**.
- Working tree clean (`git status --porcelain` empty) after my mutations were
  reverted from a backup copy.
- **Token values unchanged** from what round 1 approved: `git diff aea1e0cf..HEAD
  -- frontend/src/theme/theme.css` shows exactly the same six edits
  (light `#645e56`/`#166d43`/`#85551a`/`#af3325`, dark `#aaa49c`/`#f17b67`).
  `git show b1e19ce6 --stat` confirms cycle 3 touched **no** `.css` or `.ts` file.
- **Guard failability — two real mutations I ran myself:**
  1. Reverted light `--app-error` to `#c73a2a` → guard RED, reporting
     `--app-error on --app-error-surface over --app-surface-soft: 3.8170866…`
     — reproducing tasks.md 1.1a's independently-predicted 3.82 exactly. Both
     the assertion arm *and* the negative control failed, as they should.
  2. Reverted dark `--app-text-muted` to `#9b948a` → guard RED on 7 cells
     including `--app-warning-surface over --app-surface-strong: 3.821`,
     matching theme.css's own comment. Restored; suite green again.
- **Dev server is not stale** (MISTAKES.md trap — `start-servers.sh` printed
  "already healthy… reusing"): I fetched `http://localhost:5965/src/theme/theme.css`
  and confirmed the served bytes contain `#af3325`, `#645e56`, `#f17b67`,
  `#aaa49c` — this worktree's values, not main's.

**4. Load-bearing conclusions re-derived independently (not inherited).**

I wrote my own WCAG relative-luminance/contrast implementation from the spec
formula (`/tmp/cr2.js`, unrounded sRGB compositing of `color-mix(…,transparent)`
over each neutral parent) and re-scored a sample spanning every table:

| cell | my value | published |
| --- | --- | --- |
| L `--app-error` on error-tint(10%) / soft | 4.624 | 4.62 |
| L `--app-warning` on warning-tint(11%) / soft | 4.637 | 4.64 |
| L `--app-success` on success-tint(11%) / soft | 4.632 | 4.63 |
| L `--app-text-muted` on error-tint / soft | 4.688 | 4.69 |
| L `--app-text-muted` on `--app-bg` | 5.73 | 5.73 |
| L `--app-text` on `--app-bg` | 14.96 | 14.96 |
| D `--app-error` on error-tint(14%) / strong | 4.625 | 4.63 |
| D `--app-text-muted` on `--app-bg` | 7.63 | 7.63 |
| accent-ink `#181511` vs Orange/Red/Purple/Yellow | 6.49 / 4.83 / 4.60 / 9.49 | identical |
| Yellow light accent-on-surface min | 1.63 (vs `#efece6`) | 1.63 |
| Orange dark accent-on-surface min | 5.58 (vs `#262320`) | 5.58 |

Every sampled cell reproduces. Separation checks also reproduce: light muted
vs text 2.61:1, dark 2.15:1 — both genuinely a "muted step", not a collapse.

**Cross-intent reachability, re-run by my own method** (not the executor's
Playwright walk, and not round 1's). Two independent static enumerations over
`frontend/src/**/*.css`, parsing rule blocks with my own regex:

1. Same-rule pairing: **42** rules declare an intent-tint background (matching
   the count round 1 reported, arrived at independently). **0** of them set a
   different intent's `color` in the same rule.
2. Descendant pairing: 55 tint-background selectors × 93 intent-`color`
   selectors, checking whether any colour selector is a descendant/compound
   child of a different-intent tint selector. **0 candidates.**

So the "never renders" verdict behind the three sub-AA exceptions holds under
a method the executor did not use. I also confirmed those three cells
numerically myself: 4.47 / 4.42 / 4.24 (error on success-tint over strong;
error on warning-tint over raised and over strong) — exact matches.

**My own view on whether the three exceptions are justified:** yes. They are
artefacts of the guard scoring the *full cross-product* rather than of any
composition the app builds — which is itself the right design choice (a
hand-picked pair list could not detect a future regression). All three clear
3:1 comfortably, so even if such a composition were introduced they would
remain valid as UI-boundary/large-text. The exception is bounded, re-runnable,
and names a specific trigger for re-evaluation. The alternative — darkening
`--app-error` further to satisfy a pairing the design language never intended —
would be worse, and the ticket's own binding constraint says so.

**AC trace (each to specific evidence):**
- **AC1** — `docs/contrast-audit.md` (302 lines), tracked (`git ls-files`
  matched) and **not** gitignored (`git check-ignore` returns non-zero, the
  HEL-444 loss mode). All core pairs pass; three exceptions justified above.
- **AC2** — invariants preserved: tint percentages are byte-identical to base
  (light 11/11/10, dark 14/14/14); only foreground hexes moved. Verified
  visually below.
- **AC3** — `tokenContrastGuard.css.test.ts:382-418` asserts the accent-ink
  floor for all 8 presets in both themes with a non-vacuity check and a
  mutation arm; I re-derived four preset ratios exactly. Accent-on-surface
  shortfall published with explicit FAIL verdicts and tracked as **HEL-1061** —
  which I resolved via the Linear API and confirmed is a real, open Backlog
  ticket with matching content, not a dangling reference.
- **AC4** — guard runs inside `npm test`, which `.github/workflows/ci.yml:96`
  executes in the `frontend` job feeding the required `ci-complete` check. No
  new script/hook/CI surface added. Failable — proven twice by me.

**5. Visual judgement (mine, on the running app, both themes).**

Servers: `assert-phase.sh servers` → `PASS`. Console: **0 errors** across the
session.

Theme self-authenticated at measurement time, per the settle trap: fresh
navigation, ~2.5s settle, then read computed style off `documentElement` —
`{theme:"light", text:"#211d19", muted:"#645e56", success:"#166d43",
warning:"#85551a", error:"#af3325", accent:"#eab308"}`. The accent read as the
user's real yellow, **not** the dead `#ea580c` fallback, which is the positive
signal that `ThemeProvider` had actually settled.

To get all four intent pairings in one frame I injected a strip using the real
`.status-message`/`.status-message--error` classes and real `var(--app-*)`
references into the live page — the app's own stylesheet resolves it, so what
I judged is the shipped cascade, not a mock. Alongside it the page's own
"Failed" pipeline badge renders the same error-on-tint pair for cross-checking.

- Light (`hel533-light-intents.png`): error reads unmistakably **red**, success
  **green**, warning a warm **amber/ochre**. The warning was the one I expected
  to be at risk of flattening into brown at `#85551a` — it does not; it still
  reads as caution and stays clearly distinct from both the error red and the
  near-black `--app-text`. Muted on the error tint is legible and still
  subordinate.
- Light chrome (`hel533-light-settings.png`): nav labels, field labels and
  empty-state copy at the new `#645e56` read distinctly muted against the
  near-black headings — the "muted step" survived the darkening.
- Dark (`hel533-dark-intents.png`): error salmon-red, success green, warning
  amber; the same hierarchy and the same relative ordering as light. Parity
  holds. Muted on the warning tint over `--app-surface-strong` — the
  worst-case 4.64 cell — is comfortably legible.

No off-pattern UI, no reinvented one-off, no hardcoded value where a token
exists (the change *is* tokens), no light/dark divergence. The intent family
still reads as an intent family in both themes.

### Verdict: CONFIRM

### Non-blocking notes

1. `openspec/changes/color-contrast-audit/design.md:77` — D7's heading still
   reads "The table lives in `docs/`, generated from source", which contradicts
   the correction three lines below it and is the exact shape `MISTAKES.md:229`
   warns about. If this is touched before archive, amend the heading in place
   (e.g. "The table lives in `docs/`, hand-transcribed from the guard's own
   computation") and keep the identifier `D7` — `MISTAKES.md:235` forbids
   renumbering. Not blocking: the binding `DESIGN.md` and the durable
   `docs/contrast-audit.md` are both correct, and this artifact is
   change-scoped and about to be archived.
2. `docs/contrast-audit.md` §5 records that the live `.auth-error` instance's
   resolved parent was `--app-surface` (5.30), while the correction was sized
   against the worst *possible* parent (`--app-surface-strong`, 4.46). That is
   the conservative and correct choice, and the doc says so — but a future
   reader could misread it as "the measured instance drove the fix". One extra
   clause would settle it.
3. Review artifacts I created outside the worktree, for cleanup:
   `/home/matt/Development/helio/hel533-light-security.png` (a stray root-level
   PNG — the known parallel-Playwright hazard) and
   `/home/matt/Development/helio/.playwright-mcp/hel533-{light-settings,light-intents,dark-intents}.png`.
