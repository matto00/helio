## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

- **Ground truth diff read in full.** `git diff main...HEAD -- backend/src/main/scala/com/helio/services/PatchSetUndoInverse.scala` — the only production-code change is exactly the two named helpers gaining an `enabled` read (`PatchSetUndoInverse.scala:150-173`, commit a54b5758). No other production file touched (`git diff main...HEAD --stat`: only `PatchSetUndoInverse.scala` + 2 spec files + openspec planning artifacts).

- **AC1 (round-trip `enabled` for delete-and-recreate and full-revert paths) traced to real call sites, not just the helpers in isolation.** Read `PatchSetUndoService.scala:244-286`: `restorePipelineStepUpdate` calls `fullPipelineStepInverse` (full-revert), `restorePipelineStepDelete` calls `pipelineStepCreateRequestFromResponse` (delete-and-recreate) — these are the only two callers, matching the ticket's named scope exactly. Both are exercised: `PatchSetUndoInverseSpec.scala` unit-tests both helpers directly (disabled-JSON in → `Some(false)`/`Some(false)` out; no-key JSON in → `Some(true)`/`None` out, matching D6/D7). `PatchSetUndoServiceSpec.scala`'s DB-backed case 5.3c was extended to seed a step `enabled = Some(false)`, delete it, undo, and assert `recreated.enabled shouldBe false` — a genuine end-to-end regression test for the delete-and-recreate path.

- **Confirmed the propagated values actually reach the DB row** (not just the request object). `PipelineStepRepository.scala:106` (`updateInternal`): `enabled = enabled.getOrElse(row.enabled)` — an explicit `Some(...)` on the full-overwrite inverse genuinely overwrites the live row, closing exactly the gap D6's rationale describes. `PipelineStepRepository.scala:64` (`insert`, default `enabled: Boolean = true`) confirms `enabled = None` on the create path correctly defaults to `true`, matching D7 and `CreatePipelineStepRequest.enabled`'s own doc comment (`PipelineStepProtocol.scala:148-151`).

- **AC2 (absent `enabled` on legacy JSON) verified against both helpers' actual code**, not just asserted: `fields.get("enabled").map(_.convertTo[Boolean]).getOrElse(true)` (full-revert) vs. `fields.get("enabled").map(_.convertTo[Boolean])` (create) — matches D6/D7 exactly, and the asymmetry is justified by each request type's own pre-existing "absent means..." contract (`UpdatePipelineStepRequest`: absent = no change, so full-overwrite must not leave it absent; `CreatePipelineStepRequest`: absent = created enabled, so passing `None` through is already correct).

- **AC3 (tests + `sbt test` clean) — reproduced fresh, not trusted from the evaluator's assertion.**
  - `cd backend && sbt -no-colors "testOnly com.helio.services.PatchSetUndoInverseSpec com.helio.services.PatchSetUndoServiceSpec"` → **18/18 passed**, including the extended 5.3c case.
  - Full suite: `cd backend && sbt -no-colors test` (backgrounded, ran to completion) → **3346/3346 tests passed, 0 failed**, matching the evaluator's claimed count exactly, independently reproduced (3 min 6 sec run).
  - `npm run check:scala-quality` → "clean (128 soft warning(s))" — same pre-existing informational-only file-size warnings as reported, no new violation (`PatchSetUndoServiceSpec.scala` at 505 lines was already 492 pre-change).

- **Iron Laws (systematic-debugging.md) — probe-confirmed root cause and a test that would actually catch the regression.** Root cause is directly visible in the pre-diff code (`fullPipelineStepInverse`/`pipelineStepCreateRequestFromResponse` read only `type`/`config`/`position`, dropping `enabled` entirely) — not inferred, read directly. The extended 5.3c test is meaningful: without the fix, `pipelineStepCreateRequestFromResponse` would build a `CreatePipelineStepRequest` with `enabled = None`, which `insert`'s `enabled: Boolean = true` default would silently turn into `enabled = true` regardless of the seeded `false` — so `recreated.enabled shouldBe false` would fail. This is a real regression test, not a tautology.

- **`-n` (skip-hooks) commit bypass verified against actual precedent**, not just the commit message's claim: `git show 251e28f4 --stat` (HEL-753) and `git show 68597da8 --stat` (HEL-759) both exist on `main` with the same commit-message pattern (explicit bypass disclosure, `check:openspec`'s "complete but not archived" gate). `.husky/pre-commit` confirms `npm run check:openspec` is indeed a pre-commit hook (`package.json:13`, `.husky/pre-commit:7`), so the bypass claim is accurate, not fabricated.

- **No scope creep / non-goals honestly documented.** `PatchSetApplyRollback.scala`'s analogous gap (own `fullPipelineStepInverse`/`pipelineStepCreateRequestFromPrior`, reading a domain `PipelineStep` without `enabled`) is explicitly named as out-of-scope with a delivery-time follow-up flagged — not silently ignored, matching the ticket's own stated Non-Goals.

- **Phase 3 / UI review: N/A, correctly.** `git diff --name-only main...HEAD` confirms zero `frontend/**` files touched — this is a pure backend correctness fix. No dev-server verification needed for this gate.

### Verdict: CONFIRM

All three ACs trace to real, verified code and freshly-reproduced test evidence, not just to the evaluator's assertions. The fix is minimal, precisely scoped to the two named helpers, correctly wired through to the actual DB-persisted `enabled` value, and covered by a genuine regression test that would fail without the fix. The full backend suite (3346/3346) and the targeted spec run (18/18) both reproduced clean on independent re-run. No frontend changes, so no design-standard review applies. Ships.

### Non-blocking notes

- AC1's full-revert path (`fullPipelineStepInverse`, used by `restorePipelineStepUpdate`) has direct unit coverage (`PatchSetUndoInverseSpec.scala`) but was not additionally extended at the DB-backed level (5.3a in `PatchSetUndoServiceSpec.scala`) the way the delete-and-recreate path (5.3c) was — task 2.4 scoped the DB-backed extension to "the closest existing case" (singular), and the evaluator's report calls this out explicitly as the plan-as-written rather than a gap. Since `restorePipelineStepUpdate` calls the exact same `fullPipelineStepInverse` function the unit test exercises with equivalent input/output assertions, this is not a functional coverage gap — just an asymmetry in how thoroughly each of the two paths got DB-level (vs. unit-level) regression coverage. Worth folding a 5.3a `enabled` assertion in if this file is ever revisited, but not a reason to hold delivery.
- The `PatchSetApplyRollback.scala` sibling gap (same class of bug, different call path) remains unfixed by design — correctly out of scope here, but worth confirming a follow-up ticket actually gets filed at delivery time per the proposal's stated intent, rather than the flag silently going stale.
