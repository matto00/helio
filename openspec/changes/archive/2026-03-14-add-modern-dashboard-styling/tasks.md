## 1. Theme foundation

- [x] 1.1 Add a reusable frontend theme system with dark mode as the default
- [x] 1.2 Persist the selected light/dark theme across reloads
- [x] 1.3 Introduce shared styling tokens for colors, surfaces, spacing, radii, shadows, and restrained motion

## 2. Dashboard visual polish

- [x] 2.1 Redesign the app shell and header with a more polished full-width dashboard layout
- [x] 2.2 Restyle the sidebar, dashboard selection list, and panel area with rounded premium surfaces
- [x] 2.3 Add a `react-grid-layout` starter foundation so panels can occupy a flexible dashboard canvas
- [x] 2.4 Keep interactions polished and restrained without heavy click animations

## 3. Verification

- [x] 3.1 Add or update frontend tests for theme defaults, theme toggling, and persistence behavior
- [x] 3.2 Verify the polished shell and grid foundation remain compatible with the current dashboard selection and panel-loading flow
- [x] 3.3 Run frontend tests, repo lint, format check, and frontend build to verify the styling pass is non-breaking
