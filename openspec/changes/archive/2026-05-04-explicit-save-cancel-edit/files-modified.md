- `frontend/src/components/PanelDetailModal.tsx` — all save handlers return to view mode; `accumulatePanelUpdate` replaced with `updatePanelAppearance` thunk; `resetFormToPanel()` helper added; `handleCancel`/`handleDiscard`/Esc event handler updated for edit-mode view-mode transitions; unsaved-changes badge added to header; `handleCloseButton` added to restore ✕ button's close-modal semantics (distinct from Cancel/Esc → view mode); `discardClosesModal` flag threads through `handleDiscard` to close vs. view depending on trigger
- `frontend/src/components/PanelDetailModal.css` — added `.panel-detail-modal__unsaved-badge` styles for the muted inline indicator in the header
- `frontend/src/components/PanelDetailModal.test.tsx` — updated existing tests to assert view-mode transitions instead of modal close; added tests for save (2.2), cancel/Esc (2.3–2.6), unsaved-changes indicator (2.7–2.8), no-auto-save (2.9), and two ✕ button edit-mode scenarios (no-changes closes immediately; unsaved-changes discard confirms then closes)
- `frontend/src/app/App.test.tsx` — updated integration test that asserted `accumulatePanelUpdate` behavior to assert the new direct API call + view-mode transition
- `frontend/package-lock.json` — added missing `@testing-library/dom` transitive dependency (was causing all test suites to fail to compile)
