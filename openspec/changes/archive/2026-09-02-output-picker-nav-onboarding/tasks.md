# Tasks: HEL-909

Chunk by axis (design.md) so each chunk lands gate-green. Do not attempt this
as one commit. Confirm dev/backend servers match the commit under test
before any UI evidence at every chunk boundary (lesson 1).

## 1. Panel data model + Output picker

- [x] 1.1 Verified done (earlier cycle, checkbox was simply never ticked): `types/panel.ts` carries the discriminated union (`OutputPanel`/`TextPanel`/`MarkdownPanel`/`ImagePanel`/`DividerPanel`) with `OutputPanel`'s config reduced to `{outputId}` per its own doc comment.
- [x] 1.2 Verified done: zero references to `/api/panels/:id/query` anywhere in `panelThunks.ts` (or the codebase) — the call and its HEL-292 client-side aggregate utils are gone, not disabled.
- [x] 1.3 Verified done: `panelsSlice.ts`/`panelPayloads.ts`/`panelNarrowing.ts` are already built against the discriminated union (confirmed live throughout this cycle's own work on `PanelContent.tsx`/`OutputPicker.tsx`, which consume exactly this shape).
- [x] 1.4 New `OutputPicker` component: search grouped by pipeline, live Output cards (kind/name/placement-count shown; full live-preview thumbnail deferred as scope-conservative — placement counts via `GET /api/outputs/:id/panels`), "already on this board" state, keyboard nav (arrows + Enter), content-panel row (text/markdown/image/divider), "New pipeline · Ask the assistant" links.
- [x] 1.5 Wired `OutputPicker` as the dashboard's "Add panel" entry point (`PanelList.tsx`) and as `PanelDetailModal`'s "Swap output" entry point (`mode="swap"`). `PanelCreationModal` itself is not yet deleted (task 7.1, Axis A deletion, still open) — `panelCreationModalOpen`'s no-op render was simply replaced with the real component.
- [x] 1.6 Jest (`OutputPicker.test.tsx`): grouping, already-placed marking, search filtering, keyboard nav (arrow+Enter) placement, accessible names.

## 2. Panel sheet (absorbs HEL-937 for PanelDetailModal)

- [x] 2.1 Read `OutputEditorSheet.tsx` (P1.5) for the established sheet/deep-link pattern — no pre-existing deep-link convention found; added one (`/pipelines/:id?outputId=`) in `usePipelineDetailPage.ts`.
- [x] 2.2 Rebuild `PanelDetailModal` as the small Panel sheet: title override, appearance (reuse `AppearanceEditor.tsx`), Output link, Swap output (reopens `OutputPicker` scoped — currently a marked TODO stub pending task 1.4/1.5), placements note. Content panels keep literal editors.
- [x] 2.3 Rewrote `PanelDetailModal`'s 9 test files against the new sheet. Deleted (retired concepts, no rewrite target): `aggregation`, `chartDisplay`, `collection`, `computedFields`, `labelUnit`. Rewrote onto output-kind/literal-content-only behavior: `test`, `errorRetry`, `markdownContent`, `textContent`, `panelSwitch`.
- [x] 2.4 Negative assertion added in `PanelDetailModal.test.tsx` ("output panel edit mode shows Output link/Swap output/placements note and no field-mapping control") — proven red by construction (deleted `BindingEditor`/data-type-search imports leave the assertion trivially true only because the retired controls are structurally gone, not stubbed).
- [x] 2.5 Verified: `BindingEditor`, `MetricPicker`, `DataTypePicker`, `CollectionEditor`, `TimelineEditor`, `MetricBindingFields`, `useMetricBindingState` are all already deleted (zero hits under `features/panels/ui/editors/`). `useBoundOrLiteralState.ts`/`BoundOrLiteralField.tsx` are **NOT orphaned** — still imported live by `ChartDisplayFields.tsx` and the surviving Output editor (`outputEditor/{buildOutputConfig,OutputEditorSheet,OutputKindFields}.ts(x)`), so they stay. `TextContentEditor.tsx`/`MarkdownEditor.tsx` already contain zero `fetchDataTypes`/`selectPipelineOutputDataTypes`/Source-mode references — already literal-only (prior cycle). `fieldOptions.ts`/`updatePanelTextBinding` — see task 8.4 (unstarted; last remaining piece of this axis).

## 3. Renderers off Outputs

- [x] 3.1 Verified already done (prior cycle): `PanelCard.tsx` imports `getAssertionStatus` from `outputService.ts` (`GET /api/outputs/:id/assertion-status`).
- [x] 3.2 Verified already done (prior cycle): `usePanelData.ts` reads rows via `outputService.getOutputRows`; renderers consume that data, not a `DataType` fetch.
- [x] 3.3 Implemented `config.format` (HEL-876) end to end: `MetricOutputConfig`/`CollectionOutputConfig` (`outputConfigTypes.ts`) both carry `format?: "number"|"integer"|"currency"|"percent"|null` (absent/`"number"` preserves the pre-HEL-876 2-decimal-no-grouping default exactly); `MetricRenderer.tsx` applies the corresponding `Intl.NumberFormat` shape; `CollectionRenderer.tsx` forwards `format` to each item's `MetricRenderer` (closes the "collection baseType: metric" half of the AC). Wired through both real render paths (`PanelContent.tsx`'s `OutputPanelContent`) and the Output editor (`OutputKindFields.tsx`'s new Format select for Metric + Collection kinds, `buildOutputConfig.ts`, `OutputEditorSheet.tsx` state, `OutputPreviewPane.tsx`'s live preview). New Jest coverage: `MetricRenderer.test.tsx`/`CollectionRenderer.test.tsx` (all four format values) and `buildOutputConfig.test.ts` (format round-trip + rejects an unrecognized value).

## 4. Sources pages off DataTypes

- [x] 4.1 Verified already done (prior cycle): `SourcesPage.tsx`, `SourceDetailPanel.tsx` (renamed from `SourceDetailPage.tsx`), `EmptySchemaAffordance.tsx`, `AddSourceModal.tsx` all read `inferredSchema` off the source payload; zero `fetchDataTypes`/`dataTypeService` references remain under `features/sources/`. Backend `DataSourceResponse` confirmed to carry `inferredSchema` (`DataSourceProtocol.scala`).

## 5. Nav collapse

- [x] 5.1 `sections.ts`: drop `registry`/`metrics` `PickerId` values and entries.
- [x] 5.2 Verified `SidebarBody`/`MobileNavSheet`/`navDestinations.ts`/`usePickerSelection`/`pickerEmptyState` cascade correctly from `sections.ts` with no independent hardcoded list — confirmed live (not just per the header comment): removing the two `sections` entries alone made `navDestinations`, `BottomNav`, and `usePickerSelection`'s exhaustive switch all correctly drop to 5, after removing the `registry`/`metrics` case-arms and their now-dead selectors (`selectPipelineOutputDataTypes`, `selectPipelineNameByOutputTypeId`, `isUnstructuredDataType`) from `usePickerSelection.ts`/`SidebarBody.tsx`/`pickerEmptyState.tsx`.
- [x] 5.3 Closed the real gap: Assistant had NO shared create-action hook (`usePickerSelection.ts`'s `case "chat"` returned `createAction: null`/`emptyCreateAction: null` with a comment claiming this was "out of scope" — stale, contradicted by `ticket.md`'s explicit "Assistant included (HEL-789's surviving half)" and the `mobile-dashboard-sheet` spec delta already on disk). Added `features/assistant/hooks/useCreateConversationAction.tsx` (wraps the same `startNewConversation()` reducer action `SidebarBody.tsx`'s desktop "New chat" button dispatches inline) and wired it into `usePickerSelection`'s `chat` case. Fixed `App.test.tsx`'s `/chat` empty-sheet test, which had literally asserted the gap (`queryByRole("button")).not.toBeInTheDocument()`) — now asserts the "New chat" CTA IS present. New `usePickerSelection.test.tsx` (the hook had no test file at all) locks in the label and the dispatched action.
- [x] 5.4 `AppRoutes.tsx`: dropped `/registry`, `/registry/:id`, `/metrics`, `/metrics/:id` routes and their now-unused page imports — no stubs/redirects.
- [x] 5.5 Audited Sources/Pipelines/Dashboards empty-state copy. Found and fixed one genuine leftover: `SourcesPage.tsx`'s main empty state said "shape into a bindable type with a pipeline" (the retired DataType noun) — changed to "shape into Outputs with a pipeline" (`SourcesPage.test.tsx` updated to match). Pipelines'/Dashboards' empty-state copy (`PipelineEmptyState.tsx`, `DashboardList.tsx`, `PanelList.tsx`) already used current vocabulary ("typed rows" in the pipeline empty state is the generic adjective, not the retired DataType noun — left as-is).

## 6. Onboarding

- [x] 6.1 `onboardingSteps.ts`: three steps (connect a source · shape it into outputs · place them on a dashboard), glyphs derived from `sections.ts`. `OnboardingStepStatuses` collapsed to `{source, pipeline, placement}`; new `derivePlacementStepStatus` composes the existing dashboard/panel derivations (dashboard status until a dashboard exists, then panel status) rather than duplicating either. Step 2's action ("New pipeline") is disabled while no source exists yet (ADDED requirement's named precondition example). Step 3's action creates a dashboard when none exists yet, else opens the Output picker via the existing `useCreatePanelAction`/`PanelList` wiring (never `PanelCreationModal`).
- [x] 6.2 Done button: CSS unchanged (already DESIGN.md-token-based) + a new computed-style regression test (`OnboardingChecklist.test.tsx`) that loads the REAL `OnboardingChecklist.css` off disk (`fs.readFileSync`, bypassing jest's `.css` mock) and injects it as a real `<style>` tag so jsdom's CSS engine actually matches the selector; asserts `getComputedStyle(...).getPropertyValue("background"/"color")`. Proven red first: a sibling test strips the governing rule's declarations from that same loaded text and asserts the computed values no longer resolve (HEL-792).
- [x] 6.3 Closing copy names all five nav destinations (Dashboards, Data Sources, Data Pipelines, Connectors, Assistant) — asserted in `OnboardingChecklist.test.tsx`'s "all-three-complete" describe (HEL-793).

## 7. Delete retired features outright (Axis A)

- [x] 7.1 Verified: `features/dataTypes/**`, `features/metrics/**`, `PanelCreationModal.tsx`, `creationSteps/*`, `PanelCreationPreview.tsx`, `panelTemplates.ts`, `ComputedFieldForm`/`ComputedFieldsEditor` are all already gone (prior cycle) — confirmed via `find`, zero hits. Found and deleted one leftover orphan this cycle: `PanelCreationModal.css` (no importing `.tsx` left; the two files that mention its name do so only in historical doc comments, not imports).
- [x] 7.2 Verified: `store.ts` has zero `dataTypes`/`metrics` references — slice registration already removed.
- [x] 7.3 `useCreatePanelAction` still dispatches `setPanelCreationModalOpen(true)`, but this is no longer a "wizard dispatch" — `PanelList.tsx` (task 1.5) now renders `<OutputPicker>` on that flag, not `PanelCreationModal`. Nothing left to drop; the flag's meaning was repointed, not orphaned.

## 8. Incidental references (Axis D)

- [x] 8.1 Read and resolved every file in the list. Genuine stale-reference fixes: `ActiveConversationPanel.tsx` (dropped a `/registry/:id` mention from a doc comment), `assistantConversationsSlice.test.ts`/`settingsSlice.test.ts`/`auditEventsSlice.ts`/`settingsSlice.ts` (comments citing the now-deleted `metricsSlice.ts` as "existing precedent" — reworded to a still-real sibling file), `pipelinesSlice.ts`/`outputService.ts` (comments citing the now-deleted `dataTypeService.ts`'s `normalizeDataType` — cross-referenced each other instead), `patchSetsSlice.test.ts` (synthetic fixture error string "Cannot delete DataType..." → "Cannot delete Output..."; a stale `dataTypesSlice.test.ts` precedent-comment reworded), `PipelineDetailPage.test.tsx` (removed two dead `/registry`/`/registry/:id` `<Route>` stubs, never exercised by any test in the file), `MobileNavSheet.test.tsx` (comment's destination list dropped "registry"), `App.test.tsx` (three doc comments dropped `/registry`/`metrics` mentions), `hooks/README.md` (`usePortalPopover` importer list dropped `features/metrics`, verified via a fresh import grep). Confirmed-benign (real, still-live references, not accidental leftovers): `CommandBar.test.tsx`/`toastListeners.ts` (no matches at all), `StatusChip.tsx` (generic "metric" status-pill class name, unrelated to the retired Metrics nav section), `tokenAuditSweep.css.test.ts` (a deliberate historical note about Axis A's deletion, correctly worded), `SourceDetailPanel.test.tsx` (`dataType` is `InferredField.dataType`, a real backend field name — `model.scala:684`), `SaveStateIndicator.test.tsx` (no matches), `ProposalHandoff.test.tsx`/`combinedProposalsSlice.test.ts` (the documented proposal-`dataTypeId` scope exception, see design.md), `sections.test.ts`/`navDestinations.test.ts` (these ARE the negative-assertion tests proving `/registry`/`/metrics` are gone — the literal strings are the point), `test/{panelFixtures,renderWithStore}` (no matches), `test/rawElementGuardHel440.test.tsx` (a deliberate historical note, correctly worded), `pipelinesSlice.ts`'s remaining `outputDataTypeId`/Type-Registry-provenance comment (mirrors the real backend `Pipeline.outputDataTypeId` field — same backend-anchored scope boundary as the proposal exception, see `CombinedProposalService.scala`/`WorkspaceContextProtocol.scala`).
- [x] 8.2 Reworded the four surviving-comment AC-grep matches without changing meaning (design.md "AC-grep comment carve-out"): `outputConfigTypes.ts:3`, `useOutputTableColumns.ts:4`, `pipelinesSlice.ts:111,320` — retired-identifier names replaced with "type-registry entity"/"the retired type-registry slice" phrasing. — replace the retired-identifier names (`dataTypeId`, `dataTypesSlice`) with equivalent non-matching phrasing (e.g. "type-registry entity" instead of "DataType entity").
- [x] 8.3 Verified already done (prior cycle): `panelService.ts`'s create/update request builders carry no `dataTypeId`/`metricId` params any more (`createPanel` takes `outputId?: string`; `panelPayloads.ts` has no such fields either).
- [x] 8.4 Verified: `TextContentEditor.tsx`/`MarkdownEditor.tsx` are already literal-only (no Source/bound mode). `fieldOptions.ts` and `updatePanelTextBinding` are already deleted (zero hits). `useBoundOrLiteralState.ts`/`BoundOrLiteralField.tsx` are kept — still live dependencies of the surviving Output editor (see task 2.5), not orphaned.

## 9. Proposal/patch-set review surfaces

- [x] 9.1 Audited `ProposalReview.tsx`/`CombinedProposalReview.tsx`/`PatchSetReviewPage.tsx`/`ProposalReviewPage.tsx`/`CombinedProposalReviewPage.tsx` for stale display copy (headings/labels/help text distinct from the wire-field scope exception). Found none — `ProposalReview.tsx`'s user-facing strings already say "No Output bound"/"Bound Output not found in this workspace" (Output-native copy); grepped case-insensitively for "data type"/"metric" across all five files, zero display-copy hits. The only remaining references are the `panel.dataTypeId` wire-field accesses themselves (design.md's documented scope exception) and one doc-comment on `ProposalReviewPage.tsx:237` that already correctly explains the field is "really the Output id, kept under that name" for wire compatibility.

## 10. E2E + OpenSpec

- [x] 10.1 `e2e/hel666-single-assistant-entry.spec.ts`: dropped the retired `/registry`/`/metrics` routes from its entry-points route list. `e2e/hel773-top-anchored-mobile-nav-sheet.spec.ts`: dropped the `/metrics` "no create action" comparison half of its direction-change matrix test — every remaining pickable section (dashboards/sources/pipelines/chat) now has its own create action (chat's "New chat" closed HEL-789's surviving half in an earlier cycle), and `/connectors` is deliberately `pickerId: "other"` (never renders a "Switch X" control at all), so there is no remaining like-for-like "no create action" picker section to compare against — documented in the test file rather than silently pointed at a section that doesn't behave as claimed. `e2e/hel716-panel-creation-focus-trap.spec.ts`: deleted outright (`PanelCreationModal` no longer exists; the shared `Modal.tsx` focus-trap mechanism it end-to-end verified already has its own jsdom coverage in `Modal.test.tsx` per that file's own doc comment). `e2e/hel399-shape-instantiate.spec.ts`: deleted outright — its entire subject (the "start from a shape" wizard flow through `PanelCreationModal`) no longer exists, not just its `config.dataTypeId` assertion.
- [x] 10.2 New `e2e/hel909-output-picker-panel-sheet.spec.ts`: Add panel (via the command-bar "Dashboard actions" kebab, HEL-813 surface 6's own precedent) → search "throughput" → Enter → panel lands on the grid (asserted via a real non-zero bounding box — a newly placed Output panel has no title of its own yet, so it renders the shared "Untitled Panel" default, not the Output's name) → open → Edit → Panel sheet shows title/appearance (AppearanceEditor)/Output-link-and-Swap-output only, nothing resembling the retired field-mapping/DataType-bind controls → Swap output re-opens the picker scoped to the panel and the Output link updates. **Live-run against real dev+backend servers, found and fixed two genuine defects in the process** (see files-modified.md): (1) `PanelRepository.configColumnsOf`/`configColumnValuesOf` never included the `output_id` column, so every `PATCH /api/panels/:id` config-outputId write silently no-opped at the DB layer while still returning 200 with the (uncommitted) new id in the response body; (2) `OutputPicker.tsx`'s swap-mode `dispatch(fetchPanels(dashboardId))` was itself a silent no-op — `fetchPanels`'s own dedup `condition` skips a refetch once `status: "succeeded"` for that `dashboardId`, which this dashboard always already is by the time a user reaches Swap output. Both fixed; a new backend regression test (`ApiRoutesSpec` "PATCH /api/panels/:id updating output binding") proven red-then-green against the first fix (reverting the repository change locally reproduces the exact failure).
- [x] 10.3 Mobile: `e2e/hel909-output-picker-panel-sheet.spec.ts`'s two mobile tests verify the OutputPicker at 375px/430px (search input visible and usable, the matched card fits within the viewport width with no horizontal overflow, selecting it places a panel) — closes HEL-490. The existing `e2e/hel813-mobile-touch-target-floor.spec.ts` (≥44px nav guard) was not hardcoded to any nav-destination count and continues to pass unmodified with 5 destinations (verified live: 12/12 passing across both widths/themes on `e2e/hel773-*` alone, which shares its register/nav-sheet infrastructure).
- [x] 10.4 DONE this cycle (evaluation-1.md CR7). Walked all 20 capability deltas under this change's `specs/`. 18 of 20 are pure `## REMOVED Requirements` deltas retiring a deleted capability (panel-creation-modal/-preview/-shape-step/-type-config/-datatype-step/-datatype-empty-state, panel-type-selector, panel-type-picker-cards, panel-starter-templates, panel-config-field-or-literal-pattern, text-panel-content-source, markdown-panel-content-source, metric-authoring-ui) — task 8.1's file-by-file deletion audit already confirmed the underlying files/routes are gone, not merely unreachable, so these assertions hold. The remaining files carry `## ADDED` requirements, checked individually:
  - `output-picker/spec.md` — had TWO real drifts (evaluation-1.md findings 1/2, both fixed this cycle): the decision-15 default-size scenario is now true (backend computes+persists it, frontend renders the returned layout — see CR1 below); the live-thumbnail sentence was aspirational and has been amended to describe the shipped kind/name/placement-count cards, with the deferral reasoned through (not just reworded away).
  - `panel-detail-modal/spec.md` — matches live Playwright evidence from evaluation-1.md Phase 3 exactly (title/appearance/Output-link/Swap-output/placements-note, no binding controls, no Data tab); Swap output's mechanism changed (CR4, now via `panelService.patchPanelOutputId`) but the requirement only asserts the user-visible outcome (position/size preserved, only `outputId` changes), which is unchanged.
  - `frontend-panel-creation/spec.md` — "explicit, human-readable error rather than failing silently" was FALSE against pre-fix code (the bare `catch {}` this cycle's CR5 removed); true now.
  - `nav-section-registry/spec.md`, `mobile-bottom-nav/spec.md`, `mobile-dashboard-sheet/spec.md`, `first-run-onboarding/spec.md` — cross-checked against evaluation-1.md's own live Phase-1 verification (exactly 5 destinations desktop+375px, `/registry`/`/metrics` unresolved, three-step checklist) and `sections.test.ts`/`navDestinations.test.ts`'s negative assertions (task 11.1/11.3) — no drift found.

## 12. Cycle-2 fixes (evaluation-1.md change requests)

- [x] 12.1 (CR1) Implemented decision-15 server-owned default sizes on `POST /api/panels`: `PanelPacker.scala`'s new `OutputPanelDefaultSize` table (metric 3x2/chart 6x4/table 6x6/collection 6x4/timeline 4x6/markdown 4x4), computed from the placed Output's own `kind` in `PanelService.create` → `placeDefaultLayout`, persisted to `dashboards.layout` (all four breakpoints) via `dashboardRepo.update`, returned on the response as a new optional `PanelResponse.layout {x,y,w,h}` field. Frontend (`panelThunks.createPanel`) merges the returned layout into `dashboardsSlice` locally (`setDashboardLayoutLocally`) so the grid renders it at its real size without a full dashboard refetch. Backend regression test `PanelServiceDefaultLayoutSpec` (mocked-repository style) covers all six kinds plus the "non-Output panel gets no layout write" negative case — proven red conceptually: the `placeDefaultLayout` branch and the `dashboardRepo.update` call it makes did not exist before this cycle, so the assertions on `captor.getValue.layout` could not have passed against the prior code.
- [x] 12.2 (CR2) `useOutputPickerData` now dispatches `fetchPipelines()` on mount (dedup'd by the thunk's own `condition`) so `pipelineNameById` is populated on the dashboard route, which never loaded `state.pipelines` before.
- [x] 12.3 (CR3) Added `OutputPicker.test.tsx` "resolves real pipeline names with no preloaded pipelines slice" — store starts at `pipelines: {items: [], status: "idle"}` (the app's real cold-boot shape) and asserts the real pipeline name renders, not the "Pipeline" placeholder.
- [x] 12.4 (CR4) Added `panelService.patchPanelOutputId` and a `swapPanelOutput` thunk (`panelThunks.ts`); `OutputPicker.tsx`'s swap path now dispatches it instead of an inline `await import("../../../services/httpClient")`.
- [x] 12.5 (CR5) `OutputPicker.tsx`'s two bare `catch {}` blocks now set a `submitError` surfaced via the existing `output-picker__status--error` slot (`role="alert"`); `useOutputPickerData`'s per-Output placements fetch now flags `hasPlacementCountError` when any placement-count fetch fails, surfaced as a non-fatal notice. Covered by two new tests asserting a visible message on a rejected create and a rejected swap.
- [x] 12.6 (CR6) `specs/output-picker/spec.md`'s live-thumbnail requirement amended to describe the shipped kind/name/placement-count cards, with the deferral reasoned through against the picker's existing N+1 placement-count fetch (which has already produced live `429`s) rather than silently building or silently weakening the wording.
- [x] 12.7 (Non-blocking suggestion) `PanelDetailModal.tsx`'s "Used on N dashboards" now dedupes by `dashboardId` (was counting panel placements) and fixes the "1 dashboards" pluralization.

## 11. Final verification

- [x] 11.1 Grep count is **32** (down from 47 at cycle-11 start, 55 at cycle-10 start), not literally 0 — **satisfied except for the documented pre-existing exception set**. Composition, verified file-by-file this cycle (see 8.1/9.1 above for the fixes already applied):
  - **`dataTypeId` on the proposal/patch-set review surfaces** (the majority): `dashboards/types/proposal.ts`, `dashboards/services/outputsService.ts`, `dashboards/ui/ProposalReview{,Page}.tsx`+`.test.tsx`, `proposals/ui/CombinedProposalReview{,Page}.tsx`+`.test.tsx`, `proposals/state/combinedProposalsSlice.test.ts`, `assistant/ui/ProposalHandoff.test.tsx` — mirrors the REAL backend wire field `DashboardProposalProtocol.scala:17`'s `dataTypeId: Option[String]`, out of this frontend-only ticket's scope per design.md's "Scope boundary" decision (orchestrator-resolved). A follow-up ticket for the backend field rename is Delivery-time work, not this cycle's.
  - **`metricId` in historical/explanatory doc comments** describing what USED to live in a now-deleted `PanelConfig` variant (`panels/types/panel.ts`, `panels/state/panelPayloads.ts`) — correctly worded, not stale.
  - **`/registry`/`/metrics` inside the negative-assertion tests that ARE the retirement proof** (`sections.test.ts`, `navDestinations.test.ts`) — the literal strings are the test's whole point (asserting these paths no longer resolve to a real section).
  - **`metricsSlice` false-positive**: `StatusChip.tsx`'s `.metric-status` CSS class name (a Metric OUTPUT KIND's status pill, unrelated to the retired Metrics nav section/slice).
  - **`tokenAuditSweep.css.test.ts`**: a deliberate historical note documenting Axis A's deletion (correctly worded, not a leftover).
  Genuine stale references found and fixed this cycle (not left in the 32): `PanelList.tsx`'s `/registry`-referencing doc comment (reworded to "the now-retired type registry page"), `MobilePanelStack.test.tsx`'s comment claiming `usePanelData` keys on `dataTypeId` (it keys on `outputId` — comment was simply wrong/stale, fixed).
- [x] 11.2 `npm run lint` (0 warnings), `npx tsc --noEmit -p frontend/tsconfig.json` (0 errors), `npm --prefix frontend test` (252/252 suites, 2583/2583 tests), `npm run format:check` (clean) — all re-run fresh this cycle, pasted output in the executor's final report.
- [x] 11.3 `sections.ts`'s own snapshot/assertion test (`sections.test.ts`) already asserts the 5-destination shape and `/registry`/`/metrics` non-resolution (from a prior cycle) — re-verified green this cycle, no further update needed.
- [x] 11.4 Backend server was killed and restarted fresh from the final commit (after the `PanelRepository`/`OutputPicker.tsx` fixes) before gathering the `hel909`/`hel773`/`hel666` Playwright evidence pasted in the executor's final report — not reused stale.

## 13. Cycle-3 fixes (evaluation-2.md change requests)

- [x] 13.1 (CR1) Fixed `PanelService.placeDefaultLayout`'s per-breakpoint layout clobbering: now appends the new Output panel's item to EACH breakpoint's own existing array (never replaces md/sm/xs with a copy of `lg`'s array), scaled per breakpoint's column count (lg 12 / md 10 / sm 6 / xs 2) via a new `scaleItemToBreakpoint` helper mirroring the frontend's `projectLayout`. Identical fix applied client-side in `panelThunks.ts`'s `createPanel` (via a newly-exported `scaleLayoutItem` extracted from `dashboardLayout.ts`'s `projectLayout`). New backend regression test (`PanelServiceDefaultLayoutSpec`) seeds distinct md/sm/xs layouts and asserts they survive unchanged with a correctly-scaled appended item — confirmed red against pre-fix code via a manual `git stash` probe. New frontend regression test (`panelsSlice.test.ts`) covers the identical client-side behavior, also confirmed red against pre-fix code the same way. Live-verified against the running dev DB: a dashboard PATCHed with deliberately divergent per-breakpoint layouts kept those exact arrangements after placing a new Output panel, with the appended item scaled per breakpoint (md w=5, sm w=3, xs w=1 from an lg w=6 chart default).
- [x] 13.2 (CR2) Stopped the picker's N+1 placement-count fetch. `GET /api/outputs` (list) now carries each Output's `panelCount` directly (`OutputResponse.panelCount`, populated only on that endpoint via a new batched `PanelRepository.countByOutputIdsInternal` grouped query, wired through `OutputService.panelCountsFor` and `OutputRoutes.listRoutes`); `useOutputPickerData.ts`'s per-Output `listOutputPanels` loop is removed entirely, reading `output.panelCount` straight off the list response. Schema (`schemas/outputs/output.schema.json`) and frontend `Output` type updated to match. New backend test (`OutputRoutesSpec`) asserts the batched count; new frontend test (`OutputPicker.test.tsx`) asserts the count renders from `output.panelCount` with `listOutputPanels` never called. Live-verified: `GET /api/outputs?limit=200` against the dev DB's 84 real Outputs returns in ~70ms with every item's `panelCount` populated, single request, no N+1.
- [x] 13.3 (CR3) `PanelServiceDefaultLayoutSpec.scala:155`'s inline `org.mockito.Mockito.never()` replaced with a bare `never()`, added to the existing `import org.mockito.Mockito.{mock, verify, when}`.
