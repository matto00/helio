## 1. Tests

### Tests

- [x] 1.1 Re-confirm ground truth before editing: `titleFor()` still uses `closest("[title]")`, the three render sites still bind `title` on `.pipeline-detail-page__step-section` / `.pipeline-detail-page__tail-chain-step` wrappers, and the wiring fixture still sets `rootId` on `r1a`/`r2a`. Stop and escalate on any mismatch.
- [x] 1.2 Record a baseline green run of `npm test -- --testPathPattern=PipelineRiverView` before any edit.
- [x] 1.3 Rewrite `titleFor()` per design D1/D2: scope `closest` to the two wrapper classes as a selector list; throw `No step wrapper for "<label>"` when it returns null; throw `Step wrapper for "<label>" has no title attribute` when `hasAttribute("title")` is false; otherwise return the attribute.
- [x] 1.4 Update the helper's doc comment to state the invariant it now enforces and why `sectionFor()` and bare `[title]` are both unusable (replace HEL-985's now-stale "D3" note rather than leaving both).
- [x] 1.5 Add the D4a ancestor-inert guard for the standard site: render, take the `.pipeline-detail-page__lane-column` enclosing `Two lane first`, `setAttribute("title", "ancestor tooltip")` on it, assert `titleFor("Two lane first")` still returns that step's own path.
- [x] 1.6 Add the D4a guard for the compact site: same shape, using `Solo lane step` and its enclosing `.pipeline-detail-page__tail-chain`.
- [x] 1.7 In both D4a tests assert `mutated.contains(resolved) && mutated !== resolved` (strict-ancestor, not the wrapper, not an element below it) and that the injected title differs from the expected path. Comment both tests explicitly as GUARDS that are green on the pre-change helper — they are not the proof.
- [x] 1.7a Add the D4b proof for the standard site: build a synthetic DOM — a titled ancestor `div` containing a `.pipeline-detail-page__step-section` with NO `title` holding the label text — and assert `expect(() => titleFor(label)).toThrow(/has no title attribute/)`. Append to `document.body`, use a label string absent from the fixture, and remove it in a `finally`/`afterEach` so it cannot pollute the file's other by-text queries. Red on the old helper, green on the new one.
- [x] 1.7b Add the D4b proof for the compact site, identical but with `.pipeline-detail-page__tail-chain-step`.
- [x] 1.8 Verify the six pre-existing `nodePath wiring (HEL-985)` assertions keep their exact current expected strings and still pass; confirm no fixture step was added without a `rootId`-bearing root head (design Risk 4 / the HEL-985 indistinguishability trap).
- [x] 1.8a Mutation run (a0-i) — the pure vacuous green: locally restore the PRE-change `titleFor()` (`closest("[title]")`), delete the call site, and set the `.pipeline-detail-page__tail-chain` ancestor of `Solo lane step` to `title="root:root-1 > r1a > r1b > lane1a"` (its exact expected path). Observe that THAT assertion passes green with the wiring gone; the other five are expected red and are not part of this claim. Capture the transcript.
- [x] 1.8b Mutation run (a0-ii) — the axis collapse: same pre-change helper and deletion, but an arbitrary ancestor tooltip. Observe that it fails with the SAME string-mismatch message shape as the value-mismatch mutation, and place the two transcripts side by side. Name the single assertion being compared, exactly as 1.8a does — the other five steps have no titled ancestor and will throw `No title-bearing ancestor`, which is not the claim. Then restore the hardened helper.
- [x] 1.8c If either (a0-i) or (a0-ii) does not reproduce as described, STOP and escalate. Do not write an outcome that was not observed.
- [x] 1.9 Mutation run (b): temporarily add an ancestor `title` via post-render `setAttribute` (same mechanism as D4a; pin it in the transcript so the run is reproducible) AND delete the `nodePath()` call site in `PipelineRiverView.tsx:294`; run the suite; capture the transcript; confirm red with the D2 absent-attribute message, not a value mismatch, and confirm D4b is GREEN here (it is independent of the call site). Revert the product-code mutation immediately after.
- [x] 1.10 Mutation run (c): with the product code otherwise intact, neuter the call site to `entries[step.id] = step.id`; run the suite; capture the transcript; confirm red with a string-mismatch message on the value assertions. Revert immediately after.
- [x] 1.11 Confirm runs (b) and (c) fail with different messages on different assertions (design D5). Check against the predicted outcome: D4b green in both; D4a and the six HEL-985 value assertions red in both. Report any deviation as a defect, not an observation.
- [x] 1.12 Mutation run (d): confirm the tree is fully restored (`git diff` shows no product-code change) and the suite is green again.
- [x] 1.13 Write the full five-run transcript — (a0: both readings i and ii), (a) baseline green, (b) ancestor-title + deletion, (c) value-mismatch, (d) restored green — enumerating each by name so none can be silently omitted; write it to `openspec/changes/harden-nodepath-wiring-guard/mutation-evidence.md`, including the exact commands, the mutation diffs applied, the verbatim (a0) vacuous-green output, and the verbatim failure output for (b) and (c).
- [x] 1.14 Run `npm run lint`, `npm run typecheck`, and `npm run format:check`. Do not run any backend spec, dev server, database connection, or Playwright/e2e — hard environment constraint; escalate instead if anything appears to require them.
- [x] 1.15 Confirm the final diff touches only `PipelineRiverView.test.tsx` plus `openspec/` artifacts — zero product-code diff.
