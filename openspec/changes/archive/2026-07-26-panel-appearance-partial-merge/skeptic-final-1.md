## Skeptic Report — final gate (round 1)

### What I verified (with evidence)

1. **Full diff read** (`git show 07b1bff8`, `git diff --stat main...HEAD -- . ':!openspec'`): 11 code/schema files, 883 insertions / 42 deletions, exactly the surface the ticket describes. No `frontend/**` paths in the diff (grep for `frontend/` in `git show --stat` returned nothing) — confirmed the UI-review phase is a genuine no-op; skipped Playwright per instructions.

2. **Traced two self-constructed wire payloads through the real decode/merge logic** (read `backend/src/main/scala/com/helio/domain/model.scala` directly, not summaries):
   - `{"background": "#0a0"}` (chart key genuinely absent): `PanelAppearance.Patch.decode` → `fields.get("chart") == None` → `chart = None` (absent) → `applyPatch`'s `chart = patch.chart.fold(existing.chart)(...)` → `existing.chart` preserved wholesale, including `chartType`. Matches AC1.
   - `{"chart": {"chartType": "bar"}}`: `chart` decodes to `Some(Some(ChartAppearance.Patch(...)))` with only `chartType = Some(Some("bar"))`, all other chart-patch fields `None`. `applyPatch` calls `ChartAppearance.applyPatch(chartPatch, existing.chart.getOrElse(Default))`, which folds each field — only `chartType` changes, `seriesColors`/`legend`/`tooltip`/`axisLabels` fall through to `existing`. Matches AC2. Also confirmed the field-level exception: `chartType: null` inside a chart patch maps through `identity` (→ `None`), not `Default.chartType` (`"line"`) — the one documented deviation from the general null-resets-to-Default rule, and it is real in `ChartAppearance.applyPatch`'s `chartType = patch.chartType.fold(existing.chartType)(identity)` line, not just asserted in the MCP doc string.

3. **Independently read (not grepped-by-name) 4+ new test bodies for literal `JsObject(...)` construction with a key genuinely omitted**:
   - `PanelAppearanceMergeSpec.scala`: `val json = JsObject("background" -> JsString("#0a0"))` — no `color`/`transparency`/`chart` keys in the literal at all.
   - `ApiRoutesSpec.scala` AC1 test: second PATCH body `JsObject("background" -> JsString("#0a0"))`, no `chart` key — established chart is from a *prior* PATCH call, never resent.
   - `ApiRoutesSpec.scala` "batch appearance update preserves an omitted field": first batch call `JsObject("color" -> ...)`, second `JsObject("background" -> ...)` — `color` never resent in call 2, assertion checks it survives.
   - `ApiRoutesSpec.scala` "batch appearance update accepts a partial chart payload": second call `JsObject("chart" -> JsObject("chartType" -> JsString("scatter")))` only — `legend`/`seriesColors`/etc. established in a prior call, never resent, assertion checks survival.
   These are the actual hazard class (field genuinely absent from the `JsObject`, not `null`-substituted) — confirmed, not merely claimed.

4. **Re-ran tests myself, fresh**: `cd backend && sbt -batch "testOnly com.helio.api.ApiRoutesSpec com.helio.domain.PanelAppearanceMergeSpec"` → 207/207 passed, including all named HEL-362 AC1–AC3 tests, the null-reset tests, the top-level-null-no-op tests, and all `PanelAppearanceMergeSpec` cases. Then ran the **full suite**: `sbt -batch test` → **2050/2050 passed**, 115 suites, 0 failed. Also ran `npm run check:schemas` (PASS), `npm run check:scala-quality` (clean, 64 pre-existing soft warnings only — `ApiRoutesSpec.scala` now 4507 lines is flagged as an existing soft warning, not a new hard failure), and `npx prettier --check` on the touched TS/JSON files (all pass).

5. **Single/batch share one merge function**: `PanelServiceHelpers.resolvePatch` (line 38) calls `PanelAppearance.applyPatchJson(json, existing.appearance)`; `PanelMutationRepository.batchUpdate` (in `PanelMutationRepository.scala`) calls the same `PanelAppearance.applyPatchJson(appearanceJson, current)`. Read both call sites directly — no duplicated/parallel merge logic; the old hand-rolled `getOrElse`/`.orElse` batch block is fully deleted, not left dangling.

6. **Create path untouched**: `git diff 07b1bff8~1 07b1bff8 -- backend/src/main/scala/com/helio/services/PanelPatchApplier.scala` → 0 lines changed. `PanelServiceHelpers.resolveCreateAppearance`/`normalizeAppearancePayload` are unchanged in logic (still route through `RequestValidation.normalize*`/`validateChartType` against a wholesale `PanelAppearancePayload`), and `CreatePanelRequest.appearance` is still typed `Option[PanelAppearancePayload]` with `jsonFormat4` (only `UpdatePanelRequest`/`PanelBatchItem`'s `appearance` field changed type to `Option[JsValue]`). Matches AC5 exactly.

7. **Ticket ACs traced one by one against real code/tests**: AC1–AC7 all map to specific, read code paths and passing tests (detailed above + `ApiRoutesSpec.scala`'s dedicated `(HEL-362 AC1/AC2/AC3)`-tagged tests and the pre-existing donut-rejection tests updated to `.toJson` and still passing for AC4).

8. **Schema/doc-string consistency**: `schemas/panel-appearance-patch.schema.json` correctly models all-fields-optional including nested `chart` fields and the `chartType` null-carve-out is not separately schema-encoded (schema can't express "null means something different here") but is documented in both the schema description and the MCP tool description — and I confirmed the MCP description's specific behavioral claim ("clears just chartType (renders as the line default)") is accurate against `frontend/src/utils/chartAppearance.ts:27` (`(chart.chartType as ChartType) ?? "line"`), so a cleared `chartType` genuinely does render as line downstream — not an invented claim.

9. **`DashboardProposalService.scala` consumer fix verified correct**: `buildChartAppearance` always produces `chartType = Some(...)` (either the proposal's value or `Default.chartType` which is `Some("line")`, never `None`), and `ChartAppearance` has no other `Option`-typed field, so `chartAppearanceFormat.write(appearance)` always serializes a full 5-field `chart` object with nothing omitted — meaning the `JsObject("chart" -> ...)` patch sent to `UpdatePanelRequest.appearance` fully replaces `chart` (equivalent to today's behavior), while `background`/`color`/`transparency` are genuinely absent from that JsObject and preserve the just-created panel's `PanelAppearance.Default` values — harmless, as claimed. Read the actual `ChartAppearance` case class (`model.scala:108-114`) and `ChartAppearance.Default` (`chartType = Some("line")`) to confirm this, not merely trusted the code comment.

10. Confirmed `git diff --stat main...HEAD -- . ':!openspec'` shows exactly the 11 files in the commit stat — no stray uncommitted code changes; working tree is clean apart from `workflow-state.md` (expected orchestrator bookkeeping) and the untracked `evaluation-1.md`.

### Verdict: CONFIRM

### Non-blocking notes
- `PanelServiceHelpers.scala:52-53` — `normalizeAppearancePayload`'s doc comment still says it's "Shared by the create path (`resolveCreateAppearance`) and the single-item PATCH path (`resolvePatch`)", which is now stale (`resolvePatch` calls `PanelAppearance.applyPatchJson` instead, per line 38). Cosmetic only, matches the evaluator's own non-blocking nit — worth a one-line fix in a follow-up, not a blocker for this gate.
- Ticket's Decision 5 (dashboard appearance PATCH has the identical replace-bug) was correctly scoped out with a spinoff filed per `workflow-state.md`/design.md — did not independently re-verify the Linear spinoff ticket exists, but the design rationale for scoping it out is sound and explicitly sanctioned by the ticket's own pre-brief ("decide with the design gate whether it belongs in this ticket or a spinoff").
