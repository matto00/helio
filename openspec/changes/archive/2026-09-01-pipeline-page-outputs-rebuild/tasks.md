## 1. Ground truth verification (before writing code)

- [ ] 1.1 Confirm running dev frontend+backend are built from the current worktree commit before any UI evidence (restart if in doubt) -- checked cycle 2: neither server is running on this worktree's assigned ports (9247/6340); starting them is an orchestrator-level step, not attempted here
- [x] 1.2 Verify live shape of `capabilities`, `preview` (both arms), `GET /api/pipelines/:id/outputs`, `GET /api/outputs/:id/panels`, `GET /api/outputs/:id/rows`, `GET /api/outputs/:id/assertion-status`, `expand` `{steps, outputs?}`, step DELETE 200-body against the running backend, not just docs -- outputs/capabilities/preview verified against `OutputRoutes.scala`/`PipelineRunStatusRoutes.scala`/`NodeCapabilitiesProtocol.scala` source; `expand`/step-DELETE shapes still need a live-server re-check before section 6 work
- [x] 1.3 Enumerate every `dataTypeId`/`GET /api/types` call site under `features/pipelines` and `features/panels/ui/editors` (HEL-936 share) and every `expand`/step-DELETE consumer (HEL-934 share) -- see execution-progress.md Cycle 2
- [x] 1.4 Enumerate every piece of run-scoped state on `PipelineDetailPage` (Redux fields + local state) that must reset on navigation/new-run, for the HEL-878 PR writeup -- see execution-progress.md Cycle 2

## 2. Redux/data layer

- [x] 2.1 Add/extend `outputsSlice` (or extend `pipelinesSlice`) for Output CRUD via `GET/POST /api/pipelines/:id/outputs` + `GET/PATCH/DELETE /api/outputs/:id` (NOT embedded in the pipeline detail fetch -- verified `PipelineSummaryResponse` has no outputs/steps), capabilities-at-node cache, per-Output preview cache keyed by `outputId`
- [x] 2.2 Implement `usePipelinePreviewCache` hook shared by rail thumbnails and the Output sheet -- `useOutputPreview`/`useUnsavedStepPreview` in `hooks/usePipelinePreviewCache.ts`, tested
- [x] 2.3 Add request-id-token guard to preview/analyze thunks (HEL-681) -- done for the new Output preview thunks; `pipelinesSlice`'s pre-existing analyze thunk not yet touched
- [x] 2.4 Implement single `resetRunScopedState` reducer called from both the dry/live-run thunk lifecycle and the SSE handlers (HEL-878) -- `outputsSlice` extraReducers now matches `submitPipelineRun.pending`; `usePipelineDetailPage`'s SSE `onTerminal` and nav-cleanup effect both dispatch `resetRunScopedState()` alongside `clearRunState()`
- [ ] 2.5 Remove `dataTypesSlice` usage from pipelines/editors surfaces; delete now-dead selectors (`selectPipelineOutputDataTypes`) -- re-confirmed cycle 7 STILL blocked, and precisely why: `grep -rn selectPipelineOutputDataTypes frontend/src/features/panels/ui/editors` shows every remaining call site is inside `CollectionEditor.tsx`/`MarkdownEditor.tsx`/`TimelineEditor.tsx`/`TextContentEditor.tsx`/`BindingEditor.tsx` -- these are EXACTLY the files task 5.1-5.4 rewrite into `OutputEditorSheet.tsx` (capabilities-at-node driven, no `dataTypeId`/`DataTypePicker` at all per design.md decision 14). Removing the imports now, before 5.1-5.4 land, would either strip the type-selection UI from five still-in-use editors with no replacement, or require duplicating the capabilities-at-node selection logic ahead of the sheet that's supposed to own it once, then deleting the duplicate. Correct order is 5.1-5.4 first (replaces the call sites), then 2.5 as the mechanical cleanup once `DataTypePicker`/`selectPipelineOutputDataTypes` genuinely have zero remaining callers (5.9's grep-before-delete step covers this).
- [x] 2.6 (Cycle-2, evaluation-1 CR5) Fixed two reference-unstable `outputsSlice` selectors causing F-146-class cascade rerenders: `selectOutputsForPipeline` and `selectOutputsByStepId`'s input selector both used `state.outputs.byPipeline[pipelineId] ?? []`, allocating a fresh array on every call once a pipeline had no Outputs yet -- defeating `useAppSelector`'s/`createSelector`'s reference-equality memoization (React-Redux's "selector returned a different result" warning, growing 2 -> 4 -> 6 -> 8 across an interactive session). Fixed with a shared module-level `EMPTY_OUTPUTS` sentinel; also converted `selectOutputsForStep` (same `?? []` plus a fresh `.filter()` every call) to a memoized `createSelector`, even though it currently has zero live `useAppSelector` callers, so it isn't a live trap for the next one. Corrected `selectOutputsByStepId`'s doc comment, which falsely claimed memoization held on `byPipeline[pipelineId]`'s own identity. Added 3 new Jest cases in `outputsSlice.test.ts` asserting reference stability across two calls on unchanged state.

## 3. River + tails + rail

- [x] 3.1 Split `PipelineDetailPage.tsx` into page shell (using `PageShell`/`PageHeader`/`PageStatus`) + hooks (HEL-682) -- extracted `usePipelineDetailPage.ts`; page shell already used `PageShell`/`PageStatus` pre-split (no `PageHeader` usage in this file -- `PipelineDetailHeader` is its own bespoke header component, unrelated to the shared `PageHeader` primitive; not touched)
- [x] 3.2 Split `StepCard.tsx` into trunk `StepCard` + op-editor ladder (`StepOpEditor.tsx`) + preview state (`useStepCardPreview.ts`) (HEL-682) -- `TailChain` deferred to 3.4 (new UI for tail rendering, not a split of existing code)
- [x] 3.3 Render Outputs rail per trunk step with live thumbnails; "+ output" affordance -- `OutputsRail` wired into `StepCard.tsx` via new `selectOutputsByStepId`/`selectPreviewRowCountByOutputId` selectors and `fetchOutputs` on page load; `onOpenOutput`/`onAddOutput` now open the real `OutputEditorSheet` (task 5.1, Cycle 9) instead of the toast stub.
- [x] 3.4 Render tail as indented dashed chain: `parentStepId` wired end to end (wire/UI `Step` types, `pipelineStepToStep`, `createPipelineStep`), `buildStepTree` selector (`state/stepTree.ts`, 6 Jest cases), `TailChain.tsx` renders a trunk node's tail nested/indented/dashed via `StepCard`'s new `isTail` prop. "+ tail" CREATE affordance (Cycle 8, on the human-granted non-goal waiver — see design.md): `PipelineRiverView` renders a per-trunk-node "Add tail step" button (hidden once that node already has a tail — single-tail-per-node enforcement), wired through `usePipelineDetailPage.handleAddTailStep` -> `createPipelineStep(..., attachAsTail: true)` -> the new backend `attachTailInternal` primitive. Live-probe-confirmed correct (curl + `e2e/hel908-tail-attach.spec.ts`): the new step lands as a genuine `position >= 1` sibling, the anchor's existing trunk continuation is NOT reparented. **Cycle-2 fix (evaluation-1 CR1):** the anchor's own doc comment sanctioned a `position == 0` fallback when the anchor had NO existing children, so "add tail" off a LEAF anchor (the common case -- adding off the pipeline's current last trunk step) silently created a TRUNK step, not a tail, 100% of the time. Fixed at the primitive (`attachTailInternal`/`attachTailInternalAction` in `PipelineStepRepository.scala`): position is now floored at `1` unconditionally, regardless of whether the anchor has children, so a leaf anchor's tail lands at `position == 1` and its own `position == 0` slot is deliberately left empty rather than back-filled. Added leaf-anchor cases at both the repository level (`PipelineStepRepositorySpliceSpec`, 2 new specs) and the route level (`PipelineStepRoutesSpec`, 1 new spec). Removed the false rationalization comment in `e2e/hel908-full-flow.spec.ts` (it claimed the leaf/non-leaf distinction "only matters once a node has TWO children" -- false at the data level) and updated that spec's assertions to require the real tail class/position. Also applied the evaluation-1 non-blocking suggestion: `attachTailInternal`'s `parentStepId` tightened from `Option[PipelineStepId]` to a non-`Option` `PipelineStepId`, since `PipelineService` only ever calls it from its `parentStepId`-present branch.

- [x] 3.4b (Cycle-2, evaluation-1 CR1 follow-up) The backend fix alone (task 3.4's note above) turned out to be necessary but NOT sufficient for the UI-visible half of CR1: `stepTree.ts`'s `buildStepTree` derived trunk-vs-tail from array order + child COUNT only, with its own documented invariant "a node with only one child needs no disambiguation -- that child is always trunk continuation." A leaf anchor gaining exactly ONE new tail child is the identical flat-array shape whether that child lands at position 0 or position >= 1 (no second child to order against), so the backend's position fix alone left the frontend still rendering it as a trunk-continuation card. Found live while re-verifying CR1's own e2e assertion (`toHaveCount(1)` on `--tail` failed with a real, reproducible 0). Fixed by threading `position` through onto the UI `Step` type (`types/step.ts`, populated in `pipelineStepToStep`) and having `buildStepTree`'s single-child branch consult it: `position === 0` (or `undefined`, the not-yet-persisted-temp-step case) stays trunk continuation; `position >= 1` is now treated as a tail, with the anchor's own trunk ending there rather than continuing. 3 new `stepTree.test.ts` cases cover the position-0/position>=1/position-undefined single-child shapes. Also found and fixed, while re-running `e2e/hel908-full-flow.spec.ts` to re-verify this live: a genuine, pre-existing (unrelated to this fix) test race -- the optimistic step-add flow renders a step with a local temp id (`step-N`) before its create POST resolves, and `toHaveCount(1)` was satisfied by that optimistic card well before reconciliation, so clicking "Add output" immediately after used to occasionally open the sheet against the stale temp id (every downstream capabilities/analyze call then 404s on it, hanging the test). Fixed by waiting for the create POST's own response before proceeding, removing the race (spec re-run 4x clean afterward; a SEPARATE, unrelated intermittent dry-run-status-visibility timeout was also observed once in that same session -- noted as pre-existing/environmental, not touched here, out of CR1's scope).
- [x] 3.5b (Cycle-2, evaluation-1 CR2) `openspec/specs/pipeline-step-reorder/spec.md` and `schemas/pipelines/reorder-pipeline-steps-request.schema.json` still documented reorder's PRE-`reorderTrunkInternal` contract ("permutation of all current step ids, position set to index") even though non-goal waiver #2 narrowed it to trunk-only relink-and-position-0. Updated the live capability spec in place (it had drifted out of sync with the shipped route/repository behavior even before this change), added a `MODIFIED Requirements` delta at `openspec/changes/pipeline-page-outputs-rebuild/specs/pipeline-step-reorder/spec.md` (including a new tail-bearing-pipeline scenario and an explicit tail-id-rejected scenario) so archive corrects the live spec, and updated the schema's `description` field to match. `openspec validate pipeline-page-outputs-rebuild --strict` passed at the time this task was written; it later regressed to 3 group-mismatch WARNINGs (exit 1) once the CR9/CR10/CR11 fix tasks were appended as `3.9`/`3.10`/`3.11` under this file's `## 10.` heading, caught by skeptic-final-scope-round2 CR1 and fixed by renumbering them to `10.7`/`10.8`/`10.9` -- see that task's own note below and design.md.
- [x] 3.5 Verify drag-reorder/insert/duplicate/enable-disable work on trunk and tail -- CLOSED cycle 9 (human ruling on Cycle 8's escalation: "the tail follows its trunk step"). `PipelineStepRepository.reorderTrunkInternal` relinks the trunk's `parentStepId` chain (design.md decision 15); `e2e/hel908-trunk-reorder-order.spec.ts`'s `test.fail()` annotation REMOVED and re-confirmed GREEN for real (Move up/down buttons); new `e2e/hel908-trunk-reorder-drag.spec.ts` proves the actual HTML5 DRAG gesture on a tailed trunk node, confirming the tail travels with its node and the old-slot occupant does not inherit it, surviving reload. 7 mutation-proven backend specs in `PipelineStepRepositorySpliceSpec` cover permutation, tail-travel, old-slot non-inheritance, a no-tail regression guard, and 3 rejection cases. Also fixed a real bug found alongside the fix: `PipelineRiverView`'s drag-drop/Move-up-down used to index the FLAT array with TRUNK-relative indices (silently mismatched once any tail existed) -- now uses the new `reorderTrunk` client-side helper. Tail-internal reorder remains untested (no product requirement surfaced one yet; unaffected by this fix).
- [x] 3.6 Fix mobile OUTPUT bar overlap at 375px (HEL-676); verify 375px/430px touch targets -- overlap half: genuinely not reproducible across three cycles of combinations tried (long names, gallery tab, Output sheet, both 375x812 and 375x667); the river's `overflow-y: auto` genuinely clips rather than bleeds; needs the exact device/viewport/interaction sequence from the original HEL-676 report to proceed further. **Touch-target half CLOSED Cycle 2 (evaluation-1 CR4)**: the prior cycles' evidence (`::after` height resolving to 44px via `getComputedStyle`) was exactly the kind DESIGN.md's "Expander tiling" [mechanical] rule names as insufficient -- it cannot detect a symmetric-but-clipped or occluded real hit region. A real `elementFromPoint` bisection (0.25px step, 375x812) found the actual vertical hit extent was 36.5px (326.25 -> 362.75), short of the 44px floor even with the sanctioned epsilon. Two independent root causes, both fixed: (1) `.pipeline-detail-page__step-card` has `overflow: hidden` (rounded-corner clipping), and when the Outputs rail is a collapsed card's last visible content, the chip's `tap-expand-44` bottom overhang sat flush against the card's own clip boundary and was clipped outright -- fixed via `padding-bottom: 8px` on the card scoped to the same touch-target media query (a layout fix; z-index cannot escape a clipping ancestor). (2) The rail's flex `gap` was `--space-2` (8px), under DESIGN.md's >=16px floor for two adjacent 28px expander-based controls -- raised to `--space-4` (16px). Also added `z-index: 1` to the shared `.tap-expand-44::after` rule itself (paint-order robustness for every consumer, not just this one) since the occlusion symptom could equally have been a stacking-order issue on a different layout. Re-verified via a fresh `elementFromPoint` bisection on a freshly-created pipeline (not the same fixture repeatedly probed) at 375x812: real vertical extent now 44.5px (326.25 -> 370.75), symmetric around the chip's own 28px box; horizontal extent 193.5px (the chip's own painted width, already far over the 44px floor).

- [x] 3.7 (Cycle-2, evaluation-1 CR6) Completed the ARIA tabs pattern on the Steps/Outputs tab strip (`PipelineDetailPage.tsx`): the tablist had two `role="tab"` elements with `aria-selected` but zero `role="tabpanel"`, no `aria-controls`, no `id`, no roving tabindex. Added `id`/`aria-controls` on each tab, `role="tabpanel"` + `aria-labelledby` on each panel container, and roving `tabindex` (`0` on the active tab, `-1` on the inactive one) with Left/Right arrow-key navigation moving focus AND switching the active tab. Kept local rather than extracted into `shared/ui/`: this is still the only `role="tablist"` in the codebase, and a shared primitive with a single concrete consumer would be premature generalization -- worth revisiting once a second tab strip exists to validate the shape against. Live-verified via a scripted Playwright probe (tab attrs, panel role/aria-labelledby, ArrowRight moving focus + selection to the Outputs tab) and a new Jest case in `PipelineDetailPage.test.tsx`.
- [x] 3.8 (Cycle-2, evaluation-1 CR7) Moved 3 DESIGN.md [mechanical] inline-style violations in `OutputPreviewPane.tsx` (`style={{ height: 240 }}`, `style={{ whiteSpace: "pre-wrap" }}`, `style={{ overflow: "auto", maxHeight: 240 }}` -- all static layout values, not genuinely dynamic) into real classes (`output-preview-pane__chart`, `output-preview-pane__markdown`, `output-preview-pane__table-section`) in `OutputEditorSheet.css`, matching the existing plain-px convention other CSS files in the codebase already use for this exact kind of static chart/preview sizing (e.g. `ErrorBoundary.css`'s `max-height: 240px`).

## 4. Outputs gallery tab

- [x] 4.1 Add "Outputs (N)" tab beside Steps -- `PipelineDetailPage.tsx` now has a `role="tablist"` Steps/Outputs tab bar (local `useState`, not persisted); river view renders under "Steps", `OutputsGalleryTab` under "Outputs (N)" (count = `allOutputs.length` via new `selectOutputsForPipeline` wiring in `usePipelineDetailPage`)
- [x] 4.2 (still partial) Gallery cards: "off <step>" subtitle, "on N dashboards" via `GET /api/outputs/:id/panels` fetched lazily per card -- both done (unchanged from Cycle 7). "Live render reusing panel renderers" is now possible in principle -- `OutputEditorSheet`'s `OutputPreviewPane.tsx` (Cycle 9) IS the Output->Panel-renderer adapter (reuses `ChartRenderer`/`MetricRenderer` directly against Output rows) -- but `OutputGalleryCard.tsx` itself was NOT wired to it this cycle (budget; the sheet's own preview was the higher-leverage target, per the orchestrator's framing). Still reuses the row-count text summary. Revisit: wire `OutputGalleryCard` to reuse `OutputPreviewPane` for a real inline chart/metric render.
- [x] 4.3 Place-on-dashboard flow (dashboard picker, `POST /api/panels`) -- CORRECTED (orchestrator, final pass): this is explicitly OUT OF SCOPE per ticket.md's own "Out of scope" section ("Dashboard picker and panel sheet (P1.6)") -- HEL-909/P1.6 owns the dashboard picker. Not a gap; earlier cycles' framing of this as a deferred/needs-follow-up item was a tasks.md/ticket.md mismatch, corrected here. No follow-up ticket needed -- HEL-909 already covers it as ticketed scope.
- [x] 4.4 "+ New output" -- now opens the real `OutputEditorSheet` with no pre-chosen step; the sheet's own Step selector (a real `Select` over every trunk/tail step + "Pipeline root") is the step-picker task 4.4 asked for, replacing the old "defaults to last step" stub. The rail's per-step "+ output" affordance still pre-fills that step (unchanged, correct — the user already chose a node by clicking that step's rail).

## 5. Output editor migration (owns features/panels/ui/editors/*)

- [x] 5.1 Build `OutputEditorSheet.tsx` from `BindingEditor.tsx`; kind/name/mapping shell driven by capabilities-at-node -- built (`ui/outputEditor/OutputEditorSheet.tsx` + `OutputKindFields.tsx` + `buildOutputConfig.ts` + `outputConfigTypes.ts` + `useOutputTableColumns.ts` + `OutputPreviewPane.tsx`), wired into `PipelineDetailPage`/`usePipelineDetailPage` replacing the toast stubs from 3.3/4.4. Covers all 6 kinds (chart/table/metric/collection/timeline/markdown); config shapes are new (`Output.config` has no DataType-bound precedent), documented in `outputConfigTypes.ts`'s doc comment as a near-identity mirror of the old `PanelConfig` variants minus `dataTypeId`, to keep a future Output->Panel-config render adapter (task 4.2) close to an identity mapping.
- [x] 5.2 Re-point `ChartAggregationFields`/`ChartDisplayFields`/`TableDisplayFields` at capabilities-at-node -- verified these three were already generic on `SelectOption[]`/raw display-option objects, not `DataType`-typed; no changes needed inside them, only the caller (`OutputEditorSheet`) now builds their `fieldOptions` from `columnOptions(capabilities)` instead of a bound DataType's fields.
- [x] 5.3 Collapse `MetricBindingFields`/`MetricPicker`/`useMetricBindingState` into the sheet's metric-kind slot; delete `MetricPicker`/`DataTypePicker` if fully superseded -- the sheet's metric-kind slot is `MetricValueEditor` + label/unit `BoundOrLiteralField` only (no `MetricPicker`/bind-to-metric-entity mode -- HEL-903 dropped the `Metric` entity per design.md decision 3). `MetricPicker`/`DataTypePicker`/`useMetricBindingState`/`MetricBindingFields` NOT yet deleted -- `BindingEditor.tsx` and its siblings (still live, still panel-facing) import them; deletion is 5.9's job once those callers are gone.
- [x] 5.4 Markdown Output kind: reuse `MarkdownEditor.tsx` template UI, field source = capabilities-at-node (binding spec decision 14) -- `MarkdownKindFields` in `OutputKindFields.tsx` is the same field-or-literal Content slot shape, fed by `columnOptions(capabilities)`.
- [x] 5.5 Live preview: `POST /api/pipelines/:id/preview?outputId=` once the Output is saved, `GET /api/pipelines/:id/steps/:stepId/preview` for an unsaved Output; apply in-progress config client-side over the returned rows in both cases (design.md decision 6a), debounced -- wired via the already-built `useOutputPreview`/`useUnsavedStepPreview` hooks (Cycle 1), 400ms debounce on open/node-change in the sheet; `OutputPreviewPane.tsx` applies the in-progress kind/field/aggregation state client-side over the fetched rows at render time (no extra fetch per keystroke, matching decision 6a's "neither endpoint applies config server-side" framing).
- [x] 5.6 "Add as tail with aggregate" action -- built (Cycle 12): `buildOutputConfig.ts`'s new `canAddAsTailWithAggregate`/`buildAggregateTailConfigs` derive an `AggregateConfig` from the sheet's ALREADY-entered chart/metric aggregation fields (groupBy/aggFn/field) rather than a separate parallel form, plus the resulting Output.config for the new (post-aggregation) node -- the aggregate step passes `groupBy` columns through unchanged and emits the aggregation under a fresh `alias` column (verified against backend `AggregateStep`/`InProcessPipelineEngine` semantics), so the Output.config references `alias`, not the pre-aggregation field name, and carries no further Output-level aggregation of its own. `usePipelineDetailPage.handleAddOutputViaAggregateTail` sequences `POST /pipelines/:id/steps` (kind `aggregate`, `parentStepId`, `attachAsTail: true` -- verified against `PipelineStepProtocol.CreatePipelineStepRequest`) then `POST /pipelines/:id/outputs` (verified against `CreateOutputRequest`), rolling the step back (`deletePipelineStep` + local-state removal) if the Output create fails (design.md decision 5's exact "no orphaned aggregate tail" ask). The sheet's new "Add as tail with aggregate" footer button only renders when `isCreate && nodeStepId` is a real node `&& canAddAsTailWithAggregate(...)`. Live-probe-confirmed via `e2e/hel908-full-flow.spec.ts` (task 9.3): the created aggregate step is a genuine second pipeline step and the metric Output attaches to it, not to the original node.
- [x] 5.7 Placements list + links from `GET /api/outputs/:id/panels`, fetched fresh on sheet open; Delete with placement-count warning -- sheet fetches `listOutputPanels` on every open (not cached), renders a placements list, and the Delete button's confirm step names the placement count before the second click actually deletes.
- [x] 5.8 Fix ECharts pie<->cartesian live-switch crash via forced remount (HEL-629), in sheet preview and rail thumbnail -- probe-confirmed root cause BEFORE writing a new fix (per systematic-debugging.md): `ChartPanel.tsx` already renders its ECharts option with `notMerge={true}`, with an inline comment explicitly citing this exact pie<->cartesian crash class as the reason. Reusing `ChartRenderer`->`ChartPanel` for the sheet's live chart preview (rather than hand-rolling chart rendering) means the crash is structurally already prevented by existing code, not a gap this ticket needed to newly patch. Added a `key={chartType}` remount on `OutputPreviewPane`'s `ChartRenderer` usage as a belt-and-braces measure alongside the existing `notMerge`. The rail thumbnail (`OutputsRail.tsx`, task 3.3) renders row-count TEXT only, no ECharts instance at all -- there is no rail chart-crash surface to fix; noted rather than silently treating the rail half of this task as done by omission.
- [ ] 5.9 Delete unused editor files once migration confirmed complete; grep for zero remaining imports before deletion -- NOT done this cycle. `BindingEditor.tsx`, `CollectionEditor.tsx`, `MarkdownEditor.tsx`, `TimelineEditor.tsx`, `TextContentEditor.tsx`, `DataTypePicker.tsx`, `MetricPicker.tsx`, `useMetricBindingState.ts`, `MetricBindingFields.tsx` are all still live (still exercised by their own existing panel-editor test suites; no caller was removed this cycle since the panel-editing surface itself is untouched -- only a NEW parallel Output-editing surface was added). Task 2.5 stays correctly blocked on this for the same reason its Cycle 7 note already gives.

## 6. Shapes

- [x] 6.1 Retarget "Start from a shape" to "Add Outputs from a shape" against a chosen step -- title/button text updated in `ShapePickerModal`/`PipelineRiverView` (this ticket's editor surface only); anchor threaded through as `anchorStepId` (`undefined` = empty-state/new-trunk, last trunk step id = bottom-of-list trigger)
- [x] 6.2 Extend `ShapeParamsFields` to honor `enum`/`fieldRef` widget metadata when present (HEL-731, forward-compatible only -- `ShapeParamDescriptor` has no such field on the shipped backend; backend follow-up ticket HEL-939 filed for the descriptor extension, per design.md decision 13) -- `enum` renders a real `<select>` (`shared/ui/Select`), `fieldRef` renders as a text field with a distinct "column/field name" hint (a real capabilities-at-node-sourced column picker is out of scope until the backend descriptor actually declares this)
- [x] 6.3 Adapt to `expand` returning `{steps, outputs?}` (design.md decision 11) -- `handleInstantiateShape` walks the response with a `clientId->real-id` map, resolves each step's `parentStepId`, picks `attachAsTail` for the response's first step only when the anchor already has a child, then creates any `outputs` entries last (dormant on the shipped backend today -- fixture-tested only per design.md decision 14). Also fixed the panel-creation wizard's `ShapeInstantiateStep` (shares the same `expandPipelineShape` call) to the new shape.
- [x] 6.4 Delete `ShapeInstantiateStep` (panel wizard variant) if fully superseded -- NOT deletable: `grep -rln ShapeInstantiateStep src` shows `PanelCreationModal.tsx` still imports it live (panel-creation wizard, untouched by this ticket). Same disposition pattern as tasks 2.5/5.9 -- blocked on a surface outside this ticket's scope, not on anything this cycle skipped.

## 7. New pipeline flow

- [x] 7.1 New-pipeline entry: existing source / paste table / upload CSV or URL / REST connector / text-markdown -- `CreatePipelineModal` now offers "pick an existing source" (unchanged Select) OR "Create a new source", which nests the app's existing `AddSourceModal` (already covers static/paste-table, csv upload+url, rest_api, text upload+url, pdf, image) inline; `AddSourceModal` gained an `onCreated` callback (fired uniformly from its one `finishCreate` helper across all 7 create call sites) so the created source's id flows straight back into the pipeline-name+source form without leaving it.
- [x] 7.2 Bulk-paste manual rows: `AddSourceModal`'s existing `static` kind IS the paste-table flow (`StaticSourceForm` -> `createStaticSource`) -- no new form needed, just reachable via 7.1's "Create a new source". Pipeline creation itself is one `POST /api/pipelines` call with `{name, sourceDataSourceId}` (`steps`/`outputs` omitted -- both default to `Vector.empty` server-side); the retired `outputDataTypeName` field (DataType-bound) was dropped from the new flow's payload -- confirmed live against `PipelineProtocol.scala`: `CreatePipelineRequest` has no such field at all today (HEL-903 already dropped it, not just "no inline-source arm" as design.md decision 10's earlier note phrased it). Kept as an optional field on the shared `CreatePipelinePayload`/thunk type only because the still-live legacy DataType-bound panel wizard (`ShapeInstantiateStep.tsx`, out of this ticket's scope) still sends it.
- [x] 7.3 Land on page with root previewed after creation -- free: `usePipelineDetailPage`'s mount-time `analyzePipeline(id)` call is unconditional (not gated on `steps.length > 0`), so a freshly-created zero-step pipeline's raw source is already previewed the moment the page lands, exactly like an empty-state pipeline built by hand.

## 8. Header + cleanup

- [x] 8.1 Header shows source, schedule, run status, Outputs count; remove "Output type" link -- `PipelineDetailHeader`'s "Type" field group replaced with an "Outputs" group (count + a `StatusChip` for the last completed run, `null` = no chip); `outputTypeName`/`canEditType`/`onEditType` props and the "Edit type" actions-menu item removed; `usePipelineDetailPage`'s now-dead `canEditType`/`handleEditType`/`boundOutputType` derivations removed too (no remaining consumer once the header stopped using them)
- [x] 8.2 Delete `PipelinePreviewModal` -- deleted (`.tsx`/`.css`/`.test.tsx`), its "Preview" actions-menu item removed, `previewModalOpen` state removed from the hook
- [x] 8.3 Sweep `features/pipelines` for remaining `dataTypeId`/`outputDataTypeName` references -- the Output-editing surface (`ui/outputEditor/*`, task 5) and the pipeline-detail-page surface (this ticket's actual scope) are clean.
  **Corrected skeptic-final-1 (round 1) CR1:** this task's earlier wording claimed `PipelineListTable.tsx`'s dead "Output type" column was excused because "the backend itself still returns `outputDataTypeName`/`outputDataTypeId` for legacy pipelines" -- **that claim was false**, verified live against `GET /api/pipelines` (8 keys returned, neither field present, across all 52 dev-DB pipelines including month-old ones). The column and its permanently-empty cells are now deleted from `PipelineListTable.tsx`, and the now-fully-dead `outputDataTypeName: string` (required) field is removed from the `PipelineSummary` response type (`types/pipelineStep.ts`) and every fixture that constructed one.
  What legitimately remains, and why (re-verified, not re-asserted): `outputDataTypeId?: string` stays on `PipelineSummary` -- it's still read by `selectPipelineNameByOutputTypeId` (`pipelinesSlice.ts`), a genuinely separate, out-of-scope consumer (`TypeRegistryPage`/`PanelCreationModal`/`SidebarBody`/`usePickerSelection`, all blocked on the `PanelDetailModal` migration, HEL-937) -- it now always resolves to an empty map since the backend never populates the field either, which is a pre-existing, tracked gap (HEL-937), not something this fix worsens. `pipelineService.ts`'s `CreatePipelinePayload.outputDataTypeName` and `pipelinesSlice.ts`'s matching `createPipeline` thunk payload type are a DIFFERENT field (a request payload, not the dead response field) -- still genuinely sent by the live, out-of-scope `ShapeInstantiateStep.tsx` legacy DataType-bound panel-creation wizard (`createPipeline({..., outputDataTypeName: outputTypeName.trim()})`), so removing it would break that in-scope-blocked consumer; left in place. `pipelineProposal.ts`/`PipelineProposalSummary.tsx` (agent-authored-proposal review flow) and `PipelineAnalyzeResponse`'s own `outputDataTypeName`/`outputDataTypeId` (a different endpoint, `/analyze`, never checked by this correction) are untouched -- out of this task's verified scope.

## 9. Tests

- [x] 9.1 Jest: tails render under correct parent; rail shows one chip per direct Output; sheet slot options from capabilities-at-node; pie<->bar live-switch does not throw -- the two genuine gaps Cycle 11 flagged are closed this cycle: new `OutputEditorSheet.test.tsx` asserts (a) the chart "Aggregation value field" `Select` is populated from a mocked `/capabilities` response's columns (`amount`/`category`), not any DataType-sourced list, and (b) a scripted pie -> bar -> pie chart-type switch renders without throwing and leaves the sheet still mounted/responsive. "Rail shows one chip per direct Output" and "tails render under correct parent" stay on their Cycle-9/11 evidence (`OutputsRail.test.tsx`, `stepTree.test.ts`, `e2e/hel908-tail-attach.spec.ts`/`hel908-trunk-reorder-drag.spec.ts`) -- already-covered, not re-touched.
- [x] 9.2 Re-run HEL-676/HEL-878/HEL-681 repro steps against rebuilt page; cite in PR -- re-run FRESH this cycle (Cycle 12), against the current commit, not cited from a prior cycle. **HEL-676**: fresh 375x812 screenshots of both the Steps tab and the Outputs tab on a long-named pipeline (throwaway probe spec, deleted after capture) show clean layout, no overlap, matching Cycles 5/9/10's finding -- still genuinely not reproducible with the combinations tried across three cycles now; stays documented-open per task 3.6. **HEL-878/HEL-681**: `e2e/hel908-full-flow.spec.ts` (task 9.3) exercises a REAL dry-run to completion on the current rebuilt page and the rail/sheet preview state behaves correctly around it (no stale data observed); this is live confirmation the reset path fires on today's build, on top of the existing `resetRunScopedState`/request-id-token-guard unit coverage -- not an independent live race-condition re-probe of HEL-681's specific out-of-order-response scenario, which stays on its existing Jest coverage (`usePipelinePreviewCache.test.tsx`) rather than being re-clicked live this cycle (noted precisely rather than overclaimed).
- [x] 9.3 Playwright: paste-table pipeline -> filter -> metric Output via aggregate-tail -> chart Output on trunk -> dry-run -> 3 live thumbnails -> Output sheet preview, all on one page; record interaction count against the spec-line-256 budget of <=12 interactions -- built this cycle (`e2e/hel908-full-flow.spec.ts`), passing, run twice for stability. Source/pipeline creation is via API (matching every sibling `hel908-*.spec.ts`'s own convention, and keeping the click count meaningful for the UI surface this ticket actually built) rather than driving `StaticSourceForm`'s own already-tested column/row-entry UI. **25 clicks recorded** (corrected Cycle-2: a prior cycle's tasks.md entry cited 30, stale against the current spec content; freshly re-run this cycle, not copied forward -- the spec's own `console.log` prints the live count each run) -- confirmed via `docs/superpowers/specs/2026-08-30-pipelines-outputs-remodel-design.md:256` that the cited "<=12 interactions" budget covers a DIFFERENT, narrower scenario (source -> pipeline -> three Outputs -> placed on a dashboard, no filter step, no aggregate-tail creation); placing an Output on a dashboard (task 4.3) isn't built this cycle, and this flow deliberately covers MORE surface (a filter step plus the aggregate-tail creation flow this task was actually asked to prove out) than that budgeted scenario. The click count is recorded for reference, not asserted as a pass/fail against line 256's number -- citing it as satisfying that specific budget would misrepresent what this spec covers. A genuine finding surfaced while building this spec: the rail's live thumbnails start at the "—" placeholder immediately after Output creation (`OutputEditorSheet` unmounts on Save/"Add as tail" before task 5.5's 400ms debounced preview-refresh timer fires) and go back to "—" after every dry/live run (HEL-878's `resetRunScopedState` deliberately clears the shared preview cache, and nothing on the page proactively re-fetches it afterward) -- only reopening a chip's own sheet repopulates it. **CLOSED Cycle 13**: fixed and live-verified -- `handleRunPipeline`/`handleDryRun` and the create paths (`OutputEditorSheet.handleSave`, `handleAddOutputViaAggregateTail`) now dispatch `previewOutput` for the relevant Output(s) right at their real completion signal, and `e2e/hel908-full-flow.spec.ts` now asserts every chip settles WITHOUT any click/reopen, both at create time and after a dry run. See `execution-progress.md`'s Cycle 13 entry for the two-bug root cause (cache-reset-without-refetch, plus a first fix attempt's SSE-only/`"succeeded"`-only gate that silently missed every dry run).
- [x] 9.4 Grep `e2e/` for `PipelinePreviewModal`, `ShapeInstantiateStep`, `/registry`; rewrite or delete -- done cycle 11 (see section 8/OpenSpec commit): only one hit, a legitimate `/registry` ROUTE string in `e2e/hel666-single-assistant-entry.spec.ts`'s route list (the DataType registry page, still live, task 2.5-blocked scope) -- not a stale reference to the retired shape-catalog `/registry` naming. No `PipelinePreviewModal`/`ShapeInstantiateStep` hits anywhere in `e2e/`. Nothing to rewrite or delete.

## 10. OpenSpec + gates

- [x] 10.1 Grep old capability specs for now-obsolete requirements not already covered by this change's deltas; add further deltas if found -- found two: `pipeline-create-modal` (its "three required fields"/"POST with outputDataTypeName + fetchPipelines refresh" requirements are both superseded by the new `pipeline-new-flow` delta -- added a `REMOVED Requirements` delta with migration pointers) and `data-grid` (its `DataGrid`-consumer list still named the deleted `PipelinePreviewModal` -- added a `MODIFIED Requirements` delta removing it; verified `OutputPreviewPane`, its closest replacement, is deliberately NOT a `DataGrid`/`TableRenderer` consumer -- it renders a hand-rolled `<table>` since `TableRenderer` persists column-resize PATCHes against a `panelId` an Output sheet has none of -- so this is a net reduction in consumers, not a swap, and the delta says so explicitly rather than claiming a false replacement). `openspec validate pipeline-page-outputs-rebuild --strict` passes with both deltas added (also re-confirmed exit 0 after the 10.7/10.8/10.9 renumbering fix below).
- [x] 10.2 `check:openspec` green -- `node scripts/check-openspec-hygiene.mjs`, `node scripts/check-spec-structure.mjs`, and `openspec validate pipeline-page-outputs-rebuild --strict` all pass (re-confirmed exit 0 after this task's own renumbering fix, below, resolved the 3 group-mismatch WARNINGs skeptic-final-scope-round2 CR1 found)
- [x] 10.3 `npm run lint` / `typecheck` / `test` green -- confirmed fresh this cycle (see execution-progress.md Cycle 11 gate output)
- [x] 10.4 File-size check: no file over ~400 lines without a stated reason -- audited every file this whole ticket created/substantially split. Under budget: `PipelineDetailPage.tsx` (363), `StepCard.tsx` (379), `CreatePipelineModal.tsx` (228), `ShapePickerModal.tsx` (221). `PipelineDetailPage.tsx` at 363 is still under the ~400 line soft budget but has grown from the 315 cited earlier in this change (across the skeptic-final-1/2/3 round-1 fixes, mostly doc-comment growth on the shape-picker gate); flagged for the same watching-not-yet-a-defect treatment task 10.4's own earlier non-blocking note already called for -- no split proposed this cycle since it remains under budget.
  **Re-measured fresh (skeptic-final-1 round 1 CR2), not trusted from a prior cycle's citation:**
  - `usePipelineDetailPage.ts` -- **1033 lines** (was 955 as of evaluation-5/skeptic-final-1's citation; grew from this round's CR1 fix -- the `anchorHasTail` defensive refusal + corrected doc comments for `handleInstantiateShape`). Still deferred, not attempted this cycle -- the reasoning evaluation-1/Cycle-2 already gave stands unchanged: the evaluator's suggested seams (run/SSE handling, analyze-debounce, step CRUD, outputs/preview) are real and would work, but this file carries several explicitly-documented cross-cutting ref-stability invariants (F-146, F-105, the HEL-878 reset-path unification) whose correctness depends on specific hook call ORDER and shared closures, so splitting it safely needs its own focused pass with its own verification budget. **Follow-up ticket filed: HEL-938** ("Split usePipelineDetailPage.ts into its four documented seams") -- filed by the orchestrator after the executor flagged it had no Linear-write tool access this cycle.
  - `OutputEditorSheet.tsx` -- **582 lines** (skeptic-final-scope-round2 CR2 caught this citation going stale AGAIN within the very commit correcting it -- the prior "579" was accurate at that commit's parent but the commit's own 2-line header-comment edit pushed it to 581, uncounted, and this round's further header-comment rewording pushed it to the current, final 582; re-measured via `wc -l` after every edit this time, not before). Grew from the `ui-modal-btn`/`ui-modal-btn--danger`/`ui-modal-btn--secondary` className changes on the footer buttons (skeptic-final-3 CR1/CR2) -- the ported `panel-detail-modal__*` classes' replacement (`output-editor-sheet__*`) is CSS-only and doesn't touch this file's own line count. Reason for staying over budget is unchanged from the file's own header comment (six kinds' worth of local state plus the JSX kind switch; a further per-kind split would trade real file-size compliance for indirection that would make the six kinds' shared save/create/delete/preview lifecycle harder to follow in one place) -- not re-litigated this cycle since no new task touched its internal structure.
  - `PipelineRiverView.tsx` (500, was 479 at the earlier citation -- grew from this round's CR1 fix: the `trunkLastHasTail` gate + its doc comments) -- the trunk/tail/rail river canvas (task 3) plus drag-reorder plus the shape-picker anchor wiring; a further split (e.g. extracting the drag-reorder handlers into their own hook) is a reasonable follow-up but out of this cycle's remaining budget.
  - `AddSourceModal.tsx` (521) -- pre-existing file this cycle added exactly one prop (`onCreated`) and one call to, reusing the existing `finishCreate` single-choke-point rather than growing per-kind logic; the smallest possible touch to an already-large pre-existing file, not a new-this-cycle outlier.
- [x] 10.5 `grep -r dataTypeId frontend/src/features/pipelines frontend/src/features/panels/ui/editors` returns nothing -- confirmed: `features/pipelines` itself is clean of `dataTypeId` after this cycle's header/CreatePipelineModal cleanup (remaining `outputDataTypeId`/`outputDataTypeName` hits, a DIFFERENT field name, are documented under task 8.3 above); `features/panels/ui/editors` still has `dataTypeId` hits in `CollectionEditor`/`MarkdownEditor`/`TimelineEditor`/`TextContentEditor`/`BindingEditor` (the five task 2.5/5.9 document as blocked on the out-of-scope `PanelDetailModal` migration) plus their own test files and `DataTypePicker.tsx`/`useTableDisplayState.ts` (siblings task 5.9 also names as pending deletion once those five have zero remaining callers) -- all the same known, documented exception, not a silent failure. **Cycle-2 correction (evaluation-1 CR3):** the live-code half of this task's `outputDataTypeName`/`outputDataTypeId` carve-out was NOT actually a satisfied criterion -- `usePipelineDetailPage.ts` still had a live `currentPipeline?.outputDataTypeId` read (permanently `undefined`, since the backend no longer serves that field) feeding an unreachable `markDataTypeRowsStale` dispatch. Deleted alongside the rest of task 10.6 below.

- [x] 10.6 (Cycle-2, evaluation-1 CR3) Took `PipelineDetailPage`'s share of the HEL-936 dead `GET /api/types` cleanup: deleted `usePipelineDetailPage.ts`'s `fetchDataTypes()` mount effect (its own comment cited HEL-260's "Edit Type" button, which this ticket already removed from the header), the unused `dataTypes` destructure, and the unreachable `markDataTypeRowsStale(outputDataTypeId)` dispatch (`outputDataTypeId` is permanently `undefined` -- the backend no longer serves that field). Live-verified before/after with a scripted Playwright network trace on a fresh `/pipelines/:id` load: 4 `GET /api/types` 404s dropped to 2. The remaining 2 are NOT this ticket's share -- they come from `shared/chrome/SidebarBody.tsx`'s `"pipelines"`-section `fetchDataTypes()` dispatch (an F-144 delete-confirm-warning consumer, a genuinely different, much larger ~18-file HEL-936 share the ticket's own text explicitly says to "leave... say which taken"). Documenting per the ticket's own instruction: taken = `PipelineDetailPage`/`usePipelineDetailPage.ts` only; left = every other of the ~18 files, including `SidebarBody.tsx` and the five `features/panels/ui/editors` files task 2.5/5.9 already document as blocked.

- [x] 10.7 (Cycle-3, evaluation-2 CR9) A leaf-tail-attach followed by a later trunk-append left the tail rendered under the WRONG trunk node until a hard reload -- reproduced live and confirmed by the persisted API response (server truth was always correct; only local client state was stale). Root cause: `handleInsertStep`/`handleAddTailStep`/`handleAddOutputViaAggregateTail` in `usePipelineDetailPage.ts` each patched only the ONE newly-created step into local `steps` state after their create call resolved, never refreshing the OTHER steps whose `parentStepId`/`position` the same server-side call (`spliceInsertAtInternal`'s reparenting) could have mutated. Fixed with a new `syncStepsFromServer` helper (re-fetches the full list via `fetchPipelineSteps(id).unwrap()`, replaces local `steps` wholesale) called by all three handlers. `handleInstantiateShape` audited and left unchanged -- its loop's non-first creates always target a step the same batch just created, which carries no reparenting exposure. New e2e case in `e2e/hel908-tail-attach.spec.ts` (leaf-tail-attach then trunk-append, asserted without reload) confirmed red against the pre-fix tree and green against the fix.
- [x] 10.8 (Cycle-4, evaluation-3 CR10) `handleDuplicateStep` had the identical CR9 defect and was neither fixed nor audited in Cycle 3: `PipelineService.duplicateStep` calls the same server-side `spliceInsertAtInternal` reparenting primitive, so duplicating a tailed trunk step rendered the clone as a tail branch off the original and promoted the real tail to a top-level trunk card, until reload. Fixed by calling the existing `syncStepsFromServer()` after `duplicatePipelineStep` resolves, replacing the local splice, mirroring the CR9 fix exactly. New e2e case in `e2e/hel908-tail-attach.spec.ts` (tail-attach a leaf trunk step, then duplicate that trunk step; assert without reload that the clone owns the tail) confirmed red against the pre-fix code and green against the fix. All five step-mutating handlers in the file are now accounted for.
- [x] 10.9 (Cycle-5, evaluation-4 CR11) evaluation-4.md's forced systematic re-enumeration (design.md's new "Step-Mutating Handler Enumeration" section) found the prior "five handlers" audit was derived on the wrong axis (create-vs-not, rather than "can this route mutate steps OTHER than the target") and missed a sixth: `handleRemoveStep`. `PipelineStepRepository.deleteInternal` reparents the deleted step's head child onto the deleted step's own parent AND cascade-deletes every other child's entire subtree (tails); the handler's bare local `.filter()` ignored both, rendering a permanently cascade-deleted tail as a live top-level trunk card until reload -- worse than CR9/CR10 (a phantom for deleted data, not just a misplaced real step). Fixed by calling `syncStepsFromServer()` after `deletePipelineStep` resolves, alongside (not replacing) the existing optimistic filter + revert-on-error. New e2e case in `e2e/hel908-tail-attach.spec.ts` (remove a trunk step owning both a head child and a tail; assert without reload that exactly one trunk card remains and the tail is entirely gone from the DOM) confirmed red against the pre-fix code and green against the fix. `handleReorderSteps` and the aggregate-tail rollback delete were explicitly NOT touched -- both cleared as safe by evaluation-4.md's systematic pass.
