## Skeptic Report — final gate (round 1, skeptic-final-1.md)

### What I verified (with evidence)

- **cwd guard:** `assert-cwd.sh` → `READY ambient=/home/matt/Development/helio branch=feature/form-field-renderers/HEL-1085`.
- **Diff base:** `resolve-review-base.sh` → `9f6f4d41248103e39582500c38b95cc6d8233be5` (exit 0, checked). `git diff` against it: 31 files, matches `files-modified.md` shape (frontend form UI + shared primitive prop threading + one backend `PanelPacker.Bounds` entry + tests + openspec artifacts).
- **Code read (not narrative):** `FormFieldControl.tsx`, `FormPanelView.tsx`, `FormField.tsx`, `Toggle.tsx`, `Select.tsx` — confirmed with my own eyes that every non-`file`/non-`issue` control wires `aria-invalid`/`aria-describedby` to a real `errorId`/`hintId`, that `describedBy` resolves to exactly one of error/hint (never both), and that `FormField` renders the error `<p>` with `id={resolvedErrorId}` (not just `role="alert"` — the exact HEL-1084 trap this ticket's C1 exists to avoid).
- **Servers pinned to this worktree, not reused stale processes:** `assert-phase.sh servers` → `PASS`; confirmed via `/proc/<pid>/cwd` on both the 6517 and 9424 listeners — both resolve inside `.../HEL-1085/{frontend,backend}`.
- **Live e2e re-run by me** (`DEV_PORT=6517 BACKEND_PORT=9424 npx playwright test e2e/hel1085-form-field-renderers-keyboard.spec.ts`): **1 passed**. Read the spec source myself — it drives all six controls by keyboard only (`page.keyboard.type`/`Tab`/`ArrowDown`/`Enter`/`Space`), asserts `toHaveAccessibleName`/`toHaveAccessibleDescription` (computed accessibility tree, not DOM presence) for every field, and specifically blurs an empty required `quantity` field and asserts `aria-invalid="true"` **and** `toHaveAccessibleDescription("Quantity is required")` — i.e. the actual computed-ARIA association HEL-1084 got wrong twice. This is real, not test-name theater.
- **Frontend unit gate:** `npx jest --testPathPatterns="form"` → 331 suites / 3564 tests passed (includes `FormFieldControl.test.tsx`, `FormPanelView.test.tsx`, `useFormPanelValues.test.ts`, `formFieldValidation.test.ts`, `PanelContent.test.tsx`, `PanelDetailModal.test.tsx`).
- **Backend gate:** `sbt "testOnly com.helio.services.panels.PanelPackerSpec"` → 13/13 passed, including the new form-clamp case (`clamp(Form, w=1, h=2, cols=12) shouldBe (3,5)`) — and the e2e test independently exercises the *route* with the same undersized `w:1,h:1` via `/auto-layout`, so the clamp is proven both as a pure function and as wired.
- **Typecheck / lint:** `tsc --noEmit` clean; `eslint` on every touched form/shared-ui file clean.
- **Visual/design judgment (my own screenshots, regenerated by my own e2e run, not reused from the executor):** viewed `form-panel-fields-{dark,light}.png` — tokened surfaces/borders, `Select` trigger and `Toggle` track use the app's existing accent/surface tokens in both themes, dark/light parity holds (no hardcoded colors visible), consistent with sibling panel-card chrome. The card visibly truncates the `Active` row under `overflow-y:auto` — this is the disclosed D8/F-038 scroll-affordance trade-off (same recipe already used by metric/table panel variants per design.md), not a hidden defect.

### Acceptance criteria — traced

1. Six controls render on-panel, sized for a grid cell → `FormPanelView.tsx:84-95` + `PanelPacker.Bounds` clamp + e2e screenshot.
2. Every field keyboard-reachable/completable → e2e spec's full keyboard-only traversal, re-run and green.
3. Programmatic label by computed accessible name → e2e `getByRole(..., {name})` + `toHaveAccessibleName` on every control type (text/textarea/spinbutton/date/combobox/switch).
4. Error associated by computed ARIA state → e2e's `aria-invalid="true"` + `toHaveAccessibleDescription("Quantity is required")` pair, the literal repro of the HEL-1084 failure mode, now proven fixed.

### Design judgment — `Toggle` for `checkbox` (design.md risk, flagged for override)

Accepted as-is. `Toggle` is the app's only boolean primitive, uses a native `<input type="checkbox" role="switch">` with identical keyboard semantics (Space toggles, confirmed in the live e2e run), and DESIGN.md §6 favors reuse over a new one-off `Checkbox` component for a single ticket's need. Not overruling.

### Verdict: CONFIRM

### Non-blocking notes

- `required` on native inputs will invoke browser constraint validation once a submit button exists (HEL-1087) — already recorded in design.md's risks, not this ticket's problem.
- Per-instance (non-shared) draft state between a grid card and a modal view of the same panel is recorded, not hidden (D6) — fine for this leaf.
