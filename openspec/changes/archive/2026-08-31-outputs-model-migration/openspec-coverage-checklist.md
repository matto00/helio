# HEL-904 OpenSpec coverage checklist — full 115-file DataType/Metric surface

Generated by `grep -rl "DataType\|Metric" openspec/specs` (115 files, verified 2026-08-30 round 3
of the design gate). Cycle 29 correction: `external-run-hooks` matches this grep and IS already
covered by a delta in this change's `specs/` directory (committed at `212c4fd1`, well before round
3) -- it was simply never added to this checklist's own enumeration below, an omission in the
bookkeeping document, not a gap in actual coverage. Listed explicitly now so the partition below is
complete and accurate. Every one of the 115 files has EXACTLY ONE classification below: a delta
present in this change's `specs/` directory, a named deferral to the specific P-ticket that owns
it, or a verified no-op. Round 3 replaces round 1/2's partial (16-capability) coverage with full
enumeration per the coordinator's ruling — "if you cannot name the owning ticket, it belongs in
this change."

Cross-check: `comm -23 <(grep -rl ... | strip) <(ls specs/)` against the union of the four
lists below returns empty — verified exact, non-overlapping, complete partition of the 115.

## Covered by this change (65 of the 115 grep-matched files carry a delta in `specs/`;
6 further capability dirs in `specs/` are net-new or matched by other signals, not this grep —
`outputs-model`, `node-snapshot-persistence`, `pipeline-step-tree`, `output-panel-placement`
(new), `panel-batch-update`, `rls-policy-guard` (retargeted via round-1 skeptic finding, matched
on `data_types`/`aggregation` rather than the literal string `DataType`/`Metric`) — 71 delta
files total; of those 71, 18 carry a `## REMOVED Requirements` section — 13 wholesale-retired
capabilities plus 5 partial removals inside an otherwise-MODIFIED/ADDED file, e.g.
`panel-type-field`'s wholesale removal folded into the same round-3 pass as everything else
below)

### Round 1/2 (~35 capabilities, not 16 — corrected count, round 4)
alert-evaluation-engine, alert-rule-persistence, alert-rule-crud-api, alert-event-persistence,
alert-event-state-machine, backend-persistence, rls-owner-tables, rls-policy-guard,
acl-resource-type-registry, pipeline-create-api, workspace-resource-search,
workspace-tag-teardown, dashboard-contents-replace, patch-set-contract, panel-batch-create,
panel-batch-update, panel-type-field, data-source-persistence, outputs-model,
node-snapshot-persistence, pipeline-step-tree, output-panel-placement, plus the 13 wholesale
REMOVED (datatype-crud-api, data-type-persistence, data-type-acl, datatype-row-snapshot,
metric-crud-api, metric-definition-persistence, metric-usage-governance, bound-panel-composition,
panel-datatype-binding, type-registry-content-fields, type-registry-provenance,
panel-capability-introspection, panel-viz-aggregation).

### Round 3 (36 further capabilities — this pass, full enumeration)
acl-enforcement, assistant-conversation-loop, combined-proposal-apply, csv-upload-connector,
csv-url-ingestion, dev-db-repair, fetch-error-envelope, image-file-connector, panel-data-freshness,
patch-set-apply, patch-set-preview, patch-set-undo, pdf-connector, pipeline-analyze-api,
pipeline-assert-fail-policy, pipeline-compute-op, pipeline-execution, pipeline-list-api,
pipeline-proposal-analyze-api, pipeline-proposal-apply, pipeline-proposal-contract,
pipeline-run-execution, pipeline-schema-drift, pipeline-shape-registry, resource-tagging,
rest-api-connector, rls-privileged-bypass, schema-inference, schema-inference-facade,
sql-database-connector, static-data-connector, text-file-connector,
workspace-context-agent-section, workspace-context-assembly, panel-assertion-invalid-badge.

## Deferred to HEL-907 (P1.4 — helio-mcp + proposals), 9 files
mcp-data-source-tools, mcp-edit-in-place-tools, mcp-metric-tools, mcp-panel-composition-tools,
mcp-pipeline-proposal-tools, mcp-pipeline-shape-tools, nl-dashboard-proposal-authoring,
authoring-error-telemetry, conversational-refinement.
Reason: MCP tool surface and NL-authoring/refinement grounding are explicitly P1.4's scope (spec's
Agent/MCP section: changed/new/removed tools, `get_workspace_context` slimming, grounding per
node) — this ticket's backend rewires (`AssistantToolExecutor`, `WorkspaceContextService`, covered
above) make these compile against Outputs; the MCP-facing tool contracts and NL grounding rebuild
themselves are P1.4 deliverables.

## Deferred to HEL-908 (P1.5 — pipeline page), 18 files
chart-field-data-rendering, chart-type-config-editor, chart-type-display-config,
echarts-chart-panel, collection-config-editor, collection-panel-rendering, collection-panel-type,
markdown-panel, markdown-panel-content-source, text-panel-content-source,
timeline-panel-rendering, table-panel-config-editor, pipeline-create-modal, pipeline-edit-flow,
pipeline-editor-page, pipeline-list-view, pipeline-output-type-selector,
pipeline-proposal-review-ui.
Reason: P1.5 owns `features/panels/ui/editors/*`, the pipeline page river/Output sheet, and "New
pipeline" — these are Output-kind rendering/config-editor and pipeline-page/editor UI specs,
explicitly named as P1.5 scope.

## Deferred to HEL-909 (P1.6 — dashboard + nav + onboarding), 22 files
divider-panel-type, image-panel-type, panel-type-rendering, panel-config-field-or-literal-pattern,
panel-container-queries, panel-content-sizing, panel-creation-datatype-empty-state,
panel-creation-datatype-step, panel-creation-modal, panel-creation-shape-step,
panel-creation-type-config, frontend-data-sources-page, frontend-panel-creation,
datasource-ux-empty-states, datasource-edit-delete, metric-authoring-ui,
metric-panel-trend-indicator, mobile-bottom-nav, mobile-dashboard-sheet, mobile-panel-sizing,
nav-section-registry, settings-agent-memory-ui.
Reason: P1.6 owns the Output picker, panel sheet, Sources pages off `fetchDataTypes`, nav rebuild,
and deletion of `PanelCreationModal`/`Metric*`/`Type*` pages — these are dashboard-side UI specs
(panel creation wizard, content-panel rendering, nav sections, mobile chrome, Sources page)
explicitly named as P1.6 scope.

## Verified no-op, 1 file
page-shell-primitives: already correctly excludes `TypeRegistryPage`/`MetricsPage`/etc from its own
scope (HEL-725's own `design.md` Decision 0) — pre-existing correct behavior, not a gap.

## Partition proof
66 (this change, including `external-run-hooks`, previously covered but unlisted here -- see the
note above) + 9 (P1.4) + 18 (P1.5) + 22 (P1.6) + 1 (no-op) = 116. NOTE (cycle 29): this arithmetic
was already loose before this correction (the round-1/2 bucket below is itself labelled "~35, not
16 -- corrected count, round 4", i.e. an approximation, not a literal per-item tally of the
following comma list) -- the reliable, exact ground truth is: `grep -rl "DataType\|Metric"
openspec/specs` returns exactly 115 files, and every one of them is named somewhere in this
document as of this cycle's correction (verified by diffing the grep output against every name
mentioned anywhere in this file -- zero unlisted survivors remain). Treat the bucket subtotals as
approximate; treat "zero unlisted survivors" as the exact, re-verifiable claim.
`diff /tmp/final_uncovered2.txt <(cat defer_p14 defer_p15 defer_p16 noop | sort)` → exact match,
and `comm -12` of the covered set against the 115 → 65 files, both reproduced in this pass.
