## REMOVED Requirements

### Requirement: List all DataTypes
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: Get single DataType
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: Update DataType
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: Delete DataType
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: List all DataSources
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: Rename DataSource via PATCH
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

### Requirement: GET /api/types/:id/rows returns the current row snapshot
**Reason**: DataTypes are retired wholesale (HEL-903 decision 11) — a pipeline's output is now an Output attached to a pipeline node, not a standalone registry entry.
**Migration**: Bound panels migrate to Output panels (see output-panel-placement); row reads move to GET /api/outputs/:id/rows (P1.3).

