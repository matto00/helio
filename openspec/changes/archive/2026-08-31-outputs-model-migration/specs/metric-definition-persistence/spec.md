## REMOVED Requirements

### Requirement: MetricDefinition domain model
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

### Requirement: Aggregation allow-list validation
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

### Requirement: metrics table with owner-only RLS
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

### Requirement: MetricRepository CRUD surface
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

### Requirement: MetricDefinition JSON formatters
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

### Requirement: MetricRepository batch owner-scoped lookup
**Reason**: The metrics table and MetricDefinition domain model are dropped; aggregation is expressed only as pipeline steps.
**Migration**: See metric-crud-api migration note; the metrics table is dropped after every bound metric panel's aggregation is lifted into a tail step.

