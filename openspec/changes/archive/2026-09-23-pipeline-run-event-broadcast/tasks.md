## 1. Backend: multi-subscriber local broadcast (design.md D5)

- [x] 1.1 Change `PipelineRunRegistry`'s `refs` map to `ConcurrentHashMap[String, java.util.Set[ActorRef]]` (`newKeySet()` per pipeline id); update `subscribe` to add and `publish` to iterate the set, verified by `sbt test` on `PipelineRunRegistrySpec`.
- [x] 1.2 Wrap `subscribe`'s materialized source with `watchTermination()` (or equivalent) to remove that specific `ActorRef` from the pipeline's subscriber set on stream completion/client disconnect; verify with a new test asserting a disconnected subscriber's ref is no longer present.
- [x] 1.3 On a terminal event, notify every current subscriber in the set (not just one) before clearing the pipeline's map entry; verify with a test subscribing two clients and asserting both see the terminal event.

## 2. Backend: cross-instance bus (design.md D1-D4, D6-D9)

- [x] 2.1 Add `PipelineRunNotifyBus` (new file, `api/routes/pipelines/`): holds a random `instanceId` (D7), a dedicated `DriverManager` connection using `helio.db.url/user/password` (D3, non-privileged), a `LISTEN pipeline_run_events` issued at construction, and `notifyRemote(pipelineId, event)` sending `pg_notify` via the ordinary app pool using Slick's parameterized `sql"..."` interpolation (never string-concatenating `errorLog`/`pipelineId` into the SQL text).
- [x] 2.2 Implement the receive loop as a dedicated daemon `Thread` (D8) blocking on `PGConnection.getNotifications(timeoutMillis)`, never on a Pekko dispatcher; on reconnect failure use capped exponential backoff and log WARN/INFO around the lost-connection window.
- [x] 2.3 Encode the NOTIFY payload as `{originInstanceId, pipelineId, status, rowCount, errorLog, nodeId, nodeKind}` (spray-json); on receipt, drop any payload whose `originInstanceId` matches this instance's own (D7 self-echo guard) before routing to the local subscriber set for `pipelineId`.
- [x] 2.4 Truncate `errorLog` to a fixed UTF-8 **byte** budget (documented constant, e.g. 4000 bytes — encode to UTF-8 first, trim at a valid UTF-8 boundary, never split a multi-byte sequence) before encoding for NOTIFY only (D9, skeptic round 1 correction: byte-based, not `String.length`/char-count) — local same-instance delivery in task 1.x stays untruncated; verify the encoded payload never exceeds Postgres's 8000-byte NOTIFY limit.
- [x] 2.5 Add a nullable `eventBus: PipelineRunNotifyBus = null` param to `PipelineRunRegistry`'s constructor (mirrors the codebase's nullable-optional-collaborator convention); `publish` calls `eventBus.notifyRemote(...)` only when wired, after the local broadcast (task 1.x) always runs.
- [x] 2.6 Wire `PipelineRunNotifyBus` construction in `Main.scala` from `ConfigFactory.load()`'s existing `helio.db.*` values, passed into `ApiRoutes`'s (new nullable, appended) constructor param and through to `new PipelineRunRegistry(eventBus = ...)`; register a NEW `CoordinatedShutdown` task (skeptic round 1: this is the first use of `CoordinatedShutdown` in this codebase — there is no existing one to "register alongside") that stops the listener thread and closes the dedicated connection, best-effort (not load-bearing for correctness — events are ephemeral).
- [x] 2.7 Update the `cd-backend.yml` `--max-instances=2` comment to state the full per-instance connection count (5 app + 5 privileged + 1 listen = 11) and the `2 × 11 = 22` total, replacing the current privileged-pool-only figure (design.md D2).
- [x] 2.8 Update `application.conf`'s own now-stale comment ("total max connections per instance is 10 (5 app + 5 privileged)", near the privileged-pool config) to reflect the true post-change count of 11, so it stays consistent with task 2.7's `cd-backend.yml` update (skeptic round 1: this comment was missed in the first pass).

## 3. Tests: cross-instance and multi-subscriber proofs (design.md, ticket "Must hold")

- [x] 3.1 Add a red-first test against TODAY's registry (pre-1.x fix, run once and reverted, or written as a `pending`/skipped historical note) demonstrating the single-slot `put` overwrite drops the first of two concurrent subscribers — document the red in the PR body per `verification-before-completion`.
- [x] 3.2 Add a test with two concurrent `subscribe` calls for the same pipeline id on one `PipelineRunRegistry`, publish one event, and assert BOTH subscribers' streams receive it (closes 3.1's red).
- [x] 3.3 Add an `EmbeddedPostgres`-backed integration test (mirrors `V100ZeroRootGuardNonSuperuserSpec`'s pattern) constructing TWO separate `PipelineRunRegistry` + `PipelineRunNotifyBus` instances against the SAME embedded database, simulating two backend instances; publish on instance A's registry, assert a subscriber on instance B's registry receives the event — run this test first against a stubbed/no-op event bus to show the red (event never arrives cross-instance today), then against the real implementation to show green.
- [x] 3.4 Add a test asserting the self-echo guard (D7): publish on instance A, assert instance A's OWN local subscriber receives the event exactly once (not twice from local broadcast + its own NOTIFY echo).
- [x] 3.5 Add tests asserting NOTIFY payload size stays under 8000 UTF-8 bytes and that publishing does not throw, for TWO fixtures: an oversized ASCII `errorLog` AND an oversized non-ASCII (e.g. CJK or emoji-bearing) `errorLog` (skeptic round 1: a char-count truncation could still overflow on multi-byte content — the non-ASCII fixture is required to actually catch that class of bug).
- [x] 3.6 Add/extend a `PipelineRunStreamRoutes`-level test confirming ACL scoping is unaffected: a subscriber for pipeline X never receives an event published (cross-instance or local) for a different pipeline Y.
- [x] 3.7 Run existing `usePipelineRunEvents`/`pipelineRunFanout` frontend tests unchanged (`npm test`) and confirm the pipeline detail page + HEL-1094's dashboard fan-out both still pass with no frontend code changes, per the ticket's "must hold" list.

## Standing Constraints

- [C1] All agents (orchestrator/executor/evaluator/skeptic/auditor) run on sonnet; no model promotion for this run.
- [C2] One lane running today; migration ledger V110 is highest on main, V111 is free if a migration is needed. Claim it explicitly and follow FORCE RLS conventions.
- [C3] New env vars go in helio's CLAUDE.md prod table; check whether cd-backend.yml needs updating too.
- [C4] Every git commit call uses Bash timeout:600000; never re-run a commit while one is in flight; never git add -A.
- [C5] Budget exhaustion on any gate is a mandatory escalation, never a self-approval.
- [C6] Do not change epic HEL-1091's state.
- [C7] Only the owner approves; never describe a lane decision as owner-approved/ruled.
- [C8] Standalone follow-ups: origin_kind=followup, origin_ticket=HEL-1168, relatedTo, Follow-up label, AND v0.8 project id 28f119e2-5738-46b1-a53b-42f73e06b053.
- [C9] Cross-instance mechanism is Postgres LISTEN/NOTIFY (owner ruling, escalation_id HEL-1168-1790214379716-d1f700, answer=listen-notify): one dedicated long-lived LISTEN connection per instance outside HikariCP pools; connection-budget arithmetic 2x(10+1)=22 documented in design.md and PR, cd-backend.yml comment updated if it understates per-instance connections; LISTEN connection uses plain (non-privileged) app role, no RLS/BYPASSRLS needed; NOTIFY payload carries only pipelineId+event, single fixed server-controlled channel name, ACL check stays at pipelineExistsShared subscribe time only; reconnect with backoff, dedicated thread (never an actor/Pekko dispatcher) for getNotifications, log lost-connection windows, missed events during a gap are acceptable (ephemeral-events contract); truncate errorLog to fit the 8000-byte NOTIFY limit; guard against the originating instance double-delivering (local publish + its own NOTIFY echo); required proofs: two registry/backend instances sharing one DB (event on A reaches subscriber on B, red against today's registry), multiple subscribers on one instance (red on the put-overwrite), and the pipeline detail page + HEL-1094 dashboard fan-out both still work.
