## Skeptic Report — design gate (round 1)

### What I verified (with evidence)

1. **`openspec validate --strict` passes.** Ran `npx --no-install openspec validate pipeline-string-ops-step --strict` from the worktree root → `Change 'pipeline-string-ops-step' is valid`.

2. **Ticket vs. artifacts — scope match.** Read `ticket.md`, `proposal.md`, `design.md`, `tasks.md`, `specs/pipeline-string-ops-op/spec.md`. The six operations (`trim`/`upper`/`lower`/`split`/`extractRegex`/`concat`), config shape, and file list in `proposal.md`/`design.md` match the ticket's Scope section verbatim. Design explicitly and correctly self-approves excluding a `title`-case op the orchestrator brief mentioned but the ticket's own Scope/AC never lists (Planner Notes) — a defensible, disclosed scope decision, not silent drift.

3. **`splittext` is genuinely distinct (row-exploder), confirmed from source, not asserted.** Read `backend/src/main/scala/com/helio/domain/steps/SplitTextStep.scala:73-81` — `rows.flatMap { ... split(value.toString).zipWithIndex.map { case (segment, idx) => row + (field -> segment) + (indexField -> idx) } }`, a many→more transform. `stringops` (per design/tasks) is `rows.map` — one row in, one row out. The design's Context section correctly cites this distinction; it is not hypothetical.

4. **Append-vs-overwrite decision is pinned explicitly, symmetric apply/analyze mechanism, real precedent.** Design Decision 2: `outputColumn == field` → overwrite; apply = `row + (outputColumn -> value)`; analyze = `inputSchema.filterNot(_.name == outputColumn) :+ SchemaField(outputColumn, "string")`. Verified this exact collision-safe shape is the real, already-shipped `inferDateBucket` pattern: `backend/src/main/scala/com/helio/domain/PipelineAnalyzeService.scala:277-283` — `inputSchema.filterNot(_.name == resolvedName) :+ SchemaField(name = resolvedName, `type` = "date")`. `DateBucketStep.scala:94-98`'s `apply` uses the identical `row + (outputCol -> bucketed)` shape. This is not a hypothetical precedent — it's the exact code pattern for the exact same append-or-replace-in-place decision the ticket calls out as the crux (ComputeStep-vs-CastStep choice). Task 6.1 (execution: overwrite vs. append, row count unchanged in both) and Task 6.2 (analyze-schema: overwrite-no-duplicate vs. append-new-field) are separately enumerated tasks, and `spec.md` has four dedicated scenarios covering both apply and analyze sides of both cases — this is a real test mechanism that would fail if apply/infer diverged, not just an assertion.

5. **Per-value null/missing and regex-no-match handling are pinned per operation, each grounded in a real precedent, not hand-waved.** Decision 3 (single-field null→null, parity with `CastStep.castValue`'s `if (v == null) return null`, confirmed at `CastStep.scala:64`); Decision 4 (`concat` null-field→empty-string, explicitly flagged as a deliberate deviation with its own rationale and its own dedicated test in task 6.1/2.5); Decision 5 (`split` out-of-bounds index→null per-row vs. missing `separator`/`index`→step-level failure, mirroring `DateBucketStep`'s unsupported-`granularity` config-vs-data split); Decision 6 (`extractRegex` requires ≥1 capturing group, fails at execute time if absent; no-match→null, "matching `DateBucketStep`'s unparseable-value-yields-null precedent" — confirmed `DateBucketStep.parseToUtcDate` returns `None`→`null` on unparseable input at `DateBucketStep.scala:109-124`). Every null-handling branch has both a `spec.md` scenario and a task-6.1 test line item.

6. **Exhaustive-match consumer sites are real, not invented.** Grepped `DateBucket` across the four files named in ticket/design/tasks and confirmed each exists with a `DateBucket`-kind arm: `domain/package.scala:37-38,85-86`, `infrastructure/PipelineStepRepository.scala:215`, `services/PipelineService.scala:11,41,216` (note: actual package paths are `infrastructure`/`services`, not the bare names in ticket prose — tasks.md correctly instructs "grep an existing op kind... across the backend" rather than hardcoding paths, so this is handled correctly by indirection).

7. **Protocol wiring (`jsonFormat6`, union arms) matches the real `DateBucketStepResponse`/`DateBucketAnalyzeStepResponse` shape.** `PipelineStepProtocol.scala:91-94,204` uses `jsonFormat6` for `DateBucketStepResponse(id, pipelineId, position, createdAt, updatedAt, config)` — same 6-field shape the ticket/tasks specify for `StringOpsStepResponse`. `PipelineAnalyzeProtocol.scala:124-128,195` shows the analogous `DateBucketAnalyzeStepResponse` + `jsonFormat6`.

8. **Frontend precedent (`WindowConfig.tsx`'s conditional-field-reveal) is real, not hypothetical.** Read `frontend/src/features/pipelines/ui/WindowConfig.tsx:1-42` — confirms the "function dropdown reveals field/offset conditionally" pattern design.md task 4.3 cites actually exists and matches what `StringOpsConfig.tsx` is asked to do (operation dropdown reveals split/extractRegex/concat params conditionally).

9. **`pipelineStep.ts` "4 additions per op" wording (ticket line 35, echoed in task 4.1) undercounts against ground truth** — grepping `DateBucket` in `frontend/src/features/pipelines/types/pipelineStep.ts` shows 6 distinct edit sites (config interface, Step interface, `PipelineStep` union member, `PipelineStepConfig` union member, `AnalyzeStep` interface, `AnalyzeStepResult` union member), not 4. This is a pre-existing inaccuracy in the ticket's own prose (not introduced by design/tasks), and task 4.1 correctly hedges by instructing the implementer to "grep an existing op... for the [...] required additions per op" rather than hardcoding a count of 4 — so the plan is not misled by the wrong number. Non-blocking.

10. **Merge hazard — both migration-VNN checks are explicit, separate tasks.** Task 1.1 ("re-confirm... immediately before writing the migration... do not trust the ticket/design's stated VNN") and Task 7.1 ("re-confirm... immediately before the delivery push... do not reuse the check from task 1.1") are both present as distinct checklist items in `tasks.md`, directly addressing the HEL-382 REFUTE precedent for missing the second check. Confirmed current max migration via `ls backend/src/main/resources/db/migration/ | sort | tail` → `V69__add_fillnull_op.sql`, consistent with the ticket/design's stated "main at V69" and correctly left unhardcoded (VNN deferred to execution-time grep, not baked into any task).

11. **All AC line items trace to specific tasks and `spec.md` scenarios**: op correctness/row-count/unsupported-op-error → tasks 2.2-2.7, 6.1, spec Requirement 1; analyze parity → task 3.4, 6.2, spec Requirement 2; migration → tasks 1.1/1.2, spec Requirement 3; frontend editor + PATCH round-trip → tasks 4.1-4.4, 6.6, spec Requirement 4; MCP → task 5.1, spec Requirement 5; test suite (`InProcessPipelineEngineSpec`, analyze-schema, codec round-trip, `PipelineStepSpec` kind-parity) → tasks 6.1-6.5, and each named spec file/class exists in the repo (`InProcessPipelineEngineSpec.scala`, `PipelineAnalyzeServiceSpec.scala`, `PipelineStepConfigCodecSpec.scala`, `PipelineStepProtocolSpec.scala`, `PipelineStepSpec.scala` all confirmed present). No AC is left uncovered by any task.

12. **No placeholders, TBDs, or deferred decisions found** in proposal.md/design.md/tasks.md/spec.md — Open Questions section explicitly states "None" with justification, and every design decision (1-8) is concretely pinned with a stated rationale and a corresponding test task.

### Verdict: CONFIRM

### Non-blocking notes
- `pipelineStep.ts`'s "4 additions per op" figure (inherited verbatim from the ticket) is actually 6 edit sites per op in the current codebase; task 4.1 already hedges correctly by telling the implementer to grep and mirror an existing op rather than trust the literal count, so no action needed — flagging only so the evaluator doesn't flag task 4.1 "incomplete" if the executor makes more than 4 edits.
