- `backend/build.sbt` — added MySQL JDBC driver dependency (`com.mysql:mysql-connector-j:8.3.0`)
- `backend/src/main/resources/db/migration/V13__add_sql_source_type.sql` — migration to allow `sql` in source_type CHECK constraint
- `backend/src/main/scala/com/helio/domain/model.scala` — added `Sql` case object to `SourceType` enum; added `SqlSourceConfig` case class (dead `SqlSourceConfigResponse` removed in cycle 2)
- `backend/src/main/scala/com/helio/domain/SqlConnector.scala` — new `SqlConnector` object: DDL/DML keyword regex check, JDBC URL builder, `connect`, `execute` (with `blocking`/timeout/maxRows), ResultSet→JsValue mapping, schema inference; redundant `.asInstanceOf[JsValue]` cast removed in cycle 2
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — added `SqlSourceConfigPayload`, `SqlCreateSourceRequest`, `SqlInferRequest` case classes and their Spray JSON formatters; added `SqlSourceConfigPayload.toDomain` companion
- `backend/src/main/scala/com/helio/api/routes/SourceRoutes.scala` — extended all four routes (`POST /sources`, `POST /sources/infer`, `GET /sources/:id/preview`, `POST /sources/:id/refresh`) to branch on `sourceType == "sql"` and delegate to `SqlConnector`; extracted `handleRestCreate` helper in cycle 2 to flatten POST nesting symmetrically with `handleSqlCreate`
- `backend/src/test/scala/com/helio/domain/SqlConnectorSpec.scala` — unit tests: DDL/DML keyword rejection (all 7 keywords, case-insensitive), word-boundary non-rejection for substring identifiers, JDBC URL construction for both dialects
- `frontend/src/services/dataSourceService.ts` — added `SqlSourceConfig` interface, `inferSqlSource`, `createSqlSource` service functions
- `frontend/src/features/sources/sourcesSlice.ts` — added `inferSqlSource` and `createSqlSource` async thunks; wired `createSqlSource.fulfilled` into slice reducer; added `extractErrorMessage` helper to surface backend error body from Axios responses (cycle 2)
- `frontend/src/components/SqlTab.tsx` — new SQL tab component: dialect selector with port defaults (5432/3306), host/port/database/username/password/query fields, "Test connection" flow with schema preview and inline error display, "Create source" button
- `frontend/src/components/AddSourceModal.tsx` — added "SQL Database" tab toggle, rendered `SqlTab` when selected, wired `handleSqlSave` to dispatch `createSqlSource`
- `frontend/src/components/SqlTab.test.tsx` — Jest tests: port defaults by dialect, test connection success shows inferred fields, test connection failure shows inline error, password field type, create source button disabled state
