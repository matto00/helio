## ADDED Requirements

### Requirement: Design token vocabulary in theme.css placeholder
The `theme.css` `:root` block shall serve as the single source for the extended design token vocabulary.

#### Scenario: Placeholder for already-applied changes
- **WHEN** this spec is evaluated
- **THEN** the design token vocabulary and typography utility class requirements are already present in the compiled spec
