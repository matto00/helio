# HEL-178: Add Data Pipelines as top-level sidebar nav item

## Description

Add a Data Pipelines entry to the main sidebar navigation with its own route (e.g. /pipelines). Should sit between Data Sources and Type Registry in the nav order.

## Acceptance Criteria

- A "Data Pipelines" item appears in the main sidebar navigation
- The nav item has its own route (e.g. `/pipelines`)
- The nav item is positioned between "Data Sources" and "Type Registry" in the nav order
- Clicking the nav item navigates to the `/pipelines` route
- A placeholder page is shown at `/pipelines` (can be a simple "coming soon" or empty state)

## Priority

High

## Project

Helio v1.3 — Data Pipeline & Registry Hardening

## Parent

HEL-140
