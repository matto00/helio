# Files modified — HEL-462 pipeline schema-drift detection

- `backend/src/main/resources/db/migration/V85__pipeline_last_source_schema.sql` — new migration, adds nullable `pipelines.last_source_schema JSONB` column (baseline storage).
- `backend/src/main/scala/com/helio/infrastructure/PipelineRepository.scala` — table-local `lastSourceSchema` column def (kept out of `*`/`PipelineRow`/`Pipeline` per design D2) plus targeted `updateLastSourceSchema`/`findLastSourceSchema` projections mirroring `updateLastRun`'s pattern.
- `backend/src/main/scala/com/helio/domain/PipelineSchemaDrift.scala` — new file: `SchemaDrift`, `TypeChangedColumn`, and the pure `PipelineSchemaDrift.diff` helper (design D3).
- `backend/src/main/scala/com/helio/domain/PipelineAnalyzeService.scala` — adds the shared `deriveSourceSchema` helper (design D1) and the `schemaFieldJsonFormat` JSON codec reused by both the run-success write path and the analyze read path.
- `backend/src/main/scala/com/helio/services/PipelineRunService.scala` — threads `pipeline.sourceDataSourceId` through `onRunSuccess`/`onUnblockedRunSuccess`; persists the schema-drift baseline (best-effort, `recoverWith`) alongside the existing `updateLastRun` on a successful non-dry run only (design D4).
- `backend/src/main/scala/com/helio/services/PipelineService.scala` — `analyze` now uses the shared `deriveSourceSchema` helper, fetches the baseline via `findLastSourceSchema`, tolerant-parses it, computes drift via `PipelineSchemaDrift.diff`, and populates the new `sourceSchemaDrift` response field (design D5).
- `backend/src/main/scala/com/helio/api/protocols/PipelineAnalyzeProtocol.scala` — new `SourceSchemaDriftResponse`/`TypeChangedColumnResponse` wire types + formats; `PipelineAnalyzeResponse` gains optional `sourceSchemaDrift` (`jsonFormat7` → `jsonFormat8`).
- `schemas/pipeline-analyze-response.schema.json` — adds the optional `sourceSchemaDrift` property (design D6), not added to `required`.
- `backend/src/test/scala/com/helio/domain/PipelineSchemaDriftSpec.scala` — new: pure diff coverage (no-baseline, identical/reordered, added, removed, type-changed, mixed, duplicate-name collapsing).
- `backend/src/test/scala/com/helio/api/routes/PipelineAnalyzeRoutesSpec.scala` — route-level drift coverage (no baseline, no-drift, added/removed/type-changed) plus JSON-schema-validation coverage for `sourceSchemaDrift` present/absent.
- `backend/src/test/scala/com/helio/services/PipelineRunServiceSpec.scala` — run-success baseline persistence coverage: persists on real success, skipped on dry run and on a blocked run, overwritten on a subsequent run.
