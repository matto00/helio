## Files modified (HEL-465 column pinning)

- `frontend/src/features/pipelines/ui/outputEditor/outputConfigTypes.ts` — added `pinnedColumns?: string[]` as a flat sibling field on `TableOutputConfig`, plus tolerant `readPinnedColumns`/`readTableConfig` wiring (design.md Decision 6).
- `frontend/src/features/pipelines/ui/outputEditor/outputConfigTypes.test.ts` — Jest coverage for `readTableConfig`'s new `pinnedColumns` parse (round-trip, absent, non-array, non-string entries, malformed, independence from the other four fields).
- `frontend/src/shared/ui/DataGrid.tsx` — `pinnedColumns`/`onPinToggle` props (full variant only); `pinnedCount`/`computePinnedOffsets` pure helpers (design.md Decision 4, exported for Jest); sticky-left `<th>`/filter-row `<th>`/`<td>` rendering with per-cell z-index (design.md Decision 5); pin/unpin icon-button in the column header (design.md Decision 3), `aria-pressed` + "Pin"/"Unpin"/"Pin through" accessible naming (Decision 1 consequence).
- `frontend/src/shared/ui/DataGrid.css` — z-index scale (tiers 1/2/3, design.md Decision 5), `.ui-data-grid__pinned-cell`/`--last` (panel-surface-matched background + non-inset trailing-shadow separator, design.md Decisions 6/8), `.ui-data-grid__pin-toggle-btn` styling.
- `frontend/src/shared/ui/DataGrid.test.tsx` — `computePinnedOffsets` unit coverage (single/multiple pinned columns, live-drag-width precedence, default-width fallback, zero pinned); rendering coverage (sticky class/offset, cumulative stacking, last-pinned separator class, preview-variant no-op, non-contiguous-input degradation); pin-toggle-affordance coverage (button presence/absence, `aria-pressed`, "Pin"/"Unpin"/"Pin through" naming, click wiring).
- `frontend/src/features/panels/ui/renderers/TableRenderer.tsx` — `pinnedColumns` prop; `leadingPinnedCount` helper (Decision 1 ownership split — `DataGrid` never sees `columnOrder`); `pinnedCount` state (count-based, not key-based, so it survives reorder "by position not identity"); `persistPinnedColumns` (debounced, silent-swallow, `canWrite`-gated, mirrors `persistColumnSort`/`persistColumnFilters`); reorder effect that re-derives and re-persists the key list immediately on `columnOrder` change; `handlePinToggle` wired to `DataGrid`'s `onPinToggle`; unmount-flush extended to cover a pending pin write.
- `frontend/src/features/panels/ui/renderers/TableRenderer.test.tsx` — Jest coverage for pin persistence (leading-column pin, "pin through" non-leading pin, unpin-to-explicit-`[]`, initial-state restore, `!canWrite` no-op, unmount flush, reorder re-persist-by-position).
- `frontend/src/features/panels/ui/PanelContent.tsx` — passes `cfg.pinnedColumns` through to `TableRenderer`.
- `frontend/src/theme/elevationTokenGuard.css.test.ts` — pinned the new non-inset trailing-shadow `box-shadow` declaration as a named exception (design.md Decision 8), matching the file's existing per-declaration pinning discipline.

## Design-system guard fixes (not scope creep — required for the diff to pass pre-existing gates)

- `DataGrid.css`'s `.ui-data-grid__pin-toggle-btn[aria-pressed="true"]` uses `--app-accent-text` (not raw `--app-accent`) for its pressed-state text color, matching `accentTextClosureGuard.css.test.ts`'s requirement that any `color:` reaching the accent go through the 4.5:1-obligated derived token — same pattern already used by `.ui-data-grid__filter-toggle-btn--active` in the same file.

## Cycle 2 (evaluation-1.md, 2 blocking CRs addressed)

- `frontend/src/shared/ui/DataGrid.tsx` — pin toggle now routed through the shared `IconButton` primitive (`variant="ghost"`, `size="xs"`) instead of a hand-rolled `<button>` (DESIGN.md §5); `title` set explicitly to "Pin"/"Unpin" so it no longer inherits the enclosing `<th title={col.header}>`'s column-name tooltip (CR1).
- `frontend/src/shared/ui/IconButton.tsx` — added `aria-pressed` passthrough prop (no existing consumer needed a toggle button before this ticket).
- `frontend/src/shared/ui/DataGrid.css` — pin-toggle CSS slimmed to only what `IconButton`'s variants don't express (inline spacing, pressed-state accent color); added to the existing 44px coarse-pointer touch-target media query alongside its filter-row siblings (CR1).
- `frontend/src/features/panels/ui/renderers/TableRenderer.tsx` — the reorder-persist effect no longer computes `persistPinnedColumns` (a side effect) inside a `setPinnedCount` updater callback; it now reads the current count via `pinnedCountRef` (kept in sync by its own effect) and calls `persistPinnedColumns` directly in the effect body, so the write cannot double-fire if React ever re-invokes the updater (CR2).
- `frontend/src/features/panels/ui/renderers/TableRenderer.test.tsx` — added a `React.StrictMode`-wrapped test asserting a reorder issues exactly one `pinnedColumns` PATCH. Verified directly (not assumed) that this specific assertion does NOT reproduce red against the pre-CR2 code in this Jest/RTL/React-19 harness, despite a minimal probe confirming `React.StrictMode` does generally double-invoke a `setState` updater here — so the test is kept as a correctness assertion, not a proven regression guard; the real protection is CR2's structural fix (no side effect inside an updater).
- `openspec/changes/column-pinning-table-panels/design.md` — Decision 3 corrected to document the `IconButton` routing and why the round-1 hand-rolled control violated DESIGN.md §5.

## Cycle 3 (skeptic-final-1.md, 1 blocking + 1 non-blocking addressed)

- `frontend/src/shared/ui/DataGrid.css` — the separator (`.ui-data-grid__pinned-cell--last`) switched from a non-inset `box-shadow` (measured live: paints zero pixels under `border-collapse: collapse`, which `table-layout: fixed` requires) to a `border-right` using the same `color-mix` token, which renders correctly under `collapse` — probe-confirmed, CR1 blocking.
- `frontend/src/theme/elevationTokenGuard.css.test.ts` — removed the now-stale `box-shadow` pin (the declaration no longer exists).
- `openspec/changes/column-pinning-table-panels/design.md` — Decision 8 corrected again to document the `border-right` fix and why `border-collapse: separate` (the other probe-confirmed route) was not taken (larger blast radius on the table's other borders).
- `openspec/changes/column-pinning-table-panels/tasks.md` — task 2.6 updated to reflect the `border-right` shape.
- `frontend/src/features/panels/ui/renderers/TableRenderer.tsx` — the reorder-persist effect's mount guard switched from a boolean `pinMountedRef` (which a `React.StrictMode` mount double-invoke sails past, firing an unsolicited PATCH on every table-panel mount — confirmed live via network interceptor, CR2 non-blocking) to a value-based `lastPersistedPinnedKeyRef` (a no-op key-string comparison, which survives a repeat invocation by construction, at mount or otherwise); `handlePinToggle` updated to keep this ref in sync with its own debounced write. Corrected the prior comment's false claim that `persistPinnedColumns` "is never called from inside a function React could invoke more than once."
- `frontend/src/features/panels/ui/renderers/TableRenderer.test.tsx` — added a mount test asserting no unsolicited `pinnedColumns` PATCH under `React.StrictMode`; corrected comments on both StrictMode tests to accurately state that neither reproduces red against the pre-fix code in this Jest/RTL/React-19 harness (verified directly by reverting each fix and re-running), so both are kept as correctness assertions rather than proven regression guards — the actual evidence for these two defects is skeptic-final-1's live network-interceptor measurement against the running app, not this Jest suite.

## Cycle 4 (skeptic-final-2.md, 1 blocking, last round in budget)

- `frontend/src/shared/ui/DataGrid.css` — the separator switched a THIRD time: from `border-right` (cycle-3's fix, which painted correctly only at `scrollLeft: 0` and vanished under scroll — a collapsed table's cell borders paint at the cell's static/unscrolled layout position, not the sticky cell's on-screen position) to a `::after` pseudo-element (`position: absolute`, `right: 0`, full height, `background` using the same `color-mix` token). This paints inside the pinned cell's own content box rather than the table's border layer, so it moves WITH the sticky cell under scroll. No extra `position: relative` needed — `.ui-data-grid__pinned-cell` is already `position: sticky`, itself a valid containing block. Skeptic-verified live at `scrollLeft: 600` in both themes.
- `frontend/src/shared/ui/DataGrid.test.tsx` — added a static-source regression guard asserting the separator is implemented as a `::after` pseudo-element with the right declarations, not a `border-right`/`box-shadow` on the cell itself (both of which have now independently failed this exact class of defect). Explicitly documented as NOT proof of correct rendering under scroll — jsdom has no scroll/paint engine; the skeptic's live pixel sampling is the only real evidence.
- `openspec/changes/column-pinning-table-panels/design.md` — Decision 8 corrected a third time with the full mechanism explanation (collapsed-border static-position defect) so it isn't re-derived a fourth time.
- `openspec/changes/column-pinning-table-panels/tasks.md` — task 2.6 updated to describe the `::after` shape.
- `frontend/src/theme/elevationTokenGuard.css.test.ts` — no change needed; confirmed the guard only scans `box-shadow`/`border-radius` declarations, and the new `::after` rule uses neither (a plain `background`), so no new pin is required. Verified by running the guard, not just reading its rule text.

## Cycle 5 (skeptic-final-3.md, 1 blocking, last round in budget — owner-ratified round 4 extension)

- **Rebased onto `origin/main`** (`7a14601f`, HEL-520's focus-management merge) before making further changes, per the skeptic's instruction — clean rebase, no conflicts. Re-ran the full gate suite post-rebase to confirm nothing broke (3176/3176 frontend + 248/248 helio-mcp, before this cycle's own additions).
- `frontend/src/shared/ui/DataGrid.css` — the pin toggle switched from plain INLINE flow content in the `<th>` (which `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`, required for `table-layout: fixed`, laid out past the cell's own right edge and clipped away entirely on any column whose header truncates — measured live on a real 82-column table, 23/61 header cells) to `position: absolute`, reusing `.ui-data-grid__resize-handle`'s own escape from inline flow (HEL-253, same file, same precedent the skeptic pointed to) rather than inventing a new approach. Vertically centered (`top: 50%; transform: translateY(-50%)`), offset `right: var(--space-6)` from the `<th>`'s own right edge to clear the resize handle's own hit area. `.ui-data-grid__table thead th` is already `position: sticky` (a valid containing block), so no extra `position: relative` needed. Checked HEL-520's `outline-offset: -2px` pattern (a fix for a focus RING clipped by a too-short overflow:hidden ancestor) — not directly applicable here, since this defect was the control's own BOX being laid out outside the cell entirely, not merely its ring extending past an otherwise-correctly-placed control; the resize-handle precedent was the right one to reuse instead, matching the skeptic's own framing.
- `frontend/src/shared/ui/DataGrid.test.tsx` — added a static-source regression guard asserting the pin toggle rule uses `position: absolute` with a non-zero `right` offset (verified mutation-failable directly: reverted to the pre-fix inline-flow CSS and re-ran, confirmed red; restored the fix and re-ran, confirmed green). Explicitly documented as NOT proof the control stays clickable/focusable on a real truncating column — jsdom has no text-metrics/ellipsis engine and no functioning `elementFromPoint`; the skeptic's own live `elementFromPoint` reproduction on a genuinely-truncating column, in both themes, is the only real evidence for that.
- `openspec/changes/column-pinning-table-panels/design.md` — Decision 3 corrected a third time with the full mechanism explanation (inline-flow-vs-truncation defect, the resize-handle precedent reused, and why HEL-520's `outline-offset` carve-out doesn't apply here) so it isn't re-derived a fourth time.
- `openspec/changes/column-pinning-table-panels/tasks.md` — task 3.1 updated to reference the corrected positioning approach.

## Cycle 6 (skeptic-final-4.md, 2 blocking, last round — owner-ratified extension)

No rebase needed (`origin/main` unchanged since cycle 5). Scoped narrowly to the two specified remedies; the AC4 separator and round-3 hit-testing code were not touched (`git diff --stat` confirms only `DataGrid.css`/`DataGrid.tsx`/`DataGrid.test.tsx` changed, and `.ui-data-grid__pinned-cell--last::after` is byte-identical to cycle 4).

- `frontend/src/shared/ui/DataGrid.tsx` — the header `<th>` now carries `ui-data-grid__th--pin-reserve` whenever the pin toggle actually renders in it (`pinnable && onPinToggle`, the exact condition gating the toggle itself) — CR1, the missing half of the round-3/4 fix (round 4 gave the toggle `position: absolute` so it no longer gets clipped, but never reserved the space it now occupies, so a truncating header's label kept painting underneath the icon — measured live 8/73 bare, 28/75 with several columns pinned).
- `frontend/src/shared/ui/DataGrid.css` — `.ui-data-grid--{condensed,normal,spacious} .ui-data-grid__table thead th.ui-data-grid__th--pin-reserve` adds `calc(<density's own right padding> + var(--space-9))` (48px: the toggle's `right: var(--space-6)` offset + its own 24px width) ADDITIVELY on top of each density's existing right padding (CR1). Separately, `.ui-data-grid__table thead th { min-height: 48px; }` added inside the existing `(max-width: 430px), (pointer: coarse)` media query (CR2) — on that surface the pre-existing 44px touch-target floor on the widened pin toggle collided with `position: absolute` inside a ~35px header row that's `overflow: hidden`, clipping the control top/bottom and splitting its focus ring into two bars (skeptic-final-4, probe-confirmed: `position: static` in-browser grows the row to 61px on its own and the clipping disappears — the row's height, not the control, needed to change). The control itself was NOT shrunk below 44px.
- `frontend/src/shared/ui/DataGrid.test.tsx` — added three static-source tests: the reserve class applies only when the toggle renders (and never when it doesn't — a scoping test), the reserve rule's `calc()` shape, and the coarse-pointer media query grows the header row's `min-height` in the SAME block as the control's own 44px floor. All three verified mutation-failable directly (reverted each fix in place, confirmed red; restored, confirmed green). Explicitly documented in both the tests and design.md as NOT proof either defect is actually resolved on the real surfaces they live on (a genuinely-truncating column with several columns pinned; a real ≤430px/coarse-pointer viewport) — jsdom has no text-metrics engine, viewport/pointer-media emulation, or real layout engine. The skeptic's own live measurement on those specific surfaces is the only real evidence for either.
- `openspec/changes/column-pinning-table-panels/design.md` — Decision 3 (pin toggle) gets a fourth correction documenting both fixes' full mechanism and derivation, so neither is re-derived a fifth time.
