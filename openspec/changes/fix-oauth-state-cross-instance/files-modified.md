- `backend/src/main/resources/db/migration/V105__oauth_states.sql` — new `oauth_states` table (deny-all RLS + explicit privileged grant) for cross-process CSRF state storage.
- `backend/src/main/scala/com/helio/services/auth/OAuthStateStore.scala` — new injected collaborator trait (`issue`/`validateAndConsume`) replacing the companion-object map.
- `backend/src/main/scala/com/helio/infrastructure/persistence/auth/OAuthStateRepository.scala` — Postgres-backed `OAuthStateStore` implementation: a single atomic `DELETE FROM oauth_states WHERE state = ? AND expires_at > now()` statement (affected-row-count check, not a `RETURNING` clause — corrected per evaluation-1.md CR5) for single-use consumption; expiry computed and compared via SQL `now()` rather than a JVM `Instant` (CR4) so it is genuinely evaluated by the database clock; expiry-driven pruning on every issue; routed through `withSystemContext`.
- `backend/src/main/scala/com/helio/services/auth/AuthService.scala` — removed the JVM-wide `ConcurrentHashMap` CSRF store from `object AuthService`; `generateCsrfState`/`validateCsrfState` now delegate to an injected, required `OAuthStateStore` and return `Future`.
- `backend/src/main/scala/com/helio/api/routes/auth/OAuthRoutes.scala` — both CSRF call sites (`GET /google`, `GET /google/callback`) restructured around `onSuccess` for the now-`Future`-returning state check; no blocking calls added.
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — wires `OAuthStateRepository(dbContext)` into `AuthService`'s constructor.
- `backend/src/test/scala/com/helio/infrastructure/persistence/RlsPolicyGuardSpec.scala` — added `oauth_states` to the RLS allowlist (deny-all policy asserted by name).
- `backend/src/test/scala/com/helio/services/auth/AuthServiceSpec.scala` — updated the sole `AuthService` constructor call site for the new required `stateStore` parameter.
- `backend/src/test/scala/com/helio/api/routes/auth/GoogleOAuthRoutesSpec.scala` — wires a real Postgres-backed `OAuthStateRepository` into `makeAuthService`; `cleanDb` now also truncates `oauth_states`; forged/missing-state tests tightened from `should include("state")` to the exact `shouldBe` message (CR2); added route-level expired (4.4) and replay (4.5) tests asserting the exact `400` body (CR2); fixed a broken reference to a non-existent `OAuthStateRepositoryCrossProcessSpec` (CR6).
- `backend/src/test/scala/com/helio/infrastructure/persistence/auth/OAuthStateRepositorySpec.scala` — new spec: the load-bearing cross-process round trip (two independently constructed `OAuthStateRepository` instances), plus forged/expired/replay/concurrent/pruning/no-log-leak/instrument-sanity coverage (tasks.md 4.1, 4.3–4.10). Strengthened per evaluation-1.md CR1: the 4.1 test now asserts the issued row is durably present in Postgres (via an out-of-band connection) before validating, and a new durability-negative test deletes a row out-of-band and asserts validation then fails — together these kill the JVM-wide (companion-object-shaped) mutant that the original suite left green (8/9 passing). The pruning test now also confirms the expired row existed before pruning, distinguishing "pruned" from "never stored" (non-blocking suggestion). All `JdbcBackend.Database` pools this spec opens are now tracked and closed in `afterEach` (non-blocking suggestion — previously leaked ~20 pools per run). The red arm (4.2) was demonstrated twice during delivery — the per-instance mutant (cycle 1) and, after CR1, the companion-object mutant (cycle 2, the one that actually matches the production defect) — both captured verbatim in the delivery report, neither left in the committed file.
