- `backend/build.sbt` — add `spark-core` + `spark-sql` 3.5.5 with Akka/Pekko exclusions and Jackson `dependencyOverrides`
- `backend/src/main/scala/com/helio/spark/PipelineRunCache.scala` — new: thread-safe TrieMap-backed run status/result store
- `backend/src/main/scala/com/helio/spark/SparkJobSubmitter.scala` — new: submits pipeline jobs to Spark, implements all 6 step ops
- `backend/src/main/scala/com/helio/api/routes/PipelineRunRoutes.scala` — new: POST /pipelines/:id/run and GET /pipelines/:id/runs/:runId endpoints
- `backend/src/main/scala/com/helio/api/JsonProtocols.scala` — add RunSubmitResponse and RunStatusResponse case classes + formatters
- `backend/src/main/scala/com/helio/api/ApiRoutes.scala` — wire PipelineRunRoutes; add pipelineRunCache + sparkJobSubmitter constructor params
- `backend/src/main/scala/com/helio/app/Main.scala` — instantiate PipelineRunCache and SparkJobSubmitter, eager SparkSession init
- `backend/src/main/scala/com/helio/infrastructure/PipelineRepository.scala` — add findById method for route validation
- `backend/src/test/scala/com/helio/spark/PipelineRunCacheSpec.scala` — new: unit tests for cache put/update/get/concurrency
- `backend/src/test/scala/com/helio/spark/SparkJobSubmitterSpec.scala` — new: local-mode Spark tests for all 6 step ops
- `backend/src/test/scala/com/helio/api/routes/PipelineRunRoutesSpec.scala` — new: HTTP testkit tests for 201/404/422/200
- `backend/src/test/scala/com/helio/api/ApiRoutesSpec.scala` — update ApiRoutes constructor calls to include new params
- `openspec/changes/spark-job-submission/tasks.md` — mark all tasks complete
