# Files modified — HEL-1050

- `frontend/src/shared/ui/inputs.css` — site 1: recolour focused border from raw `var(--app-accent)` to `var(--app-focus-ring-color)`, keeping the `--app-accent-dim` halo (D1/D2).
- `frontend/src/features/dashboards/ui/DashboardList.css` — sites 2, 3, 4, 6: same recolour for the filter/create/row-rename inputs' `:focus-visible` border, and adds a conforming `border-color` to site 6's always-bordered rename input (its focus state previously conveyed only via the sub-threshold halo).
- `frontend/src/features/auth/ui/auth.css` — site 5: recolour the focused border to `var(--app-focus-ring-color)` and convert the bare `:focus` selector to `:focus-visible` (D6).
- `frontend/src/features/panels/ui/grid/PanelGrid.css` — site 7: repoint `border-bottom-color` from `var(--app-accent-strong)` (fails 3:1 for the Yellow preset) to `var(--app-focus-ring-color)`; adds a code comment naming the residual user-chosen-panel-background non-conformance owned by HEL-1051 (D8).
- `frontend/src/features/pipelines/ui/PipelineDetailPage.css` — site 8: merge the bare-`:focus`/`:focus-visible` pair into a single `:focus-visible` rule, recoloured to `var(--app-focus-ring-color)`.
- `frontend/src/shared/chrome/AccentPicker.css` — site 10: recolour the swatch's `:focus-visible` box-shadow ring to `var(--app-focus-ring-color)` and add a third shadow layer so the focused-but-not-selected state is visually distinct from `--selected` (which retains the raw-accent double ring) — fixes both the contrast defect and the focus/selected state-collision defect design.md identified. Cycle 2: the third layer's colour was corrected from `--app-surface-strong` (invisible in this component's real render context, and pixel-identical to `--selected` for the three zero-darkening accent presets) to `--app-border-strong` (a neutral token that contrasts by construction regardless of backdrop/accent/theme). Cycle 3: comment-only fix — corrected the stale claim that the component's backdrop is the UserMenu popover's `--app-surface-strong`; its only call site (`SettingsPage.tsx:62`, since HEL-728) has an `--app-bg` backdrop, 1.119:1 against `--app-surface-strong` (corrected in the final-gate cycle from an imprecise "~1.05:1"). No declaration changed.
- `frontend/src/theme/appearance.ts` — carries HEL-1046's derivation rationale forward (D7): documents why `FOCUS_RING_SURFACES` uses the surfaces closest in luminance rather than the palette extremes, and that this guarantee does not extend to a user-chosen surface outside that set (site 7's residual).
- `frontend/src/theme/focusRingTokenGuard.css.test.ts` — extends HEL-1046's guard with a second, independent check (D5/D5a): a selector-base-granularity guard over `border`/`box-shadow` indicator declarations (with the required `AddSourceModal.css` pin per D9/4.1b), its four required mutation-verification arms (4.2 a-d), and a per-accent-preset assertion that `--app-accent-strong` fails 3:1 for Yellow (parsed from `theme.css`'s `color-mix`, never hardcoded). Cycle 2 (evaluator CR3): arms (a) and (c) now additionally run through `collectAllDeclarations` over a real temporary `.css` fixture file (the full `extractRules`→`splitSelectorList`→`parseDeclarations`→`selectorBase` pipeline, not just the predicate); the original hand-built-`Map` versions are kept alongside as `synthetic` twins with corrected, non-overclaiming comments.
- `frontend/src/theme/elevationTokenGuard.css.test.ts` — updates the pre-existing `AccentPicker.css` box-shadow pin: count reduced from 2→1 (the `:focus-visible` rule no longer shares the `--selected` rule's exact declaration) and a new pin added for the three-layer, ring-coloured `:focus-visible` box-shadow (declaration text updated again in cycle 2 for the `--app-border-strong` outer-layer fix above).
- `frontend/src/theme/tokenAuditSweep.css.test.ts` — re-derives the `PipelineDetailPage.css` baseline's pinned line numbers (shifted -1 by the site-8 rule merge) per that guard's own re-derivation discipline.
- `DESIGN.md` §8 — records that the 3:1 focus-indicator obligation binds on whichever mechanism conveys focus (not `outline` alone), that a translucent halo is never credited toward it, that `--app-accent-strong` is disqualified (Yellow, 2.78:1), and updates the persistent-modality-independent-indicator carve-out's examples to reflect `auth.css`'s conversion to `:focus-visible` and `AddSourceModal.css`'s orphaned-CSS status.
- `openspec/changes/accessible-input-focus-indicator/tasks.md` — all tasks marked complete.

## Not modified (named per AC-4, per design.md/tasks.md)

- `frontend/src/features/sources/ui/AddSourceModal.css` (site 9) — orphaned CSS, zero markup references (D9). Left bare-`:focus`/unfixed; pinned as an owned exception in `focusRingTokenGuard.css.test.ts`, owner **HEL-1052** (expected to delete the pin).
- `frontend/src/features/panels/ui/detailModal/PanelDetailModal.binding.css:137` — orphaned CSS owned by **HEL-1049**; out of this change's scope.
- Site 7's user-chosen-panel-background residual (`PanelGrid.css`) — structurally unfixable by any single derived colour (D8); owned by **HEL-1051**. Confirmed measurably real on the running app (2.48:1 for a mid-grey panel background), recorded in `.concertino/runs/HEL-1050/evidence/measurement-report.md`.
