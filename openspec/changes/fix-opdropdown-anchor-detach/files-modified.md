- `frontend/src/features/pipelines/hooks/usePipelineDetailPage.ts` — the actual fix: skip the debounced `analyzePipeline` dispatch while a run is in flight (`sseActive`) or another `/analyze` call for this pipeline is already loading (`analyzeStatus === "loading"`), removing the confirmed (D2-probed) source of backend request contention that was delaying `submitPipelineRun`'s completion past test/user-perceptible timeouts. `OpDropdown.tsx` is untouched — the anchor-identity-churn mechanism named in the original ticket was directly refuted by MutationObserver instrumentation (see `probe-findings.md`). **Cycle 2 (CR2/CR3):** the suppression now DEFERS instead of dropping — `sseActive`/`analyzeStatus` added to the effect's dependency array, `pendingAnalyzeRef` + `lastAnalyzedFingerprintRef` make the deferred dispatch resume exactly once when the guard clears without looping on its own side effects. The inline comment's false claim ("the run's own response already carries fresher schema/row data") is corrected to the real, checkable reason. **Cycle 3:** fixed a real regression in cycle 2's own fix — `pendingAnalyzeRef` was armed unconditionally whenever the guard was active, including on a re-entry caused by the SAME fingerprint's own dispatch (not a new edit), which defeated the fingerprint bail-out and caused an unbounded `/analyze` redispatch loop whenever a request resolves slower than the 300ms debounce window. Now only arms the pending flag for a genuinely new, not-yet-analyzed fingerprint. **Final gate round 1 (CR1):** the `sseActive` guard could get permanently stuck (SSE stream never opens/drops/misses its terminal event — `connectionError` has zero non-test consumers, and `PipelineRunStreamRoutes`'s live, non-replaying subscribe can miss a run's terminal event outright), silently suppressing every future edit's analyze forever. Bounded the deferral in time instead of plumbing `connectionError` through this hook: added `MAX_ANALYZE_DEFER_MS` (15000ms) and a watchdog timer (`deferWatchdogHandleRef`/`pendingSinceRef`/`stepsFingerprintRef`/`forceDeferredAnalyze`/`clearDeferWatchdog`) that forces the dispatch unconditionally if the guard hasn't cleared by then, independent of the debounce effect's own dependency-triggered lifecycle.
- `frontend/src/features/pipelines/ui/PipelineDetailPage.test.tsx` — new regression test ("a reorder's debounced analyze is skipped while a run is in flight"), demonstrated RED pre-fix and GREEN post-fix. **Cycle 2 (CR2):** added the mirror test ("...is dispatched exactly once after the prior one settles"), also RED/GREEN-demonstrated; fixed a pre-existing test's `getPipelineStepsMock` resync fixture that hadn't been reflecting a newly-created step (a latent fixture-realism gap the stricter CR2 logic exposed). **Cycle 3:** added a regression guard for the loop defect ("a single edit's analyze settles once and does not redispatch indefinitely while idle") — mocks every analyze call at 600ms (slower than the debounce window, the exact condition the loop needed and every prior test's fast mocks hid), makes one edit, sits idle 5.5s, and asserts the dispatch count is bounded; demonstrated RED against the cycle-2 code (7 and climbing) and GREEN post-fix. **Final gate round 1 (CR1):** added a test ("a step edit deferred by a run whose SSE stream never terminates eventually dispatches anyway") that submits a run which resolves normally but is never followed by any SSE event at all (never touches the SSE layer, which is the point), uses fake timers to advance past `MAX_ANALYZE_DEFER_MS`, and asserts the deferred edit's analyze eventually fires; demonstrated RED against the pre-CR1 code (stuck forever) and GREEN post-fix.
- `e2e/hel912-lanes-rejoin.spec.ts` — **unrelated fixture repair, called out explicitly**: the spec's pipeline-create request still used the scalar `sourceDataSourceId` field, which HEL-913 removed outright in favor of a required `roots[]` array (no accepted alias). Fixed to `roots: [{ sourceId: source.id }]` (matching `hel968-multi-root-editor-flow.spec.ts`'s own current usage) so the spec can run at all — it was failing 20/20 at setup with a 400 before this fix. This is schema-drift housekeeping, not part of the flake fix itself.
- `playwright.config.ts` — removed `hel912-lanes-rejoin.spec.ts`'s `testIgnore` entry and its stale ~14-line rationale comment (hard acceptance criterion: the spec is back in the default suite).
- `openspec/changes/fix-opdropdown-anchor-detach/specs/pipeline-op-picker-stability/spec.md` — **Cycle 2 (CR1):** rewritten wholesale to describe what shipped (debounced-analyze/run-contention guard) rather than the refuted DOM-detach/positioning contract; the lanes-rejoin guard requirement's scenario now states the honest, measured pass rate instead of "every iteration passes". **Cycle 3 (CR4/CR5):** added the resume ("dispatched exactly once when the guard clears") and termination ("no unbounded re-dispatch") scenarios the requirement was previously missing — cycle 1's silent-drop behavior, and cycle 2's own loop regression, would each have satisfied the requirement as written verbatim. **Final gate round 1 (CR2):** rewrote the lanes-rejoin scenario to state the measured COMPOSITE red rate (~5.7%, 4/70, three signatures) rather than the single-signature ~1.7-3.3% figure, and added a scenario asserting each of the three known signatures stays attributable to a tracked ticket (HEL-992, widened).
