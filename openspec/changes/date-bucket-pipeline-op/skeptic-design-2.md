## Skeptic Report — design gate (round 2)

### What I verified (with evidence)

- Read `ticket.md`, `proposal.md`, `design.md`, `tasks.md`, `specs/pipeline-date-bucket-op/spec.md` in full (fresh, cold read — not assuming round 1's findings still hold).
- Read `skeptic-design-1.md` to enumerate the three round-1 change requests, then independently re-verified each against current artifacts and ground truth (not the prior report's narrative):

  1. **CR1 (missing frontend Jest test task) — RESOLVED.** `tasks.md` now has task 4.5: "Add `frontend/src/features/pipelines/ui/DateBucketConfig.test.tsx`, mirroring the sibling `*Config.test.tsx` files (e.g. `CastFieldsConfig.test.tsx`, `ChunkByTokenCountConfig.test.tsx`) ... covers field-select, granularity-select, and output-column-input interactions and the resulting config PATCH shape." This closes the AC4/AC6 frontend-test gap identified in round 1.

  2. **CR2 (wrong precedent cited for append-or-replace) — RESOLVED.** `design.md` Decision 4 now explicitly states `inferDateBucket` mirrors `inferSplitText`/`inferExtractHeadings`/`inferChunkByTokenCount`'s pattern, "**not** `inferCompute`'s, which unconditionally appends ... with no collision check at all," and cites exact line numbers. I re-read `backend/src/main/scala/com/helio/domain/PipelineAnalyzeService.scala` fresh (note: file lives at this path, not `com.helio.service` as round 1's prose implied — a path detail, not a defect) and confirmed line-for-line: `inferCompute` (114-136) unconditionally does `inputSchema :+ SchemaField(...)`, no collision check. `inferSplitText` lines 182-183, `inferExtractHeadings` lines 216-217, `inferChunkByTokenCount` lines 253-254 all do `filterNot(...) :+ SchemaField(...)` exactly as cited — every cited line number in design.md Decision 4 matches the current file verbatim.

  3. **CR3 (tasks.md 2.1 understated frontend types-file scope) — RESOLVED.** `tasks.md` 2.1 now spells out all four required additions: `DateBucketConfig` interface, `DateBucketStep extends BasePipelineStep` + its `PipelineStep`/`PipelineStepConfig` union entries, and `DateBucketAnalyzeStep extends BaseAnalyzeStep` + its `AnalyzeStepResult` union entry. Verified against `frontend/src/features/pipelines/types/pipelineStep.ts`: `CastConfig` (line 35), `CastStep extends BasePipelineStep` (111) with union entries at `PipelineStep =` (144-150) and `PipelineStepConfig =` (159-165), `CastAnalyzeStep extends BaseAnalyzeStep` (211) with union entry at `AnalyzeStepResult =` (244-250) — the four-addition shape the revised task now describes is exactly what `cast` required.

- **Flyway V-number re-check (stability of a round-1 finding).** `ls backend/src/main/resources/db/migration/ | sort -t V -k2 -n | tail` still tops out at `V63__pipeline_run_trigger_source.sql`; `V64` remains free. `V52__add_chunkbytokencount_op.sql`'s op list still matches `PipelineStep.Registry` (13 entries) in `PipelineStep.scala:99-114` verbatim (`rename, filter, join, compute, groupby, cast, select, limit, sort, aggregate, splittext, extractheadings, chunkbytokencount`). No drift since round 1; design.md's V64 claim and re-confirm-before-commit mitigation both still hold.
- **Frontend state file re-check.** `frontend/src/features/pipelines/state/stepNarrowing.ts` — confirmed `OP_TYPES` array (line 50), `cast` entry (`{ id: "cast", label: "Cast type", icon: faRightLeft }`, line 56), and `defaultConfigFor`'s `case "cast":` (line 79-80) are the actual shapes tasks.md 2.2 asks the executor to mirror.
- **MCP field-naming note (round 1 non-blocking, re-checked for drift).** `helio-mcp/src/tools/write.ts:151-167` — `add_pipeline_step`'s `type` field is still `z.string().min(1)` (free text, field named `type`), not a `z.enum` named `op`. `tasks.md` 3.1 still describes this as adding to "the `op` enum" — round 1 explicitly flagged this as a non-blocking, self-correcting note (the executor discovers the real field name/shape on opening the file), not a change request, so its persistence here is expected and not a new defect.
- **No missed schema-file dependency.** `ls schemas/` — no schema file enumerates pipeline-step op values (none of the 26 files matched `splittext`/`cast`-as-enum via grep); design's Impact/tasks correctly omit a `schemas/` edit. CLAUDE.md's "keep schema updates in the same change" rule doesn't apply here since there's no schema surface for op enumeration.
- **Fresh adversarial pass beyond the three CRs** — checked for new placeholders/TBDs (none), new internal contradictions between design.md and tasks.md (none — the four backend files, four frontend surfaces, one MCP file, and five test tasks in tasks.md all trace 1:1 to design.md's decisions and the ticket's Scope section), and scope creep (none — Non-Goals in proposal.md/design.md match the ticket's Out of scope section verbatim).

### Verdict: CONFIRM

### Non-blocking notes

- `tasks.md` 3.1 still frames the MCP change as adding to an "`op` enum," while the actual field is free-text `type: z.string().min(1)`. This was explicitly logged as non-blocking/self-correcting in round 1 and remains so — the executor will discover the real shape on opening `write.ts`. No action required, but flagging again for continuity since it's still visible in the artifact.
- Design.md Decision 4's file citation says `PipelineAnalyzeService.scala` without a package qualifier; the actual path is `backend/src/main/scala/com/helio/domain/PipelineAnalyzeService.scala` (not under `com.helio.service` as round 1's report prose suggested in passing). Immaterial to the executor, who will `find`/open the file directly, but noting for accuracy.
