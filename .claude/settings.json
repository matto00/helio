{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--browser",
        "chromium",
        "--executable-path",
        "/usr/local/bin/chromium",
        "--headless"
      ]
    }
  }
}
