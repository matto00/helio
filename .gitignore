.idea
.vscode/
.metals/
.bsp/
.bloop/
node_modules/
frontend/node_modules/
dist/
frontend/dist/
build/
backend/target/
backend/project/
backend/.bloop/
backend/.bsp/
backend/.metals/
backend/.ivy2/
coverage/
playwright-report/
test-results/
.DS_Store
*.log
backend/.env
backend/data/
infra/.env.deploy
frontend/.env.production
frontend/.env.local
frontend/.agents/
frontend/.claude/
frontend/skills-lock.json

# Git worktrees (managed by Claude Code orchestrator)
.claude/worktrees/
.worktrees/
.claire/

# Playwright MCP session artifacts
.playwright-mcp/
[0-9][0-9]-*.png
[0-9][0-9]-snapshot.md

# Leftover screenshot artifacts from E2E test sessions
*.png
!docs/*.png
# README/product screenshots live here and are committed deliberately.
!docs/images/*.png

# PWA icons (HEL-300) — generated by `npm run generate-pwa-assets` from
# public/orbit-mark.svg but committed deliberately for deterministic deploys
# (no CI icon-generation step). See frontend/pwa-assets.config.ts.
!frontend/public/pwa-*.png
!frontend/public/maskable-icon-*.png
!frontend/public/apple-touch-icon-*.png

# Local demo/test artifacts (occasionally created by playwright sessions, etc.)
docs/demo/

# HEL-956 self-test planted/renamed artifacts (check-no-credential-in-agent-surface.selftest.mjs) —
# a crashed run must not leave a committable artifact behind.
helio-mcp/.hel956-selftest-secret.ts
/hel956-selftest-drift-probe/
/helio-mcp-hel956-selftest-moved/
/scripts/.hel956-selftest-mutated-surfaces.mjs

# HEL-993 self-test planted/renamed artifacts (check-no-credential-in-agent-surface.selftest.mjs) —
# a crashed run must not leave a committable artifact behind.
helio-mcp/.hel993-unreadable-secret.ts
helio-mcp/.hel993-only-file.ts
frontend/src/features/assistant/.hel993-importer.ts
frontend/src/features/assistant/.hel993-reexport.ts
frontend/src/features/assistant/.hel993-credential-prop.ts
frontend/src/features/assistant/.hel993-bfs-importer.ts
frontend/src/shared/.hel993-bfs-unreadable.ts
frontend/src/features/assistant/.hel993-locked-dir/
/scripts/.hel993-selftest-mutated-entry.mjs

# HEL-846 self-test/mutation-evidence planted artifacts
# (check-no-credential-in-agent-surface.selftest.mjs / mutation-evidence.md) —
# a crashed run must not leave a committable artifact, and the executor's own
# mutation plants (design.md Decision 2a) must never be tracked.
/openspec/.hel846-plant.md
/docs/.hel846-plant.md
/notes/.hel846-plant.md
/scripts/.hel846-selftest-mutated-surfaces.mjs
/scripts/.hel846-selftest-mutated-checks.mjs

# Concertino
.concertino/
# Stray render artifacts leaked from a dirty upstream Concertino checkout
# (unreviewed WIP, never invoked from helio) — HEL-812.
scripts/concertino/pricing-table.json
scripts/concertino/report-cost.sh
